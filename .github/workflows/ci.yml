name: CI

on:
  push:
    branches: [main, master, develop, 'release/**']
    paths-ignore:
      - 'site/**'
      - '**/*.md'
      - '.beads/**'
      - 'LICENSE'
      - 'CLAUDE.md'
  # No `paths-ignore` on pull_request: this workflow is the single owner of
  # the branch-protection required contexts ("🦀 Check Rust", "🧪 Backend
  # Unit Tests", "✅ CI Complete"), so it must trigger on every PR. The
  # `changes` job below decides whether the change is docs-only; heavy jobs
  # then skip via job-level `if`, which GitHub reports as success for
  # required checks ("Handling skipped but required checks"). This replaces
  # the docs-required-shim.yml workflow, which posted duplicate check runs
  # under the same required context names on PRs touching both docs and
  # code (#2923).
  pull_request:
    branches: [main, master, develop, 'release/**']
  workflow_dispatch:

permissions:
  contents: read

# Concurrency: keyed by `event_name` + `ref` so push, pull_request, and
# workflow_dispatch are isolated from each other. `cancel-in-progress` is
# only true for auto-triggered runs (push / pull_request); a manual run
# (`workflow_dispatch`) is never cancelled by a subsequent push.
concurrency:
  group: ci-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'push' || github.event_name == 'pull_request' }}

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  SQLX_OFFLINE: "true"

jobs:
  # ════════════════════════════════════════════════════════════════════════════
  # DOCS-ONLY / BACKEND-CHANGE GATE
  # ════════════════════════════════════════════════════════════════════════════

  # Decides whether a PR touches anything beyond the docs paths that used to
  # live in this workflow's `paths-ignore` (and in docs-required-shim.yml's
  # `paths`). Replaces the shim: `paths` / `paths-ignore` cannot express
  # "fire only when the other workflow does not" (paths-ignore skips only if
  # ALL files match, paths fires if ANY file matches), so a PR touching a
  # `.md` file plus code triggered both workflows and got every required
  # context posted twice (#2923). Computing the decision here makes the two
  # outcomes mutually exclusive by construction.
  #
  # Fails open: on push / workflow_dispatch, or if the PR file listing
  # errors, `code` is "true" and full CI runs. Downstream jobs gate on
  # `code != 'false'` so an empty output also runs full CI. The file list
  # comes from the pulls/files API (server-side, same source GitHub's own
  # path filtering uses) — not from attacker-influenced checkout content.
  #
  # The same pass also classifies the PR as backend-touching or not (#3124).
  # `backend` drives whether the integration suite runs on the PR; see
  # `test-backend-integration` below. It is a strict subset of `code`: the
  # docs arm is matched first, so `backend/README.md` is docs, not backend.
  changes:
    name: 🔍 Docs-Only Gate
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
    outputs:
      code: ${{ steps.filter.outputs.code }}
      backend: ${{ steps.filter.outputs.backend }}
    steps:
      - name: Determine whether non-docs files changed
        id: filter
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ "${{ github.event_name }}" != "pull_request" ]; then
            echo "code=true" >> "$GITHUB_OUTPUT"
            echo "backend=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if ! files=$(gh api "repos/${GITHUB_REPOSITORY}/pulls/${{ github.event.pull_request.number }}/files" --paginate --jq '.[].filename'); then
            echo "PR file listing failed — failing open to full CI" >&2
            echo "code=true" >> "$GITHUB_OUTPUT"
            echo "backend=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          code=false
          backend=false
          while IFS= read -r f; do
            [ -z "$f" ] && continue
            # First arm: keep in sync with the `push` trigger's paths-ignore
            # list above (CLAUDE.md is already covered by *.md).
            #
            # Second arm: anything that can change what the integration
            # suites compile to, what schema they run against, or how the
            # job itself is defined. Keep in sync with `detect-changes`'
            # `backend` filter, minus docker/Dockerfile.backend (the
            # integration job compiles with cargo, it does not build an
            # image) and plus .github/workflows/ci.yml (so a change to this
            # gate exercises the gate).
            case "$f" in
              site/*|*.md|.beads/*|LICENSE)
                ;;
              backend/*|Cargo.toml|Cargo.lock|.sqlx/*|.github/workflows/ci.yml)
                code=true
                backend=true
                ;;
              *)
                code=true
                ;;
            esac
          done <<< "$files"
          echo "code=$code" >> "$GITHUB_OUTPUT"
          echo "backend=$backend" >> "$GITHUB_OUTPUT"
          echo "non-docs changes: $code"
          echo "backend changes: $backend"

  # ════════════════════════════════════════════════════════════════════════════
  # RELEASE-BUMP GATE
  # ════════════════════════════════════════════════════════════════════════════

  # Decides whether a PR is a version-bump-only release PR (#3045). Such PRs
  # touch only the release-bump file set (Cargo.toml, Cargo.lock,
  # backend/src/api/openapi.rs, .trivyignore, CHANGELOG.md) and, for the
  # files that feed the compile, change nothing but version-string lines —
  # so they cannot alter unit-test, clippy, or coverage outcomes. The heavy
  # ak-ci-runners jobs (check-rust, test-backend-unit, coverage) skip via
  # job-level `if`, which GitHub reports as success for required checks —
  # the same mechanics as the Docs-Only Gate above. shell-tests and
  # security-audit still run: they are cheap ubuntu-latest jobs, and
  # security-audit genuinely reads the Cargo.lock this PR touches.
  #
  # Fails CLOSED: on push / workflow_dispatch, on any API error, on any file
  # outside the allowed set, on any rename/add/delete, on a missing inline
  # patch (GitHub omits `patch` for very large diffs), or on any +/- diff
  # line that is not provably a version-string line, `bump_only` is "false"
  # and full CI runs. Downstream jobs gate on `bump_only != 'true'` so an
  # empty output also runs full CI. The file list and per-file patches come
  # from the pulls/files API (server-side, the PR's diff against its merge
  # base) — not from attacker-influenced checkout content.
  release-bump:
    name: 🔍 Release-Bump Gate
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
    outputs:
      bump_only: ${{ steps.filter.outputs.bump_only }}
    steps:
      - name: Determine whether the PR is a version-bump-only release PR
        id: filter
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ "${{ github.event_name }}" != "pull_request" ]; then
            echo "bump_only=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if ! gh api "repos/${GITHUB_REPOSITORY}/pulls/${{ github.event.pull_request.number }}/files" \
              --paginate --slurp > /tmp/pr-files.json; then
            echo "PR file listing failed — failing closed to full CI" >&2
            echo "bump_only=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          bump_only=$(python3 - /tmp/pr-files.json <<'PY'
          import json
          import re
          import sys

          # --slurp wraps each paginated response page in an outer array.
          pages = json.load(open(sys.argv[1]))
          files = [f for page in pages for f in page]

          SEMVER = r'[0-9]+\.[0-9]+\.[0-9]+(?:-[0-9A-Za-z.-]+)?(?:\+[0-9A-Za-z.-]+)?'
          # Allowed file -> regex every +/- diff line must match, or None if
          # any content change in that file is acceptable on the fast path.
          #
          # Cargo.toml: only the workspace `version = "X.Y.Z"` line (column 0)
          # may change; dependency edits use `name = { version = ... }` inline
          # tables, add/remove non-matching lines, or live in other files, so
          # they fall back to full CI.
          # Cargo.lock: a workspace version bump only rewrites the bare
          # `version = "..."` lines of workspace member [[package]] blocks. Any
          # real dependency change also touches `checksum =` / `source =` /
          # `name =` / `dependencies =` lines, which fail the pattern.
          # openapi.rs: only the `version = "X.Y.Z",` utoipa info line.
          ALLOWED = {
              'Cargo.toml': re.compile(r'^[+-]version = "' + SEMVER + r'"$'),
              'Cargo.lock': re.compile(r'^[+-]version = "' + SEMVER + r'"$'),
              'backend/src/api/openapi.rs': re.compile(r'^[+-]\s*version = "' + SEMVER + r'",$'),
              '.trivyignore': None,
              'CHANGELOG.md': None,
          }

          def full_ci(reason):
              print(f'full CI: {reason}', file=sys.stderr)
              print('false')
              sys.exit(0)

          if not files:
              full_ci('empty PR file listing')

          for f in files:
              name = f.get('filename')
              if name not in ALLOWED:
                  full_ci(f'{name!r} is outside the release-bump file set')
              if f.get('status') != 'modified':
                  full_ci(f'{name!r} has status {f.get("status")!r}, not a plain modification')
              line_re = ALLOWED[name]
              if line_re is None:
                  continue
              patch = f.get('patch')
              if not patch:
                  full_ci(f'{name!r} has no inline patch to validate (diff too large)')
              # Split on '\n' ONLY. str.splitlines() also breaks on the
              # vertical tab, form feed, file/group/record separators, NEL and
              # the Unicode line/paragraph separators -- none of which git or
              # the GitHub patch field treat as a line terminator. Every one of
              # them is also valid Rust/TOML whitespace, so splitlines() turned
              # a single physical diff line into two Python "lines" and the
              # trailing fragment -- which does not start with +/- -- was
              # skipped entirely, letting arbitrary code ride into openapi.rs
              # on a line whose prefix matched the version regex. The gate then
              # skipped Check Rust, Backend Unit Tests and Code Coverage while
              # CI Complete reported success. Belt and braces: reject outright
              # any patch containing one of those separators.
              SPLIT_ONLY_IN_PYTHON = (
                  '\x0b', '\x0c', '\x1c', '\x1d', '\x1e',
                  '\x85', '\u2028', '\u2029',
              )
              if any(sep in patch for sep in SPLIT_ONLY_IN_PYTHON):
                  full_ci(f'{name!r} patch contains a non-newline line separator')
              lines = [ln.rstrip('\r') for ln in patch.split('\n')]
              # Cargo.toml: the regex alone is position-blind. A section table
              # ([dependencies.foo], [patch.crates-io.foo], ...) also puts
              # `version = "X.Y.Z"` at column 0, so a dependency edit would
              # match. Require that the only TOML section the hunk touches is
              # [workspace.package]; anything else falls back to full CI.
              if name == 'Cargo.toml':
                  sections = {
                      ln[1:].strip() for ln in lines
                      if ln[1:2] == '[' and ln[:1] in (' ', '+', '-')
                  }
                  if sections - {'[workspace.package]'}:
                      full_ci(f'Cargo.toml hunk touches sections {sorted(sections)}')
              for line in lines:
                  if line.startswith(('+', '-')) and not line_re.match(line):
                      full_ci(f'{name!r} changes a non-version line: {line!r}')

          print('true')
          PY
          ) || bump_only=false
          case "$bump_only" in
            true|false) ;;
            *) bump_only=false ;;
          esac
          echo "bump_only=$bump_only" >> "$GITHUB_OUTPUT"
          echo "version-bump-only release PR: $bump_only"

  # ════════════════════════════════════════════════════════════════════════════
  # TIER 1: FAST CI (Every Push/PR) - Target: < 5 minutes
  # ════════════════════════════════════════════════════════════════════════════

  check-rust:
    name: 🦀 Check Rust
    runs-on: ak-ci-runners
    timeout-minutes: 20
    needs: [changes, release-bump]
    # Skip (reported as success for required checks) on docs-only PRs and on
    # version-bump-only release PRs; `!cancelled()` + `!= 'false'` /
    # `!= 'true'` fail open if either gate itself failed.
    if: ${{ !cancelled() && needs.changes.outputs.code != 'false' && needs.release-bump.outputs.bump_only != 'true' }}
    # Disable debuginfo to cut peak compile memory on the ARC runner. The
    # lib-test target compile was OOMing ("sccache: Compile terminated by
    # signal 9", #1515) even with CARGO_BUILD_JOBS capped; debuginfo is the
    # largest memory contributor for a crate this size and is not needed for
    # fmt/clippy.
    env:
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
      # Bypass sccache while the ak-ci-runners /cache volume / sccache server is
      # unavailable ("Cannot open/write log file '/cache/sccache-error.log'",
      # "Timed out waiting for server startup"), which fails every sccache-wrapped
      # rustc invocation at ~50s. Mirrors the coverage job's RUSTC_WRAPPER override;
      # the Swatinem rust-cache still caches deps. Revert once the runner is restored.
      RUSTC_WRAPPER: ""
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # PRIVATE PER-JOB CARGO_HOME (#3154). The ARC runner pods set
      # CARGO_HOME=/cache/cargo-home, a hostPath volume SHARED by up to six
      # concurrent runner pods on the node — but two things treat that
      # registry as private, mutable state:
      #   1. Swatinem/rust-cache: its restore tar-extracts registry files
      #      (unlink+rewrite) over the live shared copy, and its post-save
      #      clean DELETES registry/src for non `-sys` crates and prunes
      #      registry/cache and index caches down to the saving job's own
      #      package set (see cleanRegistry in the pinned rust-cache src).
      #   2. cargo itself: whenever those files go missing it re-extracts
      #      them (rm_rf + rewrite) under a package-cache lock that sibling
      #      pods' COMPILE phases do not hold.
      # Any job compiling while another pod restores, saves, or re-extracts
      # reads half-written or deleted sources. Observed as: aws-lc-sys cc
      # "unterminated #ifndef" on a truncated vendored header, "could not
      # execute process ... (os error 2)" (spawn cwd = deleted registry src
      # dir), and "could not parse/generate dep info ... No such file or
      # directory" cascades — the #3154 flake family. All failing jobs had
      # RUSTC_WRAPPER="" (sccache was NOT involved).
      #
      # RUNNER_TEMP is on the pod-local emptyDir work volume and the pods
      # are ephemeral (one job each), so this makes every job's cargo home
      # private and self-consistent. Warm-path cost is unchanged for jobs
      # with rust-cache (the tarball already contains registry index+cache
      # and restores into the private path). Tool binaries are unaffected:
      # the runner image bakes them into /usr/local/cargo/bin, which stays
      # on PATH regardless of CARGO_HOME (ak-runner-rust Dockerfile).
      - name: Isolate CARGO_HOME per job (#3154)
        run: echo "CARGO_HOME=$RUNNER_TEMP/cargo-home" >> "$GITHUB_ENV"

      # Cache the dependency build so only the workspace crate recompiles.
      # ~635 deps are byte-identical run-to-run; cold compile was ~9m.
      #
      # prefix-key bumped v0->v1 (#3154): pre-existing tarballs archived the
      # old shared /cache/cargo-home with absolute paths, so restoring one
      # under an old key would write into the shared volume again and leave
      # the private CARGO_HOME cold. A fresh namespace repopulates cleanly.
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          prefix-key: v1-rust
          shared-key: clippy

      - name: Check formatting
        run: cargo fmt --all -- --check

      # Forbidden-pattern gate (#1316): authorization on API-token scopes must
      # use the canonical `token_service::scopes_grant_access` helper, never an
      # inline `== "admin"` / `.any(|s| s == "admin")` raw-string scope match.
      # This grep gate fails the build if the legacy idiom reappears in
      # backend/src (test modules and the canonical helper are excluded).
      - name: No legacy admin scope check
        run: ./scripts/ci/check-no-legacy-admin-scope.sh

      # Token-minting surface pin (#1315, epic #1617 -> #1615): every handler
      # that calls AuthService::generate_api_token must be in the reviewed set
      # and carry its reject rule (enforce_admin_only_scopes or require_admin).
      # A new token-minting endpoint added without updating the pinned list
      # fails the build, forcing a deliberate review of the privilege-escalation
      # surface.
      - name: Pin token-minting endpoint surface
        run: ./scripts/ci/check-token-mint-surface.sh

      # Auth-event audit completeness (#1617 Phase 1): every token mint/revoke
      # handler must emit an ApiTokenCreated/ApiTokenRevoked audit event, and
      # every federated (OIDC/SAML/LDAP) login must audit its Login/LoginFailed
      # outcome. Guards the compliance audit trail against silent regression.
      - name: Check auth-event audit coverage
        run: ./scripts/ci/check-auth-event-audit.sh

      # Migration-ledger divergence guardrail (#2689): fail the build if `main`
      # and an active `release/*` branch assign the SAME migration version
      # number to DIFFERENT content, which bricks upgrades across the fork with
      # `sqlx migrate run` VersionMismatch (root cause of #2686). Known/handled
      # divergences (release/1.5.x slots 154/155, reconciled by
      # migration_repair::repair_release_1_5_x_divergence) are pinned by SHA-384
      # in scripts/ci/migration-ledger-allowlist.txt; only NEW, unhandled reuse
      # fails. The script self-fetches the release branches (git fetch
      # --depth=1), so the shallow default checkout above needs no depth change.
      - name: Check migration-ledger divergence
        run: ./scripts/ci/check-migration-ledger.sh

      - name: Run Clippy
        # Cap parallel rustc invocations so the lib-test compile doesn't OOM
        # the ARC runner pod ("sccache: Compile terminated by signal 9",
        # #1515/#1594). Lowered 4 → 2 (#1679): under concurrent PR load the
        # lib-test target still OOM-killed at 4, so fewer parallel codegen
        # units cut peak RSS further. CARGO_INCREMENTAL=0 disables incremental
        # codegen (`-C incremental=...`), which was observed ON during the
        # failing compile and inflates peak memory — incremental is useless in
        # CI's clean checkout anyway. Lint set is unchanged.
        env:
          CARGO_BUILD_JOBS: "2"
          CARGO_INCREMENTAL: "0"
        run: cargo clippy --workspace --all-targets -- -D warnings

      # Hit-rate data to evaluate dropping the Swatinem rust-cache step once sccache sustains >80% hits.
      - name: sccache stats
        if: always()
        run: sccache --show-stats || true

  test-backend-unit:
    name: 🧪 Backend Unit Tests
    runs-on: ak-ci-runners
    timeout-minutes: 40
    needs: [changes, release-bump]
    # Skip on docs-only PRs and version-bump-only release PRs — see check-rust.
    if: ${{ !cancelled() && needs.changes.outputs.code != 'false' && needs.release-bump.outputs.bump_only != 'true' }}
    # Same memory guard as check-rust: drop debuginfo and cap parallel rustc
    # so the lib-test compile does not OOM the runner (#1515). This step
    # previously had no CARGO_BUILD_JOBS cap and could spawn one rustc per
    # core, which is what tipped it over. Jobs raised 4 -> 8 with the same
    # measured basis as the coverage job (7.9-11.0 GiB peak RSS at 8 jobs
    # with debuginfo=0 — and this compile is lighter, no instrumentation).
    env:
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
      CARGO_BUILD_JOBS: "8"
      # Disable incremental codegen (#1679): this job compiles the same
      # lib-test crate that OOM-killed check-rust, and incremental is useless
      # in CI's clean checkout while inflating peak RSS.
      CARGO_INCREMENTAL: "0"
      # Bypass sccache while the ak-ci-runners /cache volume / sccache server is
      # unavailable (see check-rust). Without this, every rustc invocation fails
      # at ~50s ("Timed out waiting for server startup"). Revert once restored.
      RUSTC_WRAPPER: ""
    services:
      # The lib tests include DB-backed cases (OCI blob GC, blob refs, the
      # readiness gate) that silently no-op via `Fixture::setup` when
      # DATABASE_URL is unset. Provide the same Postgres the coverage job uses
      # so they actually run (#1409 review).
      postgres:
        image: ghcr.io/artifact-keeper/ci-mirror/postgres:16-alpine
        env:
          POSTGRES_USER: registry
          POSTGRES_PASSWORD: registry
          POSTGRES_DB: artifact_registry
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Shield this job from concurrent mutation of the shared registry —
      # see the #3154 comment on the same step in check-rust. A dep-info
      # ENOENT instance of that flake hit THIS job on 2026-08-10
      # (job 93341706470, PR #3237).
      - name: Isolate CARGO_HOME per job (#3154)
        run: echo "CARGO_HOME=$RUNNER_TEMP/cargo-home" >> "$GITHUB_ENV"

      # Separate cache key from check-rust: same target dir but a different
      # build profile (test vs clippy), so they must not share a cache.
      # prefix-key: see the #3154 note in check-rust.
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        with:
          prefix-key: v1-rust
          shared-key: unit

      # Catch migration-slot collisions before they reach the e2e/nightly job.
      # The nightly run on 2026-05-09 (#1128) crashed at startup with
      # `duplicate key value violates unique constraint "_sqlx_migrations_pkey"
      # Key (version)=(73)` because a forward-port attempted to reuse a slot
      # already taken on main; #3313 recorded the same shape as a near-miss
      # (193 renumbered to 195 during review).
      #
      # MERGE-RESULT PROPERTY THIS DEPENDS ON (#3313): a duplicate version is
      # invisible on either PR branch alone — each branch has exactly one file
      # at the colliding number. The `pull_request` checkout above is the PR's
      # MERGE COMMIT, which is what makes this step able to see the collision
      # at all. Do not change this job to check out `pull_request.head`.
      #
      # The check itself (numeric-normalised dedup + sequence-gap warning)
      # lives in a script so it is self-testable; its self-test runs in
      # shell-tests.
      - name: Lint migration slots are unique
        run: ./scripts/ci/check-duplicate-migrations.sh

      - name: Wait for Postgres
        run: |
          set -euo pipefail
          # The service --health-cmd already runs pg_isready, but the step can
          # start before health flips to "healthy" on slower runners. A short
          # TCP readiness loop avoids a flaky first connect.
          for i in $(seq 1 30); do
            if (echo > /dev/tcp/localhost/5432) >/dev/null 2>&1; then
              echo "Postgres reachable on attempt $i"
              exit 0
            fi
            echo "  waiting for postgres... ($i/30)"
            sleep 1
          done
          echo "ERROR: Postgres did not become reachable within 30s"
          exit 1

      - name: Run database migrations
        run: sqlx migrate run --source backend/migrations
        env:
          DATABASE_URL: postgresql://registry:registry@localhost:5432/artifact_registry

      - name: Run unit tests
        env:
          # SQLX_OFFLINE keeps the build using the committed .sqlx cache;
          # DATABASE_URL lets the DB-backed lib tests run for real against the
          # Postgres service above instead of no-op'ing.
          SQLX_OFFLINE: true
          DATABASE_URL: postgresql://registry:registry@localhost:5432/artifact_registry
          # A database is provisioned for this job, so the DB-backed tests are
          # REQUIRED to actually run. This makes `try_pool` treat a connect
          # failure as a hard failure instead of a silent skip, so an
          # unreachable/misconfigured Postgres can no longer "fiction-green" the
          # suite (#2924).
          AK_TESTS_REQUIRE_DB: "1"
          JWT_SECRET: ci-coverage-secret-at-least-32-bytes-long-for-tests
        # nextest runs each test in its own process and applies the repo's
        # .config/nextest.toml db-serial test groups, which serialize the
        # global-DB-state suites (stuck-scan cleanup, storage GC) that the
        # previous single-threaded `cargo test -- --test-threads=1` run was
        # protecting. The coverage job already executes this exact suite
        # under nextest at 8 threads.
        run: cargo nextest run --workspace --lib --test-threads 8

      # `workflow_scan_gate_tests` is the #2609 contract guard: it asserts that
      # docker-publish.yml's Trivy enforcement pass still gates (exit-code '1',
      # CRITICAL/HIGH, ignore-unfixed, .trivyignore, non-SARIF format) and that
      # every merge-* job `needs: scan-containers`. It lives in backend/tests/
      # so it is an INTEGRATION target, and it ran in NO job at all (#3437):
      # the step above is `--lib`, and the Tier 2 job passes an explicit
      # `--test` allowlist that never included it. So the guard that exists to
      # stop image publishing from silently becoming non-blocking was itself
      # non-blocking, and had been failing on `main` since #3122 split the scan
      # into two passes with nobody to see it.
      #
      # It is run HERE, in Tier 1, rather than added to that allowlist, because
      # the Tier 2 job invokes `cargo test ... -- --ignored`: every suite in its
      # allowlist is 100% `#[ignore]`d by construction, and these three tests
      # are not. Adding the file there yields `running 0 tests ... 3 filtered
      # out` and exit 0 — a green, permanently vacuous gate, which is the exact
      # defect being fixed. Tier 1 is also where it belongs on the merits: pure
      # YAML parsing, no database, no network, ~20ms, and it gates every PR
      # rather than only backend-touching ones.
      #
      # `--no-tests=fail` so a rename or deletion of the target is a red job
      # instead of a silent zero-test pass.
      - name: Run workflow contract tests
        env:
          SQLX_OFFLINE: true
        run: cargo nextest run --workspace --test workflow_scan_gate_tests --no-tests=fail

      # Hit-rate data to evaluate dropping the Swatinem rust-cache step once sccache sustains >80% hits.
      - name: sccache stats
        if: always()
        run: sccache --show-stats || true

  shell-tests:
    name: 🐚 Shell Tests
    runs-on: ubuntu-latest
    needs: changes
    # Skip on docs-only PRs — see check-rust.
    if: ${{ !cancelled() && needs.changes.outputs.code != 'false' }}
    # Self-contained bash + python3 regression tests for docker/ init scripts.
    # Runs in seconds and uses only the dependencies pre-installed on the
    # ubuntu-latest image (bash, python3, jq, curl). Kept off the Rust
    # critical path so it does not gate cargo jobs.
    # Cap wall-clock so a hung mock HTTP server (Python BaseHTTPRequestHandler
    # blocking in serve_forever) cannot pin a Tier 1 required check for the
    # 6h GitHub default. The whole script runs in well under a minute.
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: dtrack-init regression test
        run: bash docker/test-init-dtrack.sh

      # Container CVE gate honesty (#3122): a Trivy step that BLOCKS a publish
      # must actually apply the severity filter it declares. trivy-action
      # silently `unset`s TRIVY_SEVERITY when the format is SARIF, which turned
      # the documented "CRITICAL,HIGH only" gate into one that failed on a
      # fixable CVE of ANY severity — a LOW blocked all publishing from `main`
      # for days (#3121). Pure YAML inspection, no network, runs in ~1s.
      - name: Check Trivy gate severity filter
        run: ./scripts/ci/check-trivy-gate-severity.sh

      # Same species as the check above, one layer out (#3308). That one asks
      # "does the publish gate apply the filter it declares?"; this one asks
      # "does the PRE-TAG gate block on what it just measured?" It did not:
      # release-preflight.sh printed "a tag cut will likely stall" and
      # "READY: ... Safe to cut the tag." in the same run, because the
      # measured-red branch called `warn` (non-incrementing) instead of `bad`.
      #
      # It needs a test rather than a live run because the interesting case is
      # invisible whenever main is green: with a green Docker Publish the buggy
      # and fixed scripts are byte-identical in behaviour. Stubs `gh`, so no
      # network and no dependence on main's current health. ~1s.
      #
      # Also covers check 4 (#3339), which asks whether a component's pinned
      # VERSION tag is already published from different sources. Same species
      # again: the v1.7.2 tag died on that collision while Docker Publish on
      # main passed for the SAME commit, because the exact-tag check only runs
      # on a clean refs/tags/v* ref. Registry answers are stubbed too, so this
      # stays offline.
      - name: Check release-preflight gates block on what they measure
        run: bash scripts/ci/test-release-preflight.sh

      # One layer further out again (#3538). The preflight can be right and
      # still not matter: nothing asserted that one had RUN against the commit
      # being tagged. v1.7.5 burned a version number to a check that existed
      # and was never run; v1.7.7 was tagged 18 minutes after a preflight
      # reported "verdict does not exist yet", and the run it was waiting on
      # concluded failure 19 seconds after the tag push. release.yml now
      # requires evidence of a green preflight for `github.sha`, and this
      # exercises every leg of that gate -- most importantly the ones that must
      # BLOCK (in-flight, superseded green, evidence naming another commit) and
      # the break-glass override, which must be loud and must refuse a bypass
      # with no stated reason. Stubs `gh`; offline, ~1s.
      - name: Check the preflight-evidence gate blocks what it must
        run: bash scripts/ci/test-assert-preflight-evidence.sh

      # Duplicate sqlx migration versions merge CLEAN and abort the backend at
      # startup (#3313, #1128). The live gate runs in test-backend-unit against
      # the merge result; this exercises the script's failure modes (numeric
      # normalisation, gaps, unparseable prefixes) so the gate itself cannot
      # silently rot. ~1s, no dependencies.
      - name: Check duplicate-migration gate catches what it must
        run: bash scripts/ci/test-check-duplicate-migrations.sh

      # A release prep that renames `## [Unreleased]` to `## [X.Y.Z]` without
      # opening a fresh empty one strands every entry that lands after it:
      # PR branches cut before the rename anchor into the renamed, ALREADY
      # RELEASED section and merge with no conflict and no warning. That is
      # how 30 entries of 1.8.0 work ended up filed under `[1.7.5]`, a version
      # that produced no images and no release object (#3433, #3429). The
      # assertion is one grep — the first `## [` heading must be
      # `## [Unreleased]` — and it is a repo-state invariant, so it fires on
      # whichever PR reaches CI first rather than depending on who did it.
      - name: Check CHANGELOG has an open [Unreleased] section
        run: ./scripts/ci/check-changelog-unreleased.sh

      - name: Check the CHANGELOG gate catches what it must
        run: bash scripts/ci/test-check-changelog-unreleased.sh

      # The release-branch gate only runs on PRs against `release/*` — a
      # handful of runs per release, far too rare to notice a regression in.
      # It matters most for the two exemptions #3422 added (release prep,
      # narrowed backport), which must keep FAILING for everything they are
      # not: a `chore(release):` commit that also touches source, a
      # version-file-only commit under another subject, a cherry-pick trailer
      # naming a sha main does not have. Builds throwaway git repos; ~2s.
      - name: Check release-branch gate exemptions stay narrow
        run: bash scripts/ci/test-check-release-branch-commits.sh

      # Documentation describing a gate configuration that has since moved is
      # invisible to every other check here: docs are not compiled, linted or
      # executed. CLAUDE.md's mandatory pre-push checklist named
      # a plain `cargo test` unit-test run long after both CI unit-test jobs had
      # moved to nextest, so a contributor following it saw ten NuGet failures
      # unrelated to their change on a clean tree, and a hung run at the default
      # thread count — which teaches people to ignore red (#3479). This pins the
      # documented command to the one CI invokes. ~1s, no dependencies.
      - name: Check the pre-push test command matches CI
        run: ./scripts/ci/check-docs-test-command.sh

      - name: Check the docs/CI drift gate catches what it must
        run: bash scripts/ci/test-check-docs-test-command.sh

      # Six STEP-level `continue-on-error: true` on `Generate artifact
      # attestation` and `Attach VEX attestations` meant a failed provenance
      # push left the merge job GREEN and published the image anyway, with no
      # annotation and nothing in `needs.<job>.result` — so build provenance
      # had never been proven to attach to any image (#3496). Same species as
      # #2824 (cosign failed, 1.6.1 shipped unsigned). This pins the
      # invariant: attestation and cosign steps may never be soft-failed, and
      # docker-publish.yml may carry no step-level continue-on-error at all.
      # Pure YAML inspection, no network, ~1s.
      - name: Check supply-chain steps cannot fail silently
        run: ./scripts/ci/check-supply-chain-soft-fail.sh

      - name: Check the supply-chain soft-fail gate catches what it must
        run: bash scripts/ci/test-check-supply-chain-soft-fail.sh

      # The duplication gate measures whatever jscpd-prepare-sources.py hands
      # it, so a bug there silently changes the gate's verdict (#3495). The
      # dangerous direction is over-removal: an INDENTED `#[cfg(test)]` sits
      # inside another item, and treating it as a top-level boundary would
      # delete every production line up to the enclosing item's closing brace
      # — which the gate would then report as a clean measurement. Pure python
      # on throwaway files, ~1s.
      - name: Check the duplication gate's source preparation
        run: bash scripts/ci/test-jscpd-prepare-sources.sh

  coverage:
    name: 📊 Code Coverage
    runs-on: ak-ci-runners
    timeout-minutes: 45
    needs: [changes, release-bump]
    # Skip on docs-only PRs and version-bump-only release PRs — see check-rust.
    if: ${{ !cancelled() && needs.changes.outputs.code != 'false' && needs.release-bump.outputs.bump_only != 'true' }}
    # Memory guards for the instrumented lib-test compile (#1679/#1680,
    # extended here to the coverage job). This job builds the same lib-test
    # crate that OOM-killed check-rust, but under `-C instrument-coverage`
    # *and* `-C debuginfo=2` — the heaviest compile in CI, which got SIGKILL'd
    # (OOM) on larger PRs. CARGO_INCREMENTAL=0 drops incremental codegen
    # (useless in a clean checkout) and dropping debuginfo cuts peak RSS the
    # most. Coverage mapping comes from the instrumentation (`__llvm_covmap`),
    # not DWARF, so debuginfo=0 does not affect line-coverage accuracy.
    #
    # CARGO_BUILD_JOBS was capped at 2 when this job ran with debuginfo and
    # an 8Gi runner limit; both changed since (#2062 raised limits to 16Gi,
    # debuginfo=0 above). The same instrumented compile at 8 jobs with
    # debuginfo=0 measures 7.9-11.0 GiB peak anonymous RSS, comfortably
    # inside 16Gi, and matches the runner's 8-cpu limit.
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_BUILD_JOBS: "8"
      CARGO_PROFILE_DEV_DEBUG: "0"
      CARGO_PROFILE_TEST_DEBUG: "0"
    services:
      postgres:
        # Use the GHCR mirror maintained by .github/workflows/mirror-images.yml
        # to avoid Docker Hub anonymous-pull rate limits, which were aborting
        # the coverage job before tests could compile.
        image: ghcr.io/artifact-keeper/ci-mirror/postgres:16-alpine
        env:
          POSTGRES_USER: registry
          POSTGRES_PASSWORD: registry
          POSTGRES_DB: artifact_registry
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Skip the expensive coverage build on PRs that touch no Rust source.
      # Dependency bumps (Cargo.toml/Cargo.lock only), workflow edits, and
      # docs-only changes cannot move coverage numbers, and the Backend Unit
      # Tests job already validates the build. `rust_changed=true` for push
      # events preserves the existing behavior on main.
      - name: Detect Rust source changes
        id: detect
        run: |
          if [ "${{ github.event_name }}" != "pull_request" ]; then
            echo "rust_changed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # Fetch the base branch into a STABLE named remote-tracking ref
          # (origin/$BASE) instead of relying on the volatile FETCH_HEAD. A bare
          # `git fetch ... origin $BASE` only writes FETCH_HEAD, which any later
          # fetch overwrites and which races the on-disk shallow file
          # ("fatal: shallow file has changed since we read it"). A named ref is
          # deterministic and never clobbered. (#1798/#1799/#1812/#1813/#1815)
          BASE="${{ github.base_ref }}"
          git fetch --no-tags --depth=1 origin "+refs/heads/$BASE:refs/remotes/origin/$BASE"
          if git diff --name-only "origin/$BASE" -- '*.rs' | grep -q .; then
            echo "rust_changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "rust_changed=false" >> "$GITHUB_OUTPUT"
            echo "### Coverage: skipped (no Rust source changes in this PR)" >> "$GITHUB_STEP_SUMMARY"
          fi

      # Shield this job from concurrent mutation of the shared registry —
      # see the #3154 comment on the same step in check-rust. This job is
      # the most frequent victim: its instrumented build is the longest in
      # CI, so its compile phase overlaps other jobs' cache restores/saves
      # most often (observed 2026-08-07 twice as aws-lc-sys cc failures,
      # 2026-08-09 twice as dep-info ENOENT, both pods within seconds).
      - name: Isolate CARGO_HOME per job (#3154)
        if: steps.detect.outputs.rust_changed == 'true'
        run: echo "CARGO_HOME=$RUNNER_TEMP/cargo-home" >> "$GITHUB_ENV"

      # Distinct cache key: the instrumented coverage build is incompatible
      # with the clippy/unit profiles, and cargo-llvm-cov builds into
      # target/llvm-cov-target, so point rust-cache there or it caches an
      # empty target/ and silently no-ops.
      # prefix-key: see the #3154 note in check-rust.
      - uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1
        if: steps.detect.outputs.rust_changed == 'true'
        with:
          prefix-key: v1-rust
          shared-key: coverage
          workspaces: ". -> target/llvm-cov-target"

      # cargo-nextest is baked into the runner image at a pinned version
      # (ghcr.io/artifact-keeper/ak-runner-rust, see artifact-keeper-iac
      # runner-images/rust/Dockerfile) -- no per-run install needed.

      - name: Run database migrations
        if: steps.detect.outputs.rust_changed == 'true'
        run: sqlx migrate run --source backend/migrations
        env:
          DATABASE_URL: postgresql://registry:registry@localhost:5432/artifact_registry

      # Build the instrumented test binaries as a step of their own, so a
      # BUILD failure is visibly distinct from a coverage-gate failure
      # (#3154's "masquerading" half). Before this split, an aws-lc-sys /
      # dep-info build flake surfaced as "Code Coverage: fail", which reads
      # as "your new code is under the 70% threshold" and sends people off
      # to write tests they do not need. The only tell was the job duration
      # (~1-2 min vs ~14 min for a real gate run), which is not
      # discoverable. The ::error:: annotation below shows up directly on
      # the PR checks page.
      - name: Build instrumented test binaries
        if: steps.detect.outputs.rust_changed == 'true'
        env:
          SQLX_OFFLINE: true
          RUSTC_WRAPPER: ""
        # The explicit `clean --workspace` keeps the implicit-clean
        # semantics the single `cargo llvm-cov nextest` invocation had.
        run: |
          source <(cargo llvm-cov show-env --sh)
          cargo llvm-cov clean --workspace
          if ! cargo nextest run --workspace --lib --no-run; then
            echo "::error title=Coverage BUILD failed — not a coverage-gate failure::cargo could not build the instrumented test binaries, so no coverage was measured; the 70% new-code gate, the 50% floor, and the jscpd duplication gate never ran. A genuine compile error will also fail Check Rust / Backend Unit Tests on this commit; if those are green and this log shows ENOENT / 'dep info' / cc errors in vendored sources, this was the #3154 flake class — rerun the job."
            exit 1
          fi

      - name: Generate coverage report
        if: steps.detect.outputs.rust_changed == 'true'
        env:
          DATABASE_URL: postgresql://registry:registry@localhost:5432/artifact_registry
          # A database is provisioned for this job, so the DB-backed tests are
          # REQUIRED to run. A connect failure now fails the suite loudly instead
          # of silently skipping (which would also silently drop coverage). See
          # issue #2924.
          AK_TESTS_REQUIRE_DB: "1"
          SQLX_OFFLINE: true
          RUSTC_WRAPPER: ""
          # SSO credential encryption tests in auth_config_service rely on at
          # least one of these env vars being set. They skip cleanly when
          # unset (preserving local `cargo test` ergonomics), so we provide a
          # deterministic value here to exercise the create/update/decrypt
          # paths in coverage. The encryption_key panic-when-unset test
          # explicitly skips when this is set.
          JWT_SECRET: ci-coverage-secret-at-least-32-bytes-long-for-tests
        # nextest runs each test in its own process for faster scheduling.
        # Test concurrency was capped at 4 to keep parallel test processes
        # from OOMing the memory-limited ARC runner (cf. the #1515 compile
        # OOM); raised to 8 here to measure test-phase scaling now that the
        # per-process profraw I/O is pooled — revert to 4 if runner memory
        # pressure appears.
        #
        # Profile counters are written through an LLVM %Nm merge pool instead
        # of cargo-llvm-cov's default one-.profraw-per-test-process: with
        # ~12k test processes the per-process files cost tens of GB of write
        # I/O per run, while the pool online-merges the same counters into at
        # most 32 files (~92MB) with byte-equal coverage aggregates.
        #
        # NO `cargo llvm-cov clean` here: it ran in the build step above and
        # must not run again between build and test execution.
        run: |
          source <(cargo llvm-cov show-env --sh)
          export LLVM_PROFILE_FILE="${LLVM_PROFILE_FILE%/*}/%32m.profraw"
          cargo nextest run --workspace --lib --test-threads 8
          cargo llvm-cov report --lcov --output-path lcov.info

      - name: Coverage summary and gate
        if: steps.detect.outputs.rust_changed == 'true'
        run: |
          # cargo llvm-cov report resolves the profile directory from the
          # show-env environment; without it, a fresh step shell looks in
          # the default location and misses the pooled .profraw files.
          source <(cargo llvm-cov show-env --sh)

          # Total project coverage summary
          echo "### Coverage Summary" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cargo llvm-cov report --summary-only 2>&1 | tee -a $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

          # Total coverage floor (never let overall coverage drop below 50%)
          cargo llvm-cov report --fail-under-lines 50

      - name: New code coverage gate
        if: github.event_name == 'pull_request' && steps.detect.outputs.rust_changed == 'true'
        run: |
          # Fetch base branch so we can diff against it (checkout is shallow by default).
          #
          # Fetch the base into a STABLE named remote-tracking ref (origin/$BASE)
          # rather than relying on the volatile FETCH_HEAD. The previous code did
          # two fetches in a row — `git fetch ... origin $BASE` (set FETCH_HEAD)
          # then `git fetch --deepen=200 origin` — and the second fetch rewrote the
          # on-disk shallow file and clobbered/ambiguated FETCH_HEAD while later
          # commands were still reading it, producing the intermittent
          #   fatal: shallow file has changed since we read it
          #   fatal: ambiguous argument 'FETCH_HEAD': unknown revision ...
          #   ##[error]Process completed with exit code 128
          # that failed PRs with no real coverage problem
          # (#1798/#1799/#1812/#1813/#1815). A named ref is written atomically and
          # never overwritten by a subsequent fetch, so it is race-free.
          #
          # Deepen BOTH the base branch AND the PR head: the head is checked out
          # shallow (fetch-depth: 1), so without head-side history `git merge-base`
          # has nothing to walk on the head side and finds no common ancestor.
          # Deepening only the base (the original #1671 behavior) is not enough.
          # The deepen targets the head and does NOT touch origin/$BASE, so the ref
          # we diff against is resolved deterministically regardless of its outcome.
          BASE="${{ github.base_ref }}"
          git fetch --no-tags --depth=200 origin "+refs/heads/$BASE:refs/remotes/origin/$BASE"
          git fetch --no-tags --deepen=200 origin "$BASE" || true

          # Diff against the MERGE-BASE between the PR head and the base branch tip,
          # NOT the raw base tip (origin/$BASE). Diffing against the tip counts changes
          # from OTHER PRs that merged after this branch was cut as a phantom delta,
          # producing false coverage failures on stale branches (see issue #1646).
          # The merge-base is where this branch actually diverged, so the diff
          # reflects only the lines THIS branch changed.
          #
          # `git merge-base` exits non-zero when it cannot find a common ancestor
          # (e.g. the shallow clone is still missing it). This step runs under the
          # default `bash -e`, so an unguarded command substitution would ABORT the
          # whole step here, before the `if [ -z ]` fallback could run — a false
          # coverage failure rather than a graceful degrade (regression #1689 from
          # #1671). `2>/dev/null || true` keeps the step alive so the fallback runs.
          MERGE_BASE=$(git merge-base "origin/$BASE" HEAD 2>/dev/null || true)
          if [ -z "$MERGE_BASE" ]; then
            echo "::warning::Could not compute merge-base on shallow clone; falling back to base tip (issue #1646 trade-off)"
            MERGE_BASE=$(git rev-parse "origin/$BASE")
          fi
          echo "Diffing new code against merge-base: $MERGE_BASE"
          export MERGE_BASE

          # Check coverage on new/changed lines only (not the entire file).
          # Uses unified diff to extract exact line numbers that were added or modified,
          # then cross-references with lcov to measure coverage of those specific lines.
          CHANGED_FILES=$(git diff --name-only "$MERGE_BASE" -- '*.rs' 2>/dev/null || true)
          if [ -z "$CHANGED_FILES" ]; then
            echo "No .rs files changed, skipping new code coverage check"
            echo "### New Code Coverage: N/A (no Rust changes)" >> $GITHUB_STEP_SUMMARY
            exit 0
          fi

          # Parse the unified diff to find which lines were added/changed in each file,
          # then cross-reference with lcov.info to compute coverage on those lines only.
          RESULT=$(python3 -c "
          import subprocess, re, os

          # Step 1: Parse unified diff to find added line numbers per file.
          # Diff against the merge-base (passed in via env) so intervening merges
          # from other PRs do not show up as a phantom delta (issue #1646).
          merge_base = os.environ['MERGE_BASE']
          diff = subprocess.run(
              ['git', 'diff', '-U0', merge_base, '--', '*.rs'],
              capture_output=True, text=True
          ).stdout

          new_lines = {}  # filename -> set of added line numbers
          current_file = None
          for line in diff.split('\n'):
              if line.startswith('+++ b/'):
                  current_file = line[6:]
              elif line.startswith('@@') and current_file:
                  # Parse @@ -old,count +new,count @@ format
                  m = re.search(r'\+(\d+)(?:,(\d+))?', line)
                  if m:
                      start = int(m.group(1))
                      count = int(m.group(2)) if m.group(2) else 1
                      if current_file not in new_lines:
                          new_lines[current_file] = set()
                      for i in range(start, start + count):
                          new_lines[current_file].add(i)

          if not new_lines:
              print('none')
          else:
              # Step 2: Parse lcov.info and check coverage only for new lines
              hit = miss = 0
              lcov_file = None
              lcov_match = None
              for line in open('lcov.info'):
                  line = line.strip()
                  if line.startswith('SF:'):
                      path = line[3:]
                      lcov_match = None
                      for f, lines in new_lines.items():
                          if path.endswith(f):
                              lcov_match = lines
                              break
                  elif line.startswith('DA:') and lcov_match is not None:
                      parts = line[3:].split(',')
                      lineno = int(parts[0])
                      count = int(parts[1])
                      if lineno in lcov_match:
                          if count > 0:
                              hit += 1
                          else:
                              miss += 1
              total = hit + miss
              if total == 0:
                  print('none')
              else:
                  pct = (hit * 100) // total
                  print(f'{pct} {hit} {total}')
          ")

          if [ "$RESULT" = "none" ]; then
            echo "No instrumented lines in new code"
            echo "### New Code Coverage: N/A (no instrumented new lines)" >> $GITHUB_STEP_SUMMARY
            exit 0
          fi

          PCT=$(echo "$RESULT" | awk '{print $1}')
          HIT=$(echo "$RESULT" | awk '{print $2}')
          TOTAL=$(echo "$RESULT" | awk '{print $3}')

          echo "### New Code Coverage: ${PCT}% (${HIT}/${TOTAL} new lines)" >> $GITHUB_STEP_SUMMARY
          echo "New code coverage: ${PCT}% (${HIT}/${TOTAL} new lines)"

          # Skip gate for very small changes (< 10 instrumented new lines) since
          # the percentage is statistically noisy at that scale
          if [ "$TOTAL" -lt 10 ]; then
            echo "Too few new lines ($TOTAL) to enforce coverage gate, skipping"
            echo "Skipped: fewer than 10 instrumented new lines" >> $GITHUB_STEP_SUMMARY
            exit 0
          fi

          if [ "$PCT" -lt 70 ]; then
            echo "::error::New code coverage is ${PCT}%, below 70% threshold"
            echo "Coverage on new/changed lines is below 70%. Add tests for new code." >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

      - name: Setup Node.js for jscpd
        if: always() && github.event_name == 'pull_request'
        uses: actions/setup-node@v6
        with:
          node-version: '20'

      - name: Code duplication gate
        if: always() && github.event_name == 'pull_request'
        run: |
          # Install jscpd (copy-paste detector, same approach as SonarCloud's CPD engine)
          npm install -g jscpd@4 --silent

          # Get changed .rs files. Fetch the base into a STABLE named
          # remote-tracking ref (origin/$BASE) rather than the volatile FETCH_HEAD:
          # a later fetch (or the coverage gate's deepen in the same job) rewrites
          # the shallow file and clobbers FETCH_HEAD mid-read, causing the
          # intermittent "fatal: shallow file has changed since we read it" /
          # "ambiguous argument 'FETCH_HEAD'" exit-128 flake. A named ref is
          # race-free. (#1798/#1799/#1812/#1813/#1815)
          #
          # Diff against the MERGE-BASE between the PR head and the base branch
          # tip, NOT the raw base tip — the same #1646 fix the coverage gate
          # already has. Diffing against the tip counts files changed by OTHER
          # PRs that merged after this branch was cut as a phantom delta, so a
          # stale branch inherits e.g. formats/helm.rs (and its pre-existing
          # clones) from main and fails the gate on code it never touched
          # (#1824/#1825/#1827/#1796 all failed on identical helm.rs clones).
          BASE="${{ github.base_ref }}"
          git fetch --no-tags --depth=200 origin "+refs/heads/$BASE:refs/remotes/origin/$BASE" 2>/dev/null || true
          git fetch --no-tags --deepen=200 origin "$BASE" 2>/dev/null || true
          MERGE_BASE=$(git merge-base "origin/$BASE" HEAD 2>/dev/null || true)
          if [ -z "$MERGE_BASE" ]; then
            echo "::warning::Could not compute merge-base on shallow clone; falling back to base tip (issue #1646 trade-off)"
            MERGE_BASE=$(git rev-parse "origin/$BASE")
          fi
          echo "Diffing changed files against merge-base: $MERGE_BASE"
          CHANGED_RS=$(git diff --name-only "$MERGE_BASE" -- '*.rs' 2>/dev/null | grep -v '_test\|tests/' || true)

          # Exclude the per-ecosystem package-format handlers
          # (backend/src/api/handlers/<format>.rs). These are inherent structural
          # near-duplicates of one another (~18% cross-file duplication on main,
          # dominant clones are whole-file matches like sbt.rs <-> vscode.rs incl.
          # the shared import block). Without this exclusion ANY PR that legitimately
          # edits the download/upload path across several formats trips the 3% gate
          # regardless of code quality (#1704). The exclusion list is the single
          # source of truth in .jscpd.json; we filter the changed-file list here
          # rather than relying on jscpd's own ignore globs so the behavior is
          # deterministic regardless of jscpd version. The gate stays STRICT on all
          # other code (services, storage, middleware, non-format handlers, ...)
          # where duplication is a real smell.
          IGNORE_RE=$(python3 -c "
          import json, re
          cfg = json.load(open('.jscpd.json'))
          pats = [p for p in cfg.get('ignore', []) if p.startswith('backend/src/api/handlers/')]
          print('|'.join(re.escape(p) for p in pats))
          ")
          if [ -n "$IGNORE_RE" ]; then
            CHANGED_RS=$(printf '%s\n' "$CHANGED_RS" | grep -Ev "^(${IGNORE_RE})$" || true)
          fi

          if [ -z "$CHANGED_RS" ]; then
            echo "No measurable .rs source files changed (none, or only ignored format handlers), skipping duplication check"
            echo "### Code Duplication: N/A (no measurable Rust changes)" >> $GITHUB_STEP_SUMMARY
            exit 0
          fi

          # Write changed files to a list for jscpd
          echo "$CHANGED_RS" > /tmp/changed-files.txt

          # Copy the changed sources with their inline `#[cfg(test)]` modules
          # removed, and keep a map of the removed line ranges so failures can
          # still report real line numbers (#3495).
          #
          # WHY: `backend/src` is 474k lines of which 265k -- 56% -- are inline
          # test modules; `repositories.rs` alone is 26,328 lines, 22,164 of
          # them test code. .jscpd.json already ignores `**/*_test.rs` and
          # `**/tests/**`, and the CHANGED_RS filter above already drops
          # `_test`/`tests/` paths, so test code is out of scope BY POLICY.
          # Inline test modules were out of scope only BY ACCIDENT, because the
          # files holding them were too big for jscpd to open (see the limits
          # below). Measured with the limits raised and nothing else changed,
          # the non-ignored tree scores 3.38% and 23 of the last 44
          # code-touching commits on main would have failed this gate, with 58
          # of 61 clones sitting entirely inside `#[cfg(test)]`. Stripping them
          # applies the existing policy: 2.27% tree-wide, 2 of those 44
          # commits over threshold, both on genuine production-code clones.
          rm -rf /tmp/jscpd-src /tmp/jscpd-linemap.json
          echo "Preparing sources (inline test modules are out of scope):"
          python3 scripts/ci/jscpd-prepare-sources.py \
            /tmp/jscpd-src /tmp/jscpd-linemap.json \
            $(echo "$CHANGED_RS" | tr '\n' ' ')

          # A source with fewer than --min-lines lines is legitimately not
          # analysed, and jscpd emits NO REPORT AT ALL for a set containing
          # only such files. Distinguish that from "the whole file set was
          # skipped", which must fail below.
          MEASURABLE=$(python3 -c "
          import os
          n = 0
          for path in open('/tmp/changed-files.txt').read().split():
              prepared = os.path.join('/tmp/jscpd-src', path)
              if not os.path.exists(prepared):
                  continue
              if len(open(prepared, encoding='utf-8', errors='replace').read().split('\n')) >= 10:
                  n += 1
          print(n)
          ")
          if [ "$MEASURABLE" -eq 0 ]; then
            echo "No changed file has 10+ production lines after inline test modules are excluded; nothing for jscpd to detect."
            echo "### Code Duplication: N/A (no changed file has 10+ production lines)" >> $GITHUB_STEP_SUMMARY
            exit 0
          fi

          # jscpd's own defaults are maxLines 1000 / maxSize 100kb, and a file
          # over EITHER is skipped SILENTLY -- no error, no report entry (#3495).
          # That skipped 152 of 336 files under backend/src, 125 of them not on
          # the ignore list, so any PR whose Rust changes landed only in one of
          # them got a duplication verdict computed from an empty file set.
          # .jscpd.json is the single source of truth for the limits, the same
          # way it is for `ignore`; they are passed explicitly so the behaviour
          # does not depend on jscpd's config discovery or version.
          read -r MAX_LINES MAX_SIZE <<<"$(python3 -c "
          import json
          cfg = json.load(open('.jscpd.json'))
          print(cfg['maxLines'], cfg['maxSize'])
          ")"
          echo "jscpd limits from .jscpd.json: max-lines=${MAX_LINES} max-size=${MAX_SIZE}"

          # Run jscpd on the prepared copies (min 10 lines, threshold 3%).
          # `cd` so the report names the real repo-relative paths.
          RESULT=$(cd /tmp/jscpd-src && jscpd --min-lines 10 --threshold 3 \
            --max-lines "$MAX_LINES" --max-size "$MAX_SIZE" --reporters json \
            --format rust --output /tmp/jscpd-report \
            $(echo "$CHANGED_RS" | tr '\n' ' ') 2>&1) || true

          # A missing report used to be reported as "0% (no duplicates found)"
          # and passed. That is exactly what a whole-file-set skip looks like,
          # so it must be a failure, not a clean bill of health.
          if [ ! -f /tmp/jscpd-report/jscpd-report.json ]; then
            echo "::error::jscpd produced no report for $(echo "$CHANGED_RS" | wc -l) changed file(s). It analysed nothing, so this gate measured nothing (#3495)."
            echo "$RESULT"
            echo "### Code Duplication: FAILED (jscpd produced no report)" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          # Every changed file must actually appear in the report. jscpd skips
          # silently for several reasons (over max-lines, over max-size,
          # unreadable, unknown format) and reports whatever the remainder
          # measured; a file below --min-lines is the one legitimate omission.
          SKIP_FAIL=0
          python3 - <<'PYSKIP' || SKIP_FAIL=1
          import json, os, sys

          report = json.load(open('/tmp/jscpd-report/jscpd-report.json'))
          analysed = set(
              report.get('statistics', {})
              .get('formats', {})
              .get('rust', {})
              .get('sources', {})
              .keys()
          )
          cfg = json.load(open('.jscpd.json'))
          max_lines = int(cfg['maxLines'])
          max_size = cfg['maxSize']
          units = {'b': 1, 'kb': 1024, 'mb': 1024 ** 2, 'gb': 1024 ** 3}
          number = max_size.rstrip('abgkm')
          suffix = max_size[len(number):].lower() or 'b'
          max_bytes = float(number) * units[suffix]

          missing = []
          for path in open('/tmp/changed-files.txt').read().split():
              if path in analysed:
                  continue
              prepared = os.path.join('/tmp/jscpd-src', path)
              if not os.path.exists(prepared):
                  missing.append((path, 'was not prepared'))
                  continue
              lines = len(open(prepared, encoding='utf-8', errors='replace').read().split('\n'))
              size = os.path.getsize(prepared)
              if lines < 10:
                  continue  # below --min-lines; nothing to detect
              if lines > max_lines:
                  missing.append((path, f'{lines} lines exceeds maxLines {max_lines}'))
              elif size > max_bytes:
                  missing.append((path, f'{size} bytes exceeds maxSize {max_size}'))
              else:
                  missing.append((path, f'skipped by jscpd for an unknown reason ({lines} lines, {size} bytes)'))

          if missing:
              print('::error::The duplication gate did not analyse every changed file, so its verdict is not a measurement of this change (#3495).')
              for path, why in missing:
                  print(f'  {path}: {why}')
              print('Raise maxLines/maxSize in .jscpd.json, or exempt the file there deliberately.')
              sys.exit(1)
          PYSKIP
          if [ "$SKIP_FAIL" -ne 0 ]; then
            echo "### Code Duplication: FAILED (a changed file was not analysed)" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          DUP_PCT=$(python3 -c "
          import json
          data = json.load(open('/tmp/jscpd-report/jscpd-report.json'))
          stats = data.get('statistics', {})
          total = stats.get('total', {})
          pct = total.get('percentage', 0)
          dups = total.get('duplicatedLines', 0)
          lines = total.get('lines', 0)
          clones = len(data.get('duplicates', []))
          sources = len(stats.get('formats', {}).get('rust', {}).get('sources', {}))
          print(f'{pct:.1f} {dups} {lines} {clones} {sources}')
          " 2>/dev/null) || DUP_PCT="0.0 0 0 0 0"

          PCT=$(echo "$DUP_PCT" | awk '{print $1}')
          DUP_LINES=$(echo "$DUP_PCT" | awk '{print $2}')
          TOTAL_LINES=$(echo "$DUP_PCT" | awk '{print $3}')
          CLONES=$(echo "$DUP_PCT" | awk '{print $4}')
          SOURCES=$(echo "$DUP_PCT" | awk '{print $5}')

          echo "### Code Duplication: ${PCT}% (${DUP_LINES}/${TOTAL_LINES} lines, ${CLONES} clones, ${SOURCES} files analysed)" >> $GITHUB_STEP_SUMMARY
          echo "Duplication: ${PCT}% (${DUP_LINES}/${TOTAL_LINES} lines, ${CLONES} clones, ${SOURCES} files analysed)"

          # Fail if duplication exceeds 3%
          OVER=$(python3 -c "print('yes' if float('${PCT}') > 3.0 else 'no')")
          if [ "$OVER" = "yes" ]; then
            echo "::error::Code duplication is ${PCT}%, exceeding 3% threshold"

            # Show the duplicate blocks, with line numbers translated back
            # through the stripped test modules to the real file.
            python3 - <<'PYCLONES'
          import json

          data = json.load(open('/tmp/jscpd-report/jscpd-report.json'))
          line_map = json.load(open('/tmp/jscpd-linemap.json'))

          def original(name, line):
              shift = 0
              for start, end in line_map.get(name, []):
                  if start <= line + shift:
                      shift += end - start + 1
              return line + shift

          for d in data.get('duplicates', []):
              fa, sa = d['firstFile'], d['secondFile']
              a1, a2 = original(fa['name'], fa['startLoc']['line']), original(fa['name'], fa['endLoc']['line'])
              b1, b2 = original(sa['name'], sa['startLoc']['line']), original(sa['name'], sa['endLoc']['line'])
              print(f"  {fa['name']}:{a1}-{a2} <-> {sa['name']}:{b1}-{b2}")
          print("Clones may pre-date this change: the gate measures whole changed FILES, not changed lines (#1619).")
          PYCLONES

            exit 1
          fi

      - name: Upload coverage artifact
        if: always() && steps.detect.outputs.rust_changed == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: lcov-coverage
          path: lcov.info

  # ════════════════════════════════════════════════════════════════════════════
  # TIER 2: INTEGRATION (main/release push + every backend-touching PR)
  # ════════════════════════════════════════════════════════════════════════════

  # Until #3124 this job was gated to `main` / `release/*` pushes only, so it
  # could not gate a pull request: a PR could break every integration test and
  # still show all-green, with the breakage surfacing only after merge on a
  # `main` push that blocks nothing and notifies nobody. That is exactly how
  # #2905 shipped — its own CI shows `🔗 Backend Integration Tests` SKIPPING
  # next to a green `✅ CI Complete` — and `main` stayed red for 11 days
  # (#3118).
  #
  # The push arm is unchanged. The new arm runs the same suite, unmodified, on
  # pull requests that touch backend code (`needs.changes.outputs.backend`).
  # Non-backend PRs — web, docs, scripts, IaC — still skip it and pay nothing.
  #
  # Fails open to RUNNING: the PR arm tests `!= 'false'`, so if the `changes`
  # job dies outright and the output is empty the suite runs anyway. The
  # matching enforcement is in `ci-complete`, which fails closed: on a
  # backend-touching PR a skipped integration job is a hard failure, not a
  # pass.
  test-backend-integration:
    name: 🔗 Backend Integration Tests
    runs-on: ak-ci-runners
    timeout-minutes: 40
    needs: [changes, release-bump]
    if: >-
      ${{ !cancelled()
          && needs.release-bump.outputs.bump_only != 'true'
          && (
               (
                 (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/heads/release/'))
                 && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
               )
               || (github.event_name == 'pull_request' && needs.changes.outputs.backend != 'false')
             ) }}
    # Disable incremental codegen (#1679): consistent with check-rust and the
    # other heavy Rust compile jobs. Useless in CI's clean checkout, inflates
    # peak RSS.
    env:
      CARGO_INCREMENTAL: "0"
      # This job carries no rust-cache (see the sccache note below), so it
      # resolves and downloads the whole dependency graph from crates.io on
      # every run — more registry exposure than any other Rust job here. Now
      # that a failure blocks a PR rather than just reddening a `main` push,
      # a transient registry hiccup costs a developer a rerun, so let cargo
      # retry the fetch itself before the step fails.
      CARGO_NET_RETRY: "10"
    services:
      postgres:
        # Use the GHCR mirror to avoid Docker Hub anonymous-pull rate limits.
        image: ghcr.io/artifact-keeper/ci-mirror/postgres:16-alpine
        env:
          POSTGRES_USER: registry
          POSTGRES_PASSWORD: registry
          POSTGRES_DB: artifact_registry
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Shield this job from concurrent mutation of the shared registry —
      # see the #3154 comment on the same step in check-rust. The
      # "stale/truncated crates.io index" failure documented on the fetch
      # step below is the same corruption class (a sibling pod rewriting
      # the shared registry index mid-read). This job carries no
      # rust-cache, so a private CARGO_HOME means it now downloads the
      # dependency graph from crates.io each run instead of reading the
      # shared warm registry; CARGO_NET_RETRY and the fetch retry below
      # already absorb network hiccups, and correctness beats the ~1-2
      # minutes of download.
      - name: Isolate CARGO_HOME per job (#3154)
        run: echo "CARGO_HOME=$RUNNER_TEMP/cargo-home" >> "$GITHUB_ENV"

      # Resolve and download the dependency graph in its own step, ahead of the
      # test steps, with a retry.
      #
      # Why: on 2026-08-07 this job failed on `main` (run 31176621357) with
      #   error: failed to select a version for the requirement `axum = "^0.7"`
      #          (locked to 0.7.9)
      #   candidate versions found which didn't match: 0.6.6, 0.6.5, ...
      # i.e. cargo saw a stale/truncated crates.io index. No test ever ran.
      # That was the only non-test failure of this job on `main` in the last
      # 13 runs, and under this PR the same hiccup would red a developer's PR
      # instead of a post-merge push.
      #
      # This retries the *resolution and download* only. It deliberately does
      # NOT wrap `cargo test`: retrying the test steps would mask exactly the
      # real regressions this gate exists to catch. A genuine dependency
      # problem still fails after the last attempt.
      #
      # No `--locked`: the test steps below do not use it either, so adding it
      # here would newly fail PRs with a legitimately-updated Cargo.lock.
      - name: Fetch dependencies
        run: |
          set -euo pipefail
          for attempt in 1 2 3; do
            if cargo fetch; then
              exit 0
            fi
            echo "cargo fetch failed (attempt ${attempt}/3) — retrying" >&2
            sleep $(( attempt * 10 ))
          done
          echo "ERROR: cargo fetch failed after 3 attempts" >&2
          exit 1

      # The integration suites under `backend/tests/` insert into real tables
      # (users, api_tokens, repositories, ...) via the SQLx pool. The service
      # container above starts an empty Postgres, so we MUST apply migrations
      # before running tests or the suites panic with `relation "users" does
      # not exist` (regression after #1190 added api_token_cache_flush_tests
      # to this job). Matches the `coverage` job's pattern: sqlx-cli is
      # pre-installed on the `ak-ci-runners` image (see
      # artifact-keeper-iac/runner-images/rust/Dockerfile).
      - name: Wait for Postgres
        env:
          PGPASSWORD: registry
        run: |
          set -euo pipefail
          # The service container's --health-cmd already runs pg_isready
          # internally, but GitHub Actions starts the step before health
          # transitions to "healthy" on slower runners. A short readiness
          # loop from the runner side avoids a flaky first connect. Uses a
          # plain TCP probe via bash so we do not depend on postgresql-client
          # being installed on the runner image.
          for i in $(seq 1 30); do
            if (echo > /dev/tcp/localhost/5432) >/dev/null 2>&1; then
              echo "Postgres reachable on attempt $i"
              exit 0
            fi
            echo "  waiting for postgres... ($i/30)"
            sleep 1
          done
          echo "ERROR: Postgres did not become reachable within 30s"
          exit 1

      - name: Run database migrations
        env:
          DATABASE_URL: postgresql://registry:registry@localhost:5432/artifact_registry
        run: sqlx migrate run --source backend/migrations

      # `-- --ignored` runs the postgres-backed integration suites under
      # `backend/tests/` that were silently skipped before (#1031). The job
      # provisions postgres but NOT a running HTTP backend, Meilisearch,
      # Trivy, or live cloud credentials, so the suite list below is
      # restricted to tests that need only a Postgres connection:
      #
      #   - lifecycle_policy_tests, api_token_cache_flush_tests,
      #     search_reindex_tests, scan_convert_to_reused_tests,
      #     virtual_members_atomicity_test, virtual_members_duplicate_test,
      #     grpc_sbom_tests (in-process gRPC, no HTTP server),
      #     oci_chunked_upload_cross_repo_tests (#1317 cross-repo session
      #     rejection regression),
      #     sso_oidc_login_callback_e2e_tests (#1617 OIDC login/callback
      #     regression harness; drives the real sso handlers against an
      #     in-process wiremock IdP bound to a non-loopback interface),
      #     sso_saml_acs_e2e_tests (#2212 SAML ACS regression harness; drives
      #     the real saml_login/saml_acs handlers against bergshamra-signed
      #     assertions — no outbound fetch, so no wiremock/SSRF dance).
      #
      # Deliberately excluded (would panic on first .expect() without
      # extra infra, so `--ignored` stays the right gate):
      #
      #   - azure_rbac_live_test, azure_shared_key_live_test, s3_integration
      #     (live cloud creds via std::env::var().expect)
      #   - integration_tests, replication_integration,
      #     tag_filtered_replication_tests, storage_backend_tests
      #     (read TEST_BASE_URL, need a running HTTP backend)
      #   - incus_upload_tests (most cases construct the router without the
      #     optional_auth middleware, so handler `Extension<AuthExtension>`
      #     extracts panic with a 500. Only
      #     `test_chunked_upload_cross_repo_session_rejected` wires the
      #     middleware in, so we run that case specifically as a named
      #     filter below to cover issue #1317 without dragging in the
      #     broken siblings).
      #
      # `--test-threads=1` because the DB-backed suites share schema state
      # through global DELETEs in their cleanup helpers and would race under
      # parallel execution.
      - name: Run integration tests
        env:
          DATABASE_URL: postgresql://registry:registry@localhost:5432/artifact_registry
          SQLX_OFFLINE: true
        run: |
          cargo test --workspace --verbose \
            --test lifecycle_policy_tests \
            --test api_token_cache_flush_tests \
            --test grpc_sbom_tests \
            --test search_reindex_tests \
            --test scan_convert_to_reused_tests \
            --test stuck_scan_janitor_tests \
            --test virtual_members_atomicity_test \
            --test virtual_members_duplicate_test \
            --test oci_chunked_upload_cross_repo_tests \
            --test sso_oidc_login_callback_e2e_tests \
            --test sso_saml_acs_e2e_tests \
            --test cache_invalidation_fanout_tests \
            --test age_gate_tests \
            --test scan_policy_validation_tests \
            --test ansible_galaxy_tests \
            -- --ignored --test-threads=1

      # `incus_upload_tests::test_chunked_upload_cross_repo_session_rejected`
      # is the only case in that file currently wired to run without an HTTP
      # server. Run it explicitly so the #1317 incus regression has matrix
      # coverage too.
      - name: Run incus cross-repo session regression
        env:
          DATABASE_URL: postgresql://registry:registry@localhost:5432/artifact_registry
          SQLX_OFFLINE: true
        run: |
          cargo test --workspace --verbose \
            --test incus_upload_tests \
            test_chunked_upload_cross_repo_session_rejected \
            -- --ignored --test-threads=1

      # Hit-rate data to evaluate dropping the GHA rust-cache from the unit/clippy jobs (this job has no rust-cache; stats give a no-GHA-cache baseline).
      - name: sccache stats
        if: always()
        run: sccache --show-stats || true

  # ════════════════════════════════════════════════════════════════════════════
  # SMOKE E2E (Every Push/PR) — self-contained via Docker-in-Docker on ARC
  # Spins up fresh backend + postgres, runs native client tests, tears down
  # ════════════════════════════════════════════════════════════════════════════

  smoke-e2e:
    name: 🔥 Smoke E2E Tests
    runs-on: ubuntu-24.04
    # Depend on the required fast-CI gates as well as the image build so a
    # known-red Check Rust or unit-test run never launches smoke. Smoke only
    # runs when all three succeeded; if the backend image was skipped (no
    # backend changes) or an upstream gate failed, smoke is skipped.
    needs: [check-rust, test-backend-unit, build-backend-image]
    if: needs.check-rust.result == 'success' && needs.test-backend-unit.result == 'success' && needs.build-backend-image.result == 'success'
    permissions:
      contents: read
      packages: read
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Wait for Docker daemon
        run: |
          echo "Waiting for DinD daemon..."
          for i in $(seq 1 30); do
            if docker info >/dev/null 2>&1; then
              echo "Docker daemon ready"
              docker version
              break
            fi
            echo "  Attempt $i/30..."
            sleep 2
          done
          docker info >/dev/null 2>&1 || { echo "ERROR: Docker daemon not available"; exit 1; }

      - name: Install Docker Compose
        run: |
          # Skip install if docker compose is already available
          if docker compose version >/dev/null 2>&1; then
            echo "Docker Compose already available:"
            docker compose version
            exit 0
          fi

          DOCKER_CONFIG=${DOCKER_CONFIG:-$HOME/.docker}
          COMPOSE_VERSION="v2.32.4"
          COMPOSE_BINARY="$DOCKER_CONFIG/cli-plugins/docker-compose"
          mkdir -p "$DOCKER_CONFIG/cli-plugins"

          echo "Downloading Docker Compose ${COMPOSE_VERSION}..."
          curl -fSL "https://github.com/docker/compose/releases/download/${COMPOSE_VERSION}/docker-compose-linux-x86_64" \
            -o "$COMPOSE_BINARY"

          # Verify download succeeded (curl -f returns non-zero on HTTP errors,
          # but also check file size as a safeguard)
          FILESIZE=$(stat -c%s "$COMPOSE_BINARY" 2>/dev/null || stat -f%z "$COMPOSE_BINARY" 2>/dev/null || echo "0")
          if [ "$FILESIZE" -lt 1000000 ]; then
            echo "ERROR: Downloaded file is ${FILESIZE} bytes (expected >60MB). Download likely failed."
            rm -f "$COMPOSE_BINARY"
            exit 1
          fi

          chmod +x "$COMPOSE_BINARY"
          docker compose version

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Download backend image from build job
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: backend-image
          path: /tmp

      - name: Load backend image
        run: |
          gunzip -c /tmp/backend-image.tar.gz | docker load
          docker tag artifact-keeper-backend:pr-test artifact-keeper-backend:test

      - name: Start smoke E2E stack
        run: docker compose -f docker-compose.test.yml --profile smoke up -d

      - name: Wait for tests to complete
        run: |
          echo "Waiting for test containers to finish..."
          timeout 480 docker wait e2e-test-pypi e2e-test-npm e2e-test-cargo e2e-test-proxy-virtual || {
            echo "ERROR: Tests timed out. Container statuses:"
            docker ps -a --filter "name=e2e-test" --format "table {{.Names}}\t{{.Status}}"
            echo ""
            echo "Backend logs (last 50 lines):"
            docker logs e2e-test-backend --tail 50 2>&1 || true
            exit 1
          }

      - name: Show test results
        if: always()
        run: |
          failed=false
          for test in pypi npm cargo proxy-virtual; do
            container="e2e-test-${test}"
            exit_code=$(docker inspect --format='{{.State.ExitCode}}' "$container" 2>/dev/null || echo "N/A")
            echo ""
            echo "═══════════════════════════════════════════"
            echo "  ${test} — Exit Code: $exit_code"
            echo "═══════════════════════════════════════════"
            docker logs "$container" 2>&1 || true
            if [ "$exit_code" != "0" ]; then failed=true; fi
          done
          if [ "$failed" = "true" ]; then exit 1; fi

      - name: Cleanup
        if: always()
        run: docker compose -f docker-compose.test.yml --profile smoke down -v --remove-orphans

  # ════════════════════════════════════════════════════════════════════════════
  # CONTAINER BUILDS (push + PR, path-filtered, parallel)
  # ════════════════════════════════════════════════════════════════════════════

  # Until #3166 this job carried `if: github.event_name == 'push'`, while the
  # section header above claimed "PR only". The header was the aspiration and
  # the `if` was the behaviour, so the whole chain hanging off this job --
  # `build-backend-image`, `build-openscap-image`, and `smoke-e2e` via the
  # image -- never ran on a pull request. All three reported `skipped`, and
  # `ci-complete`'s `allow_skip` passes a skip, so a PR could break the
  # container build or the smoke suite and still show green; the breakage
  # surfaced only after merge, on a `main` push that blocks nothing. Same
  # failure mode as #3124/#3165, different jobs.
  #
  # `dorny/paths-filter` supports `pull_request` natively -- it reads the
  # changed-file list from the API (hence `pull-requests: read`) rather than
  # from git history -- so dropping the `if` is the whole fix. Push behaviour
  # is unchanged: the same filters, evaluated the same way.
  #
  # The PR arm is deliberately ADVISORY in `ci-complete` rather than blocking.
  # See the container-chain block there for the measured reasoning and the
  # criteria for promoting it to blocking.
  detect-changes:
    name: 🔍 Detect Changes
    runs-on: ubuntu-latest
    timeout-minutes: 8
    permissions:
      contents: read
      pull-requests: read
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      openscap: ${{ steps.filter.outputs.openscap }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4
        id: filter
        with:
          filters: |
            backend:
              - 'docker/Dockerfile.backend'
              # Covers backend/src/**, backend/migrations/** and
              # backend/.sqlx/** (the crate-local query cache; the root
              # '.sqlx/**' below is the workspace one).
              - 'backend/**'
              - 'Cargo.toml'
              - 'Cargo.lock'
              - '.sqlx/**'
              # So a change to this gate exercises the gate -- the same
              # reasoning the `changes` job states for its own backend arm.
              # Without this, a ci.yml-only PR (this one included) leaves the
              # entire container chain skipped, which is precisely how #3166
              # went unnoticed: the jobs were never observed NOT running.
              - '.github/workflows/ci.yml'
            openscap:
              - 'docker/Dockerfile.openscap'
              - 'scripts/openscap-wrapper.py'

  build-backend-image:
    name: 🐳 Build Backend Image
    runs-on: ak-ci-runners
    timeout-minutes: 45
    needs: detect-changes
    if: needs.detect-changes.outputs.backend == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4

      - name: Build backend image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          file: ./docker/Dockerfile.backend
          platforms: linux/amd64
          push: false
          load: true
          tags: artifact-keeper-backend:pr-test
          # Read-only registry cache, warmed by docker-publish.yml on main.
          # No cache-to: PR-triggered jobs must not get packages:write (fork
          # PRs would inherit it — pwn-request risk), and anonymous GHCR
          # reads work because the packages are public.
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}-backend:buildcache-amd64

      - name: Verify image starts
        run: |
          docker run --rm -d --name ak-test -p 8080:8080 \
            -e DATABASE_URL=postgresql://test:test@localhost/test \
            artifact-keeper-backend:pr-test || true
          sleep 2
          docker logs ak-test 2>&1 | head -20 || true
          docker rm -f ak-test 2>/dev/null || true

      - name: Image size
        run: |
          echo "## Backend Image" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          docker images artifact-keeper-backend:pr-test --format "table {{.Repository}}\t{{.Tag}}\t{{.Size}}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      - name: Save image for smoke tests
        run: |
          docker save artifact-keeper-backend:pr-test | gzip > /tmp/backend-image.tar.gz
          ls -lh /tmp/backend-image.tar.gz

      - name: Upload image artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: backend-image
          path: /tmp/backend-image.tar.gz
          retention-days: 1

  build-openscap-image:
    name: 🐳 Build OpenSCAP Image
    runs-on: ak-ci-runners
    timeout-minutes: 30
    needs: detect-changes
    if: needs.detect-changes.outputs.openscap == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4

      - name: Build openscap image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          file: ./docker/Dockerfile.openscap
          platforms: linux/amd64
          push: false
          load: true
          tags: artifact-keeper-openscap:pr-test
          # Read-only registry cache — see backend image build for rationale.
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}-openscap:buildcache-amd64

      - name: Verify image starts
        run: |
          docker run --rm -d --name oscap-test -p 8091:8091 \
            artifact-keeper-openscap:pr-test || true
          sleep 3
          docker logs oscap-test 2>&1 | head -20 || true
          docker rm -f oscap-test 2>/dev/null || true

      - name: Image size
        run: |
          echo "## OpenSCAP Image" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          docker images artifact-keeper-openscap:pr-test --format "table {{.Repository}}\t{{.Tag}}\t{{.Size}}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

  # ════════════════════════════════════════════════════════════════════════════
  # SECURITY (Runs on all events, non-blocking)
  # ════════════════════════════════════════════════════════════════════════════

  security-audit:
    name: 🔒 Security Audit
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: changes
    # Skip on docs-only PRs — see check-rust. (Docs-only PRs never change
    # Cargo.lock, and previously did not run this job either: ci.yml did not
    # trigger at all.)
    if: ${{ !cancelled() && needs.changes.outputs.code != 'false' }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-audit
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-audit

      - name: Audit Rust dependencies
        run: cargo audit

  # ════════════════════════════════════════════════════════════════════════════
  # FINAL STATUS
  # ════════════════════════════════════════════════════════════════════════════

  ci-complete:
    name: ✅ CI Complete
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [changes, release-bump, check-rust, test-backend-unit, shell-tests, coverage, test-backend-integration, smoke-e2e, build-backend-image, build-openscap-image, security-audit]
    if: always()
    steps:
      - name: Check CI status
        # Results are passed through the environment rather than interpolated
        # directly into the script body so a job result string can never alter
        # the shell control flow.
        env:
          RESULT_CHECK_RUST: ${{ needs.check-rust.result }}
          RESULT_UNIT: ${{ needs.test-backend-unit.result }}
          RESULT_SHELL: ${{ needs.shell-tests.result }}
          RESULT_COVERAGE: ${{ needs.coverage.result }}
          RESULT_INTEGRATION: ${{ needs.test-backend-integration.result }}
          RESULT_SMOKE: ${{ needs.smoke-e2e.result }}
          RESULT_BUILD_BACKEND: ${{ needs.build-backend-image.result }}
          RESULT_BUILD_OPENSCAP: ${{ needs.build-openscap-image.result }}
          RESULT_SECURITY: ${{ needs.security-audit.result }}
          CODE_CHANGED: ${{ needs.changes.outputs.code }}
          BACKEND_CHANGED: ${{ needs.changes.outputs.backend }}
          BUMP_ONLY: ${{ needs.release-bump.outputs.bump_only }}
        run: |
          set -euo pipefail
          echo "## CI Results" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"

          failed=false

          # Docs-only PRs legitimately skip the Tier 1 jobs (see the
          # `changes` job). Version-bump-only release PRs legitimately skip
          # the heavy Rust jobs — check-rust and test-backend-unit, but NOT
          # shell-tests, which still runs on that path (see the
          # `release-bump` job). Any other skip/failure of a Tier 1 job is
          # fatal.
          docs_only=false
          if [[ "$CODE_CHANGED" == "false" ]]; then
            docs_only=true
            echo "📝 docs-only change — Tier 1 jobs skipped by design" >> "$GITHUB_STEP_SUMMARY"
          fi
          bump_only=false
          if [[ "$BUMP_ONLY" == "true" ]]; then
            bump_only=true
            echo "🔖 version-bump-only release PR — heavy Rust jobs skipped by design" >> "$GITHUB_STEP_SUMMARY"
          fi

          # Required jobs run on every event. Anything other than success,
          # including a skip or a cancellation, is a hard failure so an
          # unexpected non-run can never be reported as green — except on a
          # docs-only change, where these jobs are skipped by design.
          require_success() {
            local name="$1" result="$2"
            if [[ "$result" == "success" ]]; then
              echo "✅ $name" >> "$GITHUB_STEP_SUMMARY"
            elif [[ "$result" == "skipped" && "$docs_only" == "true" ]]; then
              echo "⏭️ $name (docs-only)" >> "$GITHUB_STEP_SUMMARY"
            elif [[ "$result" == "skipped" && "$bump_only" == "true" \
                    && ( "$name" == "check-rust" || "$name" == "test-backend-unit" ) ]]; then
              # Only the two heavy Rust jobs are skippable on the bump path.
              # shell-tests and security-audit still run and must succeed.
              echo "⏭️ $name (version-bump-only release PR)" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "❌ $name: ${result:-missing}" >> "$GITHUB_STEP_SUMMARY"
              failed=true
            fi
          }

          # Conditionally-required jobs are legitimately skipped in some events
          # (no backend changes, PR-only skips). A skip passes, but a failure or
          # cancellation fails so an aborted upstream job is never masked.
          #
          # #3166 asked, for every allow_skip call site: can this job be skipped
          # for a reason that should NOT pass? Current answers:
          #   - integration-tests: YES, and that is handled below (#3124) --
          #     on a backend-touching PR a skip is a hard failure.
          #   - smoke-e2e / build-backend-image / build-openscap-image: the
          #     skip itself is legitimate (nothing container-shaped changed).
          #     The bug was never the skip semantics; it was that the jobs had
          #     no way to run on a PR at all. #3166 fixes that in
          #     `detect-changes`.
          allow_skip() {
            local name="$1" result="$2"
            if [[ "$result" == "success" ]]; then
              echo "✅ $name" >> "$GITHUB_STEP_SUMMARY"
            elif [[ "$result" == "skipped" ]]; then
              echo "⏭️ $name (skipped)" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "❌ $name: ${result:-missing}" >> "$GITHUB_STEP_SUMMARY"
              failed=true
            fi
          }

          # Reports a real result without blocking. Distinct from allow_skip:
          # a failure is surfaced loudly in the summary but does not set
          # `failed`. Used only for the container chain on pull requests.
          advisory() {
            local name="$1" result="$2"
            if [[ "$result" == "success" ]]; then
              echo "✅ $name" >> "$GITHUB_STEP_SUMMARY"
            elif [[ "$result" == "skipped" ]]; then
              echo "⏭️ $name (skipped)" >> "$GITHUB_STEP_SUMMARY"
            else
              echo "⚠️ $name: ${result:-missing} (advisory — not blocking)" \
                >> "$GITHUB_STEP_SUMMARY"
            fi
          }

          require_success "check-rust" "$RESULT_CHECK_RUST"
          require_success "test-backend-unit" "$RESULT_UNIT"
          require_success "shell-tests" "$RESULT_SHELL"
          require_success "security-audit" "$RESULT_SECURITY"

          # Integration tests are REQUIRED on a pull request that touches
          # backend code (#3124). A skip there is precisely the hole that let
          # #2905 merge green and leave `main` red for 11 days (#3118), so it
          # must fail rather than pass. Fails closed: an empty BACKEND_CHANGED
          # (the `changes` job itself died) counts as backend-touching, which
          # matches the job's own fail-open-to-running condition.
          #
          # A skip still passes everywhere else: docs-only and version-bump-only
          # PRs, PRs that touch no backend path, and pushes to branches other
          # than main / release/*, none of which schedule the job.
          if [[ "$GITHUB_EVENT_NAME" == "pull_request" \
                && "$docs_only" != "true" && "$bump_only" != "true" \
                && "$BACKEND_CHANGED" != "false" ]]; then
            require_success "integration-tests" "$RESULT_INTEGRATION"
          else
            allow_skip "integration-tests" "$RESULT_INTEGRATION"
          fi

          # Container chain (build-backend-image -> smoke-e2e, plus the
          # OpenSCAP image). As of #3166 these finally RUN on pull requests.
          # On a PR they are ADVISORY; on push they keep the blocking
          # allow_skip semantics they have always had, so nothing about the
          # `main` signal is weakened.
          #
          # Why advisory first, rather than making them required immediately:
          # `build-backend-image` and `build-openscap-image` run on
          # `ak-ci-runners`, a self-hosted pool that is already the binding
          # constraint on this repo's CI. Measured over the last 37 runs of
          # this workflow, queue wait (job created -> job started) on that
          # pool is:
          #
          #     check-rust                median  66m   p90 316m   max 416m
          #     coverage                  median  53m   p90 289m   max 453m
          #     test-backend-unit         median  25m   p90 276m   max 412m
          #     test-backend-integration  median   0m   p90  74m   max 342m
          #
          # The pool already carries check-rust, test-backend-unit, coverage,
          # test-backend-integration (added to every backend PR by #3165) and
          # build-windows-dev, and is shared with docker-publish.yml. The
          # image build itself is not slow -- 14.9m / 14.9m / 17.4m over its
          # last three successful runs, well inside its 45m timeout, and
          # smoke-e2e is ~2m on a GitHub-hosted runner -- but adding a ~15m
          # job to that queue on every backend-touching PR, and then making a
          # PR unmergeable until it clears a queue whose p90 wait is over five
          # hours, is how a well-intentioned gate becomes a merge-queue jam.
          #
          # Advisory still fixes the reported defect: the jobs run, and a
          # broken container build or smoke suite is now visible ON THE PR
          # instead of being discovered after merge. What it does not yet do
          # is block.
          #
          # Promote to blocking by deleting the `pull_request` branch below
          # (one line) once EITHER holds:
          #   1. ak-ci-runners p90 queue wait is under ~15m for a sustained
          #      week, or
          #   2. build-backend-image moves off ak-ci-runners onto a
          #      GitHub-hosted runner, where it competes with nothing.
          if [[ "$GITHUB_EVENT_NAME" == "pull_request" ]]; then
            advisory "smoke-e2e" "$RESULT_SMOKE"
            advisory "build-backend-image" "$RESULT_BUILD_BACKEND"
            advisory "build-openscap-image" "$RESULT_BUILD_OPENSCAP"
          else
            allow_skip "smoke-e2e" "$RESULT_SMOKE"
            allow_skip "build-backend-image" "$RESULT_BUILD_BACKEND"
            allow_skip "build-openscap-image" "$RESULT_BUILD_OPENSCAP"
          fi

          # Coverage is non-blocking.
          if [[ "$RESULT_COVERAGE" == "success" ]]; then
            echo "✅ coverage" >> "$GITHUB_STEP_SUMMARY"
          elif [[ "$RESULT_COVERAGE" == "skipped" && ( "$docs_only" == "true" || "$bump_only" == "true" ) ]]; then
            echo "⏭️ coverage (skipped by gate)" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "⚠️ coverage: ${RESULT_COVERAGE:-missing}" >> "$GITHUB_STEP_SUMMARY"
          fi

          echo "" >> "$GITHUB_STEP_SUMMARY"

          if [[ "$failed" == "true" ]]; then
            echo "❌ **CI Failed**" >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi

          echo "✅ **CI Passed**" >> "$GITHUB_STEP_SUMMARY"

  # ════════════════════════════════════════════════════════════════════════════
  # WINDOWS DEV BUILD (main push only)
  # ════════════════════════════════════════════════════════════════════════════

  build-windows-dev:
    name: 🪟 Windows Cross-Build
    runs-on: ak-ci-runners
    timeout-minutes: 30
    # Only build Windows on main push (not PRs).
    # The release workflow builds Windows binaries for actual releases.
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # Shield this job from concurrent mutation of the shared registry —
      # see the #3154 comment on the same step in check-rust. No rust-cache
      # here either (cf. the integration job note): the graph is downloaded
      # per run, which this main-push-only job can afford.
      - name: Isolate CARGO_HOME per job (#3154)
        run: echo "CARGO_HOME=$RUNNER_TEMP/cargo-home" >> "$GITHUB_ENV"

      - name: Build Windows binary (cross-compile)
        env:
          SQLX_OFFLINE: true
          CARGO_TERM_COLOR: always
          CARGO_TARGET_X86_64_PC_WINDOWS_GNU_LINKER: x86_64-w64-mingw32-gcc
          # Bypass sccache while the ak-ci-runners /cache volume / sccache server
          # is unavailable (see check-rust) — it crashed the cross-compile at
          # startup. Revert once the runner cache is restored.
          RUSTC_WRAPPER: ""
        run: cargo build --release --target x86_64-pc-windows-gnu --features vendored-openssl

      - name: Package binary
        run: |
          mkdir -p dist
          cp target/x86_64-pc-windows-gnu/release/artifact-keeper.exe dist/artifact-keeper-windows-amd64.exe
          cd dist
          sha256sum artifact-keeper-windows-amd64.exe > artifact-keeper-windows-amd64.exe.sha256
          echo "Built: $(ls -lh artifact-keeper-windows-amd64.exe | awk '{print $5}')"

      - name: Upload artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: artifact-keeper-windows-amd64
          path: |
            dist/artifact-keeper-windows-amd64.exe
            dist/artifact-keeper-windows-amd64.exe.sha256

      # Hit-rate data to evaluate dropping the GHA rust-cache from the unit/clippy jobs (this job has no rust-cache; stats give a no-GHA-cache baseline).
      - name: sccache stats
        if: always()
        run: sccache --show-stats || true
