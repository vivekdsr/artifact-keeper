# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Security

- **A backup archive's integrity check could be switched off by editing the archive** (#3373). #3011 gave every archive a payload checksum and made `restore` verify it before ingesting a row — but the expected value travelled inside the very file it was supposed to authenticate, and `verify_archive_checksum` returned early whenever `manifest.checksum` was empty. Blanking one JSON field disabled the control; deleting `manifest.json` outright disabled it the same way, because `read_manifest` returning `None` skipped the call entirely. That is the case integrity checks exist for: restore sits behind `admin_middleware`, so the caller is already an admin, and what is untrusted is **the archive** — one tampered with at rest, or pulled back from compromised or third-party storage. `ALLOWED_EXPORT_TABLES` necessarily contains `users`, `roles`, `user_roles`, `permission_grants` and `api_tokens`, so a crafted `database/users.json` carrying `is_admin: true` was ingested and the restore reported `errors: []`. Reproduced against a database on the parent commit: the crafted admin row landed and the call returned success.

  The expected digest now lives **outside** the archive. Every completed backup records its payload SHA-256 on the `backups` row (`payload_checksum`, migration 208), and a restore checks the archive it read back against it before anything is ingested. `restore` resolves its archive through a `backups` row and there is no path that ingests a foreign archive, so that row is always available and is not reachable from the archive bytes. An archive rewritten to be internally consistent — new payload, manifest regenerated over it — passes every check that lives inside itself and still fails this one. The archive length already recorded in `size_bytes` is checked first, but only as a cheap pre-filter that refuses the common case without decompressing: gzip tolerates arbitrary trailing bytes, so a tamperer can pad an archive to exactly the recorded length. The digest is what decides.

  Archives captured before this release have no recorded digest and fall back to the checksum in their own manifest, which detects corruption but not tampering; the restore reports which anchor it used (`integrity_anchor`: `recorded`, `manifest` or `waived`) and records it on the audit event. An archive carrying neither is no longer silently accepted: it is refused unless the caller sets `allow_unverified_archive`, which is an explicit, audited operator decision and never waives a checksum that is present and does not match. A restore on either of the weaker anchors also says so in its `errors` list rather than returning a clean-looking empty one, because until you re-run your backups every existing archive is on the `manifest` anchor — which is exactly where the original weakness still lives. **Re-run your backups after upgrading** so their digests are recorded; until then, the guidance in the 1.8.1 notes still applies — restore only archives you produced and can vouch for.

  Two related gaps in the same path close with it. `extract_entries` decompressed an archive with no bound of any kind: measured on the parent commit, a 521,905-byte archive expanded to 536,870,912 bytes (1028x) and was returned in full, and a 6,252,652-byte archive drove a deployed backend to 4.596 GiB resident before it had checked anything about that archive. Extraction is now bounded by entry count and by total decompressed bytes, with the byte budget wrapped around the decoded stream **before** the tar reader sees it — which is the only place it can be complete. `tar` consumes GNU LongName/LongLink and PAX extended headers inside the entry iterator's own `next()`, reading the full size the record declares, so any bound applied to the entries the iterator yields never sees those bytes at all; and `build_backup_tar` emits GNU LongLink for every path over 100 characters (#758), so these records are in ordinary archives, not just crafted ones. Measured through the real entry point at the default limits: a 1,043,854-byte archive declaring a 1 GiB long name took the process to +2118 MiB and returned success with a bound that read only the entries, and is refused at +132 MiB with the budget on the reader. This is the same `util::bounded_archive` budget the other ~20 extractors in the codebase already use.

  The ceiling is `clamp(compressed_len x 100, 32 MiB, 8 GiB)`: the floor so a small legitimate backup is not refused for being small, the ratio so a large one is not refused for being large, and the absolute cap because the length the ratio scales from is one a padded archive can choose. 8 GiB is a ceiling on what this restore path can do at all — it holds the compressed archive and every decompressed entry in memory at once — rather than a policy about backup size; archives beyond it need a streaming restore, not a larger number.

  Entry paths that are absolute or contain `..` are refused at extraction rather than relied on being sanitised downstream. On the filesystem backend `key_to_path` does strip them, and on S3 (`object_store` percent-encodes `.` and `..`) and GCS (`urlencoding::encode`) the key is encoded — but the **Azure** backend interpolates the raw key straight into the blob URL (`blob_url`, `storage/azure.rs`), and URL parsing removes dot segments, so a `..` in a restored artifact key resolves out of the configured container. On that backend this was a live cross-container write, not defence in depth. `tar::Builder` refuses to write such a path, so no archive this product produced contains one.

  Finally, `target_repository_id` was accepted by `POST /api/v1/admin/backups/{id}/restore`, carried on `RestoreOptions`, and read by nothing: a caller who scoped a restore to one repository got a full global ingest of the identity tables and a `200`. It is now refused with a `400` rather than silently ignored.

  Manifest **signing** — the third box on #3373 — is deliberately not in this change. It needs key generation, storage, rotation and a verification trust store, which is a design rather than a patch, and #2466 already owns bundle signing and provenance for the air-gap bundle format; a second scheme invented here would leave the product with two incompatible ways to sign an archive. Signing is what is needed when an archive moves between instances, which is exactly #2466's scope; the recorded digest is what is needed for the threat this issue describes, and that is what ships here.

- **An empty repository-key segment let an unauthenticated caller make the server buffer a whole upload in memory** (#3508). `repo_visibility_middleware` short-circuits when the extracted repository key is empty, and since #3444 that early return handed the request to the format handler with an anonymous `Option<AuthExtension>` inserted. It runs *before* the middleware's own gates, so the #508 write-auth gate — 129 lines further down — never ran for those paths; the handler did, and its extractors evaluate in order, so a body extractor (`Bytes`, `Multipart`) buffered the ENTIRE request body before the handler's in-line `require_auth` could reject it. The 401 the caller finally received was therefore emitted only after the server had read and allocated the upload.

  That is an unauthenticated memory-exhaustion primitive: no credential, no repository, no configuration. The ceiling per request is `MAX_UPLOAD_SIZE` (`MAX_UPLOAD_SIZE`, 10 GiB by default) and the number of them in flight is bounded only by `GLOBAL_MAX_CONCURRENCY` (512 by default). Measured on a build of the parent commit, backend restarted between runs: an idle 37 MiB backend peaked at 1070 MiB for one anonymous 512 MiB `PUT /npm//pkg` and at 7045 MiB for one anonymous 4 GiB request, and the cost is linear in concurrent requests. Every format prefix reaches the branch (`/npm//pkg`, `/maven//`, `/pypi//`, `/cargo//api/v1/crates/new`, `/api/cargo//…`, `/gems//api/v1/gems`, `/conda//upload`, `/debian//upload`, `/lfs//objects/<oid>`, and the rest — 22 of 36 probed prefixes buffered a 64 MiB anonymous body). Two of those deserve separate mention: `POST /conda/t/upload` is a **single-slash** path, so a reverse proxy that collapses `//` does not filter it, and `/lfs/` is in `BYTE_TRANSFER_PREFIXES`, which exempts it from the global request timeout — so that arm can also be drip-fed to hold the allocation open.

  An empty key names no repository — repository keys are non-empty by construction, so no row can ever match and no format route can serve anything there. The middleware now answers that branch itself with the same existence-hiding 404 the no-repo branch produces, instead of running the handler. Nothing reads the body, and the #3443 defect the fall-through was added for stays fixed for the stronger reason that the handler (and its strict `Extension` extractor) is never invoked at all, so there is no unauthenticated 500 and no internal type path in a response. Authorization outcomes on non-empty keys are untouched.

  One key that only *looked* empty is corrected alongside it, because the new 404 would otherwise apply to a real repository: `extract_repo_key` skipped the conda token-channel pair `t/<TOKEN>` whenever a conda path's second segment was `t`, including on two-segment paths like `/conda/t/upload` or `/conda/t/channeldata.json` that are the plain conda routes for a repository whose key is literally `t`. Those requests resolved to an empty key, so the repository got no visibility, token-scope or permission check and the caller's credential was replaced with the anonymous value (they answered 401 to a valid credential on `main`, and 500 before #3444 — they have never worked). The skip now applies only when a segment actually follows the token, which is the only shape a real token channel has. Its sibling `extract_conda_url_token` got the same shape test: it read the third segment of any `/conda/t/...` path as a URL-embedded credential, so an anonymous read of a repository keyed `t` was rejected as an *invalid credential* it never sent. Real token channels (`/conda/t/<TOKEN>/<channel>/…`) are unchanged in both.

- **The `/api/helm` and `/api/cargo` alias routes evaluated authorization against the wrong repository** (#3443, #2941, #3000). Both aliases are mounted (#3392): `/api/helm` for the ChartMuseum `cm-push` plugin, which POSTs to `{host}/api{repo_path}/charts`, and `/api/cargo` for the sparse index the generated cargo config points at. Their paths are therefore `/api/{format}/{repo_key}/...`, but `extract_repo_key` models a format route as `/{format}/{repo_key}/...` and returns the second segment — so `repo_visibility_middleware` was handed the literal `"helm"` or `"cargo"` as the repository key, while axum strips the nest prefix and hands the handler the *third* segment. The middleware checked one repository and the handler served another.

  With no repository named `cargo` or `helm` present, that split fails closed and is only the compatibility defect it was reported as (#3443): nothing resolves, the middleware answers from its no-repo branch — 401 for an anonymous caller, existence-hiding 404 for a valid credential — and both aliases are mounted but unreachable, so `helm cm-push` and `cargo publish` against the generated sparse-index URL still fail. **When a repository whose key is exactly `cargo` or `helm` does exist, the same split is an authorization bypass.** Those are accepted repository keys and they are the names an operator naturally gives a cargo or a Helm repository. Every gate the middleware owns — visibility, API-token repository scope (#504), and the write/delete permission gate (#508 / #2603) — was evaluated against that repository while the handler served the one named in the third segment:

  - with a **public** repository named `cargo`, an anonymous caller read a **private** repository's sparse index (`/api/cargo/{private}/config.json`, `/api/cargo/{private}/ve/ri/{crate}`) and downloaded its `.crate` bytes (`/api/cargo/{private}/api/v1/crates/{name}/{version}/download`);
  - a token scoped (`allowed_repo_ids`) to the repository `cargo` published into any other repository through `PUT /api/cargo/{other}/api/v1/crates/new`;
  - a token scoped to the repository `helm` deleted charts out of any other repository through `DELETE /api/helm/{other}/charts/{name}/{version}`, and `POST /api/helm/{other}/charts` answered `409 already exists` — an existence oracle over repositories the caller cannot see.

  The identical requests on the native `/cargo/{repo}` and `/helm/{repo}` mounts were refused (401/403) throughout; only the alias path lost the ceiling. This is the same family as the `/ext` WASM-proxy bypass (GHSA-9rqp-mgmw-5879) and the raw-segment key of GHSA-fv45-mwhh-q23r: a mount whose repository key is not the second segment, extracted as if it were.

  **Am I affected:** `v1.8.0`, `v1.8.1-rc.1` and `v1.8.1` carry the `/api/{cargo,helm}` mounts; earlier releases have no such route and are not affected. On an affected version, exposure requires a repository whose key is exactly `cargo` or `helm` — `SELECT key, is_public FROM repositories WHERE key IN ('cargo', 'helm');` answers the question, and renaming that repository closes the bypass without upgrading. The anonymous read half additionally requires that repository to be public. Treat any repository-scoped token that has been used against `/api/cargo` or `/api/helm` while such a repository existed as having held its owner's reach over every repository reachable through that alias, for the period it was valid.

  The extraction now skips the `/api` prefix for exactly those two format names, so the visibility, token-scope and permission checks evaluate the same repository key the handler resolves. The predicate enumerates the two aliased formats rather than treating any `/api/{x}` as a format mount: `/api/v1` is the REST API and its second segment is a resource name, not a repository key, and a broad rule would silently reclassify it. The two exceptions already living in the function — the conda `/conda/t/{token}/{channel}/...` token channel and the `/ext/{format_key}/{repo_key}/...` WASM proxy — have the same shape for the same reason. `/api/v1/...`, `/api/packages/...` and the native mounts are unchanged, including a repository literally named `cargo` or `helm`, which resolves correctly on both its native and its alias path.
- **The LDAP login endpoint no longer reveals whether a username exists in the directory** (#3371). `POST /api/v1/auth/sso/ldap/{id}/login` is unauthenticated, and `AppError::Authentication` passes its message straight through to the response body, so the wording of a failed login was visible to anyone. On the search-then-bind path a username with no matching directory entry answered `401 {"message":"User not found in LDAP"}` while an existing username with the wrong password answered `401 {"message":"Invalid credentials"}` — a user-enumeration oracle that needs no credentials and no rate-limit evasion beyond the login limiter: an attacker walks a username list, keeps the accounts the directory confirms, and sprays passwords at a verified target set instead of a guessed one. Every credential-level arm — absent entry, rejected bind, empty input — now returns the identical body, and the username, the base DN and the underlying LDAP error stay in the server log at WARN under the `security` target where operators debugging a user filter still have them.

  Affected: deployments with an LDAP/AD provider configured **and** a service account set (`bind_dn` + `bind_password`), which selects search-then-bind; the direct-bind fallback used when no service account is configured already answered uniformly because both cases fail at the same bind. All versions through 1.8.1 carry the leak. No configuration change is needed and no credentials were exposed — the leak is account existence, not authentication material.

  **Infrastructure failures are deliberately not folded into the generic message.** A connection failure, a failed search and an authentication timeout stay `AppError::Internal` (5xx), so a directory outage still reads as an outage to operators instead of being reported to every user as a bad password; a regression test pins that distinction from the failing side. Two related items are **not** addressed here and are tracked separately: the residual timing difference between the two paths (an absent user short-circuits before the second bind; measured at roughly 1 ms against a same-host directory, with the distributions overlapping), and the same class of oracle on the local login endpoint, where a username belonging to a federated account answers `Use SSO provider to authenticate` and a locked account answers with the lockout message, both only ever for usernames that exist.

### Changed

- **`:latest` and the floating `X.Y` tags now move only after the release gate has certified the bytes they name** (#3540). `docker-publish.yml` runs on the tag push, which is necessarily *before* the gate — it is what builds the images the gate tests — so a `type=raw,value=latest` line in a merge job moved `:latest` before any verdict existed. On 1.8.1 the publish finished at 14:59:49 and the release published at 16:17:54: `:latest` named gate-uncertified bytes for 78 minutes on a cut where nothing went wrong. The same three merge jobs are parallel siblings, so a floating tag also moved as soon as *its own* image passed, whatever happened to the other two — the shape that left `backend:1.7.2` and `backend:latest` publicly pullable for hours for a version that was then retired with the gate never run.

  A tag push now publishes only the immutable `:X.Y.Z`. Every floating tag is written by one job that fans in from all three merge jobs, and for the AK-versioned images that job runs only in the post-gate promotion `release.yml` dispatches once the release gate and the GitHub Release have both succeeded. The promotion rebuilds nothing: it re-points `:latest` and `:X.Y` at the manifest-list digest `:X.Y.Z` already names, so the digest is unchanged and the cosign signature and provenance attestation bound to it keep covering it. The release job then asserts that both tags, on ghcr.io and Docker Hub, resolve to the digest the gate tested.

  Which floating tags may move is decided by the published-release set rather than by the tag being pushed: a floating tag may only name a version with a published, non-draft, non-prerelease GitHub Release, and only if that version is newest in the line the tag represents. A backport therefore advances `:1.7` and leaves `:latest` alone, and a version whose images exist but whose release was never published — the 1.7.2/1.7.5 shape — cannot be named by any floating tag at all.

  **The failure mode this creates is deliberate: when the gate fails, `:latest` and `:X.Y` stay on the previous release.** After a failed cut the registry looks like the release did not happen — `:X.Y.Z` exists and is immutable, but nothing an operator pulls by default has changed. That is the intended end state, and it is also the rollback: to move `:latest` back off a bad release that did publish, delete or mark that GitHub Release as a prerelease and re-dispatch the promotion for the previous version. Prereleases keep taking no floating tag, and the standalone recovery dispatch (`-f promote_version=X.Y.Z` with no `promote_floating`) keeps its existing behaviour exactly: mirror ghcr's current state, move nothing.

  The scanner-adapter's own floating line (`X.Y`/`X`/`latest`) gains the sibling barrier but is deliberately still applied at publish time: `:1` is required by `verify-images-published` before the release job runs, and those tags intentionally follow an unchanged-source rebuild that the adapter's exact tag does not (#2391). That gap is recorded on the issue rather than closed here.

### Fixed

- **Native format routes refused reads a principal was entitled to, and misreported the refusal as a missing member configuration** (#3452, #3387, #3386). Two independent defects on the same request path, reported three times.

  The first is an authorization gate that disagreed with every other gate in the codebase. `repo_visibility_middleware`'s READ arm resolved a non-admin's access from the `permissions` table alone (`check_permission`), while the same middleware's write/delete arm, the ~23 REST read surfaces behind `require_visible`, and the virtual-member filter `try_authorize_virtual_members` all resolve through `check_repository_action` — which additionally honours a `role_assignment` when no fine-grained rule applies to the principal. So writing the FIRST fine-grained rule against a repository, for any principal including a completely unrelated one, silently revoked native-protocol READ for every principal whose grant is a role assignment: the creator auto-grant, `repository-owner`, and the rows migration 172 wrote on upgrade. Reproduced on a clean instance: with an unchanged `developer` role assignment, `GET /maven/{repo}/…/x.pom` went 200 → **403** the moment an unrelated user was granted `read`, while `PUT` to the same path stayed 201 and `GET /api/v1/repositories/{key}` stayed 200 — the same principal could write a repository it could not read. The read arm now consults `check_repository_action` as well. This can only widen, never narrow: an applicable rule carrying the action (or `admin`) already satisfied the previous two checks. What is added is `check_repository_action`'s rule in full — **(a)** a role assignment carrying `admin` wins over everything, including an applicable rule; **(b)** otherwise an applicable direct/group/project rule is authoritative for the principals it names; **(c)** otherwise the principal keeps its role capabilities. An applicable `{write}` rule therefore still denies `read` to a `developer`, but **not** to a `repository-owner`: that role carries `admin`, and `check_repository_action` OR-s the admin-role term outside its rule-authoritative `CASE`. The practical consequence, stated because it is easy to miss — **`POST /api/v1/permissions` cannot narrow a repository owner's read on the native routes**, and `repository-owner` is auto-granted to every repository creator. That is not introduced here: the write/delete arm of the same middleware, `require_visible` and `try_authorize_virtual_members` have all resolved through this function since #3331, and `GET /api/v1/artifacts/…/download` already answered 200 for exactly that principal before this change. Revoking an owner means removing the role assignment, not writing a narrower rule. The two cached calls are kept ahead of the canonical query to short-circuit the population that IS named by a rule; note that for the role-assignment principal this change unblocks, `check_permission` returns false every time, so the uncached query does run on their allowed reads too (~0.5 ms cold, ~0 warm).

  The second is the message. A virtual-repository member walk that resolves to zero usable members has two causes — no member rows, or every member removed by caller authorization — and they were reported with three different bodies across two content types: maven answered `text/plain` `Virtual repository has no members`, npm and pypi answered `application/json` `{"code":"NOT_FOUND","message":"Virtual repository has no members"}`, and the download primitive's filtered arm answered `Artifact not found in any member repository`. That divergence broke two things. It left open the existence oracle `resolve_virtual_download`'s own comment claimed to close — only the filtered arm had been changed, so "this virtual is empty" and "this virtual aggregates a repository you may not see" stayed distinguishable. And `has no members` names a *configuration* fault, so an operator whose members are configured correctly re-checks them through the admin API, finds them present, and concludes the member resolver is broken. Every one of these paths (maven, npm, pypi, cargo, composer, and the shared download/metadata primitives) now returns one constant, `Virtual repository has no accessible members`, in the JSON error envelope on every format — including PyPI's two hand-rolled member walks (`serve_file` and the PEP 658 `.metadata` route), which bypass those primitives and were a live oracle of their own. The adjacent "members were visible, the artifact was not there" case keeps its own distinct message — a caller reaching it has already learned a member is visible to it — but that message, too, now renders as the JSON envelope on every format instead of `text/plain` on maven and JSON on pypi. The response body is identical by construction; the two causes remain distinguishable by TIMING (the filtered arm does the member-filter work the empty arm skips, ~2x median), which is structural and pre-existing and is not addressed here. The distinguishing detail moved to the server log, which is where it is safe: the member filter records how many members it dropped and why, and the middleware now states its decision on the existence-hiding 404 for a private repository the caller holds no grant on — previously neither the response nor the log said which of the two it was.

  #3386 ("service user ACL is reset on every update") is the same defect seen from the upgrade side and is closed as a duplicate: no ACL row is lost. Verified live — a redeploy that re-runs migrations leaves `permissions`, `role_assignments` and `user_group_members` byte-for-byte unchanged, `PATCH /api/v1/service-accounts/{id}` writes only `display_name`/`is_active`, and no migration deletes or rewrites any of the three (172 is purely additive). What changed across the upgrade was the evaluation above; recreating the account and reassigning permissions appeared to fix it because that writes an explicit `permissions` row, the one grant shape the old read gate honoured.

  Not changed, deliberately: `POST /api/v1/users/{id}/roles` writes `user_roles`, a table no authorization gate reads, so a user can be reported as holding `developer` with `["read","write"]` and still reach nothing. Wiring it up moves the gate in the fail-open direction with instance-wide blast radius — the table carries no repository scope, `developer` carries `write`, and SSO role mapping rebuilds it on every federated login — so it is documented on both endpoints and pinned by a characterization test, with the decision tracked in #3522. Group grants are unaffected and already work: `permissions` rows with `principal_type = 'group'` resolve through `user_group_members` in every gate.

- **Every partially-successful migration was recorded as a total failure** (#3497). A migration job in which some artifacts transferred and some did not resolves to `completed_with_errors` (`determine_final_status`, since #2457), and `MigrationJobStatus` renders that as the string `completed_with_errors`. The CHECK constraint on `migration_jobs.status` was written in migration 020 and never altered, and it did not list that value — so finalizing a mixed-outcome job raised `new row for relation "migration_jobs" violates check constraint "migration_jobs_status_check"`, the error propagated out of the worker, and the spawn wrapper stamped the job `failed` with the Postgres message in `error_summary`.

  The operator therefore saw a total failure for a run whose transferred artifacts were present and correct, and the one field that exists to say *which* items failed carried a constraint violation instead. The natural response — re-run the whole job — was the wrong one. `completed_with_errors` was also unreachable while `is_terminal_status` already advertised it as a terminal state on the SSE progress stream, so a client waiting for it waited forever. Migration 207 adds the value to the constraint; nothing else changes, and the constraint still rejects any value outside the model's set.

  **Reconciling past runs:** historical rows are deliberately not rewritten, but they are identifiable. `SELECT id, completed_items, failed_items, finished_at FROM migration_jobs WHERE status = 'failed' AND error_summary LIKE '%migration_jobs_status_check%';` lists the jobs that were in fact partial successes; their `completed_items` is the work that really landed. Re-labelling them (`UPDATE ... SET status = 'completed_with_errors', error_summary = NULL`) is safe after upgrading, but is left to the operator rather than done by the migration.

- **A Remote (proxy) Helm repository served an empty `index.yaml`, so `helm pull`, `install` and `upgrade` could not resolve any chart through it** (#3448). `helm` resolves a chart name and version entirely from the repository index before it requests any tarball, and for a Remote repository that index was built only from locally-catalogued artifacts — which for a proxy is empty. `helm repo add` and `helm repo update` both succeeded against `entries: {}`, so the failure presented as "no such chart" rather than as a server problem, while fetching the same tarball by its full path worked (200). Serving was fine; discovery was not. Anything pinning a complete chart URL was unaffected, which is likely why it survived: a Virtual repository with a Remote member already merged its members' upstream indexes, so only a repository pointed straight at an upstream was broken.

  A Remote repository's index is now the upstream's index, merged over whatever the repository holds locally, with every URL rewritten to this repository's own `charts/` route so the client comes back through the proxy for the tarball. The merge prefers the locally-held entry for a `(name, version)` it already has, because that entry carries the artifact's real stored filename — which is what the download route resolves before it consults upstream — and an upstream entry would advertise the `{name}-{version}.tgz` convention instead. That set is exactly what the download route can serve, so the index no longer promises more or less than the repository can deliver.

  When the upstream cannot be reached the answer depends on what is held. With charts held locally they are still served, so an upstream outage cannot break resolution of a chart the repository already has; with nothing held, the upstream's own error is surfaced instead of an empty index, because `entries: {}` is indistinguishable from "the upstream publishes no charts" and would leave `helm repo update` reporting success — the silent failure this issue is about. The status is the upstream's, not a uniform one: an upstream 5xx becomes 503, an upstream 404 (a mistyped `upstream_url`, the most common misconfiguration) stays 404, and a connect/DNS failure is 502 — so the operator can tell an unreachable upstream from a wrong URL from an upstream that is merely down. The proxy cache already applies RFC 5861 `stale-if-error` to the `index.yaml` object itself, so a warm proxy keeps serving the last good upstream index before that fallback is reached.

  Two robustness corrections come with proxying a third party's document. Upstream entries are dropped from the proxied index when their `name`/`version` cannot form a single path segment (`/`, `\`, `?`, `#`, `%`, control characters): the index rebuilds every URL from those attacker-influenced strings, and an entry like `../../../../etc/passwd` produced a still-same-origin URL — the `/helm/{repo}/charts/` prefix is fixed, so no host can be smuggled — that no longer addressed the chart the index had just promised. Percent-encoding was the obvious alternative and was rejected: an encoder aggressive enough to cover `%` also rewrites `+`, moving the URL of every legitimate `1.0.0+build.5` chart. Separately, `urls`, `created`, `digest` and `generated` now default when an upstream omits them; an index is parsed as one document, so previously a single non-conforming entry anywhere in it failed the whole parse and took the entire repository's discovery with it. `name` and `version` are still required, because there is nothing to advertise without them.

  The upstream index fetch is shared with the chart-download path, which resolves real chart URLs out of the same document, and its byte ceiling moves from the 8 MiB default to the large-metadata ceiling. A Helm index is the whole-repository listing — the direct analogue of the PyPI simple-index and npm packument documents already on the larger ceiling — and real upstreams exceed 8 MiB (`charts.bitnami.com`'s is ~26 MiB), which the default would have turned into a 502 for exactly the repositories this fix is for. The fetch now also reserves against the shared buffered-metadata budget (#2665/#2684), which the previous unbudgeted 8 MiB read did not, and that reservation is held across the parse, the merge and the render before riding the response body — the lifetime the RPM repodata proxy already established, so no part of the derived working set (the parsed tree, the chart vector cloned out of it, the rendered document) sits outside it.

  What the budget does and does not bound was measured rather than assumed, because the reservation is sized to the 128 MiB ceiling and not to the body. It therefore admits eight concurrent buffered index requests process-wide and makes further ones wait, which caps peak memory by CONCURRENCY rather than by bytes. Against a 27 MB upstream index on a cold backend, peak RSS above baseline was +1.94 GiB at 16 concurrent index requests, +2.34 GiB at 64 and +2.48 GiB at 256 — a sixteenfold increase in connections for a 28% increase in memory — while a 450 KB index moved it by +0.008 GiB at 64, confirming the cost tracks index size and not connection count. So the total no longer grows with load, but eight admitted requests whose individual working set exceeds its 128 MiB reservation still peak above the 1 GiB budget. Bounding it in bytes would need the whole-request working-set reservation the VS Code gallery path uses, which is a change to the shared budget's contract rather than to this handler, and is deliberately not made here.

  The chart-download path shares that fetch and therefore joins the same budget, having previously been unbudgeted. Its `index.yaml` lookup now waits for a reservation under heavy concurrent metadata load instead of buffering unconditionally; it is the hotter of the two paths, and this is a deliberate trade of tail latency for participation in a bound it was previously outside.

- **The mandatory pre-push checklist named a test command that fails on a clean tree, and `.config/nextest.toml` explained itself with a CI job that no longer exists** (#3479). `CLAUDE.md` told contributors to run a plain single-process `cargo test` for unit tests. Both CI unit-test jobs — Tier 1 and coverage — have run `cargo nextest run --workspace --lib --test-threads 8` for some time, and the difference is load-bearing: nextest gives each test its own process, and a number of unit tests depend on that isolation because they touch process-global state or are serialized only by the `db-serial` groups in `.config/nextest.toml`, which a plain `cargo test` run does not honour. Following the documented command on an unmodified tree produced ten NuGet `*_db_tests` failing with `503 Too many concurrent uploads for this repository` — the process-global per-tenant upload semaphore shedding across tests — and, at the default thread count, a run that did not terminate at all (#3377). Neither has anything to do with the contributor's change, and a checklist that is red for everyone teaches people to ignore red, which is more expensive than the original defect.

  The checklist now names the command CI runs, and documents the environment it needs — `DATABASE_URL`, `AK_TESTS_REQUIRE_DB=1`, `SQLX_OFFLINE`, `JWT_SECRET` — because without `AK_TESTS_REQUIRE_DB=1` every DB-backed test skips and reports success, which is a green run that proved nothing. The Tier 2 entry is corrected alongside it: CI does not run the whole `backend/tests/` tree either, but a fixed list of files' `#[ignore]`d cases, run serially.

  `.config/nextest.toml`'s rationale is re-derived from the current setup. It claimed a single-process `cargo test` unit-test job kept its in-process mutexes working, "which is why that gate stays green while coverage fails". No such job exists, and the premise did not generalize regardless — the `sync_worker` entries have no in-process mutex at all and fail today under a multi-threaded single-process run, so for those the test groups are the only serialization there has ever been. Reading the old comment, a maintainer would have concluded that removing an entry merely degrades one gate; it breaks the unit-test gate outright.

  A `shell-tests` guard now pins the documented command to the one CI invokes, with its own self-test. This defect class — documentation describing a gate configuration that has since moved — is invisible to every other check in the repository, because documentation is not compiled, linted or executed.

- **Proxy-cache content was read at a key nothing ever writes, on both S3 with `S3_PREFIX` set and on the filesystem backend** (#3368, #3463; layout documented for #3454). One storage backend holds two key spaces: artifact bytes, scoped to the deployment (`S3_PREFIX` on S3, `<STORAGE_PATH>/<repo_key>` on filesystem), and proxy-cache content — `proxy-cache/<repo>/<path>/__content__`, its `__cache_meta__.json` sidecar, and `proxy-cache-staging/` write staging — anchored at the shared root. Which scope applied depended on *which handle a caller happened to hold*: `StorageRole::ProxyCache` drops the prefix and is rooted at `STORAGE_PATH`, `StorageRole::ArtifactSource` keeps the prefix and is rooted at the repository's own directory. Every reader of a proxy-cache key therefore had to guess, and a `Remote` repository's `artifacts` row is read through the artifact handle by the local-artifact path of every format. Those reads resolved `<S3_PREFIX>/proxy-cache/...` (S3) or `<STORAGE_PATH>/<repo_key>/proxy-cache/...` (filesystem) — keys no writer produces.

  The result was a structurally guaranteed miss with no error anywhere: `serve_metadata`'s storage read missed and routed into `coordinated_retry_get`, which only re-reads the same key and answers **507 Insufficient Storage** — at a 1:1 ratio with the miss, permanently (#3463) — while the object sat untouched at the shared root; and the PyPI download arm's cache-hit branch missed on every request and re-fetched upstream. With no `S3_PREFIX` the two S3 layouts coincide, which is why the S3 half was invisible on most deployments; the filesystem half needs no configuration at all.

  The scope policy now follows the **key** rather than the handle. `proxy-cache/` and `proxy-cache-staging/` are reserved namespaces anchored at the shared root, applied where each backend picks its scope: `make_full_key` on S3, and `StorageRegistry::backend_for` handing the filesystem handle the global `STORAGE_PATH`. Both handles now address the same object. This fixes the reads for every format at once (npm, Maven, NuGet, Go, Debian, Helm, cargo, OCI and PyPI all record proxy downloads). Reading artifact bytes without their scope (#3171) stays fixed and is pinned by negative controls, including one asserting that a repository still cannot see another repository's hosted files. GCS and Azure register a single shared instance with no per-repository scope and are unaffected. `S3_PREFIX` set to a reserved namespace is now flagged at startup, since the two key spaces would overlap.

  **Am I affected?** Only deployments **upgraded from an older release** are. Current proxy traffic records cached objects in `proxy_cache_artifacts` and never writes a `proxy-cache/` key into `artifacts` (#1278), so on a database created fresh on 1.8.x these read paths are dormant. It is the surviving legacy `artifacts` rows that carry proxy-cache keys, and they are what the affected paths resolve: `SELECT count(*) FROM artifacts WHERE storage_key LIKE 'proxy-cache/%' AND is_deleted = false;` answers the question.

  **No migration and nothing is stranded.** Objects are not moved: the change makes reads target the location the proxy cache has always written to, so existing cached objects become readable again rather than being orphaned. On a prefixed S3 deployment the old miss-recovery left duplicate bodies under `<S3_PREFIX>/proxy-cache/` that no reader consulted; they can be deleted at any time (`aws s3 rm --recursive s3://<bucket>/<S3_PREFIX>/proxy-cache/`).

- **A refetched proxy-cache body is no longer written back through the artifact handle** (#3368, #3147). On a cache miss the PyPI download arm re-fetches upstream and teed the body into storage under the row's key. That refetch already runs through the proxy service's own cache path, which commits the body **and** its `__cache_meta__.json` sidecar under the `CachePersister` contract (#1618 S9) — so the second write added nothing except a body with no sidecar, no #1365 zero-byte guard, no #1051 ETag re-pin, no `proxy_cache_artifacts` row and no quota accounting, racing the proxy's own writer for the same object. Because `is_fresh` falls back to comparing the stored object's size against the sidecar's `size_bytes` when the pinned ETag is multipart-shaped, a same-length out-of-band body kept the entry "fresh" and the redirect path could sign a presigned URL for bytes the sidecar never vouched for. The truncation compensator would also `delete()` the live cache object. The tee is now restricted to keys the artifact handle actually owns. This has been live on every deployment without `S3_PREFIX`; on prefixed ones it landed on the unread shadow path, so anchoring the key layout without this would have made it live everywhere.

- **Neither the storage GC nor the repository-delete purge collects proxy-cache objects any more** (#3368). Both sweeps drive off `artifacts.storage_key` and neither consults `proxy_cache_artifacts`: the GC orphan predicate examines `artifacts`, `oci_tags`, `oci_blobs` and `oci_manifest_refs`, and the repository-delete purge's exclusivity guard (`no other artifacts row shares this key`) asks only about other `artifacts` rows. Proxy-cache keys are derived purely from `(repo_key, path)`, so a soft-deleted — or simply repo-owned — `artifacts` row names the *same* object a live `proxy_cache_artifacts` row points at. Either sweep would have destroyed a live cache entry and left a dangling catalog row whose `size_bytes` only clears on the next fetch. Neither bit before, because the delete was issued against a key nothing had written (`<S3_PREFIX>/proxy-cache/…`, or the per-repository filesystem root) and silently hit nothing; anchoring the layout makes those deletes land, so the exclusions land with it. Proxy-cache objects are reclaimed by `purge_repo_cache` — which the repository-delete path already runs — and by TTL/lifecycle expiry.

  The GC spares the *object* but still hard-deletes the stale *row*, deliberately: that hard-delete is the only reaper of soft-deleted `artifacts` rows in the codebase, so excluding the key from the candidate scan outright would have left those rows unreclaimable. A pass that skips a cache object no longer counts it toward `storage_keys_deleted` or `bytes_freed`, since nothing was reclaimed from storage.

- **A PEP 658 `.whl.metadata` miss on a proxy-cache-backed row no longer pays for a repair that cannot work** (#3463). `coordinated_retry_get` is the repair path for a *locally stored* artifact: it takes a cluster-wide hydration lease, re-reads the same key, and answers 507 if the object is still absent. Nothing local ever writes a `proxy-cache/` key, so on such a row the lease, the second read and an ERROR-level log line bought nothing — one deployment recorded the miss and the 507 exactly 32,681 times each in 24h, without a single coordinated re-read ever succeeding. `serve_metadata` now answers 507 straight away for those rows, which is the signal `serve_virtual_metadata` already recovers from by re-fetching the member's own upstream (the same provenance the wheel download resolves to). To be precise about the scope: this removes wasted work and the ERROR-log volume; it does not add a recovery that was missing, because the caller's own upstream re-fetch was already reached, and re-fetching inside `serve_metadata` as well would duplicate it. #3368 above is what stops the miss happening in the first place. The `#3372` invariant is unchanged — the answer stays 507 and never the 404 that would let virtual resolution fall through to a *different* member — as is a non-`NotFound` storage error, which is still surfaced rather than reinterpreted, and a hosted row, which keeps the hydration repair.

- **The storage layout and the IAM permissions the backend needs are now documented** (#3454). `.env.example` states that `S3_PREFIX` scopes artifact bytes only, lists the `proxy-cache/` and `proxy-cache-staging/` root paths the proxy cache requires, and gives the resource list an IAM policy must cover. A policy scoped to `<S3_PREFIX>/*` alone starts and serves hosted artifacts, then 403s every proxy-cache write, which the cache absorbs by silently re-fetching upstream on every request. The staging-prefix lifecycle-rule recommendation is stated in the same place. This documents the current layout; it does not move cache content under `S3_PREFIX`, and #3454 stays open for that.

- **A paused migration that is resumed and stopped again keeps the record of what it already migrated, and a running dry run reports real progress** (#3510). `process_job` restarts its in-memory counters at zero every time it is entered, while the job row's counters do not — a resumed job still carries the `completed_items`, `skipped_items` and `transferred_bytes` of the pass that was paused. The run-end and interruption publishes added in #3378 wrote those per-run counters to the row *absolutely*, so a job paused at, say, 73 of 100 items and then resumed and paused again before it had re-listed anything had its numerator and its transferred-byte total rewritten to zero — while the transfers they described were still recorded in `migration_items` and still present in the destination, and while the guarded `total_items` held at 100. The row read `0/100` at 0%, which is the same symptom the fix set out to remove, and because `generate_report` takes `summary.total_bytes_transferred` straight off that row, the job's migration report claimed that nothing had been transferred at all.

  Per-run counters are not themselves wrong: `resume_job` deliberately re-lists from offset 0 and re-classifies the earlier pass's work as `skipped`, so once a run has enumerated the whole source its figures are a complete description of the job. The error was publishing them before that point. A run may now only *replace* the row's figures once it has enumerated the whole source; while its view is still partial it may only *advance* them — the three item counters move as a group and only when the run has accounted for at least as many items as the row already claims, `transferred_bytes` never shrinks, and the denominator may grow but not fall back. Replacing stays available at the end of a run because that is what corrects an assessment estimate the content never matched, which is what #3378 asked for; and the `WHERE status NOT IN ('paused','cancelled')` guard on the denominator (#3378) and on the terminal status write (#3380) is unchanged, so a stopped job is still never stamped terminal.

  Two related corrections in the same function:

  - **A running dry run, and a re-run that finds everything already present, now report progress while they run** rather than only in the row they leave behind. The numerator went out once, at the end of the run, while the denominator went out per page, so for its whole running life such a job read `0/N` at 0% and then jumped straight to `N/N` — true of the final row but not of the running migration. The counters are now published at the end of every page as well, alongside the denominator that page already published. Per page rather than per artifact keeps the write rate of an all-skip delta run where it was.

  - **A migration the operator cancels in the moment after starting it is no longer run to completion and stamped `completed`.** `POST /migrations/{id}/start` sets the row to `running` and then spawns the worker as a detached task, so a cancel can land in the window before that task is scheduled. `process_job` opened with an unguarded `update_job_status(Running)` that put the row back to `running`; the guarded finalize then had nothing to protect and stamped the cancelled job `completed`, having migrated every artifact of a migration the operator had cancelled and received a `200 OK` for. The worker now claims the job under the same `WHERE status NOT IN ('paused','cancelled')` guard the finalize carries, and returns without contacting the source if the job was stopped first.

- **A running migration reports a real denominator instead of `N/0` at 0%, and a dry run or an all-duplicate re-run no longer reports `0/N` at 0% either** (#3378). `migration_jobs.total_items` and `total_bytes` were only ever written by `MigrationService::add_migration_items` and `save_assessment`, and the worker calls neither — it inserts each row through its own `add_migration_item`, which tracks the item but never touches the job's totals. Both the REST response and the SSE stream then divided the incrementing processed count by that untouched zero and short-circuited to `0.0`, so every job displayed a correct numerator over a zero total for its entire life and finished the same way. The worker now publishes the totals it enumerates, one update per page of source listings and once more when the run ends, and the two write paths no longer contend for the columns: the assessment's figures are a pre-run estimate that the run overwrites with what the source actually yields (the estimate itself is unaffected in `config.assessment`, which is what `GET /assessment` reads). A running total is the only honest option here, and deliberately so — neither Artifactory's AQL nor the Nexus component API reports a result-set count, which is exactly what the reporter observed about the Nexus paging model, so a total is knowable only by enumerating and any figure produced up front would be invented. The cost is that the percentage can fall back when a new page lands: a finished page of 1000 reads as nearly full until page two arrives and the same work reads as half. It is exact once enumeration ends, which is when it matters most, and the alternative — carrying an "unknown total" through the API for a web UI that has no way to render one — would leave every existing client showing nothing at all. Items the transfer loop skips before writing a `migration_items` row (duplicates, dry runs) still count toward the total, because they are counted in `skipped`/`completed` and the two sides of the fraction have to be drawn from the same population.

  The numerator had the same hole from the other side, which is why the report also covers dry-run executions. The only per-artifact `update_job_progress` sits at the end of the transfer block, and the three arms that `continue` before reaching it — a dry run, an item an earlier pass already completed, and a duplicate already present in the destination — never write anything to the row. A job made entirely of those ran all the way to a terminal status with `completed_items`/`skipped_items` still at the zero they were created with, so `N/0` at 0% simply became `0/N` at 0%: the same stall, and the reason a dry run or a delta re-run that found nothing left to do reported no progress at all. Every exit from `process_job` — a clean finish, an operator pause, a cancel — now publishes the counters and the discovered totals one last time, so the row a stopped or finished job leaves behind describes what the run actually did.

  Three smaller corrections ride along. **The run no longer clears the totals to `(0, 0)` before its first source listing.** The per-page publish is absolute and overwrites whatever it inherits, so clearing bought nothing and cost a window in which a resumed job showed a literal "73/0 items, 0%" — #3378's exact symptom — for as long as the first page fetch took; since #3380 made pause/resume reachable for single-repository jobs at all, that is the normal path rather than a corner. **`update_job_totals` now carries the same `WHERE status NOT IN ('paused','cancelled')` guard `finalize_job_status` has**: a page listing already in flight when the operator stops a job used to land afterwards and leave a dead job advertising a denominator covering work it never processed (`cancelled`, `1000/2000`, 50%) — it never touched `status`, so it could not revive a job, but the figures were fiction. And **a running job no longer reports a full 100%**: because the denominator is published a page ahead of the items in that page, a job that had just drained a full page read `1000/1000` and parked at a hard `100.0` until the next listing arrived, so a client keying on `progress_percent >= 100` would call it finished several pages early and a progress bar would fill and empty repeatedly. The reported share is now held just below full until the job reaches a terminal status, where the totals can no longer grow; `status` remains the authoritative completion signal and the underlying arithmetic is unchanged. That arithmetic now lives in one place, `models::migration::progress_percent`, with `reported_progress_percent` layering the ceiling on top, rather than in three subtly identical copies across the model, the job response and the stream loop — and the terminal-status predicate the stream stops polling on is now the same one the percentage consults, so the two cannot drift. It clamps at 100 because repository-level failures (a source repo missing from the listing, a destination conflict) land in `failed_items` without ever having been enumerated as items, and could otherwise push a job past full.
- **Pausing or cancelling a migration job during its final repository no longer flips it to a terminal status** (#3380). A pause or cancel observed between artifacts returned the same result as a fully drained repository, so when the interruption landed during the final (or only) repository the worker stamped a terminal status and `finished_at` over `paused`/`cancelled`; resume then rejected the job, leaving delete as the only option — for single-repository jobs pause always ended the job. The interruption now propagates distinctly from repository processing, and the terminal write is guarded at the database level (`UPDATE … WHERE status NOT IN ('paused','cancelled')`), so a pause racing the final write cannot be clobbered either. Uninterrupted jobs keep the exact prior behavior.
- **An empty repository-key segment no longer answers 500 with an internal type path** (#3443). `repo_visibility_middleware` returns early when the extracted key is empty — there is no repository to authorize — but it returned *without* inserting the `Option<AuthExtension>` every format handler binds, so axum rejected the request with `500 Missing request extension: Extension<core::option::Option<...AuthExtension>>`. Every format route reached it: `/npm//pkg`, `/pypi//simple/`, `/maven//`, `/lfs//objects/batch` and the rest all answered an unauthenticated 500. The middleware now answers that branch itself with an existence-hiding 404 rather than falling through, so the handler — and the strict extractor that produced the 500 — never runs. (The first version of this fix inserted an anonymous extension and let the handler run; that reintroduced the request body on an unauthorized path and was replaced, see the Security entry for #3508 above.)
- **The two guards that decide whether a Maven `maven-metadata.xml` rollup is still in use now anchor correctly for every stored key** (#3492). Both guards build a directory-prefix `LIKE` pattern *from* a stored key — the storage GC sweep's rollup guard (`ORPHAN_MAVEN_FLAT_PREDICATE_SQL` guard 3) and the collector that lists a repository's flat Maven objects before the repository is deleted — and neither carried an `ESCAPE` clause. Postgres's default `LIKE` escape character is a backslash, so for the subset of keys containing one the pattern no longer described the directory it was derived from: `maven/g/a\b/` became the pattern `maven/g/ab/`. The guards are `NOT EXISTS` tests, so a pattern that misses means "nothing is using this" — and for a rollup whose own subtree is still live, that is the wrong answer in the destructive direction.
- **Deleting a Docker/OCI manifest through the REST artifact API now unwinds the OCI index, and does it in the same transaction as the delete** (#3476). `DELETE /api/v1/repositories/<key>/artifacts/v2/<image>/manifests/<reference>` only flipped `artifacts.is_deleted`. The manifest's `oci_tags`, `oci_manifest_refs`, `manifest_blob_refs` and `oci_manifest_subjects` rows survived, so a "deleted" image was gone from the UI, the artifact listing and quota accounting while `GET /v2/.../manifests/<tag>` still served it, `tags/list` still advertised it and `docker pull` still succeeded. `HEAD` by tag and by digest both answered 200 with a matching `Docker-Content-Digest`, which is the `ErrAlreadyExists` short-circuit containerd-based pushers (`buildx --push`, `nerdctl`, `crane`, `skopeo`) use to skip the upload — so a re-push could not resurrect the row either. The surviving `manifest_blob_refs` are also what `BLOB_PROTECTED_BY_REFS_SQL` consults, so the config and layer blobs stayed permanently unreclaimable by storage GC.

  Both guards now go through one shared fragment, `metadata_rollup_dir_anchor_sql`, which carries `ESCAPE ''`. That makes the invariant the code has always documented — the anchor test can only ever over-*protect* — true for every character rather than only for `%` and `_`. `%` and `_` keep widening the pattern deliberately: over-matching spares a document that could have been reclaimed, which is a recoverable storage leak, while under-matching removes one that is still being served, which is not. Behaviour is unchanged for every key without a backslash, and unchanged for genuinely unreferenced documents, which are still reclaimed. Routing both call sites through the same fragment is also what stops the two delete paths drifting apart again, the divergence behind #3156 and #3197.

  The repository-delete collector is the more consequential of the two: it has no opt-in gate (unlike the sweep, which is behind `MAVEN_FLAT_GC_ENABLED` since #3431), and its whole purpose is to spare a rollup that *another* repository is still serving. On a shared cloud key namespace — the pre-#2624 layout, still selectable with `STORAGE_KEY_SCHEME=flat`, and the layout every object written before #2624 still lives in — deleting one repository could therefore remove a `maven-metadata.xml` a different repository was still serving. Repository-scoped keys (the default since #2624) are not exposed to that cross-repository case, because the directory prefix contains the owning repository's id.

  Regression tests cover both paths, with `%`/`_` fixtures pinning the over-protect direction and positive controls so that genuinely unreferenced documents — including ones whose directory contains a backslash — are still collected. No migration and no data repair: the guards decide what to delete, and nothing needs backfilling.
- **`OIDC_ADMIN_GROUP` is read by the environment bootstrap, and is definitive like every other `OIDC_*` setting** (#3420). The bootstrap read every other documented `OIDC_*` variable but never this one, so the env-managed provider's `attribute_mapping.admin_group` — the key the OIDC callback reads for admin elevation — never followed the variable. Renaming the group in the IdP and updating `OIDC_ADMIN_GROUP` left the stale group elevating, and the login-time env fallback could not cover it because it only engages when the mapping carries no value at all. The variable now flows through both bootstrap paths: creation persists it into the mapping, and the per-boot reconcile applies it to the existing provider.

  Both guards now go through one shared fragment, `metadata_rollup_dir_anchor_sql`, which carries `ESCAPE ''`. That makes the invariant the code has always documented — the anchor test can only ever over-*protect* — true for every character rather than only for `%` and `_`. `%` and `_` keep widening the pattern deliberately: over-matching spares a document that could have been reclaimed, which is a recoverable storage leak, while under-matching removes one that is still being served, which is not. Behaviour is unchanged for every key without a backslash, and unchanged for genuinely unreferenced documents, which are still reclaimed. Routing both call sites through the same fragment is also what stops the two delete paths drifting apart again, the divergence behind #3156 and #3197.

  The repository-delete collector is the more consequential of the two: it has no opt-in gate (unlike the sweep, which is behind `MAVEN_FLAT_GC_ENABLED` since #3431), and its whole purpose is to spare a rollup that *another* repository is still serving. On a shared cloud key namespace — the pre-#2624 layout, still selectable with `STORAGE_KEY_SCHEME=flat`, and the layout every object written before #2624 still lives in — deleting one repository could therefore remove a `maven-metadata.xml` a different repository was still serving. Repository-scoped keys (the default since #2624) are not exposed to that cross-repository case, because the directory prefix contains the owning repository's id.

  Regression tests cover both paths, with `%`/`_` fixtures pinning the over-protect direction and positive controls so that genuinely unreferenced documents — including ones whose directory contains a backslash — are still collected. No migration and no data repair: the guards decide what to delete, and nothing needs backfilling.

- **The scheduled storage-GC tick no longer runs on every replica at once, and its slowest guard is no longer a full scan of `artifacts` per candidate** (#3384). Each replica's scheduler computes the same cron occurrence independently, so on a multi-replica deployment every replica fired the hourly pass at the same instant and ran the same Maven flat-object orphan scan concurrently — observed in `pg_stat_activity` as several identical copies of that query sharing one `xact_start` and running for 20+ minutes. The pass is now wrapped in the cluster-wide `SchedulerLease` singleton already used for lifecycle-policy execution, under the job name `storage_gc`: exactly one replica runs it per tick, the rest return immediately, and the lease is released at the end of the pass rather than left to lapse. The lease is renewed for as long as the pass runs, so a sweep that outlives the TTL on a large registry does not lose it mid-run. **The on-demand endpoints are deliberately not gated** — `POST /api/v1/admin/storage-gc` and the per-repository panel call the GC service directly and keep working while a scheduled tick holds the lease.

  **This closes part of the tick, not all of it.** The lease covers the artifact-level GC pass only. The OCI blob-GC mark/sweep and the storage-stats recompute that share the same tick still run on every replica — with two replicas, the leased `maven_flat_object_owner` scan drops to one per minute while the un-leased `oci_blobs` scans stay at four to six. A replica that loses the lease now also returns immediately instead of after a two-minute GC, so the un-leased half fires on every replica simultaneously with none of the accidental stagger the slow path used to provide. Extending the coordination over the rest of the tick is tracked in #3503.

  The tick's other cost was one guard. The rollup guard that spares a `maven-metadata.xml` while any live artifact remains under its directory builds its `LIKE` pattern per candidate row, and Postgres only rewrites a `LIKE` into an indexable range scan when the pattern is a plan-time constant — so the guard was a hash anti-join against the entire `artifacts` table, re-evaluated once per candidate, and accounted for essentially the whole run. A partial `gin_trgm_ops` index on `artifacts.storage_key` (`WHERE is_deleted = false`, matching the guard's own filter) turns it into a bitmap index scan with the correlated pattern as an index condition. Measured on a 400,000-row `artifacts` table at the attribution-table size the report describes (136 rows), the candidate scan drops from **54.7s to 151ms** — the un-indexed plan removes 54,400,000 join-filter rows, one full pass over `artifacts` per rollup candidate, so it is quadratic and the gap widens with the candidate count: at 20,000 attribution rows the indexed scan still ran in 2.5s while the un-indexed one had not finished after 52 minutes. The result set is identical — GIN trigram scans are lossy by construction and Postgres always rechecks the real `LIKE` against the heap row — and it composes with the `ESCAPE ''` anchoring from #3492, which still plans as an indexable `~~`.

  **Operator note — this migration briefly blocks writes to `artifacts`.** `sqlx` runs each migration inside a transaction, so `CREATE INDEX CONCURRENTLY` cannot be used; the plain build takes a `SHARE` lock on `artifacts` for its duration. Reads are unaffected (`SELECT` takes `ACCESS SHARE`, which is compatible), but uploads across *every* format block until it completes, and it runs at each replica's startup migration — so the stall lands in the middle of a rolling deploy. Measured: 1,492 ms for a 24 MB index over 400k rows, roughly linear in row count, so around 40s at ~10M artifacts and over 3 minutes at ~50M. Size and build time also scale with key length, which is user-influenced (`storage_key` is `varchar(2048)`): at a fixed 20,000 rows, typical Maven keys built in 223 ms / 3.9 MB while max-length high-entropy keys took 5,463 ms / 115 MB. Sites that cannot accept the stall should build the index out of band before upgrading — the migration is `IF NOT EXISTS`, so it then becomes a no-op:

  ```sql
  CREATE INDEX CONCURRENTLY idx_artifacts_storage_key_trgm
    ON artifacts USING gin (storage_key gin_trgm_ops)
    WHERE is_deleted = false;
  ```

  A second index on `maven_flat_object_owner.storage_key` was proposed with this change and **dropped after measurement**: at 20,000 attribution rows over 400k artifacts the planner did not choose it in the real query (the five-way anti-join estimates a single surviving row, so an ordered index scan is never believed to terminate early), and it made no measurable difference — 2,522 ms with it against 2,468 ms without, `Seq Scan` plus top-N heapsort both ways. It would have cost a write-blocking build and ongoing write amplification for nothing.

  Two smaller items ride along: the block's fixed 120-second startup sleep now uses the same jittered startup delay as every other periodic block in the scheduler, and the tick body moved out of the scheduler closure into `StorageGcService::run_scheduled_tick`, where it is directly testable. Neither the opt-in `MAVEN_FLAT_GC_ENABLED` gate nor the source allowlist from #3431 is touched — both are re-asserted through the leased entry point by regression tests, since this change makes that sweep reach its candidates far more reliably than before.
- **The unit-test suite hung indefinitely under a plain `cargo test` run, and four `read_json_capped` tests failed at lower thread counts** (#3377). `test_configured_proxy_host_is_exempted_from_guard` sets `HTTP_PROXY` — and clears `NO_PROXY`, removing the usual loopback exemption — for the synchronous window in which it builds its own client (#2570). `std::env::set_var` is process-wide, so under the single-process, multi-threaded runner any client another test happened to build inside that window was captured, because reqwest reads the proxy environment at client-build time. Requests those tests aimed at their own loopback listener went to the sibling test's proxy listener instead.

  What that looked like depended on timing. At `--test-threads 8` the proxy listener was already gone, so four `services::http_client::tests::test_read_json_capped_*` failed with `Connection refused` on a port none of them had opened — the shape reported. At the default thread count the proxy listener was still accepting, so the captured request waited for a response that never came while the test's own spawned server task sat in a bare `let _ = server.await` over `listener.accept()`, which cannot return if the connection never arrives: the whole run never terminated. Three further tests were caught through a second consumer of the same variables — the SSRF DNS resolver builds its proxy-host exempt-set from them (#2570), so a resolver constructed inside the window exempts `localhost`, the loopback hard-block those tests assert on stops firing, and an intact security control is reported as broken.

  Every client in the module that talks to a listener the test itself started is now built with `no_proxy()`, which is immune to the environment instead of depending on each call site remembering to take a lock. The three tests that assert the resolver REFUSES a host are built inside the existing `PROXY_ENV_LOCK` window instead, because reqwest's proxy setting cannot undo the resolver's exempt-set and only excluding the window can. Every spawned server task is now joined under a wall-clock bound, so a future miswiring fails with a diagnosis rather than hanging the binary, and a source guard keeps both shapes from returning.

  No CI gate was affected: CI runs `cargo nextest`, which gives each test its own process, so the process-wide variable cannot cross between them. The cost was borne locally, where a contributor's pre-push run either failed on four tests unrelated to their change or never finished at all.

- **Build provenance attestation now fails the publish job instead of being swallowed, and VEX attachment says out loud that it is best-effort** (#3496). Six STEP-level `continue-on-error: true` sat on `docker-publish.yml`'s supply-chain steps: `Generate artifact attestation` on all four published images (backend, OpenSCAP, scanner-adapter, backend-alpine) and `Attach VEX attestations` on the two that carry VEX. Step-level is the worst available shape for a control — unlike job-level it leaves the JOB result `success`, so `needs.<job>.result`, required checks and every downstream gate see green, nothing is written to the run summary, and no annotation is raised. An OIDC token outage, a registry rejection, a rate limit or an `actions/attest-build-provenance` outage was therefore indistinguishable from success, and the only trace was a small icon in a log nobody opens. **Build provenance had consequently never been proven to attach to any published image.** It is the same failure the project already paid for in #2824, where the cosign step failed and 1.6.1 shipped unsigned and was caught only because someone went looking; the four `Sign image with cosign (keyless)` steps have hard-failed since then and were not part of this defect.

  Provenance attestation is a **control** and now hard-fails. That is affordable here for a reason worth stating: the attestation step runs *after* `Apply ghcr tags` and `Copy to Docker Hub`, so hard-failing does not block a publish that would otherwise have happened — the image is already on both registries either way. What it changes is that the missing predicate becomes a red job instead of nothing at all, and the recovery is `Re-run failed jobs`, because every step in these jobs is idempotent by construction (`imagetools create` re-points the SAME digest, `cosign sign` and `attest` are safe to repeat).

  VEX attachment is **best-effort**, and stays so — `docker scout attestation add` depends on an external service and on Docker Hub credentials, and an outage there is not a reason to redden a release. But the decision moved out of the invisible YAML key and into the script, where a reader can see it: failures are collected per file and reported with a `::warning::` annotation and a `$GITHUB_STEP_SUMMARY` section naming each VEX document that did not attach, and the step still exits 0. `docker-publish.yml` now carries no `continue-on-error` at all.

  `scripts/ci/check-supply-chain-soft-fail.sh` pins the invariant so it cannot come back: no attestation (`actions/attest-*`) or cosign signing/attesting step in `.github/workflows/` may set `continue-on-error` to anything but a literal `false` — an expression that could evaluate true counts as soft — and the publish workflow may carry none on any step or job. It fails rather than passing vacuously if signing and attestation disappear entirely, and it ships with a self-test (`test-check-supply-chain-soft-fail.sh`, wired into the shell-tests job) that asserts each of the shipped shapes is rejected. Run against the pre-fix workflow it reports all six steps by name.

  Two things are deliberately **not** in this change. There is still no post-publish verification that a published digest actually carries the provenance predicate and its signature — `verify-published` probes for tag existence only — and adding an unvalidated `gh attestation verify` hard gate to the tag path would be the same class of mistake as the one being fixed, so it is tracked separately along with a one-off audit of the digests published while the control was soft.
- **The #2609 container-scan publication guard has been failing on `main` since #3122 and running in no CI job at all** (#3437). Two independent defects in the same file, and the guard was worthless for both reasons at once.

  **It was wrong.** `backend/tests/workflow_scan_gate_tests.rs` asserted that *every* `aquasecurity/trivy-action` step in `docker-publish.yml` carries `exit-code: '1'`. #3122 deliberately split the scan into two passes per image — a SARIF **visibility** pass at `exit-code: '0'` that feeds the Security tab at every severity and never blocks, and a table-format **enforcement** pass at `exit-code: '1'` that is the control the `merge-*` jobs depend on — because `trivy-action` runs `unset TRIVY_SEVERITY` for SARIF, so the old single pass discarded its own `CRITICAL,HIGH` filter and blocked every publish from `main` on a LOW (#3121). Three of the guard's four assertions only ever made sense on the enforcement pass, so from #3122 onward it reported the gate as disabled while the gate was in fact working. The guard now models the **pair**: each scanned image must have exactly one visibility pass and exactly one enforcement pass, the enforcement pass must gate on the stated policy (`exit-code: '1'`, `CRITICAL,HIGH`, `ignore-unfixed`, `.trivyignore`, and a non-SARIF format so the filter survives the action), and the visibility pass must not block and must not declare a `severity:` it cannot honour. Pairing is what keeps it non-vacuous: filtering to enforcement steps and checking only those would accept a workflow that had quietly lost its enforcement pass altogether, which is the regression #2609 exists to catch.

  **It never ran.** The file is an integration target under `backend/tests/`, so Tier 1 (`cargo nextest run --workspace --lib`) never compiled it, and Tier 2 runs a fixed 16-name `--test` allowlist that does not include it. The issue's premise that `cargo test --workspace` is the Tier 2 gate and is red is not correct — no job runs that command, so there was no red signal anywhere; a guard against image publishing silently becoming non-blocking was itself silently non-blocking. It now runs in **Tier 1**, as its own step in the unit-test job, with `--no-tests=fail`. Adding it to the Tier 2 allowlist as first suggested would have re-created the defect in a new place: that job invokes `-- --ignored`, every suite in its allowlist is 100% `#[ignore]`d by construction, and these tests are not — the invocation reports `running 0 tests ... 3 filtered out` and exits **0**. Tier 1 is also the right home on the merits: pure YAML parsing, no database, no network, ~25 ms, and it gates every pull request rather than only backend-touching ones.

  The sibling `scripts/ci/check-trivy-gate-severity.sh` (#3122) is deliberately left alone: it covers the same policy from the other side — every *gating* Trivy step in *every* workflow applies the filter it declares — with no Rust compile, and the two together now fail from either direction.

- **The code-duplication gate silently skipped 45% of `backend/src`, so for the repository's largest files it had never been able to fail** (#3495). jscpd defaults to `maxLines: 1000` and `maxSize: "100kb"` and skips a file over **either** limit silently — no error, no warning, no entry in the report — after which the gate reports whatever the remaining files measured. The CI step never overrode those limits, so **152 of 336 `.rs` files under `backend/src` were never analysed, 125 of them not on `.jscpd.json`'s ignore list**, including `services/`, `storage/` and `middleware/` files that the config's own comment goes out of its way to keep in scope. A pull request whose Rust changes landed only in one of them got a duplication verdict computed from an empty file set: with `repositories.rs` and `services/artifact_service.rs` as the entire changed-file list the gate reported `Code Duplication: 0% (no duplicates found)` and passed, having analysed nothing at all. A deliberate 79-line clone injected into `artifact_service.rs` passed the same way.

  (The issue this fixes named three files, on the basis that they exceed both limits. jscpd applies the two limits independently, so the real set is every file over *either* — about 45% of the tree.)

  **Raising the limits alone would have made the gate unusable, which is why the measurement changed too.** `backend/src` is 474k lines, of which 265k — **56%** — are inline `#[cfg(test)]` modules; `repositories.rs` is 26,328 lines with 22,164 of them test code. With the limits raised and nothing else changed the non-ignored tree scores **3.38%** and **23 of the last 44** code-touching commits on `main` would have failed the 3% gate, with 58 of 61 clones in the reported sample sitting entirely inside `#[cfg(test)]`. That is not a duplication problem; it is the gate measuring something it never meant to. `.jscpd.json` already ignores `**/*_test.rs` and `**/tests/**`, and the CI step already drops `_test`/`tests/` paths from the changed-file list — test code is out of scope **by policy**, and inline test modules were out of scope only **by accident**, because the files holding them were too big for jscpd to open. They are now excluded explicitly, by `scripts/ci/jscpd-prepare-sources.py`, which copies each changed source with its top-level `#[cfg(test)]` items removed and keeps a line map so a failure still reports real line numbers. Only **column-0** attributes count as boundaries: an indented `#[cfg(test)]` is inside another item, and treating it as top-level would delete production code — 22 files under `backend/src` have one. With that in place the tree scores **2.27%** and 2 of those 44 commits exceed the threshold, both on genuine production-code clones in `storage_gc_service.rs` and `auth_config_service.rs`.

  **Two silent passes are now failures.** A changed file that jscpd did not analyse — for any reason, not only size — fails the gate with the file named and the reason given, rather than being averaged out of the result; and a run that produces no report at all now fails instead of printing `0% (no duplicates found)`, which is precisely what a whole-file-set skip looked like. A changed-file set where nothing reaches jscpd's 10-line floor is still reported as N/A and passes, because that is the one legitimate omission. The limits live in `.jscpd.json` next to the ignore list, the CI step reads them from there and passes them explicitly, and `scripts/ci/test-jscpd-prepare-sources.sh` (wired into the shell-tests job) pins the preparation step's behaviour from the failing side.

  Two consequences worth stating. Files it now measures may hold pre-existing clones that the PR touching them did not introduce, because the gate's scope is whole changed **files**, not changed lines (#1619). And `oci_v2.rs` stays on the ignore list: that exemption is deliberate and documented (#1704), and is unaffected by this change either way.


## [1.8.1] - 2026-08-21

### Fixed

- **Storage GC no longer deletes Maven objects whose attribution row an operator wrote, and the flat-object sweep is now opt-in** (#3431, reported by [@ThaSami](https://github.com/ThaSami)). `cleanup_orphan_maven_flat_objects` enumerated its delete candidates **from `maven_flat_object_owner`** and reclaimed every row whose catalog anchors were gone — live, on the hourly scheduled pass. But since #2574/#2585 that same row is what makes a *row-less* legacy object **readable**: it is the last layer of read-time owner resolution, and inserting one is the documented remediation for keys the catalog-only backfill of migrations 163/170 cannot see. For exactly that class — an object whose only catalog record is its owner row — every anchor arm of the sweep's predicate is true by construction and permanently. The row that made the object servable was the row that queued it for deletion: an operator who repaired a fail-closed 404 by attributing the key got a 200 for one hour, and then lost the bytes. The flat key is the only physical copy, and the sweep deleted the attribution row with it, so nothing recorded that the key had ever been hand-attributed.

  Two independent fixes, either of which would have prevented the loss. **(1)** The predicate now reclaims only rows the system itself **wrote and can re-derive** — `primary_row`, `metadata_files`, `derived_checksum`, `metadata_rollup` (migrations 163 and 170) and `write_claim` (the runtime claim path). Those rows are caches over other catalog state, so once the anchors vanish they are stale by definition; an operator-inserted row is a primary record that nothing can reconstruct. It is an **allowlist**, not a denylist, so any value a future code path, an external repair tool, or a hand `INSERT` stamps is protected by default — the failure direction is leaked storage, never lost bytes. `write_claim` stays reclaimable deliberately: an orphaned claim from a crashed first publish is precisely the leftover this sweep exists to collect. **(2)** The sweep is gated behind `MAVEN_FLAT_GC_ENABLED`, mirroring `BLOB_GC_ENABLED`'s "bias to leaking storage over losing data". Unset — the default — it still runs and **reports** what it would reclaim (`maven_flat_objects_gated` on the GC result, plus a log line) but deletes nothing. That gate is the deeper fix: this sweep's entire subject matter is objects *the catalog cannot see*, and on an instance migrated from another registry that absence is the **expected** state of legitimate legacy data — it is why the attribution table exists — so catalog absence alone is not proof of garbage without an operator saying so. The gate is applied in the service, so the per-repository admin GC endpoint, which shares the predicate constant with the scheduled pass, shares the gate too.

  **Operator note.** What was lost: the storage objects for row-less Maven flat keys — GAV-grouped companion files (`.pom`, `.module`, `-sources.jar`), verbatim `maven-metadata.xml`, and checksum/signature sidecars — together with their `maven_flat_object_owner` rows. Hand-attributed keys were the affected class: a key with no owner row was invisible to this sweep, so **attributing a key to repair a 404 is what made it eligible for deletion**, and an untouched broken key was left alone. Deleted objects are **not recoverable** from within Artifact Keeper — the sweep deletes the object first and the attribution row in the same pass, and neither is journalled anywhere; recovery depends entirely on storage-side retention (S3/GCS/Azure object versioning or a bucket lifecycle noncurrent window, a bucket replica, or a filesystem backup), and once the attribution row is gone the database no longer records *which* keys were operator-attributed. The one in-product record of what was taken is the log: each reclaim emits `Storage GC: reclaimed orphaned row-less Maven flat object` at INFO with the `storage_key`, so a backend log with sufficient retention reconstructs the key list to restore from bucket versions. Before upgrading, snapshot `maven_flat_object_owner` (`storage_backend`, `storage_key`, `repository_id`, `source`) — with this fix that snapshot is what identifies the rows now protected. Two upgrade notes: the sweep now **deletes nothing by default**, so anyone relying on it to reclaim genuinely-orphaned Maven storage must set `MAVEN_FLAT_GC_ENABLED=true` (check `maven_flat_objects_gated` first to size the pass); and the 1.7.3 guard rebuild plus the #3385 indexing made this scan *complete* on large attribution tables where it previously timed out, so upgrading to 1.7.3+ **for those fixes** is what activated mass reclaim of hand-repaired data at up to 1000 keys per pass, per replica. v1.7.6 and v1.8.0 both carry the unfixed sweep.
- **Proxied downloads are counted for Docker/OCI, cargo, NuGet, Debian, Helm and Go, and a class guard now stops the next format from shipping uncounted** (#3446). Two functions count a download and they are not interchangeable: `record_download` is keyed on an `artifacts.id` and writes `download_statistics`, while `record_proxy_download` is keyed on `(repository, path)` and writes `proxy_download_statistics`. Proxy-cached content is deliberately not registered in `artifacts` (#1278), so a format calling only the first *looks* instrumented on inspection and records nothing at runtime — the id lookup returns `None`, nothing errors, and the counter never moves. That is precisely how ~19 formats stayed broken behind #3388, which fixed Maven and concluded Maven was the only one. Cargo's Remote arm returned the upstream stream directly and never reached the `record_download` fifteen lines below it; OCI called `record_oci_manifest_download`, which resolves a manifest through `artifacts.storage_key` and silently no-ops for every proxy-cached object, so **every** `docker pull` through a proxy counted zero. Recording is now in place on all six high-traffic proxy formats, keyed on the same proxy-cache path the fetch commits under so the count lands on the catalog row the artifact listing renders (#3388's display half). The unit of a Docker pull is unchanged — counted once at the manifest, never per blob (#2260) — and a HEAD still counts nothing on every path.

  The guard is the part that generalises. `every_proxy_serve_path_records_a_download_or_is_explicitly_exempt` scans every format handler for the primitives that put upstream bytes in front of a client and fails unless each site either records (directly, via the recording `try_remote_or_virtual_download`, or one call hop away — pypi records in the caller of its streaming helper, npm in the callee, and both are correct) or carries a written `UNRECORDED-PROXY-SERVE:` marker stating why. Not counting is sometimes right, so the marker is a greppable token that keeps the judgement in review rather than in an allowlist that drifts. It immediately found a serve path the per-format audit had missed: RPM's catch-all `/rpm/{repo}/*path` route records nothing even though RPM's main download path does. The 14 formats not fixed in this pass (alpine, chef, cocoapods, composer, conan, conda, gitlfs, jetbrains, pub_registry, rpm's catch-all, sbt, swift, terraform, vscode) each carry a marker naming #3446, and a companion test pins that count at 14 so the remaining work can only shrink. Repositories in those formats keep reporting 0 downloads — a reporting gap, not a serving defect; artifacts are served correctly either way.
- **The release-branch gate can now express a maintenance-branch-only change, so it no longer has to be bypassed to cut a release** (#3422). The gate indexes `git patch-id` over main's last 1500 commits and requires an exact match, which is right for catching genuine drift but cannot represent two legitimate shapes — and during the 1.7.6 cut both were waved through with `release-process: approved` four times in one release, which is how a required gate stops meaning anything. A **release prep** (`chore(release): prepare 1.7.6`) bumps `Cargo.toml`, `Cargo.lock`, `**/openapi.rs`, `CHANGELOG.md`, `.github/release-notes/**` and `docker/*/VERSION` for a version that exists *only* on the maintenance branch; there is nothing on main to trace to and there should not be. A **narrowed backport** carries a correct `(cherry picked from commit <sha>)` trailer but a different patch by construction, because hunks that could not apply were resolved away (a guard test depending on scaffolding the branch lacks, a CHANGELOG hunk targeting `[Unreleased]` when the branch needs `[1.7.6]`). Both are now accepted on their own terms: the release prep only when the subject prefix *and* the whole path set qualify, the backport only when the trailer names a sha that is actually on main — and the narrowing is logged as `narrowed backport of <sha>` rather than passed silently. A trailer naming a sha main does not have is now its own distinct failure, since it asserts a provenance that is not there. Everything else still fails exactly as before, and the `release-process: approved` label remains for the genuine remainder. The logic moved out of the workflow into `scripts/ci/check-release-branch-commits.sh` with a self-test (`test-check-release-branch-commits.sh`, wired into CI's shell-tests job), because a gate that only runs on release PRs is far too rare to notice a regression in — the self-test exercises both exemptions from the failing side, so a `chore(release):` commit that also touches source, or a version-file-only commit under another subject, cannot slip through.
- **Every `release/**` branch publishes container images on push, not just `release/1.1.x`** (#3422). `docker-publish.yml`'s push trigger hardcoded a single maintenance branch, so a commit landing on `release/1.7.x` produced no images at all. Preflight check 3 then correctly reported "no Docker Publish run exists for HEAD" (it resolves the run by `head_sha` since #3338), and every 1.7.x cut needed an undocumented manual `workflow_dispatch` first. The trigger is now a `release/**` glob. The one thing in that workflow that keyed on the branch name — the `X.Y-dev` floating alias, hardcoded as `1.1-dev` in eight metadata blocks — is now derived from the branch in `publish-preflight` and exported as an output, so `release/1.7.x` publishes `1.7-dev` exactly as `release/1.1.x` publishes `1.1-dev`, and a new maintenance line gets one the moment it is created with no workflow edit.
- **A release-prep commit can no longer silently strand CHANGELOG entries under an already-released heading** (#3433). `chore(release): prepare 1.7.5` renamed `## [Unreleased]` to `## [1.7.5]` without opening a fresh empty `## [Unreleased]` above it. Every PR branch cut before that rename still anchored its CHANGELOG hunk on the old heading, so the entries merged into the renamed, *already released* section — no conflict, no warning, no signal of any kind. Thirty entries of 1.8.0 work ended up filed under `[1.7.5]`, a version that produced no images and no release object (#3429), and would have shipped undocumented; two were upgrade-affecting (#3286 storage accounting, #3231 Rekor SET verification). `scripts/ci/check-changelog-unreleased.sh` now asserts that the first `## [` heading in `CHANGELOG.md` is exactly `## [Unreleased]` — a repo-state invariant, so it fires on whichever PR reaches CI first rather than depending on who made the change — with a self-test alongside it so the gate cannot rot, both wired into the shell-tests job.

### Changed

- **`RELEASING.md` tells you to check the versioned component source sets before tagging a backport** (#3429, #3340). `v1.7.5` died because a **comment-only** edit to `docker/Dockerfile.scanner-adapter` rode along in a backport: the publish gate treats any change under a versioned component's source set as a source change and refuses to republish an existing exact version tag, so the tag's publish failed — and the ruleset forbids deleting or moving it, so the version was burned. `v1.7.2` died the same way. The cut sequence gains a required step: confirm no change touches `docker/*/VERSION`'s directory or its sibling Dockerfile unless that `VERSION` is bumped in the same change, with the two commands to run. Step 3 also now states *why* the fresh `## [Unreleased]` is mandatory rather than leaving it in parentheses.

### Security

- **`h2` is bumped to 0.4.16, closing RUSTSEC-2026-0258 (unbounded empty DATA frames)** (#3449). `h2` < 0.4.16 accepts and queues empty HTTP/2 DATA frames without limit; if the receiving streams are not actively drained this grows memory without bound, or panics when the accumulated length overflows. It reaches us transitively through `hyper 1.8.1`, `reqwest` and `tonic`, and the path is reachable from the network rather than theoretical: the gRPC listener (`0.0.0.0:9090`, `GRPC_PORT`) is HTTP/2-only h2c and answers before any application-level authentication, `axum` is used with its default `http2` feature so the REST and registry surface serves HTTP/2 as well, and remote/proxy repository fetches negotiate HTTP/2 outbound — so a hostile upstream can aim the same frames at the registry. Upstream rates it low severity, and it is: the frames are empty, so the attacker trades bandwidth for the server's per-frame bookkeeping rather than for payload. The fix costs a two-line lockfile change, which is a poor trade to decline on an unauthenticated listener. Lockfile-only — `hyper`, `reqwest` and `tonic` all accept `^0.4`, so no direct dependency moves and no HTTP stack major bump was needed. **`v1.8.0` and earlier tags carry the vulnerable `h2` 0.4.13**; deployments that expose port 9090 (or terminate `h2` in front of the registry) should take the patch release.

- **The bundled `grype` CLI is built against `golang.org/x/mod` v0.40.0, clearing CVE-2026-56864 and CVE-2026-56865** (#3465). Both are HIGH and both are fixed in x/mod v0.40.0; grype v0.117.0 pins v0.38.0, so `Security Scan` reports `usr/local/bin/grype  Total: 2 (HIGH: 2)` against the backend image and fails. Docker Publish on `main` failed on this for two consecutive runs (32337069636, 32419016885) with every manifest-merge job skipped, so **no images published from `main` between 2026-08-19 and this fix** and the `v1.8.1` tag could not be cut. The same finding burned `v1.7.7` on `release/1.7.x`, where rulesets forbid deleting or moving a tag and the version number was lost.

  Neither lever the `grype-bin` stage already had could reach it. This is a **module**, not the standard library, so the `GO_MIN_PATCH` toolchain floor (#3352) does nothing; and there is no release to bump to — v0.117.0 is the latest anchore/grype release and `main` (`ffbca56`) still pins v0.38.0, with no open or merged PR to move it. Upstream has not made this bump anywhere, which is what makes this an **override** rather than the version bump that clears the same CVE pair in the scanner-adapter's Trivy (upstream v0.74.0 carries x/mod v0.40.0 itself). `docker/Dockerfile.backend` and `docker/Dockerfile.backend.alpine` therefore apply `go get golang.org/x/mod@v0.40.0` after the commit-pin verification and before `go build`, with three build-time guards: the pinned source must still require v0.38.0 (so the tag bump that makes the override redundant is also what stops the build until it is deleted), `go get` must move only the five declared `golang.org/x/*` modules MVS pulls with it, and the compiled binary's Go build info must record v0.40.0 — because that build info is exactly what Trivy reads. `grype --version` still prints `grype 0.117.0` and `Supported DB Schema` is still 6, so `scanner_version` and the DB seeded at build time are unaffected.

  **The vulnerable code was never linked in**, and that is worth recording rather than implying urgency the finding did not have. Both advisories are scoped to `golang.org/x/mod/sumdb` (`Client.Lookup`, GO-2026-6180) and `golang.org/x/mod/sumdb/tlog` (`tileHashReader.ReadHashes`, GO-2026-6179) — the GOSUMDB transparency-log client — and neither package is in grype's build graph: `go mod why golang.org/x/mod/sumdb` answers "main module does not need package", and `go list -deps ./cmd/grype` links only `semver`, `module`, `modfile` and `internal/lazyregexp`, reached via `cmd/grype/cli/commands/internal/jsonschema` → `x/tools/go/packages`. Trivy flags Go binaries at module granularity from the embedded build info and cannot see that, while `govulncheck` would call the binary unaffected. It is fixed at source anyway rather than suppressed in `.trivyignore`, which is reserved for findings with no fix available in a tool we can build.

  This clears the backend image only. The same CVE pair was also reported against `usr/local/bin/trivy` in the scanner-adapter image and is cleared separately by repointing at a Trivy build carrying x/mod v0.40.0; **both were required before `main` could publish again**, and both ship in this release.
- **The scanner-adapter bundles a trivy binary rebuilt on go1.26.7, clearing all 10 HIGHs that were blocking Docker Publish, and the adapter's own Go build now asserts a `GO_MIN_PATCH` floor** (#3352). `docker/Dockerfile.scanner-adapter` pinned `ghcr.io/artifact-keeper/trivy` at the `0.73.0-r2` manifest list, whose binary was compiled on go1.26.5 and carried 10 HIGH findings against `usr/local/bin/trivy`. They are **not** all standard library, and grouping them as such — as the original entry did — hides which lever moves which: **six** are Go stdlib against that go1.26.5 build (CVE-2026-33818, -56853, -56858, -56859, -56860, -56862), **two** are `golang.org/x/net` (CVE-2026-39821 in `x/net/idna`, and CVE-2026-46600), and **two** are `golang.org/x/mod` (CVE-2026-56864, CVE-2026-56865). Only the first six move with a toolchain floor; the other four are modules and move with the source pin. **All ten surfaced at main's gate** — none of them is suppressed in main's `.trivyignore`, and the `x/net` pair that `release/1.7.x` still lists as a live token there is a `# RETIRED:` tombstone on main (#3352, #3309), so 1.7.x's gate saw eight of these where main's saw all ten. The Trivy gate in `docker-publish.yml` runs `severity: CRITICAL,HIGH` with `ignore-unfixed` and `exit-code: 1`, so it failed on every push to main; because the gate is a `needs:` of `merge-scanner-adapter`, no multi-arch manifest was assembled and the whole publish chain stopped. That is why `v1.8.1` could not be tagged on its originally prepped date of 2026-08-19. The pin now names the `0.74.0-r1` manifest list (`sha256:37317c08…`), an in-house build of upstream trivy v0.74.0 on go1.26.7 that picks up `x/mod` v0.40.0. Measured against the same vulnerability DB, the trivy binary goes **10 HIGH → 0** at the gate's settings and **11 → 1** at all severities with no ignorefile, adding nothing; the survivor is `GO-2026-5932` in `x/crypto`, which has no fixed version and is UNKNOWN severity, so it is outside the gate on both filters. Nothing in the backend reads the trivy version string — the adapter probes it at startup — so the move from 0.73.0 to 0.74.0 is contained to this image.

  The second half is the durable part. The adapter's Go stage builds on the floating `golang:1.26-alpine` tag with no floor, an asymmetry with `docker/Dockerfile.backend{,.alpine}` that this file's own comment flagged and said to close "if this adapter ever fails a scan on the same class". It has: `release/1.7.x`'s adapter binary (1.2.3) carries the same six Go-stdlib HIGHs, because a floating tag is only as good as the last rebuild and that image was built before go1.26.6 existed. The same `ARG GO_MIN_PATCH=1.26.6` plus `sort -V` assertion the backend Dockerfiles use is now in the adapter's build stage, so a lagging mirror or a stale builder cache fails the build loudly instead of shipping a vulnerable stdlib silently. On main that has not bitten: the floating tag currently resolves to go1.26.6, so main's adapter binary already scanned clean before this change and still does. **The floor clears no finding on main** — it is purely a regression guard. `docker/scanner-adapter/VERSION` goes to **1.2.7**: the adapter's version space is shared across branches, 1.2.6 is reserved for `release/1.7.x`, and exact version tags are never republished (#3429).

### Sponsors

Thank you to our backers for supporting ongoing development:

- **Ash A.** ([@dragonpaw](https://github.com/dragonpaw))
- **Gabriel Rodriguez** ([@injectedfusion](https://github.com/injectedfusion))

[Become a sponsor](https://github.com/sponsors/artifact-keeper)

### Thank You

- **[@ThaSami](https://github.com/ThaSami)** — for diagnosing the Maven flat-object GC data-loss inversion (#3431): that the attribution table's read-path role and its GC-candidate role are in direct conflict, that the fix is a system-written source allowlist with `write_claim` deliberately left reclaimable, and that the 1.7.3 guard rebuild is what turns the latent bug into an active one.


## [1.8.0] - 2026-08-17

### Security

- **A suppressed virtual-PyPI member's cached distribution is no longer served, on either the download or the PEP 658 metadata path** (#3404). The virtual member loop tries local storage first for *every* member, Remote ones included, because Remote-typed repositories legitimately carry `artifacts` rows (direct upload, replication, promotion) and because a distribution and its `.metadata` sidecar must resolve from the same member. But the PEP 708 ownership decision (#1600 / #2311 / #2937) was computed ~50 lines *after* that local-first serve and consulted only by the upstream-fetch branch, so a member the dependency-confusion guard suppresses still answered from its cache. A Case-B distribution — a version only the remote has, for a name a higher-priority local member owns — was therefore correctly hidden from the simple index while remaining downloadable through the same virtual repository, permanently, for any file that member had ever cached. Such rows accumulate through ordinary operation: any request that legitimately resolved the distribution before a local member claimed the name, while the remote was ranked equal-or-higher, or under a since-removed PEP 708 `tracks` declaration, leaves one behind. `uv.lock` and hashed `requirements.txt` pin exact filenames, so a resolution made while the remote was permitted kept succeeding long after an operator re-ranked the members to stop it.

  The decision is now resolved once per request and applied at the *top* of the member loop, so a suppressed Remote member is skipped before any of its bytes, its cache row, or its per-member gates are consulted. `serve_virtual_metadata` carried an independent copy of the same ordering defect and is fixed with it — fixing only the wheel would have left the sidecar describing exactly the distribution the wheel now refuses. Both paths share one `PypiOwnershipGuard`, so they can no longer drift. Unchanged: the #2937 Case-A carve-out (a platform/ABI-distinct wheel of a version the owner already provides still downloads, because the index still lists it), Local/Staging members (never the suppressed side), Remote members the operator ranked equal-or-higher than the owning local, and the #3220 fail-closed download-gate block. The index/download symmetry invariant #2937 states — a distribution the index advertises is downloadable and one it hides 404s on both — now also holds for members with a local row.
- **Content-negotiated PyPI simple indexes declare `Vary: Accept`, and a credentialed caller's index is no longer marked shared-cacheable** (#3406). The simple index is negotiated on `Accept` (PEP 691 JSON vs PEP 503 HTML) and was served `Cache-Control: public, max-age=60` with a body-hash `ETag` but no `Vary`. Under RFC 9110 §12.5.1 a shared cache — a corporate forward proxy, a CDN, nginx, Squid — is then entitled to serve either stored representation to any client and to validate one representation's `ETag` against the other's. `uv` rejects a simple index whose `Content-Type` is not one it accepts, so a mis-served representation is a hard install failure, not a cosmetic one; this is the downstream twin of the defect #3290 fixed in AK's own proxy cache and of the upstream mislabelling #2801 handles.

  Two directives, for two distinct reasons. `Vary: Accept` is now emitted on every negotiated emitter — both simple-index builders, both representations, and the `304` shortcut, which must carry the same cache-key metadata as the `200` it stands in for. Separately, `Cache-Control` is downgraded to `private, max-age=60` when the request carries a credential (`Authorization`, `X-API-Key`, or the `ak_access_token` session cookie), because AK's indexes are caller-dependent: a virtual repository's listing is built from the members the *caller* is authorized to read (#2073 / #3323 / #3399), and a private repository's requires credentials at all. `Vary: Accept` alone would not have addressed that, and `public` is not merely a missing opt-out — under RFC 9111 §3.5 it is the explicit permission that overrides a shared cache's default refusal to store a response to an `Authorization`-bearing request. Anonymous reads of public repositories keep `public, max-age=60` and stay CDN-cacheable, which is the case the 60s TTL exists for. The credential test is shape-based and reuses the middleware's own carrier list, so a fourth carrier cannot be added without this following. Non-negotiated callers of the shared helper (Maven `maven-metadata.xml`, Conda repodata) are deliberately untouched: adding `Vary` there would fragment their caches for no gain.
- **OpenPGP repository-metadata signatures now carry an explicit expiration window, and the signed-Release cache is capped below it** (#1327). Signatures produced by `SigningService::sign_openpgp_detached` / `sign_openpgp_cleartext` had no `SignatureExpirationTime` subpacket, so a signed `InRelease` copied to a mirror stayed indefinitely valid on its face. `apt` does not enforce signature expiry, but mirror-validation tooling does (`debmirror`, `apt-mirror2`, `gpgv --check-quiet`), and an unbounded signature gives those tools nothing to check a stale mirror against. Every on-demand metadata signature now carries a validity window — default **7 days**, matching the cadence official Debian archives re-sign at — configurable via `SIGNATURE_EXPIRY_SECONDS`.

  The in-process signed-Release cache is what makes this safe rather than merely declarative. That cache is keyed by content hash, so a distribution whose `Release` never changes would otherwise keep serving one signature forever, straight past its own stated expiry. Cache entries now carry the instant they were signed and age out at **the signature window minus one hour**, so the registry stops serving an entry strictly before the signature inside it stops verifying, and re-signs on the next request. The expiry/rotation arithmetic lives in pure helpers (`signature_expiry_duration`, `signed_cache_max_age`, `signed_cache_entry_is_fresh`) that are unit-tested without a database or a keyring, including the boundary case and a backwards clock step.

  **Rollover semantics — what happens to already-published metadata:**

  - **Nothing already published becomes unverifiable.** This changes only metadata the registry **re-signs on demand**: Debian `InRelease` and `Release.gpg`, and the live RPM `repomd.xml.asc`. Each is recomputed from current repository content on request, so the client always receives a signature minted well inside its window.
  - **Signatures that are minted once and stored are deliberately excluded.** RPM published `@N` snapshots write `repomd.xml.asc` to storage and never regenerate it — an expiring signature there would silently stop verifying once the window elapsed. Signature expiry is therefore **opt-in per call site** (`SigningService::with_signature_expiry`) rather than a constructor default, so any signing path not explicitly audited for re-signing keeps its previous behaviour. `@N` snapshots continue to verify indefinitely.
  - **No key rotation is involved.** The signing key, its fingerprint, and the published public key are unchanged; only the signature carries a window. Signatures made before this change have no expiration subpacket and remain valid exactly as before — there is no flag day and no re-import.
  - **Clients need to do nothing.** `apt` ignores the subpacket. Mirror-validation tooling gains a stale-mirror check it did not previously have. A mirror that refreshes at least weekly is unaffected; one that has not refreshed in over 7 days will now be reported as carrying an expired signature, which is the intended signal.

  **Upgrade note for operators:** if you mirror an Artifact Keeper Debian repository with tooling that enforces signature expiry, ensure the mirror refreshes more often than `SIGNATURE_EXPIRY_SECONDS` (default 7 days), or raise the window. Air-gapped exports that sit longer than the window between refreshes should set a longer window, or `SIGNATURE_EXPIRY_SECONDS=0` to restore the previous no-expiry behaviour exactly. Values below 7200 or above 31536000 are clamped into that range rather than rejected, so a bad value cannot take the registry down.
- **The OCI `/v2` read path now enforces the token's `read:artifacts` action scope** (#3408). `oci_scopes_grant` is the action ceiling for an API token presented directly to `/v2` (as a Bearer token, or in the Basic password field). It was called at seven sites and every one of them asked for `write:artifacts` or `delete:artifacts`; the pull path asked for nothing. `oci_read_permitted` enforced the scan-token pin and the token's *repository* allow-list, then the admin and public-read bypasses, then the `read` permission — but the token's declared *action* ceiling was never consulted for a read. A token minted with `scopes = ["write:artifacts"]` and nothing else could therefore `docker pull` every repository its owner was permitted to read: the push-only CI publish credential was silently also a read credential.

  A pull now takes the same gate the other six verbs take, in the same shape and the same position — immediately after authentication, so it runs **before** the admin bypass, for the same reason the repository allow-list is documented as binding admins: an action ceiling the principal themselves asked for should bind them regardless of `is_admin`. The refusal is `403` with the OCI `DENIED` envelope naming the missing scope, matching the write gate; a `404` would be indistinguishable from a deleted repository and would tell the operator nothing about their token. It applies to every read verb — manifest `GET`/`HEAD`, blob `GET`/`HEAD`, `tags/list`, `referrers`, and `_catalog`. `Claims.scopes` is deliberately untouched: threading the ceiling there would deliver it by demoting `is_admin` through `with_scope_gated_admin`, which roughly 34 raw handler checks also read, changing what the principal *is* everywhere the resulting `AuthExtension` travels (decided on #3322).

  So that the next `/v2` verb cannot re-open the hole, the property is enforced structurally rather than by a test pinned to the sites fixed here. `authenticate_oci` — the wrapper that returned claims and discarded the scope tuple — is deleted, so a handler cannot acquire the identity without also acquiring the ceiling that governs it; and a class-level test scans this module and fails on any production authentication that is not followed by an `oci_scopes_grant` check naming an `OCI_SCOPE_*` constant, unless the call site carries a written `OCI-SCOPE-EXEMPT` justification. This is the same greppable-exemption shape #3399 and #3331 used.

  **Upgrade note.** This is a fail-closed behaviour change. Affected: any API token presented directly to `/v2` whose scope list omits `read:artifacts` and is neither `*` nor `admin` nor the bare parent `read`, being used to pull — concretely, `write:artifacts`-only CI publish tokens that also `docker pull`. Such a pull now returns `403 DENIED` ("Token does not have required scope: read:artifacts"); the fix is to add `read:artifacts` to the token's scopes. Unaffected: unscoped credentials (JWT login sessions, `docker login` with a password, and any token whose ceiling is absent — `oci_scopes_grant` passes `None` through), tokens carrying `*` or `admin`, scanner pull tokens, anonymous pulls of public repositories, and every write or delete path.
- **REST content paths now require the `read` action, not merely any grant, on a private repository** (#3331). `RepositoryService::user_can_access_repo` answered *"does this principal hold any grant on this repository"* and took no action parameter at all, so every one of its call sites got the same any-grant answer — including the ones that return artifact **bytes**. A principal whose only entitlement on a private repository was `{write}` or `{delete}`, or who held a role carrying no `read`, was therefore served that repository's contents through the REST API while the equivalent OCI `/v2` and native-format reads of the same repository correctly refused. The grant predicate tests only `actions <> '{}'`, and `permissions.actions` is `TEXT[]` rather than an enum, so a rule carrying an unrecognised action string qualified too; the `role_assignments` arm never joined `roles`, so a role's own permissions were never consulted.

  `user_can_access_repo` now takes a required `RepoAccess` argument. `RepoAccess::READ` layers the canonical action choke-point — `PermissionService::check_repository_action`, the same function with the same arguments that the direct `/v2` read gate and `repo_visibility_middleware` call — on top of the unchanged tenant gate, so a repository is readable through REST exactly when a direct read of it succeeds, by construction rather than by assertion. All eleven call sites were classified individually. Six now ask for `read`: the artifact-by-id gate and its label/security/SBOM/quality-gate siblings, `/api/v1/tree/content` (which serves bytes), the repo-scoped SBOM and CVE-history reads, the quarantine reason text, and `require_visible` — which alone fans out to roughly 23 read surfaces including `GET /api/v1/repositories/{key}`, artifact listings and downloads, storage trees and storage-usage trees, virtual member listings, and the security, signing, curation, approval, promotion-rule, quality-gate and repository-label reads.

  Five sites deliberately keep the tenant gate and say so in the source with a `TENANT-GATE-ONLY` marker: the REST write/delete gate (whose action half is layered separately and canonically by `require_repo_action(.., "write"/"delete", ..)` — naming `read` there would refuse a legitimate publish-only identity), repo-token management (a visibility bar; the delegation ceiling is enforced per-scope at mint time), cross-tenant promotion and the two curation tenant-ownership gates (tenant identity, not a capability, and enforced independently of the admin flag), and repo-scoped webhook management. Making the parameter required means a new call site cannot inherit the action-blind behaviour by omission, and a structural test scans the whole backend source tree and fails on any `RepoAccess::TenantOnly` that does not carry a written justification — the class-level shape #3399 used for virtual member walks, chosen because a test pinned to the sites fixed here would go green while the next content path re-opened the hole.

  **Who is affected.** Only callers reading a **private** repository, and only those whose grant does not carry `read`. Concretely: a fine-grained `permissions` rule with `actions` of `{write}`, `{delete}`, `{write,delete}`, or any value the action vocabulary does not recognise; and a custom role whose `permissions` array omits `read`. The built-in roles all carry it (`admin`, `developer`, `reader` and `repository-owner` each include `read`), so a default installation sees no change. Public repositories, anonymous reads, global admins, and every write or delete path are unaffected. Publish-only automation identities that were reading through these surfaces will now be refused — that is the intended capability removal, and the fix is to add `read` to the grant. This closes the asymmetry #3323 and #3326 left behind, where the virtual-member path was stricter than a direct REST read of the same member.
- **Virtual-repository reads authorize the member set against the caller across every format** (#3323). The format-route visibility middleware authorizes only the repository named in the URL — the virtual PARENT — while everything a virtual repository aggregates is a separate repository with its own ACL, so an anonymous request to a **public** virtual over **private** members reached each member's content unchecked. The filter existed (`proxy_helpers::authorize_virtual_members`) but was applied on a minority of call sites, and the two shared metadata primitives (`resolve_virtual_metadata`, `collect_virtual_metadata`) took no caller at all, so every one of their callers inherited an unfiltered walk. All content-serving virtual read paths — PyPI simple index (root and per-project), npm packument and version metadata, cargo sparse index, Helm `index.yaml`, NuGet V2/V3 search, registration and version indexes, Conda repodata/channeldata, CRAN `PACKAGES`, Hex names/versions/package, RubyGems spec indexes and quick gemspecs, Conan search/revisions/package listings, Composer `packages.json` and p1/p2 metadata, Pub package and version info, Swift release listing and metadata, Go `@v/list` and `@v/*.info`, Debian `Release`/`InRelease`/`Packages`, Alpine `APKINDEX`, Maven root listing, and the Hugging Face `ETag`/`X-Repo-Commit` resolution — now resolve members through one shared helper, `proxy_helpers::authorized_virtual_members`, which is `fetch` composed with the same caller predicate a direct read of that member would apply. The two primitives now REQUIRE a caller, so their remaining callers cannot regress silently, and a structural test scans every format handler and fails on any raw member walk that neither authorizes nor carries an explicit `UNFILTERED-ENFORCEMENT` marker. Enforcement walks — the OCI scan-verdict deny-set, the PEP 708 isolation and shadowing guards, priority maps, cache invalidation and cycle detection — are deliberately left unfiltered, because narrowing a deny-set or a shadowing decision by caller visibility would let a caller who cannot see a member escape that member's gate.

  Three caller-INDEPENDENT caches sat in front of documents that are now caller-dependent, which would have moved the leak rather than closed it: the npm computed-packument cache, the cargo in-process sparse-index cache, and the Maven virtual `maven-metadata.xml` merge cache. Each is now used only when the aggregated document cannot vary by caller — that is, when every member is public, the exact condition under which the authorized member set is identical for anonymous and authenticated callers. A virtual repository with a private member computes per request; all-public virtuals keep their cache.

  Not included: the RPM `repodata` walk (`repomd.xml` and friends). Its document is rendered once and cached per virtual repository id, and `repomd.xml.asc` is a detached signature over the rendered bytes, so a per-caller document breaks both the shared cache and the reproducible-signature invariant (#2636). It carries an explicit `UNFILTERED-DEFERRED` marker in the source and remains tracked on #3323 pending a cache re-keying design.
- Unrecognised scanner severity tokens now classify as `high` instead of `info`, so ungraded findings fail closed at severity gates (#3306). This applies to every scanner adapter (Trivy/Grype/Harbor image reports, filesystem scans, OpenSCAP), covering tokens such as Trivy's `UNKNOWN`, XCCDF's `unknown`, and any vendor-specific grade the classifier does not recognise. Explicitly graded tokens are unchanged, `negligible` (a recognised distro triage grade) stays at `info`, and the OSV/GHSA advisory-ingestion fallback remains independently at `medium`. Note this is deliberately stronger than the `medium` originally proposed on #3306: a `max_severity='high'` policy blocks only `critical` and `high`, so a `medium`-bucketed ungraded finding would still have been served under the default posture.
- **Alpine/apk repository signatures are now verifiable by real `apk`** (#3021). A signed Alpine repository was rejected by `apk` with `BAD signature`, so operators had to either fail closed or fall back to `--allow-untrusted` — a signature the ecosystem client cannot check is a false assurance, not a protection. Three parts of the `.SIGN.RSA` contract were wrong at once: the signature was computed over the *uncompressed* `APKINDEX` text (apk digests the *compressed* index segment as served), with a SHA-256 digest (apk fixes SHA-1 for a `.SIGN.RSA.` entry; `RSA256`/`RSA512` select the others), and the result was appended as an extra member of the index tar (apk expects a separate, "cut" leading gzip stream, the layout `abuild-sign` produces). A served `APKINDEX.tar.gz` is now the concatenation of a signature gzip stream — one `.SIGN.RSA.artifact-keeper.rsa.pub` tar entry, no end-of-archive blocks — and the index gzip stream it signs, byte-for-byte as signed. Verified end-to-end against apk-tools 2.14: `apk update` and `apk add` succeed against a signed repository with `artifact-keeper.rsa.pub` in `/etc/apk/keys/`, while a tampered index and an untrusted key are both refused. Unsigned repositories are unaffected, remote/virtual repositories still proxy upstream indexes untouched, and a signing failure still fails closed with a 500 rather than serving an unsigned index (#2660).
- **Cookie-authenticated web-UI mutations must carry the `X-Requested-With` header** (#3065). The web UI authenticates with an httpOnly session cookie and had no CSRF defense-in-depth behind the cookie's `SameSite=Strict` attribute. State-changing requests (`POST`/`PUT`/`PATCH`/`DELETE`) that are authenticated by the session cookie *and* are browser-originated are now refused with `403` unless they carry the custom header the web UI sends on every request — a header a cross-site HTML form cannot set at all, and that a cross-origin `fetch()` cannot set without a preflight this server does not approve. The browser-origination test reuses the existing Fetch-Metadata/`Accept` detector added in #3082 rather than adding a second one. The `SameSite=Strict` half of the contract, which remains the primary mitigation, is now pinned by a test that scans every backend source file for session-cookie literals, so a new `Set-Cookie` call site cannot silently omit it.

  **Upgrade note:** this is a behavior change for any non-browser client that authenticates to `/api/v1` with the `ak_access_token` **cookie** and issues state-changing requests while also looking browser-like (sending `Sec-Fetch-*` or an `Accept` listing `text/html`) — such a client must now send `X-Requested-With` (any value) or, preferably, switch to a Bearer/API token. Native package-manager clients (`pip`, `npm`, `cargo`, `docker`, `maven`, …) and every token- or Basic-authenticated API call are explicitly exempt and unchanged: the requirement is keyed on the credential actually being the cookie, matching the middleware's existing credential precedence.
- **`GET /v2/_catalog` is scoped to the repositories the caller can read** (#3355, #3269). The catalog handler authenticated the caller and then discarded the identity, so the listing enumerated every repository holding OCI content regardless of who asked — the last OCI read surface not brought under the per-repository read gate. It now filters through `oci_read_permitted`, the decision core extracted behaviour-preservingly from `require_oci_repo_read_access`, so a catalog listing and a pull can never answer from different predicates; the fine-grained `permissions` table is honoured and not merely role assignments, which is why the existing set-based repo-listing helper was deliberately not reused. The authorized-id filter is injected inside the existing paginated subquery **before** `LIMIT`, so `n`/`last`/`Link` pagination operates on the authorized set and a trailing unauthorized row never signals `has_more`; a repository whose decision errors is omitted, so reads fail closed. Unchanged in both directions: a global admin with no token repo-scope ceiling and no scanner pin skips the filter and runs the pre-existing unfiltered query byte-for-byte, and an anonymous request still receives the `401` + `WWW-Authenticate: Bearer` challenge Docker and Podman rely on to reach `/v2/token` — broadening anonymous access to a public-only listing would be a separate product decision.

### Added

- **Ansible Galaxy client-contract integration tests and an operator docs page** (#3283). The Ansible format had no file in `backend/tests/` at all, and the conformance scripts that do exist assert on HTTP status codes only. Between them, three client-visible breaks shipped behind green CI: discovery registered at `/api/` while `ansible-galaxy` requests `/api` (#3137), `Authorization: Token <key>` rejected as a malformed header (#3137), and a publish response missing the `task` key the CLI indexes unconditionally (#3282). None of them is visible to a test that picks its own URL spelling, its own auth scheme, and reads only a status code.

  `backend/tests/ansible_galaxy_tests.rs` deliberately inverts each of those blind spots. It drives the **production composition** — the real handler table nested at `/ansible` behind `repo_visibility_middleware`, which is where the reported `401` came from and which the handler's own unit tests never reach — against a **private** repository, so the middleware is load-bearing rather than waved through. It authenticates with `Authorization: Token <api_key>` exclusively, the byte-for-byte header ansible-core emits (`GalaxyToken.token_type = 'Token'`), including on the `auth_required=False` discovery probe that `_add_auth_token` attaches it to anyway. It navigates the install path by **following the hrefs the server advertises** rather than constructing URLs, so a wrong advertised URL fails the test instead of being quietly bypassed, and it round-trips the advertised `download_url` back to the published bytes. It re-implements ansible-core's own `_urljoin` and its `task`-to-poll-URL derivation (keep the last non-empty path segment, rebuild from `api_server`) so the publish contract is checked the way the client checks it, through `state`, `finished_at` and `messages`. `list_collections`, `version_list` and `version_info` had zero coverage of any kind before this and are now covered, as are the `403` on a token missing `write:artifacts` and the rule that a rejected upload carries no `task`. Registered in the CI integration-test job.

  Also adds `docs/ansible-galaxy.md`: `ansible.cfg` / `--server` / `ANSIBLE_GALAXY_SERVER_*` configuration against an Artifact Keeper repository, how to mint the API key the `Token` scheme carries and which scopes it needs, the publish/import-task contract, an endpoint reference, and a troubleshooting table. Note the Starlight documentation site now lives in `artifact-keeper-site`; this page should be mirrored there.
- **`docs/sso-saml.md`: how to find and formulate the SAML ACS URL** (#2583, partial). The ACS URL embeds `saml_configs.id`, a server-generated UUID, and there was no documentation for obtaining it — operators were reading it out of Postgres or scripting Terraform `null_resource` API calls to recover it. The new page documents the URL shape, three ways to obtain the id (the unauthenticated `/api/v1/auth/sso/providers` list, the admin `GET /api/v1/admin/sso/saml` list keyed by the already-`UNIQUE` provider `name`, and capturing it from the creation response), what `use_absolute_acs_url` changes about the `Destination`/`Recipient` binding, and the workarounds for a rebuilt environment. The requested *configurable* identifier is not implemented here: the analysis on #2583 explains why the safer shape is an additive, validated `slug` accepted alongside the UUID rather than a primary-key change, which would rewrite the ACS URL of every deployment that currently works.
- **Per-repository outbound proxy configuration** (#2469, #2811, migration 205). A remote repository can now route its upstream traffic through a specific egress proxy, so a segmented-network deployment can send different upstreams out via different network paths. Configured at `PUT`/`GET /api/v1/repositories/{key}/egress-proxy`, which — like `upstream-auth` (#2603) — requires repository `admin`, not merely `write`.

  **Precedence, stated explicitly because an egress control whose effective value is ambiguous is a bug waiting to happen.** A per-repository setting **overrides** the process-wide `HTTP_PROXY`/`HTTPS_PROXY`/`NO_PROXY` environment; it never composes with it. Three modes:

  - `inherit` — follow the process proxy environment. The default, and the state of every repository that predates this change, so behaviour is unchanged unless an operator opts a repository in.
  - `direct` — never proxy this repository, even when the environment sets a proxy. This is the other half of #2811 ("certain repos require a proxy, others do not") for a deployment whose global posture is proxied.
  - `explicit` — use this repository's own `proxy_url` and `no_proxy` list, ignoring the environment entirely.

  Merging the per-repo and global settings was rejected deliberately: composing two proxy URLs and two `no_proxy` lists has no unambiguous meaning. A stored `explicit` config whose URL is missing resolves to `direct`, not `inherit`, so a half-written config fails closed instead of silently falling back to the ambient path.

  **Credentials.** A proxy URL may embed `user:pass@`, so it is treated as credential material on the same path the upstream credentials already use rather than a second secret-handling scheme: encrypted at rest in `repository_config` with the same hex-wrapped AES-256-GCM envelope as `upstream_auth_credentials`, never returned by the API in raw form (the read-back is redacted to `http://***@proxy:3128`, plus a `proxy_credentials_configured` boolean, mirroring `upstream_auth_configured`), never logged (`EgressProxyConfig` has a hand-written redacting `Debug`), and never placed inside the URL string handed to `reqwest` — the userinfo is split off and applied via `Proxy::basic_auth` so the proxy object's own rendering carries nothing either. `proxy_url` / `egress_proxy_url` are also added to the audit-detail redaction key list. Switching a repository away from `explicit` deletes the stored ciphertext rather than orphaning it.

  **SSRF.** A per-repo proxy is attacker-adjacent outbound routing, so three things hold. (1) The proxy URL is validated in a new `TrustedInternal` trust class: RFC1918/CGNAT/IPv6-ULA are permitted, because a corporate egress proxy legitimately lives on the internal network and requiring an allowlist entry would make the feature unusable in exactly the deployments it exists for — but cloud-metadata, loopback, link-local and unspecified addresses stay hard-blocked, so the proxy field cannot be repurposed into a metadata reader or a localhost port-scan oracle. (2) The destination keeps its existing upstream-context guard; a configured proxy never relaxes it. (3) Most importantly, routing through a proxy means `reqwest` does not resolve the destination locally at all — it dials the proxy and hands over the hostname — so the connect-time SSRF DNS guard would silently stop covering that repository. The proxied path therefore re-applies the destination check explicitly before issuing the request. That check is resolution-at-check-time, so it cannot close DNS rebinding for proxied fetches the way the resolver does for direct ones (we are not the party doing the final resolution); this is recorded alongside the pre-existing residual-gap note in `api::validation`. The proxy's own address is exempted from the DNS guard by exact full-host equality only, extending the existing #2570 mechanism to per-repo proxies without widening it for any other target.

  All outbound paths for a proxied repository go through its client — buffered and streaming fetches, conditional `If-None-Match` revalidation, the OCI bearer token exchange and its retry — so the routing cannot be escaped via the auth flow. Clients are cached per repository (60 s TTL, mirroring the custom-User-Agent cache) and invalidated in-process on write so a change applies immediately.

  Two existing weaknesses surfaced while implementing this and are fixed here. `log_proxy_env()` printed `HTTP_PROXY`/`HTTPS_PROXY`/`ALL_PROXY` verbatim at INFO on every client build, including any embedded `user:pass@`; it is now redacted the same way. And AWS's IPv6 instance-metadata address `fd00:ec2::254` sits inside the unique-local range `fc00::/7`, which the `TrustedInternal` class permits unconditionally — so an operator-configured endpoint could be pointed at IMDS over IPv6 even though the IPv4 form was refused. IPv6 cloud-metadata addresses now have their own hard block in `api::validation`, applying to every context, which closes the same gap for the scanner-adapter URL (#2389) as well.
- **Proxy-cached artifacts are now visible to the security surface, and have an SBOM** (#3394, #3395, #3396, migration 202). Proxy-cached content has been scanned at download time and blocked on policy violation since #2954, but none of it was readable: proxy bytes deliberately have no `artifacts` row (#1278/#1280), so every artifact-keyed security read — CVE history, SBOM, security score — is structurally empty for them.

  - `GET /api/v1/repositories/{key}/security/proxy-scans` — verdict summary and cached-path listing, carrying the repository's `scan_on_proxy` / `proxy_scan_action` enforcement context so `vulnerable` is not ambiguous between a repository that blocks the pull and one that serves it anyway.
  - `GET /api/v1/repositories/{key}/security/proxy-sbom` — CycloneDX or SPDX, generated on demand from the recorded component inventory. Proxy SBOMs are deliberately **not** stored in `sbom_documents`: that table's `artifact_id` is `NOT NULL` with an FK to `artifacts`, so making it nullable would touch every SBOM query in the codebase and put a cross-tenant `ON DELETE CASCADE` in the path. No existing SBOM handler or query was modified.
  - **Per-CVE detail for proxy content** (#3395). `proxy_scan_results` stored counts and a max severity but not *which* CVEs were found, so a blocked build could be told "9 findings, 4 high" and still not learn which CVE caused it — identifying them required re-scanning the cached bytes by hand, outside the product. The findings already existed in memory at `aggregate_proxy_verdict` and were discarded once the counts were computed; they are now persisted to a digest-keyed `proxy_scan_findings` table and returned on a single-path (`?path=`) read of `/security/proxy-scans`. Written alongside the verdict but independently fallible, so a detail-write failure can never prevent a vulnerable artifact being recorded as vulnerable, and deduplicated on the way in for the same pin-duplication reason that inflated `findings_count`.
  - **On-demand rescan** (#3396). `POST /api/v1/repositories/{key}/security/proxy-scans/rescan` scans a path already in the cache — `proxy_cache_artifacts.storage_key` is `NOT NULL`, so the bytes are read straight from object storage with no upstream fetch and no dependency on the artifact being requested again. This is the remedy for anything cached before inventory recording shipped, which otherwise had no SBOM and no way for an operator to ask for one. It records through the same `proxy_scan_and_record` write path the inline gate uses, so a rescanned verdict and a pulled verdict cannot disagree; it requires repository **write** access and is throttled to one rescan per repository per 30s (`429` with `Retry-After`) because it converts one request into a full scanner run.

  All of these are **path-keyed, never digest-keyed**. Verdicts and inventories are global by content digest, so accepting a digest parameter would turn them into a cross-tenant lookup oracle; a path is inherently scoped to the calling repository. All authenticate unconditionally, including on public repositories, where `require_visible` would otherwise return early.
- **2FA enforcement policy: require TOTP enrollment for admins, or for everyone** (#2805, migration 198). TOTP was fully implemented per user but strictly opt-in, with no way for an operator to require it. A new system-wide policy — `disabled` (the default, byte-for-byte the historical behaviour), `required_for_admins`, `required_for_all` — is read and written at `GET`/`PUT /api/v1/admin/settings/totp-policy` and can be pinned by the `TOTP_POLICY` environment variable.

  **Chosen semantics, stated explicitly because this changes login behaviour when enabled:**

  - **Scope.** The policy is evaluated only on the interactive local password login (`POST /api/v1/auth/login`). API tokens, service accounts, package-client basic auth, already-issued sessions and the refresh grant are untouched, so enabling it never breaks CI, `docker pull`, or a running `pip install`.
  - **Exemptions.** SSO/LDAP/CI-authenticated users are exempt — their MFA is delegated to the identity provider and Artifact Keeper holds no password for them. Service accounts are exempt because they cannot type a code.
  - **Grace = "finish enrolling inside this login", not a countdown.** A covered user who has not enrolled is never rejected. Instead of a session, the login returns `totp_enrollment_required: true` plus a 10-minute, single-use, type-scoped *enrollment ticket*. The ticket authorizes exactly two new public endpoints — `POST /api/v1/auth/totp/enroll/setup` (repeatable: returns the secret and `otpauth://` URL) and `POST /api/v1/auth/totp/enroll/complete` (single-use: verifies the first code and returns the real session **and** the backup codes in the same response). There is no state in which a valid password yields no way in.
  - **Lockout safety, guard 1.** `PUT` refuses to *tighten* the policy unless the administrator making the change already has TOTP enabled — a "you first" rule. That administrator's sign-in path (password + `/auth/totp/verify`) is the pre-existing, already-working one, so at least one administrator provably retains access regardless of how any particular client handles the new enrollment exchange. Loosening the policy is **never** refused, in either direction, so an operator cannot be trapped in the strict state.
  - **Lockout safety, guard 2 (offline break-glass).** `TOTP_POLICY=disabled` plus a restart overrides the stored setting and needs no working login at all. While the environment variable is set the policy is pinned and `PUT` returns `409` explaining how to unpin it. An unparseable `TOTP_POLICY` is ignored with a warning rather than defaulting either way.
  - **A policy change does not invalidate anything.** Existing sessions and refresh tokens survive; enforcement begins at each user's next interactive login, so the enabling administrator keeps their session. Completing forced enrollment *does* revoke that user's prior sessions and refresh-token families, since enrollment is a credential-posture change.
  - **`POST /auth/totp/disable` returns `409` while the policy covers the caller** — enforcement that only bit at the next login would not be enforcement. This adds no lockout path: disabling already required a valid current code, so anyone who can reach it can still sign in.
  - `TOTP_ENROLLMENT_REQUIRED` (classified as a `denied` outcome — the password was right but no session was issued) and `TOTP_POLICY_CHANGED` audit actions are recorded. `GET` also reports how many local administrators/users are enrolled, so the blast radius of a change is visible before making it.

  Backend only. The web UI lives in `artifact-keeper-web`; until it handles `totp_enrollment_required`, an operator on `required_*` should complete enrollment through the API (or use the `TOTP_POLICY` break-glass). The "you first" guard exists precisely so that gap cannot lock everyone out.
- **`auth.admin_break_glass_enabled` in `GET /api/v1/system/config`** (#2571). The public system-config endpoint now advertises whether the verified-admin break-glass local login is available, mirroring `local_login_gate` for an admin caller: `true` unless an SSO provider is enabled *and* the operator opted into strict SSO-only via `SSO_DISABLE_ADMIN_BREAK_GLASS` (#2018). This is the backend flag the login UI needs to render a discoverable "Sign in with admin" button next to the SSO buttons instead of hiding break-glass behind the undocumented `?fallback=local` parameter. Display-only: the login endpoint keeps enforcing the same policy server-side, so the flag never widens access. The existing `local_login_enabled` flag is unchanged and still only advertises the full local form on explicit `ALLOW_LOCAL_ADMIN_LOGIN=true` opt-in.
- **Artifact responses carry the uploader** (#3271). `artifacts.uploaded_by` has been on the row since the column was added and was surfaced on `ArtifactVersionResponse` by #2397, but the artifact list/detail responses the web UI actually reads never serialized it, so the artifact detail popup had no uploader to show. `ArtifactResponse` now carries `uploaded_by` (the user id, always serialized, `null` for anonymous uploads and for proxy-cached objects, which have no `artifacts` row) alongside `uploaded_by_username`, resolved server-side so the UI does not have to follow a bare UUID with an admin-only user lookup. The name is resolved in one batched query per listing page — never one per row — and is best-effort: a lookup failure omits names rather than failing the listing, and a deleted uploader keeps its id with a `null` name. The last-downloaded half of the same UI request needs no backend work; `GET /api/v1/artifacts/{id}/stats` already returns `last_downloaded`.
- **OCI 1.1 Referrers API** (#3108). `GET /v2/<name>/referrers/<digest>` now returns an image index of every manifest in the repository whose `subject` points at the digest — empty index (200) when there are none, `400 DIGEST_INVALID` for a malformed digest, `artifactType` filtering with the `OCI-Filters-Applied` header, and descriptors carrying `artifactType`/`annotations` per the distribution spec. Subject edges are recorded at manifest-PUT time in the new `oci_manifest_subjects` table (same transaction as the tag upsert) and removed when the digest's last tag entry is deleted; a subject-carrying push now answers the `OCI-Subject` header, which is how oras/cosign discover Referrers support instead of falling back to the tag scheme. Closes the remaining distribution-spec v1.1.0 conformance content-discovery failures.
- **Admin endpoints to inspect and invalidate a proxy scan verdict by digest** (#3244, split from #3105). `GET /api/v1/admin/proxy-scan-verdicts/{digest}` returns every stored `proxy_scan_results` row for a content digest (accepts `sha256:`-prefixed or bare hex); `DELETE` removes them, forcing re-assessment on the next pull — previously the only recourse for a stale or false-positive verdict was raw `psql` against the production database. Both routes are instance-admin only (the verdict store is content-addressed and global across repositories and tenants, so no repo-scoped principal should hold this lever), and the deletion is recorded in the audit log with the discarded verdict summary. Deliberately **not** a waiver: a still-flagged image re-records `vulnerable` on its next scan; risk-acceptance semantics (scoped, expiring waivers à la Harbor CVE allowlists / Xray ignore rules) remain a separate feature.
- **Artifact Keeper is a governed VS Code extension gateway** (#3253, closing #3252). A protocol-faithful Open VSX gallery proxy: extension query, latest-version resolution, gallery manifest, and platform-qualified asset delivery. Client-visible metadata and VSIX delivery URLs are rewritten back through Artifact Keeper so editors never contact public CDNs directly and the repository's egress posture actually holds, and `enforce_repository_age_gate` is applied during query and latest resolution and on exact VSIX delivery for Remote and Virtual repositories. Validated against real VSCodium 1.126 and code-server 4.131 install and update flows with external egress blocked.

  **Two things real-client testing forced that no fixture would have.** Open VSX answers a client's `IncludeVersions` follow-up with complete version histories — 28 MiB and 12,592 versions for `rust-analyzer` alone, 55 MiB on one measured `@popular` page — which no fixed metadata cap survives, so version-heavy searches `502`d. Buffered passthrough is therefore replaced by bounded per-extension composition: a typed version skeleton plus a latest-per-platform detail query, newest 30 per (platform, channel), with asset URLs synthesized from the gateway's own templates. Separately, the gallery `assetUri` was being built from `X-Forwarded-Host` — a header-injection bug reachable only behind a real edge.

  `target_platform` is canonicalized to a single coordinate shared by cache, upstream and policy, so one artifact cannot occupy unbounded permanently-immutable cache entries. The public-only contract is explicit: a private Remote gallery route answers `403` and a non-Remote repository `501`. That split is deliberate rather than cosmetic — `501` is heuristically cacheable by default while `403` is not, so a cached `501` would survive an operator flipping the repository public. Curation enforcement and boundary validation on the legacy download route land with it; the remaining follow-ups stay tracked on #3254, #3255 and #3256.

### Changed

- **The proxy/OCI inline scan gate no longer blocks on `Info`-severity findings** (#3243 stage 2). `ProxyScanVerdict::is_vulnerable()` was `findings_count > 0`, severity-blind, so every Debian/Ubuntu-derived base image — which carries a long tail of `Negligible` CVEs, all bucketed at `Info` — produced a `vulnerable` verdict and a 403, making fail-closed OCI proxying unusable against ordinary images. The verdict now counts findings **above** `Info`: `Negligible`/`info`/`informational` findings are still scanned, recorded in the verdict row, and reported, but they no longer 403 a pull. This matches Harbor, which documents `negligible` as never blocking at any threshold. **Upgrade note:** repositories with `scan_on_proxy` enabled whose only blocking findings were `Info`-severity start serving those digests on the next scan (stored verdicts re-record on their next freshness re-scan, or within the 30-day TTL). Findings that carry any graded severity — `low` and above — block exactly as before.
- **`scan_configs.block_on_policy_violation` and `severity_threshold` are now enforced on the proxy/OCI inline scan gate** (#3243 stage 3, #3246). Both columns shipped in migration 022 with full CRUD, UI, and CLI plumbing but no enforcement reader anywhere — the UI promise "findings at or above this severity count as a policy violation" was never kept. `block_on_policy_violation` (default **false**) is now the explicit per-repo opt-in: when set, a `vulnerable` verdict blocks the pull **only** when its highest observed severity is at or above the repo's `severity_threshold`; a verdict whose severity is known and strictly below the threshold serves. The gate fails closed on every ambiguous shape — a legacy verdict row with no stored `max_severity`, an unparseable severity token, or an unreadable `scan_configs` row all still block — and the threshold applies uniformly at all four consumption sites: the shared per-format verdict gate (PyPI/npm/OCI manifests), the inline scan outcome, and both OCI blob re-block queries (single-repo and virtual member-spanning), with virtual repositories taking the stricter-of-two gate over the virtual's and the member's configs so a virtual can never become the lax route. **Upgrade note:** a repository on column defaults changes nothing — the opt-in defaults to false, so the historical block-on-any-finding posture is preserved byte-for-byte. Only repositories where an operator had explicitly set `block_on_policy_violation = true` begin getting the documented threshold semantics (their previously-inert `severity_threshold`, default `high`, becomes live).
- **`trivy filesystem` scans no longer drop ungraded (`UNKNOWN`) CVEs at the scanner invocation** (#3296). Three Trivy producers — the hosted-artifact filesystem scanner, the incus rootfs scanner, and the scanner-adapter's filesystem mode — pinned a `--severity CRITICAL,HIGH,MEDIUM,LOW` allowlist, so a CVE Trivy could not grade was filtered out before the report was written: no `scan_findings` row, no count contribution, invisible to every gate, score, and triage workflow, and unrecoverable by any operator-side policy because the data was discarded at the CLI. All three now share one severity list (`TRIVY_FS_SEVERITY_LIST` in the backend; the adapter's `fsSeverity` mirrors it) that includes `UNKNOWN`, matching the adapter's image path, which always admitted it. **Upgrade note:** this is fail-closed in direction — strictly more findings reported, so security scores can drop and finding counts rise on the next rescan of content carrying ungraded CVEs. How an ungraded finding is *classified* once reported is #3306 (tracked separately).
- **`.trivyignore` entries can now be retired** (#3309). Release-preflight check 1 required main's suppression set to be a superset of every active `release/*` branch's, which made the set monotonic: a suppression provably dead for main's images could never be deleted while any release branch still listed it, and nine dead tokens were stuck on main in `RETIRED` prose comments. Check 1 now accepts an explicit `# RETIRED: <token>` tombstone line as accounting for a release-branch token, so a deliberate, measured removal is a reviewable one-line diff the gate reads by machine — while a token a release branch has and main neither lists nor tombstones (the #3039 forward-port miss) still hard-fails, and a token listed both live and retired fails as a contradiction. The eight measured-dead tokens are deleted and tombstoned; `CVE-2026-46600` stays live because it was re-measured as present in the grype v0.117.0 binary's Go stdlib. Both directions are covered by `scripts/ci/test-release-preflight.sh`.
- **The scheduled `cargo audit` job is renamed to "Security audit report (non-gating)"** (#3310). It shared the display name "Security Scan" with docker-publish's hard publish gate while being structurally unable to fail (every step ends in `|| true`), so "Security Scan is green/red" was ambiguous between a meaningful gate and a report generator. The stale "red by design until #1611/#1609 lands" soft-fail comments on the cache-correctness and concurrency e2e suites (both issues long closed) are replaced with named waivers pointing at the real, current blocker: both harnesses abort at repo creation because the SSRF guard rejects their docker-internal mock upstreams (#3363); the suites flip to blocking when that is fixed.

### Documentation

- **The supported rollback procedure is documented in the operations docs, not only in a changelog entry** (#3314). `SKIP_MIGRATIONS=true` is how you roll a deployment back to an earlier release, and the recipe previously lived only in the 1.7.3 upgrade note — which scrolls out of view at the next release. [`docs/operations/rollback.md`](docs/operations/rollback.md) now covers it permanently: why an older binary refuses to start (a `_sqlx_migrations` **ledger** check that runs before any data is read, not a data incompatibility — operators who misread it as the latter hesitate to proceed, and the hesitation is the expensive part), why the flag is safe rather than merely a silenced safety check (it takes a branch that never constructs the `Migrator`, and migrations are additive so the older binary keeps writing correct rows), why deleting rows from `_sqlx_migrations` is the wrong alternative, the boundary condition where the recipe stops holding (a migration that drops or renames a column the older binary reads, which the flag cannot help with — plus the `git diff` that checks for one, since nothing today marks a migration destructive), and how far back it is actually verified (the 1.7.3 → 1.7.1 pair; anything wider is labelled untested rather than implied). `ARCHITECTURE.md` links to it from the Database section.

### Fixed

- **PyPI PEP 658 `.metadata` resolves artifacts stored at a bare path** (#3405). `serve_metadata` open-coded its artifact lookup as `path LIKE '%/' || $2`, which requires a `/` before the filename, while the distribution download resolves the same artifact through `proxy_helpers::resolve_local_artifact_by_suffix` — which falls back to an exact `path = $2` match when the suffix match misses. An artifact whose `path` is just its filename (the shape produced by the generic upload endpoint, by imports and migrations that synthesise no `{name}/{version}/` prefix, and by replication of such rows) therefore downloaded fine and returned `404` for its `.metadata` sidecar. Because the simple index advertises `core-metadata: true` for every `.whl` unconditionally, the index told the installer the sidecar existed; modern `pip` and `uv` both treat a `404` on an advertised PEP 658 sidecar as a hard failure rather than falling back to downloading the wheel, so the install failed outright while the wheel was demonstrably fetchable. `serve_metadata` now routes through the same resolver as the download path, so there is one filename→artifact rule instead of two that can drift, and the metadata route inherits the indexed reverse-suffix lookup as well. `serve_virtual_metadata` calls `serve_metadata` per member, so direct and virtual repositories are both covered. The bug was invisible to the existing fixtures, which all seed `{name}/{version}/{file}` paths; the regression test seeds a bare-path artifact and asserts the wheel and its sidecar resolve consistently.
- **`docker-publish` no longer cancels its own in-flight image builds on a burst of merges to main** (#2099). The workflow cancelled superseded branch pushes to "stop building stale containers". Its arm64 legs run on GitHub-hosted `ubuntu-24.04-arm` and finish fast, but its amd64 legs run on the capped self-hosted `ak-ci-runners` pool and were still in flight when the next merge landed — so the cancel killed them mid-build and the multi-arch **manifest** jobs, which fan in from both arches and are the only jobs that move the `:dev`/`:main` floating tags, never ran at all. After merging #2092, #2090 and #1927 back-to-back on 2026-07-01, `artifact-keeper-scanner-adapter:dev` was simply never published and iac PR #186's `Verify Image References` gate had nothing to resolve. `cancel-in-progress` is now the literal `false`: runs queue and every one of them finishes. This workflow *publishes*, so a cancelled run is not a re-runnable pass/fail verdict, it is a registry left pointing at a stale digest — the intermediate runs cost build minutes but leave the registry consistent at every step, whereas cancelling saved those minutes by gambling that the next run completes, and when merges arrive faster than the amd64 build takes, that gamble loses every time. Release **version** tags were never affected (tag pushes were already excluded). A regression guard in `backend/src/config.rs` scans **every** `.github/workflows/*.yml`, classifies any workflow that pushes container images (`push: true`, `docker push`, `imagetools create`, ignoring comments), and fails if one declares a top-level `cancel-in-progress` that is not the literal `false` — an expression is explicitly not good enough, since the one this replaced evaluated to true on exactly the branch pushes that publish the floating tags.
- **Flaky NuGet `_db_tests` failing with `503` under full-suite parallelism** (#3407). `api::handlers::nuget::{push_db_tests, read_db_tests}` failed intermittently with `503 Service Unavailable` on the request itself rather than on any assertion — up to ten together on a bad run, clean on others, on branches touching no NuGet code.

  **The reported diagnosis was wrong, and the correction is the fix.** #3407 attributed the shed to the bcrypt auth-concurrency semaphore. Running the full suite against a real database shows zero bcrypt sheds; every failure carries `Too many concurrent uploads for this repository; please retry shortly`, which is the **ingest-extraction** budget in `util/bounded_archive.rs`, an entirely different limiter.

  That budget has two fast-fail (never queueing) caps: a global one defaulting to 8 concurrent archive decodes, and a per-tenant sub-limit of 4. The sub-limit is keyed on an ambient `TenantKey` installed by `run_with_tenant_scope`, which `repo_visibility_middleware` sets once the repository resolves. The handler tests drive routers built by `tdh::Fixture::router_with_auth`, which does not include that middleware — so `current_tenant()` returns `TenantKey::Unattributed` for **every** test in the binary, and all ~15k of them contend for a single four-permit bucket. The fifth simultaneous decode anywhere in the suite sheds, immediately, with no queue. Both caps are now sized for the harness under `cfg(test)` (never compiled into the shipped binary; `backend/tests/` links the library without `cfg(test)` and keeps production values). Production defaults, env overrides and the fairness behaviour are untouched — only the number of permits the test binary hands itself changes.

  Also landed from the original diagnosis, on its own merits: the crate now hashes at bcrypt cost 4 under `cfg(test)`. It was not the cause of this flake, but `hash_password` holds an auth permit for ~100-300 ms at `DEFAULT_COST` and the fixtures throughout the crate already hash at cost 4, so paying a production work factor in a test binary was pure waste. The constant-time rejection pad (`dummy_bcrypt_hash`) now tracks the same cost factor instead of being pinned at 12, because bcrypt reads its work factor out of the hash being verified: a pad at a *different* cost from real hashes would make the "token prefix not found" path take visibly different time from the "wrong secret" path, reintroducing exactly the timing oracle the pad exists to close. Separately, the in-crate fixtures each hardcoded `auth_max_concurrency: 8`, and because that semaphore installs through a `OnceLock` the effective cap was whichever fixture built an `AppState` first; they now share one constant sized for the harness.

  Deliberately **not** fixed with a serial lock. That is the correct fix for #3402 (genuinely shared database state) and would make this worse by holding permits longer while serialised tests queue behind them. The two share only the symptom.
- **The weekly container scan now reports what it actually found, and scans the tag that tracks `main`** (#3262). The scheduled scan targeted only `:latest` and filed an issue claiming every result was a "CRITICAL/HIGH finding with an available fix". Both halves were wrong, and together they made a benign result look like a live release blocker.

  `:latest` only moves on a **stable release**, so it can lag `main` by weeks — the image the 2026-08-10 run scanned was `v1.6.3`, built 2026-07-24. Both findings it reported (`CVE-2026-71556` HIGH and `CVE-2026-71557` MEDIUM, `github.com/go-git/go-git/v5` v5.19.1) were located in the vendored `usr/local/bin/grype` binary and were **already fixed on `main`**: a direct scan of `ghcr.io/anchore/grype:v0.117.0` — the tag both backend Dockerfiles pin since #3307 — shows no go-git package at all. They clear on the next stable publish and are explicitly **not** suppressed; `.trivyignore` now records that measurement so they are not added reflexively, since suppressing a finding from a build we no longer produce is precisely what that file's header warns against.

  The scan step sets no `severity:` filter (it is visibility-only, `exit-code: 0`), so labelling all results CRITICAL/HIGH overstated them. Severity is now read from the SARIF rule and reported as a per-severity breakdown, alongside the file each finding sits in. The scan additionally covers `:dev`, which tracks `main` and where a finding *is* actionable by a PR, and every filed issue now names the image ref, its `org.opencontainers.image.version` label and its build date, so a stale-release artifact is distinguishable from a regression at a glance.

  A repo-wide regression test (`bundled_grype_pin_is_consistent_across_images_and_trivyignore`) scans **every** `docker/Dockerfile*` rather than the two that vendor grype today, and fails if they pin different grype versions or if `.trivyignore` stops naming the pinned version — the drift class CLAUDE.md calls out from #2126/#2059, where a fix landed in one file and a second copy silently rotted. Removing the bundled grype entirely is tracked on #2881, and rebuilding it from source with a patched Go toolchain (as #3123 did for trivy) on #3352.
- **Federated logins no longer invent a broken placeholder email address** (#3416). `users.email` is `VARCHAR(255) UNIQUE NOT NULL`, but every federated protocol treats the address as optional, and three login paths had never been migrated onto the shared derivation #3161 introduced for the OIDC and SAML *service* paths.

  The consequential one is the **DB-configured OIDC provider callback** (`handlers/sso.rs`) — a second, independent OIDC implementation that the #3161 fix never touched. It read a missing `email` claim as `.unwrap_or_default()`, collapsing "absent" to a shared `""`. Because the column is `UNIQUE`, the *first* user whose IdP omits the claim provisioned fine with `email = ''` and the **second** failed on `users_email_key` — so a login broke later, for a different user, with an error that named neither the real cause nor the account that caused it, and the row holding `''` stayed put, so it never self-healed. That is bug #3119 verbatim, in a path nobody had noticed existed.

  The two LDAP sites (`extract_user_from_entry`, and the bind-identity fallback taken when the directory search fails) derived `{username}@unknown`. That was keyed on the *pre-uniquification* username, so two directory entries that are distinct identities but normalize to the same username derived the same address and collided on the same constraint; and `.unknown` is not a reserved TLD, so a synthetic address could in principle resolve rather than being inert by construction. Both now key on the entry's **DN** — the value `get_or_create_user` already stores in `users.external_id` and looks the account up by, which makes the synthetic address exactly as unique and as stable as the account row itself — under a new RFC 2606 `no-email.ldap.invalid` domain alongside the existing OIDC and SAML ones.

  All three now call `services::federated_email::resolve_federated_email`, so a fourth variant cannot drift. No privilege boundary is involved: federated identity matching keys on `external_id` + `auth_provider` in every provider path, never on email, so a colliding placeholder could not resolve one user as another — the `UNIQUE` constraint turned it into a hard provisioning failure instead. The impact was availability and diagnosability. Two existing LDAP tests asserted the old `@unknown` values and have been updated deliberately. Users already provisioned with a placeholder address keep it; nothing is rewritten on upgrade.
- **CVE blast radius no longer answers zero for proxy-cached content** (#3397). `GET /api/v1/admin/security/cve/{cve_id}/blast-radius` joins `download_statistics` and `artifacts`, both keyed on `artifacts.id`. Proxy-cached content has no `artifacts` row by design (#1278/#1280) and its pulls land in `proxy_download_statistics` keyed by `proxy_cache_id`, so for a CVE present only in proxied content the report did not merely undercount — it was structurally incapable of returning anything but zero, and a zero to "who is affected by this CVE?" reads as "nobody". The response now carries a `proxy_exposure` block (affected digests, repositories, downloads, distinct downloaders, anonymous flag) computed over the new `proxy_scan_findings` table joined to the proxy cache catalog and `proxy_download_statistics`. It is present even when every field is zero, with an explicit `coverage` of `included`, so "searched, nothing found" is distinguishable from "not searched"; an artifact-id target reports `not_applicable`, since an artifact id can never name proxy content. The proxy numbers are reported alongside the hosted summary rather than folded into it — the two have no join key in common, and merging them would make `affected_artifact_count` mean two different things at once. The download window is applied to the `LEFT JOIN` rather than to a `WHERE`, so a windowed query still reports digests that are cached but were not pulled inside the window.
- **Proxy-cached artifacts are no longer told they cannot be security scanned** (#3344). `ARTIFACT_NOT_ANALYZABLE_MSG` claimed "SBOM generation and security scanning are available only for artifacts hosted in this registry". The SBOM half was accurate; the scanning half had been false since #2954, which built a parallel digest-keyed scan pipeline (`proxy_scan_results`, enforced at serve time by `proxy_helpers::gate_proxy_scan_serve`) without touching `sbom.rs`, so nothing pointed at the string. Acting on the message would have led an operator to mirror everything into hosted repositories to obtain coverage they already had. The single blanket constant is replaced by one message per capability — SBOM generation, on-demand scanning, signing, CVE history — each stating only its own limitation, naming the formats download-time proxy scanning is actually wired for (PyPI, npm, Docker/OCI, not Maven/Go/NuGet/RubyGems), and pointing at the endpoint that answers the question for proxied content. Signing deliberately still dead-ends: there is no proxy signing equivalent, and inventing a pointer would be the same class of stale claim. `signing.rs` carried the same phrasing and is corrected too.
- **Proxy scan `findings_count` is no longer doubled for PyPI wheels** (#3394). `ScanWorkspace::prepare_pinned` catalogs a wheel's component twice — its own `METADATA` plus the synthetic pin written into the workspace root — so the scanner reported every CVE against both copies and the recorded count was exactly double the true one. Findings are now deduplicated on `(vulnerability, component)` before the counts and severity buckets are computed. Relatedly, cached upstream index responses (HTML/JSON directory listings, which have no package inventory) are excluded from proxy artifact listings and scan counts; they were previously counted as `not_scanned` with reason `unknown`, a permanent false positive no operator action could clear.
- **Large artifact uploads no longer fail once they take longer than the global request timeout** (#3263). `GLOBAL_REQUEST_TIMEOUT_SECS` (120s by default) was applied as a `tower` timeout over the *whole* router, and that clock covers the time the client spends streaming the request body — so on upload and download routes a duration cap silently became a **size** cap. A ~90 MB `PUT /api/v1/repositories/{repo}/artifacts/{path}` over a typical WAN link exceeds 120s and was aborted mid-body; because the error response is emitted while the client is still writing, the connection is reset and the client never reads the status at all (`curl: (56) Recv failure: Connection was reset`, `HTTP_STATUS:000`). Raising `MAX_UPLOAD_SIZE` could not help: the limit being hit was time, not bytes. Artifact **byte-transfer** routes — repo-scoped `/artifacts` and `/download` paths, the resumable `/api/v1/uploads` session API, OCI blob/manifest transfers, and the Git LFS / incus / lxc protocols whose routers already declare multi-GB bodies — are now exempt from that timeout. Upload size is still bounded by `MAX_UPLOAD_SIZE` (enforced mid-stream), and the load-shed + `GLOBAL_MAX_CONCURRENCY` backstop still bounds how many transfers run at once, so the "no request path can pin every worker" protection is preserved for the server-side work it was written for; every non-transfer route keeps the timeout unchanged, with the same 503 body.
- **Promotion within a shared storage namespace is no longer blocked by its own source repository** (#3266). The #2511 cross-repository write guard excluded only the *target* from its ownership query, but a promote copies the **source** artifact's content-addressed `storage_key` into the target — so on a shared-namespace (cloud) backend the source came back as an "other owner" and every `generic-staging → generic-local` promotion failed with `409 ... already owned by another repository`. The single, bulk, and approval-execute copy paths now pass both ends of the promotion as legitimate owners. The guard's actual job is untouched: a *third* repository holding that key still blocks the write, repo-isolated (filesystem) backends are still skipped, and direct uploads still allow only the writing repository.
- **Deleting a user no longer fails with "db operation failed" when they created migration jobs or signing keys** (#2878, migration 200). `created_by UUID REFERENCES users(id)` in `020_migration_tables.sql` and `027_signing_keys.sql` had no `ON DELETE` clause, which Postgres defaults to `NO ACTION` (RESTRICT), so the `DELETE FROM users` aborted with a foreign-key violation surfaced as an opaque 500 — the OIDC accounts in #2875 could never be removed. Every `REFERENCES users(id)` now carries a deliberate disposition: **`SET NULL`** for provenance/attribution columns (`source_connections`, `migration_jobs`, `signing_keys`, `signing_key_audit`, `promotion_history`, `cve_history`), which keeps the audit rows themselves and matches the pattern already used everywhere else in the schema; **`CASCADE`** for the `NOT NULL` `user_id` of transient upload-session rows (`oci_upload_sessions`, `upload_sessions`, `incus_upload_sessions`), mirroring `083_download_tickets_cascade.sql`. Cascading the audit tables was rejected — deleting a user must not erase who signed a key or approved a promotion. Should a future table reintroduce a bare reference, the delete now returns `409` naming the blocking relation instead of "db operation failed".
- **One oversized federated group name no longer drops all of a user's groups** (#2835). `groups.name` is `VARCHAR(255)`, and `sync_federated_groups_to_local_groups` ran every group in a single transaction with no length check: an IdP that claimed one group name longer than 255 characters aborted the whole transaction (`value too long for type character varying(255)`), so **none** of the user's groups synced — including the legitimate `id_token` ones — while login itself still succeeded, leaving the user silently unauthorized. Names that cannot fit are now rejected before any statement is issued (measured in characters, matching `varchar` semantics, so multi-byte names are not over-counted) and logged as a skip, and each remaining group's DB work runs inside its own `SAVEPOINT` so a residual per-group failure rolls back only that group. Blank claim entries are skipped exactly as before, and the #2759 ownership refusal for name-colliding operator-managed groups is unchanged.
- **An Incus finalize left in flight by a pre-#3072 server is refused instead of double-appending** (#3083, migration 201). #3072's seal-by-rename closed the reclaim double-append for all new upload sessions, but a session already in `finalizing` under pre-#3072 code and reclaimed *after* the upgrade could still append the completing request's body a second time: old code appended it straight onto the staging file and could advance `bytes_received` past it, which on disk is indistinguishable from a legitimately committed session. `incus_upload_sessions.finalize_phase` is that missing bit — current code stamps it on every write that moves a session into `finalizing`, so a `finalizing` row that still has `NULL` there can only have come from the old path. Such a row is no longer reclaimed once its lease has lapsed; the finalize returns `409` telling the operator to cancel the session and re-upload, rather than publishing a corrupt image. The refusal is deliberately scoped to a **lapsed** lease: during a rolling upgrade an un-upgraded replica may still be actively finalizing one of these sessions under a live lease, and that finalize completes correctly under the old algorithm, so a concurrent retry keeps the ordinary "finalize already in progress" response instead of being told to throw the upload away. New sessions never reach this state, so the refusal is bounded to the upgrade itself.
- **A virtual PyPI repository now describes a distribution the same way the direct repository does** (#2748). Three emitters render a Simple index for a virtual repo, and which one runs depends on whether a remote member happened to answer — a fetch miss is a silent `debug!` — so the *same URL* changed shape from request to request. Concretely: (1) the PEP 691 union emitter never set the PEP 658/714 `core-metadata` flag on local-member wheels, though both the direct emitter and the virtual **HTML** emitter did, so through a virtual repository `pip`/`uv` lost the metadata fast path and re-downloaded whole wheels to read `Requires-Dist`; (2) the #2937 ownership filter's HTML rebuild emitted a bare `<a href>`, discarding every `data-*` attribute — so a Case-A platform wheel (the windows build of a version the local owner already ships) lost `data-requires-python`, offering an incompatible build to every interpreter, and lost `data-yanked`, presenting a **withdrawn release as an ordinary resolution candidate**; its JSON sibling only filters entries and always kept both, so the two representations of one index disagreed about installability and about yanking; (3) `versions` was ordered lexicographically on both virtual emitters (`1.10` before `1.9`), regressing the PEP 440 ordering #3106 established for the direct path. The local-entry JSON builder and the anchor rebuilder are now single implementations shared by the direct and virtual paths, so the emitters cannot drift again, and version ordering goes through one PEP 440 comparator. Sharing the anchor rebuilder also gives the ownership filter the proxy rebuild's filename-metacharacter rejection, which it did not have. No change to the #1600/#2311 dependency-confusion boundary or to #2937's Case-A/Case-B decision — a remote-only version of a locally-owned name stays suppressed unless a PEP 708 `tracks` declaration or a member re-ranking permits the union.
- **No more native Basic-auth popup over the login screen when guest access is disabled** (#2936, #3082). With `AK_GUEST_ACCESS_ENABLED=false`, the guest-access guard answered every unauthenticated request — including the web UI's own `fetch()` calls — with `401` + `WWW-Authenticate: Basic`, and browsers respond to that challenge by raising their native credential dialog before the user ever sees the login/OIDC page. The guard (and the optional-auth middleware's invalid-credential 401) now classify browser-originated requests via a pure header check — any Fetch Metadata header (`Sec-Fetch-Mode`/`Sec-Fetch-Site`, forbidden headers that every modern browser sends and no package client does) or an `Accept` listing `text/html` — and omit the `Basic` (and cargo-only `Cargo`) challenge for them, keeping the `Bearer` challenge for RFC 7235 compliance. Browsers therefore land on the web UI's login/OIDC flow directly. Package-manager clients (pip, npm, docker, cargo, maven, conan, …) are never classified as browsers and keep the byte-identical challenge set they had before, so credential retry semantics are unchanged.
- **An OIDC provider seeded from `OIDC_*` env vars no longer outlives its configuration** (#2819, migration 197). `bootstrap_oidc_from_env` created/reconciled a provider row on boot, but removing `OIDC_ISSUER`/`OIDC_CLIENT_ID`/`OIDC_CLIENT_SECRET` and restarting left that row enabled: the login page kept advertising a flow that could no longer complete while `local_login_enabled` stayed `false` because an enabled provider row still existed — locking operators out of the UI. Env-seeded rows are now marked (`oidc_configs.env_seeded`), and a boot without the env vars disables — never deletes — any still-enabled marked row, restoring local login; re-adding the env vars re-enables it through the normal reconcile path. Admin ownership is respected in both directions: rows created via the admin API are never touched (the column defaults to `false`), and editing or re-enabling a previously env-seeded provider through the admin API clears the marker so later boots leave it alone.
- **A backup run whose durable claim is lost now aborts instead of finishing a second archive** (#3084). The claim-renewal loop added in #3072 stopped renewing when ownership was lost, but never told the worker already writing the archive: under a DB partition (or any renewal failure during a long backup) the original replica kept writing while another replica legitimately reclaimed the occurrence — one `backup_schedule_runs` row, two archives in storage. The renewal loop now hands the scheduler a cancellation token that fires the moment a renewal attempt reports ownership loss, and the backup worker observes it between chunks of work — each table export, each artifact read, and (decisively) before the final archive write. Transient renewal errors do not cancel — the claim is still held until its TTL — and dropping the renewal guard only stops the heartbeat, never the worker.
- **Backup and restore now leave audit events, the manifest carries a real checksum, and `BackupType` is honored** (#3011). Four gaps in the same subsystem: (1) `BACKUP_STARTED/COMPLETED/FAILED` and `RESTORE_STARTED/COMPLETED/FAILED` were defined and classified but never emitted — every backup and every restore, the highest-privilege operations in the product, ran unlogged; they are now written (best-effort) from the service so both the API and the scheduler paths are covered, and the restore handler records the requesting user. (2) The manifest shipped `checksum: ""` and `total_size_bytes: 0` in every archive ever produced; it now records the payload byte total and a SHA-256 over every entry's name and content, and the restore path verifies it before touching anything — a corrupted archive is refused outright, while pre-existing archives (empty checksum) restore as before. This closes the checksum half of the archive-integrity gap called out in the 1.7.5 restore-allowlist advisory (GHSA-95fx-g94v-8jqg); archive **signing** remains tracked in #3373. (3) A `metadata` backup no longer silently produces a byte-identical full archive: it captures the database dump and manifest without artifact bytes, and an `incremental` backup without a `since` anchor is rejected at creation instead of masquerading as incremental; incremental *schedules* anchor on the schedule's previous run automatically. (4) `ak_backup_operations_total` / `ak_backup_duration_seconds` are now emitted for API-triggered backups too, not only scheduled ones.
- **The password-expiry notification claim is heartbeated while SMTP runs** (#3086). The claim taken before the send had a fixed 300s expiry and no renewal, so an SMTP attempt that outlived the TTL let a second replica reclaim the row and email the same user again. The claim now gets the same token-fenced heartbeat the curation lease and backup runs use; the TTL remains the failover boundary for a dead worker.
- **The local `peer_instances` row follows `PEER_INSTANCE_NAME` / `PEER_PUBLIC_ENDPOINT` on every boot** (#2832). The row was seeded once at first boot and never reconciled again — `peer_instance_identity` was updated on restart but the Peers UI kept showing the stale first-boot name/endpoint (typically `artifact-keeper-local` / `http://localhost:8080`) forever. The `is_local` row is now reconciled from env on every boot, in place — matched by `is_local`, not by name — so a rename keeps the row's id and its repository assignments.
- **Cargo endpoints answer under the documented `/api/cargo` prefix** (#3000). The generated/documented cargo client config points the sparse registry index at `sparse+{base}/api/cargo/{repo}/`, but the backend only mounted the cargo router at `/cargo`, so cargo's very first fetch — `/api/cargo/{repo}/config.json` — 404'd and every publish against a freshly created repo failed with `config.json not found`. The full cargo router is now additionally mounted at `/api/cargo` (same visibility middleware, same upload limit), so `config.json`, the sparse index files, downloads and publishes resolve under both prefixes.
- **npm Virtual repositories merge every member's packument instead of first-match-wins** (#2844). The virtual metadata walk returned as soon as ANY member knew the package, so a hosted member holding only a fork build (e.g. `19.2.25-myorg.1`) masked the proxy member entirely and `npm install pkg@19.2.25` failed with "no matching version" even though the proxy member carried that release — deleting the fork artifact "fixed" it. Member contributions are now merged in priority order: `versions`, `dist-tags` and `time` federate across members with the higher-priority member winning per key, other packument fields keep the highest-priority member's values, per-member scope policies and age-gate filtering apply to each contribution exactly as before, and an unreachable member is still just a miss. The version-metadata path (`GET /{pkg}/{version}`) resolves through the same merge, so a version that only a lower-priority member carries now resolves there too.
- **Helm charts push with a plain `helm cm-push chart.tgz <repo-url>`** (#2941). The ChartMuseum `cm-push` plugin constructs its push URL as `{host}/api{repo_path}/charts`, so for a repository URL of `{host}/helm/{repo}` it POSTs to `/api/helm/{repo}/charts` — not the native `/helm/{repo}/api/charts` — and the push 404'd unless the non-obvious `--context-path` flag was supplied. The upload and delete handlers are now also mounted under the plugin's default shape (`POST /api/helm/{repo}/charts`, `DELETE /api/helm/{repo}/charts/{name}/{version}`).
- **The NuGet flat-container repair re-materializes the row's own storage object** (#2921). The #2919 streaming repair warms the shared proxy cache under `v3/flatcontainer/...`, which is a different key namespace from the row's `artifacts.storage_key`, and since #1278 proxy-cached content is not recorded in `artifacts` — so nothing ever healed the row, and every subsystem that reads `storage_key` directly (vulnerability scanning, quality gates, peer replication, promotion, signing, backup/export, the NuGet V2 OData download) kept seeing a missing blob permanently. When the repair finds a committed proxy-cache body, it now copies it back to the row's storage key — no second upstream pull — and serves from storage like the primary path; a cold cache still streams from upstream and heals on the next request. Entries that are negative-cached, age-gate-held, or content-coded are never copied; the digest-verified buffered repair (#2929) already wrote back and is unchanged.
- **Remote NuGet/Chocolatey metadata caching survives large OData queries** (#3291). A Chocolatey `choco search` sends OData queries whose sanitized cache segment exceeded the 255-byte filesystem component limit, so every sidecar write failed with `File name too long (os error 36)`, the entry was treated as a permanent cache miss, and each search/list re-queried upstream. Segments over 200 bytes are now truncated and suffixed with a SHA-256 prefix of the raw query — deterministic, collision-safe, and inside every filesystem's component limit — while shorter segments keep their exact historical keys so existing cache entries stay hits.
- **Artifactory source connections ping deployments fronted by the JFrog Platform router** (#2847). The migration connection test probed only the classic `{base}/api/system/ping`, which JFrog Cloud and self-hosted platform installs answer with `404 page not found`; those instances expose the Access service's ping at `{platform_root}/access/api/v1/system/ping` instead. The ping now falls back through the Access-service shapes (with a trailing `/artifactory` context stripped for the platform-root candidate) and reports reachable on the first success, so dry-run imports no longer fail at the connection test.
- **Maven grouped components for remote/proxy repositories list their files again** (#3270, #3265). The grouped view (`?group_by=maven_component`) reconstructs a remote repository's components from the `packages` catalog, which carries no per-file detail, so every remote component shipped `artifact_files: []` while hosted ones were populated from the `artifacts` table. The web UI renders one row per file (with a special-cased POM row), so a proxied component rendered as an empty shell — no POM, no jar, an empty "Files" tab. The individual cached objects are already enumerated in the `proxy_cache_artifacts` catalog (migration 159), so the file list is now recovered from there for **only the page being returned**: one bounded `LIKE ANY` over this page's GAV directory prefixes, capped at 64 files per component, so the query stays O(page) no matter how large the proxy cache grows. Attribution is re-checked in Rust rather than trusted from SQL — `_` is legal in a Maven artifactId and is a single-character wildcard under `LIKE`, so an over-fetched row is dropped instead of being attributed to a neighbouring component — and paths nested below the GAV directory are excluded, matching the hosted listing's one-row-per-file view.
- **Proxied Maven artifacts show their real coordinates and download count** (#3265). A remote repository's flat listing is reconstructed from the cache catalog, which stores only a path, so a proxy-cached row reported `name` as the bare filename (`guava-33.6.0-jre.jar` where a hosted row says `guava`) and no version at all. The UI reads those two fields as the artifactId and version, so a proxied artifact showed a blank version, the wrong artifactId in the package view, and a "copy install command" snippet built from those wrong coordinates. Maven-family remote listings now derive `(artifactId, version)` from the cache path, matching what a hosted upload of the same GAV reports. `maven-metadata.xml` and its checksum sidecars are deliberately excluded — they sit one directory *above* the version directory, where the coordinate parser would read the artifactId segment as the version — while a checksum sidecar of a real asset resolves normally, exactly as a hosted repository lists it. Separately, the download counter stayed at zero because the Maven remote branch was the one proxying format that never called `record_proxy_download`; pypi, npm and everything routed through the shared `try_remote_or_virtual_download` already did. It now records on both of its exits (the sha1-sidecar-gated fetch and the plain streaming fetch), HEAD-guarded and best-effort as elsewhere, and the recorded count is surfaced on the listing through one batched lookup per page instead of the hardcoded `0`.
- **OCI: upload-status probe (`GET /v2/<name>/blobs/uploads/<uuid>`) returned 405** (#3275). The catch-all dispatcher had no `("GET", "uploads")` arm, so clients recovering a chunked upload after a dropped connection or a 416 could not retrieve the current valid offset — docker/buildkit fell back to restarting the whole blob. The probe now answers `204 No Content` with `Location`, `Range: 0-<end>` and `Docker-Upload-UUID`, per the spec's active-upload contract.
- **OCI: setup and demo guards emitted REST-shaped JSON on `/v2`** (#3284). The global `setup_guard` and `demo_guard` layers sit in front of the nested `/v2` router and answered 403 with REST bodies (`{"error": ...}`) that docker/oras cannot render — a fresh unconfigured instance reported a `docker login` failure with no usable message. Refusals on the OCI surface are now the distribution-spec envelope `{"errors":[{"code":"DENIED","message":...}]}` via a shared middleware helper; REST routes keep their existing bodies.
- **OCI: `HEAD /v2/<name>/blobs/<digest>` skipped the scan re-block** (#3258). The #3003 blob re-block (a blob belonging to an image with a `vulnerable` verdict must not serve) had exactly one call site — `handle_get_blob` — so HEAD answered `200` with an exact `Content-Length` for content whose GET was `403`, leaking the existence and size of blocked content and making the two verbs disagree on the same resource. The check is extracted into a shared `enforce_blob_scan_reblock` helper (including the #3245 `scan_on_proxy` scoping and the #3023 virtual-member spanning) and applied to both verbs.
- **OCI proxy: Docker schema1 (v2s1) manifests evaded the scan-on-proxy gate** (#3024). The runnable-image predicate keys on a `config` descriptor, which v2s1 (`schemaVersion: 1` / `fsLayers`) predates — so the one legacy-runnable manifest shape proxied through a scan-enabled Remote repository `200` unscanned while every schema2/OCI variant was gated. The scanner cannot reassemble a v2s1 image (no config blob), so under a scan-on-proxy policy the deprecated schema is now refused outright (`403 DENIED`); with scanning disabled it still proxies unchanged.
- **Upstream `Content-Encoding` is re-declared on the remaining verbatim buffered forwards** (#3273). The Pub registry's non-JSON metadata pass-through (`proxy_pub_meta_get`'s fallback arm) and the buffered slow path of `proxy_fetch_or_redirect` both served upstream bytes verbatim while dropping the coding the upstream declared — the same #3211/#3260 shape at two sites that fix deliberately left out of scope. Both now thread the coding through and re-declare it (RFC 9110 §8.4), with `Content-Length` describing the coded bytes actually sent; the Pub JSON-rewrite arm keeps dropping it, since the bytes it emits are its own.
- **goproxy `@v/list` and `@latest` handle upstreams that store a `Content-Encoding`** (#3280). `@v/list` lossily decoded coded bytes into a run of U+FFFD and served it as `200 text/plain`; `@latest` failed the JSON parse on the coded bytes and returned 404 for every coded-upstream module. The list/latest fetch now reports the upstream coding, both parse paths strip a recognised coding through the shared bounded decoder (decompression-bomb budget included) and fail closed with 502 on an unrecognised or corrupt one, and `@latest` — which forwards the upstream document verbatim after gating — re-declares the coding on its response.
- **Virtual verbatim metadata forwards serve the member upstream's `Content-Type`** (#3281). `resolve_virtual_metadata` discarded the member's `Content-Type`, so all four Virtual verbatim forwards (hex `package_info`, rubygems `gem_info` and `quick_spec`, goproxy `.info`/`.mod`) overrode it with a hardcoded literal — a `mix` client asking a Virtual repository for `/packages/{name}` got the upstream's signed **protobuf** labelled `application/json`, where the Remote arm of the same endpoint forwards the type correctly. The transform seam now passes the member's type through, keeping each format literal only as the fallback for a member that declares none.
- **PyPI HTML and PEP 691 JSON proxy caches no longer drift apart** (#3290). The two content-negotiated representations of a Simple project index are cached under separate keys with independent lifetimes, and two seams let them diverge: the stale-entry conditional revalidation probed upstream **without** the `Accept` header the cached variant was fetched under (so the JSON variant's ETag was validated against the HTML representation, RFC 9110 §12.5.1), and explicitly invalidating one representation left the other serving an older snapshot — `uv` (which prefers JSON) reported versions missing that the HTML index already listed. Revalidation now replays the selecting `Accept`, and `POST /:key/cache/invalidate` on a PyPI repository evicts both variants of a Simple-index path in one operation.
- **A just-cached streaming proxy entry is readable to closely-following requests** (#3335). The streaming tee completes the client response as soon as the last byte is forwarded, but the cache writer still has its publish tail to run (staging copy, ETag pin, sidecar write) — and until the sidecar landed, a request for the same path classified the entry as a miss and opened its own upstream fetch. That both amplified upstream traffic behind a completing fetch and, on mutable paths, re-served whatever the upstream returned inside the window. Streaming publishes now register in an in-process registry; a cache read that misses while a tail-phase publish is in flight waits (bounded, 5 s) for the writer and re-reads the sidecar — dropping any memoised miss from the metadata LRU — instead of refetching. Reads during the body transfer are unaffected (the single-flight coordinator already coalesces those), and a rejected write still self-heals as before.
- **Proxy scan verdicts now invalidate when the CVE database updates, not only when the scanner binary is upgraded** (#3287). The freshness check (`verdict_is_fresh`, #2976) documents its reuse rule as "a CVE-DB bump changes the version string", but the stored token was the scanner **CLI** version alone (`grype-0.83.0`) — and Grype ships database updates daily while the CLI changes rarely, so a withdrawn advisory kept an image blocked (and a newly published one kept a clean verdict serving) for the full 30-day TTL. The stored token now composes the CLI version with the vulnerability-database build date — `grype-0.83.0+db-2026-08-10` from `grype db status`'s `Built` line, `trivy-0.62.1+db-2026-08-10` from `trivy --version`'s `Vulnerability DB: UpdatedAt` block — so the practical stale window drops from ~30 days to ~1. A failed DB probe degrades to the bare CLI token (TTL-only freshness, exactly the previous behavior), and existing stored verdicts mismatch the new token shape once, forcing a single re-scan per digest after upgrade — the self-healing direction.
- **Attestation verification authenticates the Rekor `integratedTime` by verifying the Signed Entry Timestamp** (#3231). The SET is the log's signature over a tlog entry's `(body, integratedTime, logIndex, logID)`, and it is the only thing that binds `integratedTime` — it is neither part of `canonicalizedBody` nor part of the Merkle leaf, so the inclusion-proof glue #3199 added does not cover it, and `sigstore` 0.14.0's `Verifier::verify_digest` skips it outright (`TODO(tnytown) SET verification`). That made `integratedTime` a value the bundle's author chose, and `integratedTime` is exactly what step 7 of `verify_digest` compares the signing certificate's validity window against, so "the certificate was valid when it signed" was trivially satisfiable by anyone already producing the bundle. Verification now runs the SET against the Rekor log key from the pinned trusted root, as an eighth mandatory check (`Check::RekorSet`) ahead of the inclusion proof, and **fails closed in both directions**: a SET that does not verify is a rejection, and so is a tlog entry carrying no `inclusionPromise` at all, since an absent SET means an unauthenticated timestamp rather than an acceptable omission. Every PyPI PEP 740 bundle observed carries one, and PyPI is the only format this verifier accepts. Not hand-rolled crypto: the payload is the field set of the crate's own `cosign::bundle::Payload`, canonicalised with the same RFC 8785 (JCS) library the crate uses for the same payload in its private cosign path, verified with the same `CosignVerificationKey`. The red-team fixture whose only mutation is a rewritten `integratedTime` is now rejected by the SET check directly, and `pypi-whl-bundle--forged-set-and-proof` now fails at the SET rather than incidentally at the proof.
- **A persisted attestation verification survives re-evaluation** (#3230). Migration 195 added `attestation_state` / `_identity` / `_issuer` / `_owner` / `_verified_at` / `_error` to `curation_packages` and the scheduler wrote them, but nothing read them back: `CurationPackage` did not carry the fields, so the `SELECT *` behind every catalog read decoded and dropped them, and the trust decision lived only in the memory of the tick that ran the verifier. Any later re-evaluation of the same row — an operator resetting a package to `pending`, a bulk re-evaluate, a manual sync — therefore lost `verified = true`, fell back to the fail-safe Flag, and silently returned an `approved` package to `review`; it also re-downloaded the whole distribution from the upstream, because there was no cached verdict to consult. The columns are now the source of truth: both the scheduler's on-demand loop and `re_evaluate_pending` read the record and derive the trust marker from it, under guards that all fail toward re-verifying rather than trusting — the record must say `verified`, carry all three certificate-bound values, name an issuer **still** on the current allowlist (so narrowing the allowlist takes effect at the next evaluation, not the next re-verification), be no older than 30 days, and predate nothing: a row re-ingested from the upstream since the record was written is verified again. The record is also surfaced on the curation package API responses, so "which packages verified, and against which certificate identity" is answerable without querying the database.
- **The on-demand curation ingestion seam is covered by tests and capped** (#3233). #3199 moved the PyPI seam to after the serve call returns, because in its original position it ran ahead of the repository access check and ahead of any upstream contact — an unauthenticated caller could write `curation_packages` rows for packages that do not exist, into a staging repo belonging to another tenant. That relocation shipped without a test; the predicate now lives in `proxy_helpers::response_admits_ondemand_ingest` and is pinned in both directions (2xx and the #1555 presigned `307` ingest; 401/403/404/451/5xx never do), alongside a DB-backed positive control that a served download enqueues exactly one pending row per curating staging repo and a repeat download adds none. Proxy-driven ingestion is additionally bounded: a staging repo at `MAX_PENDING_ONDEMAND_ROWS` (5000) pending rows drops **new** rows with a warning while continuing to refresh existing ones, so proxy traffic cannot inflate the catalog without limit or push real packages behind a head-of-line backlog that `MAX_PENDING_PER_TICK = 500` drains 500 rows at a time.
- **Blob GC's readiness gate can be opened again on registries serving multi-arch images** (#3285). The gate (`any_live_manifest_missing_refs`) was strictly wider than the backfill candidate set: it counted the children of every tagged index, while the candidate query selected only *tagged* digests and therefore never selected an index child, which carries no tag of its own. A child without `manifest_blob_refs` rows pinned the gate `true` with no way to clear it — the startup backfill logged `candidates_scanned=0 edges_inserted=0` while `BLOB_GC_ENABLED=true` stayed a permanent no-op, so soft-deleted artifacts were reclaimed from the database by the storage GC while their blobs stayed in object storage forever. On the reporting instance 365 manifests held it closed and an external SQL + S3 `DeleteObjects` job had to reclaim 2.27 TiB by hand. Both queries are now derived from one shared relation, so the gate can only be held closed by something the backfill also selects. That relation adds index children, but only where a **live `artifacts` row in the same repository** sits at the child's `oci-manifests/<digest>` key — the locality proof that this repository actually committed that manifest body, which is what previously made a bare `oci_manifest_refs` edge unsafe to backfill (on a shared cloud namespace it would have imported another repository's manifest). A child that is merely *referenced* is now in neither set: a proxy cache legitimately holds only the architecture it was asked for, so an unfetchable child can neither be backfilled nor gate — and with no manifest body there are no local blobs attributable to it for a sweep to reclaim. Nested indexes are excluded from the child arm by the same structural guard #1409 applied to tagged indexes. When the gate is closed, the scheduler now names up to 20 of the offending `digest@repository` pairs in its per-tick warning, so a stuck gate is diagnosable without reconstructing the query by hand.
- **Storage accounting no longer double-counts OCI layer bytes** (#3286). `REPO_OBJECT_UNION_SQL` unioned live `artifacts` rows with `oci_blobs`, on the premise that a Docker/OCI repository contributes only a few KiB of manifests through `artifacts`. It does not: `artifacts.size_bytes` for an image manifest holds `config.size` plus the sum of `layers[].size` — the aggregate *image* size, deliberately, because GC, quota and scanning enumerate that table — so every layer was counted once through the inflated manifest row and again through `oci_blobs`. The reporter measured `instance_storage_stats.unique_bytes` at 3097 GB against 1.14 TiB of actual bucket usage, a 2.7x over-report on a metric named `unique_bytes` that an operator reasonably reads as deduplicated physical usage. `oci-manifests/%` rows are now excluded from the physical union, so OCI layers are counted exactly once, via `oci_blobs` at their true size. **Upgrade impact: reported storage will drop sharply on OCI-heavy instances after the next stats refresh — that is the correction, not data loss, and no bytes are deleted.** **Quota enforcement is unchanged in both directions**: `RepositoryService::check_quota` reads the live logical `SUM` over `artifacts` and never consulted this accounting layer (#2056 §7), so no repository's effective limit moves. The manifest JSON documents themselves are no longer counted — a deliberate, bounded undercount (~164 MB across 56k manifests on the reporting instance, ~0.01%), since no column holds their true object size; rewriting `artifacts.size_bytes` to fix that was rejected precisely because it *would* have silently loosened every OCI repository's quota. The per-path tree (`PATH_REF_UNION_SQL`) is unchanged: it is a logical view that excludes layer blobs entirely, so it has no double count to correct. The same conflated column also produced the OCI migration reindex's false positives — its cheap pre-read guard compared a 4 MiB *object* cap against the image size and skipped 8,069 otherwise-ordinary manifests with `manifest body exceeds 4194304 bytes (got 322036591); refusing to buffer` — so `oci-manifests/%` rows now skip that pre-read guard and are bounded by the unconditional post-read `body.len()` cap like every other manifest.
- **The proxy-cache catalog backfill repairs the placeholder row it races** (#3305). On a cache serve, `record_proxy_download` is awaited inline on the request and inserts a `size_bytes = 0`, NULL-checksum placeholder so the first download counts (#2537), while `spawn_lazy_catalog_backfill` runs detached and normally *loses* that race. Its `ON CONFLICT` arm bumped only `last_accessed_at`, so the losing backfill returned without ever writing the real size, checksum or content type, and nothing else repaired the row — the #2218/#2270 self-healing was inert for exactly the pre-catalog entries it exists for, and storage accounting read zeros for them. The conflict arm now repairs a row that is still a placeholder (`size_bytes = 0` **and** checksum NULL) and continues to leave every authoritative row untouched, so a genuinely populated entry — including a real zero-byte object, which carries its checksum — is never overwritten by a stale sidecar. The bounding #3289 added is untouched; a skipped backfill still simply retries on the next hit.
- **S3 `get_stream` runs the Artifactory-migration fallback that `get` and `get_range` already performed** (#2927). S3 was the only fallback-capable backend whose streaming read skipped `try_fallback_get` — GCS and Azure both run it — so in a deployment configured with the Artifactory migration path format, an object living only at the legacy key layout was found by the buffered `storage.get()` and reported `NotFound` by `storage.get_stream()`. That mattered because handlers are being converted from buffered to streaming reads (#895 / #1608 / #2192): each conversion silently inherited the gap, turning a warm local read into either a hard 404 or a redundant upstream re-download. The fallback body is buffered and wrapped in a single-item stream, matching GCS and Azure exactly. `try_fallback_get` is gated on `path_format.has_fallback()`, so **nothing changes for deployments not in migration mode, or on any other backend**. A regression test pins the invariant across all three fallback-capable backends so it cannot drift again.
- **Release-preflight check 3 measures the Docker Publish run for the commit being tagged, not the newest run** (#3338). Selecting by recency produced a blocking false red on the v1.7.2 cut when the "latest run" query returned a ten-day-stale run, and it was the wrong question even with a consistent API: a tag is cut on a commit, and only that commit's publish determines whether the release chain stalls. The run is now selected by `head_sha`, and two previously conflated states are reported distinctly and block: *no run for this commit* ("not built yet" — the images a tag would pin do not exist) and *run still in flight* (its verdict does not exist yet). `PREFLIGHT_ALLOW_RED_PUBLISH` continues to override only a measured red.
- **CI fails on duplicate sqlx migration version numbers with a numeric comparison** (#3313). Two migration PRs claiming the same version merge CLEAN with no textual conflict and abort the backend at startup (`_sqlx_migrations_pkey`, #1128); the inline string-prefix dedup in `ci.yml` would additionally have passed `007_x.sql` alongside `7_y.sql`, which sqlx treats as the same version. The check now lives in `scripts/ci/check-duplicate-migrations.sh` — numeric-normalised duplicate detection, an error on unparseable prefixes, and a non-blocking warning on sequence gaps (the renumber-in-flight signature) — runs against the PR merge result as before, and is exercised by a self-test in the shell-tests job so the gate itself cannot rot.
- **The 8.1 MB compiled aarch64 `scanner-adapter` binary is removed from the repository** (#3341). It sat inside `docker/scanner-adapter/`, the exact path set the publish job diffs to decide whether the published version tag may be reused — so any local `go build` that got committed would invalidate the published tag with no source change and no clue in the diff, the same gate that killed the v1.7.2 cut. Nothing consumed it: the image compiles the adapter from source (`COPY docker/scanner-adapter/*.go`), and no other Dockerfile or compose file references it. The path is now gitignored, and `docker/scanner-adapter/VERSION` is bumped to 1.2.4 because the removal itself changes the diffed source set.
- **NuGet V2 package downloads read from the resolved virtual member's own storage** (#3350, #3329). `v2_download` resolved the winning artifact row across the caller-authorized member repositories but then opened the storage backend at the URL (parent) repository's `StorageLocation`. A member's `storage_key` is rooted at the **member's** own backend and path, so on the filesystem backend the resolved path did not exist and every virtual-member V2 download answered `500 Storage error`. The lookup now also selects the winning row's `repository_id` and opens storage from that repository's own location — the source of truth the V3 flat-container path already used — which additionally covers a member whose backend differs from the parent's (member on S3, parent on filesystem). The #3324 member-authorization filter, the quarantine check and download recording are unchanged.
- **Non-unique Maven SNAPSHOT artifacts are classified mutable** (#3357, #3295). `classify_maven` treated any file carrying a Maven artifact extension as `Immutable`, and its SNAPSHOT-directory mutable branch covered only non-artifact files (metadata and listings). A non-unique SNAPSHOT jar or pom therefore fell through to `Immutable`, and the shared release-immutability guard then rejected a redeploy over a soft-deleted tombstone with `409 Artifact version already exists and is immutable` — but a non-unique SNAPSHOT keeps the literal `-SNAPSHOT` token in its filename and is redeployed in place by definition; it is a mutable coordinate, not a released one. A file inside a `-SNAPSHOT/` version directory whose filename still carries the token is now mutable. Unique (timestamped) SNAPSHOTs, where the token is replaced by the timestamp, and release coordinates are unchanged, so release immutability is preserved; because every consumer reads through `classify()`, the correction applies at one point. Deliberate and benign consequence: the proxy cache now revalidates remote non-unique SNAPSHOTs on TTL instead of caching them forever, matching Maven's volatile-SNAPSHOT semantics.
- **Conan recipe uploads populate the packages catalog** (#3358). The Conan upload handlers wrote `artifacts` and `artifact_metadata` but never the `packages` / `package_versions` catalog that the Packages tab reads, so a Conan repository always showed an empty tab while every other publishing format called the shared helper after its artifact insert. The recipe path now does too, keyed on the recipe reference identity `name@user/channel` — collapsing to a bare `name` for the Conan `_/_` defaults — so recipe revisions and the several physical files of one upload upsert into a single catalog row per reference while distinct user/channel combinations stay distinct rows. The call is fire-and-forget, matching npm and composer, so a catalog-table failure never blocks the `201`. The package-binary path is deliberately not wired: keying on binaries would produce one catalog row per build configuration.
- **`hf download` works against Xet-backed models on a remote HuggingFace repository** (#3359, #3334). The proxy handed upstream tree and model-info JSON back verbatim, and that JSON carries a per-file `xetHash`. `huggingface_hub` 1.26 and later caches the tree listing on disk, reads the hash back out of that cache, skips the HEAD call entirely, and requests a Xet read token at a route this server does not expose — so the download aborted partway through with a bare `404` for any Xet-backed model, which today is most of the Hub. Proxied metadata now has `xetHash` and `xetEnabled` removed from every object at any depth, covering all three shapes the Hub serves (the tree array, the model-info object with its `siblings`, and the deeper nesting of `tree?expand=true`), so clients fall back to the ordinary `/resolve/` path that already worked. This removes the need for the `HF_HUB_DISABLE_XET=1` client-side workaround. Bytes that do not parse as JSON pass through untouched, so a passthrough that works today cannot start failing because of the filter. Proxying `xet-read-token` was rejected deliberately: once a client holds a Xet token it fetches chunks directly from `cas-server.xethub.hf.co`, bypassing the proxy entirely, which loses caching and defeats an air-gapped or controlled-egress deployment.
- **The lazy proxy-cache catalog backfill is bounded, and no longer reads the same object twice** (#3289, #3298). Every streaming proxy-cache **hit** unconditionally spawned a detached task to self-heal the `proxy_cache_artifacts` catalog for objects cached before that table existed (#2218/#2270). Nothing capped how many could be in flight, and each one took a connection from the **same** pool serving request traffic — so a burst of concurrent hits on a popular package put background bookkeeping in direct competition with real requests for a fixed 50-connection budget, contributing to the `Server overloaded` 503s in the report. Each task also re-read the sidecar metadata object from storage even though the caller had loaded exactly that `CacheMetadata` moments earlier to evaluate freshness, so every cache hit paid for two storage reads where one sufficed. The spawn is now bounded and the already-loaded metadata is handed to the task.
- **Two sources of shared-database interference in `cargo test --workspace --lib`** (#3403, #3402). Test-only; no production code changed. `oci_migration_reindex`'s queries join `repositories` rather than binding a repository id — instance-wide by design, because in production that job repairs the whole instance — and all three tests in the module call `run_repair`, so against one shared database a sibling registered or reconciled the rows another had just seeded. The tell was `candidates_scanned` varying run to run for a test that seeds a fixed number of candidates. The module now takes a session advisory lock, the same shape and for the same documented reason as the existing blob-GC serial lock. Scoping the repair scan to a single repository was rejected: it would make the tests pass by narrowing a job whose whole purpose is to be instance-wide. Separately, `admin_security::test_accessible_users_enumeration_db` asserts a global active-user count across two adjacent queries and takes no serial lock, so it races with anything creating an active admin — which the 2FA policy tests (#3401) began doing. The policy tests no longer set `is_admin` (nothing under test reads it; the admin check is route middleware and policy activation keys on the actor's TOTP state), so the race is removed at the source rather than by editing the test it disturbed.

### Upgrade note — security grades drop and severity gates tighten in volume on the next rescan

Ungraded findings previously landed at `info`, which is in no `max_severity` block set, carries zero penalty weight, and is invisible to promotion severity counts — so they affected nothing. After this change each ungraded finding is filed at `high`: it now counts against `scan_policies.max_severity` thresholds of `high`, `medium`, and `low`, contributes `high`-level weight to repository security scores, and is counted by promotion rules that read high-severity counts.

Expect this to move **in volume** on the first rescan, not at the margin. In particular, OpenSCAP: XCCDF's default rule severity is `unknown`, and the bundled wrapper emits the literal `unknown` for any rule that declares no severity — on a typical SCAP profile that is **most reported rules**. Repositories with OpenSCAP findings should expect their security grade to drop by one or more bands and, if a `max_severity` policy of `high` or stricter is enabled, downloads/promotions of affected artifacts to start being blocked. Trivy image scans with `UNKNOWN`-severity CVEs move the same way at smaller volume. To triage, acknowledge individual findings (acknowledged findings are excluded from the gate) or grade the rules in your SCAP content; do not widen the policy threshold past `high` unless you intend to stop blocking known-high findings too.

## [1.7.6] - 2026-08-17

> **`v1.7.5` is a dead tag — do not use it.** It was tagged, its Docker Publish
> failed on a scanner-adapter version-collision gate, and no images or release
> object were ever produced. Repository rulesets forbid deleting or moving a
> tag, so the version number was burned rather than reused. Everything intended
> for 1.7.5 ships in 1.7.6 unchanged. See #3429.

### Security

- **Backup restore rejects tables outside the export allowlist** (GHSA-95fx-g94v-8jqg) - the restore path never consulted `ALLOWED_EXPORT_TABLES`, so a crafted archive could drive inserts into tables the backup format never legitimately carries (`signing_keys`, `audit_log`). Restore now ingests only the allowlisted set and rejects anything else. **Read the scope carefully:** the allowlist itself contains `users`, `roles`, `user_roles`, `permission_grants` and `api_tokens` - the identity and authorization tables - because a backup that could not restore them would not be a backup. This fix narrows *which tables* an archive can reach; it does **not** make a hostile archive safe, and an operator who restores a tampered archive can still be handed an attacker-controlled admin user and API token. **The real control is archive integrity, and it is not yet implemented**: `BackupManifest.checksum` is written empty and never verified on restore, so restore trusts archive contents as much as it trusts the database. Treat backup archives as privileged material - restore only archives you produced and can vouch for, and store them somewhere a tamperer cannot reach. Signing and checksum verification are tracked in #3373.
- **Percent-encoded repository keys can no longer bypass repository visibility** (GHSA-fv45-mwhh-q23r) - `repo_visibility_middleware` evaluated the raw (still-encoded) path segment while handlers resolve the decoded key, so an authenticated caller with no grant could read a private repo via e.g. `/maven/privat%65/...`; the middleware now percent-decodes before lookup and fails closed with an existence-hiding 404 when no repository matches.
- **Rate limiting no longer trusts the leftmost X-Forwarded-For entry** (GHSA-8jm4-4x6c-6787) - behind an appending trusted proxy, a client-supplied XFF prefix previously controlled the resolved client IP (bucket rotation, exemption spoofing); the resolver now walks right-to-left and selects the first non-trusted, parseable hop. No behavior change for the default compose deployment (Caddy as the single trusted proxy); multi-hop deployments must list every trusted hop in `RATE_LIMIT_TRUSTED_PROXY_CIDRS`.
- **The bundled `grype` CLI is built from source with a patched Go toolchain, clearing every Go-stdlib CVE in the backend images** (#3352). `docker/Dockerfile.backend` and `docker/Dockerfile.backend.alpine` copied the `grype` binary out of `ghcr.io/anchore/grype:v0.117.0`. That binary is compiled with go1.26.3, and six HIGH Go **stdlib** advisories were live against it — `CVE-2026-33818` (`encoding/asn1`, DoS via recursion in `Unmarshal`), `CVE-2026-56853` (`net/http`, DoS on unencrypted connections), `CVE-2026-56858` (`html/template`, XSS via pathological input), `CVE-2026-56859` (`encoding/xml`, DoS via recursion depth), `CVE-2026-56860` (`net/url`, DoS via quadratic path complexity) and `CVE-2026-56862` (`crypto/tls`, indefinite `KeyUpdate`). `Security Scan` in `docker-publish.yml` failed on five consecutive runs on `main` and on `release/1.7.x` (run 31919400928), so **no images published from either branch** and both a `v1.7.5` security tag and `v1.8.0` were blocked.

  Bumping the pin could not fix this and no future bump can: a stdlib CVE is a property of the compiler, not of grype, and v0.117.0 was already the current release. The only lever left was `.trivyignore`, which is why the same class had already produced seven suppressions (#3262, #3307) and why the 1.7.4 publish had to be unblocked by editing that file rather than by shipping a fix. Both Dockerfiles now **compile upstream grype v0.117.0 from source** on a Go toolchain floored at go1.26.6 — the same treatment #3123 gave trivy — cross-compiling from `$BUILDPLATFORM` to `$TARGETARCH` the way `Dockerfile.scanner-adapter` does. The pinned tag is verified to resolve to the pinned commit at build time, and the toolchain floor (`GO_MIN_PATCH`) is a hard assertion rather than a comment, so a lagging mirror fails the build loudly instead of silently shipping a vulnerable stdlib. The next Go stdlib advisory is now a one-line `GO_MIN_PATCH` bump instead of a new suppression.

  Measured with trivy 0.69.3 using the gate's own invocation (`--severity CRITICAL,HIGH --ignore-unfixed`), rebuilt vs. the upstream image: findings against the grype binary drop from **12 to 1** at HIGH+fixed and from **21 to 8** across all severities, with **zero findings added**. All thirteen removed are stdlib. Seven `.trivyignore` tokens are consequently retired (`CVE-2026-27145`, `-39821`, `-39822`, `-42504`, `-42505`, `-42507`, `-46600`) as `# RETIRED:` tombstones per the #3309 protocol, and the six blocking CVEs were never added to that file.

  **Behaviour is deliberately unchanged.** The source tag does not move, so this is a repackaging and not a fork: `grype --version` still prints exactly `grype 0.117.0` — which matters, because `GrypeScanner` parses that string and #3287 composes it with the CVE-DB build date into `scanner_version`, so a changed shape would break scan-verdict freshness. `grype version` still reports the upstream `GitCommit`/`GitDescription`, and `Supported DB Schema` is still 6, so the build-time DB pre-seed for air-gapped operation (#1001) still loads; the seeded DB, the `/home/artifact/.cache/grype` cache paths and the `GRYPE_DB_AUTO_UPDATE=false` / `GRYPE_DB_VALIDATE_AGE=false` / `GRYPE_CHECK_FOR_APP_UPDATE=false` posture are all untouched. The only field that moves is `GoVersion`. Because we now distribute the binary rather than copy it, grype's Apache-2.0 licence ships at `/licenses/grype` alongside it, matching how the scanner-adapter carries its self-built trivy. The `grype-bin` stage joins `no-cache-filters` in `docker-publish.yml`, so the toolchain is re-resolved on every publish for the same reason `dnf upgrade` and `grype db update` are.

  **Not fixed, and measured rather than assumed:** `github.com/docker/docker` v28.5.2 (`CVE-2026-34040` HIGH, plus four unfixed/MEDIUM siblings) is a *dependency*, not the toolchain, and stays suppressed. Building from source makes an override possible in principle, but there is nothing to override to — against the v0.117.0 source tree `go mod edit -require=github.com/docker/docker@v29.3.1+incompatible` fails module resolution with "unknown revision v29.3.1", so the fixed version the DB reports is not resolvable for that module path. That is recorded in `.trivyignore` with a note to retry on the next grype bump.

  The regression guard in `backend/src/config.rs` is rewritten to match: it scans **every** `docker/Dockerfile*` (not the two known today) and fails if any pulls a prebuilt `ghcr.io/anchore/grype:` binary, if the source-build pins drift between images, or if any of the thirteen toolchain-fixed stdlib CVEs reappears as a *live* `.trivyignore` suppression — a `# RETIRED:` tombstone stays fine, a live token does not.

### Fixed

- **Repeated PyPI PEP 658 `.whl.metadata` requests are served from the proxy cache** (#3300). `serve_remote_metadata` fetched the sidecar with `fetch_upstream_direct_with_link`, which bypasses the proxy cache in both directions, so the bytes were never stored and no request could ever hit. Every request re-ran the entire upstream resolution: the simple index — read through `proxy_fetch_uncached` to rediscover the distribution's download URL — plus the `.metadata` resource itself, two upstream round-trips for a resource that cannot change. An uncached `uv lock` fan-out requests one sidecar per candidate distribution, so a CI job regenerating several lock files multiplied that across every resolver run. The cache is now probed **before** the fetch target is resolved — behind that resolution a hit would still pay the index read — keyed on `simple/{project}/{dist}.metadata`, which is what `resolve_pypi_remote_fetch_target` already computes and is distinct from the distribution's own key. A miss fetches through `fetch_artifact_with_cache_path_capped`, which stores the body together with its upstream `Content-Encoding`, so a later hit re-emits the coding #3193 established rather than serving coded bytes bare. **Both direct Remote repositories and Virtual members are covered**, because virtual metadata resolution routes through the same function. Separately, the proxy-cache classifier matched `.whl.metadata` against no PyPI distribution extension and therefore gave it the mutable default — a five-minute TTL, and a revalidation against upstream on every request after it, even once the body was stored. A PEP 658 sidecar holds the `METADATA` of an immutable distribution, so it now classifies as immutable, with the suffix stripped ahead of the extension test exactly as the Maven checksum and signature sidecars are. The upstream-`404`-to-wheel-extraction fallback is deliberately unchanged: only the `200` path is cached, so an upstream that advertises metadata it does not serve still re-extracts from its (cached) wheel per request.
- **Corrected swapped GHSA labels** in `backend/tests/security_regression_tests.rs` - the Maven LIKE-escape and IPv6/cloud-metadata SSRF regression tests were annotated with each other's advisory IDs; coverage was always correct, only the comments were wrong.

## [1.7.4] - 2026-08-14

A security-only release. It carries exactly one functional commit on top of 1.7.3 — four repository advisories fixed together (#3347) — plus the version bump. There are **no feature changes and no schema migrations**, so for anyone already on 1.7.3 this is a low-risk upgrade that can be taken quickly. Three of the four advisories need action beyond upgrading — credential rotation for GHSA-78h6, a proxy-cache judgement call for GHSA-qxv7, and token rotation for GHSA-9rqp — and the GHSA-78h6 fix carries one deliberate behaviour change on Docker Hub pull-through; all of it is written up below.

### Upgrade note — credentialed Docker Hub pull-through now authenticates anonymously

This is a consequence of the GHSA-78h6-3wp8-2542 fix rather than a separate decision, and it is the only behaviour change in the release.

Docker Hub splits its registry and its token endpoint across two hosts: pulls go to `registry-1.docker.io` while the realm advertised in its `401` is `https://auth.docker.io/token`. Under the strict origin rule the fix introduces, those are **different origins**, so a Docker Hub remote/proxy repository with configured upstream credentials now performs its token exchange **without them**. Docker Hub does not reject that request — it answers with a valid *anonymous-scope* token — so the failure does not land at the token endpoint where the comment in the fix anticipates it. It lands one hop later, on the manifest retry, and `validate_upstream_status` renders the upstream's `401` as **`502 Upstream returned error status 401`**. That misleading error is the diagnostic signature: if a private Docker Hub pull-through starts answering `502` after this upgrade, this is the cause, and the Docker Hub credential itself is not the problem.

**Am I affected?** Only if you proxy an upstream whose token realm lives on a different host from the registry itself **and** you configured Basic credentials on that repository. Expect two effects: **private images stop being pullable** through that repository, and pulls that previously authenticated now fall under the upstream's **anonymous rate limits** — on a busy CI fleet, the second is what you notice first. Registries advertising a same-origin realm are unaffected and keep authenticating exactly as before: GHCR serves pulls from `ghcr.io` and advertises `ghcr.io/token`, as do most self-hosted Harbor, Nexus and Artifactory deployments. Check your own upstreams rather than assuming — the realm host is in the `WWW-Authenticate` header of an unauthenticated `GET` against the upstream's `/v2/`.

There is deliberately **no configuration switch to opt back in**, and no well-known-realm allowlist. The codebase pins credentials to the operator-configured origin everywhere (#2925, #3130), and a cross-host allowance would reopen the advisory for every upstream that has one.

### Upgrade note — what to do beyond upgrading

Three of the four fixes do not fully remediate on their own.

**Rotate upstream Basic credentials that may have been forwarded (GHSA-78h6).** If a remote/proxy repository carried stored upstream *Basic* credentials and its upstream could have advertised a realm on a host you do not control, those credentials may have reached a third party; Basic credentials are replayable indefinitely, and where the realm was plain `http://` they also crossed the wire in cleartext. Upgrading closes the channel but cannot un-send them. Repositories configured with an upstream *bearer* token were never exposed — only the Basic arm forwarded. The assessment is a configuration review, not a log search: enumerate the remote/proxy repositories carrying stored upstream credentials and judge each upstream on whether someone could have controlled its `401` response.

**Decide whether to purge proxy-cache entries admitted before the digest gate existed (GHSA-qxv7).** Nothing in this release evicts or revalidates what is already cached — there is no migration, no startup revalidation, no backfill. An npm tarball, PyPI distribution or Maven release artifact whose bytes disagreed with its published digest and was cached before the upgrade stays cached and keeps being served warm. **It will not age out**: proxied package files classify as `Mutability::Immutable`, so `evaluate` returns `Fresh` unconditionally, the stamped TTL is a ~10-year backstop, and upstream is never consulted for that object again. Eviction is explicit and per-path — enumerate a remote repository's cached entries with `GET /api/v1/repositories/{key}/artifacts` (the listing carries `path`, `size_bytes` and the stored `checksum_sha256`) and call `POST /api/v1/repositories/{key}/cache/invalidate?path=<path>` for each, which requires `repository:admin`, takes **one path per call** with no prefix, glob or bulk form, and hard-deletes the `__content__` body, the `__cache_meta__.json` sidecar and the `proxy_cache_artifacts` row. The only whole-subtree purge in the codebase (`purge_repo_cache`) is reachable solely from the repository-DELETE background task, so a genuinely clean slate means deleting and recreating the remote repository. Proxy-cache entries are re-fetchable from upstream by definition, so evicting costs bandwidth and a cold first pull rather than data: if you have any reason to suspect a bad entry — a corrupted install report, a flaky upstream or mirror, an upstream incident inside your retention window — evict and let it re-populate under the new gate. With no such signal, taking the fix going forward is defensible. **GHSA-4cw7 needs no purge**, and that is worth stating next to the manual loop above: the proxy caches the raw upstream index body and the page is rebuilt in the handler on every serve including warm cache hits, so a poisoned page already in the cache is sanitized on read the moment this is deployed.

**Also rotate tokens used against third-party plugin routes (GHSA-9rqp).** Every credential presented to a `/ext` URL served by a v2 WASM plugin was copied into that plugin's memory. Keep it in proportion — the sandbox gives a plugin no network and no filesystem, so reading a credential and exfiltrating it are not the same step, and plugin installation is admin-only with `plugins_require_signed` defaulting to `true` — but if you ran a plugin you do not fully control, rotate the tokens of principals that used those routes.

### Upgrade note — exploitation is mostly undetectable after the fact, with one exception

Worth stating plainly, because it changes what a post-upgrade review can achieve.

**GHSA-9rqp leaves no trace at all.** A successful `/ext` request writes no audit entry, never reaches the download-event dispatcher so records no `download_statistics` row, and emits no log line at the shipped `RUST_LOG=info` level. Request metrics label by matched route (`/ext/:format_key/:repo_key/*path`), carrying neither the repository key nor the principal, and no span records the caller. Even against an external reverse-proxy access log, an exploiting request is byte-identical in shape to a legitimate one — same method, same URL — and the only distinguishing fact is which grants the caller held, which nothing records. If the affected configuration was present, treat those repositories' inventories as potentially disclosed.

**GHSA-78h6 is silent on a production deployment.** The realm that was contacted appeared only in a `tracing::debug!` line, and the shipped container images pin `RUST_LOG=info`; no metric, audit row or persisted record retains it, and the bearer-token cache is in-memory and keyed on the realm string. A competent exfiltration returns well-formed token JSON and produces no error line either. A deployment running the development compose defaults (`info,artifact_keeper=debug`) *did* emit that line, so those logs are worth a look if they are still held. Going forward the new cross-origin refusal is logged at `warn` under the `security` target — useful for spotting the Docker Hub change above, but it says nothing about the past.

**GHSA-4cw7 lets you check what is cached now, not what was served.** The proxy caches the raw upstream body, so a currently-cached index page can be inspected directly — on filesystem storage at `<storage>/proxy-cache/<repo_key>/simple/<project>/__content__`, on S3/GCS under the same key prefix at bucket root (the configured `S3_PREFIX` is deliberately not applied to the proxy-cache handle), with `proxy_cache_artifacts` available to enumerate candidates first. Grep those for `<script`, `onerror=` and similar. Upstream response bodies are never logged anywhere, so whether a poisoned page was *ever served* is unanswerable.

**GHSA-qxv7 is the one that does support an offline check.** Every cached entry stores a `checksum_sha256` in its metadata sidecar and in the `proxy_cache_artifacts` catalog, surfaced through `GET /api/v1/repositories/{key}/artifacts`. For PyPI that value is directly comparable to the `#sha256=` fragment the upstream index advertises for the same filename — pull the listing, fetch the upstream index, diff, with no artifact re-download. For npm and Maven the published digests are SHA-512 and SHA-1, so the stored SHA-256 is not comparable and the cached object must be re-hashed. No record of a *mismatch* was ever kept, before or after the fix, so this compares against today's upstream digest and therefore detects divergence rather than proving what happened at the time: against an upstream that was compromised and has since been corrected it finds the bad entry; against one still serving the attacker's digest it will not.

### Security

- **The `/ext` WASM plugin proxy evaluated the plugin format key as the repository key, so any authenticated caller could read a private repository's artifact inventory** (GHSA-9rqp-mgmw-5879, #3347). The plugin protocol proxy is mounted at `/ext` and routes as `/ext/<format_key>/<repo_key>/…` — the plugin's format key first, the repository second. Every other format route puts the repository key in the second path segment, and `extract_repo_key`, which `repo_visibility_middleware` uses to decide *what it is authorizing*, applied that general rule to `/ext` as well; it returned the **plugin format key**. No repository matched that string, so the middleware fell into its no-repository branch — the branch that exists for routes carrying no repository at all. Anonymous callers were still refused there, but a valid credential inserted the auth extension and ran the handler, so **every check in the repository branch was skipped**: the public/private gate, the repository-scope ceiling `can_access_repo`, and the fine-grained read/write ACLs. The pre-fix handler performed no authorization of its own, so it then looked the real repository up and handed the plugin its full artifact metadata list. **What crossed the boundary is inventory, not content** — `path`, `version`, `content_type`, `size_bytes` and `checksum_sha256` for every live artifact, which the plugin renders into the requested index. The guest receives no artifact bytes (the v2 `handle-request` interface takes only the metadata list, and the runtime grants no network and no preopened directories), there is no write path through the interface, and the `download_base_url` a plugin embeds in its index points at `/api/v1/repositories/{key}/download`, which enforces `require_visible` independently — so those links `404` for an ungranted caller. Two consequences go beyond the headline: a repository-scoped API token was **not** confined, so a token restricted to one repository could read another's inventory; and the format-key mismatch reply named the repository's actual format with a `400` where a missing repository answered `404`, giving any authenticated caller an existence-and-format oracle over **every** repository in the instance — which did not require the format-key match and therefore reached further than the inventory disclosure itself. **Am I affected:** only if you run a WASM plugin exporting the **v2** `request-handler` interface (`capabilities.handle_request`, which is what compiles `compiled_v2` and what `has_handle_request` gates on — a v1 plugin is not enough) **and** at least one non-public repository was created against that plugin's custom format key. The handler requires the target repository's own `format_key` to equal the plugin's, and built-in formats resolve to a `None` format key, so Maven, npm, PyPI, OCI and the other native formats were never reachable this way. Plugin installation is admin-only and `plugins_require_signed` defaults to `true`, so this is not a self-service foothold. The fix has three parts: `extract_repo_key` skips the format-key segment when the first segment is `ext`, so the middleware evaluates the real repository and applies the standard gate (anonymous `401` with the credential challenge, authenticated-without-grant existence-hiding `404`, granted member passes) and the token-scope ceiling and fine-grained ACLs — inert on `/ext` before — now apply; the handler calls `require_visible` itself rather than trusting the middleware to have parsed the path, failing closed with a `500` if the access check errors; and the header forwarding is allowlisted, below. A short `/ext` path yields an empty repository key and 404s at the router before reaching the handler. The regression test is DB-backed and returns early when `DATABASE_URL` is unset, so it proves the fix only in the coverage job that seeds Postgres.
- **The `/ext` proxy no longer copies the caller's credentials into plugin memory** (GHSA-9rqp-mgmw-5879, #3347). A second, distinct exposure in the same handler, fixed in the same commit. `headers_to_pairs` had no filter: **every** client request header was copied into `WasmHttpRequest.headers` and thence into guest memory, so plugin code received the caller's `Authorization` header (`Bearer` JWT, `ApiKey`, or `Basic` user:password), `Cookie`, `Proxy-Authorization`, `X-API-Key`, `X-NuGet-ApiKey` and any custom header on every `/ext` request. Forwarding is now restricted to `FORWARDED_HEADER_ALLOWLIST` — `accept`, `accept-encoding`, `accept-language`, `content-length`, `content-type`, `host`, `if-modified-since`, `if-none-match`, `if-range`, `range`, `user-agent` — the benign protocol and content-negotiation headers a protocol plugin legitimately needs; credential and hop-by-hop headers are never copied. `HeaderMap` names are already lowercase, so the allowlist matches lowercase. Stated precisely, because the remediation advice depends on it: the diff supports "caller credentials entered plugin memory", not "credentials left the host" — the guest has no network and no filesystem, so the only channel out is the host's own stdout via `inherit_stdio`. **Am I affected:** only on the `/ext` precondition above, and the practical risk is a plugin you did not author; rotate the tokens of principals that used those routes.
- **The OCI bearer-realm token exchange no longer forwards stored upstream Basic credentials to a cross-origin realm** (GHSA-78h6-3wp8-2542, #3347). When a proxied fetch receives a `401`, the upstream names its own token endpoint in `WWW-Authenticate: Bearer realm="…"` — **upstream-controlled input**. `exchange_bearer_then` validated that realm with `validate_outbound_url`, which is an SSRF check and nothing more (scheme in `{http, https}`, host present, not a private/loopback/link-local/metadata address), and then passed the repository's decrypted upstream credentials straight into `obtain_bearer_token`, which attaches them as HTTP Basic to the token request. A hostile or compromised registry — or anyone able to tamper with its `401` — could therefore name an attacker-controlled host as its realm and be handed the username and password the operator configured for that upstream. Plain `http://` realms were accepted, so in that case the credentials also crossed the wire in cleartext. Three narrowings are worth stating because they bound the severity honestly: only the **Basic** arm forwarded (a configured `UpstreamAuthType::Bearer` was never matched by that branch and never leaked); **there is no attacker on the Artifact Keeper side** — a caller can trigger the fetch but cannot influence the realm, so exploitation requires a hostile or compromised upstream, a TLS interception of it, or a credentialed proxy repository deliberately pointed at an untrusted registry; and although the advisory is framed around OCI, `exchange_bearer_then` sits on the generic buffered, streaming and conditional proxy fetch paths, so any upstream answering `401` with a `Bearer realm=` challenge reached it. This was **not** also an SSRF primitive — internal addresses were the one thing already covered, both at validation time and by the shared client's per-hop resolver guard — and redirects were already safe, because the HTTP client strips `Authorization` on any cross-host hop. The fix adds `realm_matches_upstream_origin`: credentials follow the realm only when its **scheme, host and port** match the upstream request URL's, with the host lowercased and default ports normalized through `port_or_known_default`, and an unparseable URL on either side matching nothing. The baseline is the operator-configured upstream the request URL was built from, not the URL of whatever answered, so a redirect chain cannot move it. A cross-origin realm gets an **anonymous** token request rather than a hard failure — and a `warn!` under the `security` target with the realm redacted — because anonymous token endpoints answer such requests. There is deliberately no suffix allowance: a subdomain of your upstream is cross-origin. **Am I affected:** only with a remote/proxy repository carrying stored upstream **Basic** credentials whose upstream could have advertised a realm on a host you do not control. **Rotate those credentials if so** — upgrading stops future forwarding but cannot recall what was already sent. See the upgrade note above for the Docker Hub consequence, and the detectability note for why a log search will not answer this for you.
- **Proxied PyPI simple-index pages are rebuilt from parsed anchors instead of forwarding upstream HTML** (GHSA-4cw7-mgqj-hgmg, #3347). `rewrite_upstream_urls` edited the upstream document in place: a regex found `<a … href=…>` and substituted a local URL. Everything else survived. The only element actively removed was `<base>` — stripped to a fixpoint, because removing one can splice surrounding bytes into a new one — since a surviving `<base href>` would re-anchor the rewritten root-relative links back onto the upstream origin. A `<script>`, a `<form>`, an `<iframe>`, arbitrary wrapper markup: all forwarded verbatim and **served from this registry's own origin**. The anchor's own attributes before and after `href` were re-emitted raw as well, so an upstream `onerror=` rode through untouched. Independently, the rewritten `href` was assembled by string interpolation, so upstream-controlled bytes could break out of the attribute they were written into: a single-quoted upstream href whose fragment carries a double quote yields a rewritten anchor with arbitrary attacker-chosen attributes on it. **Two qualifications on severity, because the advisory title invites more than the code supports.** The application's own CSP middleware sets `script-src 'self'` with no `'unsafe-inline'` on every response, and the per-project proxied page carries no tighter handler CSP of its own, so the global default applies and blocks both injected `<script>` blocks and inline event handlers in a CSP-respecting browser — what was genuinely reachable is stored **HTML injection** on a same-origin page, a defence-in-depth failure, not demonstrated script execution or session theft. (The comment in `security_headers.rs` describing a tight `default-src 'none'` for the PyPI simple index refers to the **root** listing, which emits its own CSP; the affected per-project page goes through `cacheable_response`, which sets none.) And the pre-fix rewriter re-pointed every anchor it recognised at a local proxy path, so this did **not** give an upstream a way to steer pip to an off-registry host or to bypass the download-path gates — the breakout injects attributes, not a foreign href. The rewriter now emits a **fresh minimal PEP 503 document** built only from what it can vouch for, mirroring the #2967 ownership rebuild: one `<a>` per upstream anchor whose href yields a usable filename, its href rebuilt as `/pypi/{repo_key}/simple/{project}/{filename}` plus the upstream `#fragment`; the anchor's `data-*` attributes, entity-decoded and re-escaped into fresh double-quoted values (or emitted bare, as PEP 714 permits); and the escaped filename as the link text. Every other upstream byte is dropped, `<base>` among them, so `BASE_TAG_RE` and its fixpoint loop are gone — nothing survives for it to guard against. Absolute, root-relative and plain-relative upstream hrefs all keep routing through the proxy and no upstream host, path or query survives. **No client-visible information is lost:** `data-requires-python`, the PEP 658/714 `data-dist-info-metadata` / `data-core-metadata` attributes, `data-yanked` and the `#sha256=` fragment are all preserved, so version resolution, PEP 658 metadata fetching and hash checking are unaffected. One behaviour worth knowing: an anchor whose filename contains `<`, `>`, `"`, `'`, whitespace or a control character is **dropped rather than escaped and emitted** — such a "filename" is an attribute-breakout payload rather than a distribution name, the same posture `is_safe_upload_filename` takes at the ingest choke-point (#3107); no legal wheel or sdist filename contains those characters. The shared anchor and href matchers are now used by both this path and `filter_remote_case_a_html` rather than duplicated. **Am I affected:** only with a remote/proxy PyPI repository (or a virtual repository with a remote PyPI member), on the per-project route `/pypi/{repo}/simple/{project}/`; hosted repositories render their own index from database rows, and the `/simple/` root listing is rebuilt from normalized names, so neither was ever affected. On a public repository the page is served to unauthenticated callers. Note where the trust boundary sits — the payload has to come from the upstream index document, so the realistic actor is a hostile or compromised private mirror, corporate index or proxied registry, or someone able to tamper with that response; a publisher to public PyPI cannot reach this, because Warehouse escapes its own output and enforces a filename grammar excluding the necessary characters. **No cache purge is needed:** the proxy caches the raw upstream body and the page is rebuilt in the handler on every serve including warm cache hits, so a poisoned page already in the cache is sanitized on read as soon as this is deployed. **One route is deliberately not claimed as closed:** `GET /api/v1/repositories/{key}/download/simple/{project}/` on a Remote repository falls through to `proxy_fetch_streaming`, which streams the raw cached upstream body with the upstream's own content type (the `application/octet-stream` argument is only a fallback for when upstream sends none) and no rewriting. The same CSP ceiling applies, so the severity is unchanged rather than worse, but the class is not eliminated on that path.
- **npm, PyPI and Maven proxy-cache commits are gated on the upstream's published digest** (GHSA-qxv7-p3mq-88fv, #3347). Cargo (#2929) and OCI (#2274) already refused to *cache* a proxied body whose digest disagreed with the digest the upstream published for it. npm, PyPI and Maven each had that digest within reach and passed `None` to the gate, so the `dist.integrity` value the server itself served in a packument, the `#sha256=` fragment it served in a simple index and the `.sha1` sidecar it served beside a jar were decorative on the server side: a body that disagreed with them was committed to the proxy cache and served warm from then on. All three now resolve the digest and route through `fetch_artifact_streaming_with_cache_path_gated_digest`, a new algorithm-flexible sibling of the SHA-256-only gate — npm and Maven digests (SHA-512, SHA-1) are never observed by the storage layer, so the tee hashes the forwarded bytes for those algorithms. **npm** parses `dist.integrity` as SRI, preferring SHA-512 over SHA-256 over SHA-1 and skipping SHA-384 (no tee-hasher support), falling back to the legacy `dist.shasum`; the entry is matched to the request by the tarball URL's **filename**, never by parsing a version out of the requested filename (hyphenated names and prerelease tags make that ambiguous — the curation gate refuses to for the same reason, #2930), and the packument is re-read through the same cache-keyed helper the metadata handler uses, so an npm client that just fetched it finds it warm. **PyPI** lifts the `#sha256=` fragment from the same index page that vouched for the download URL; PEP 658 `.metadata` resolutions carry no fragment and stay ungated. **Maven** fetches the upstream `.sha1` sidecar, accepting the bare digest or the two-field `md5sum` form and only when the token is bare lowercase 40-hex, and gates **release versions only** — `-SNAPSHOT` is mutable and a racing re-deploy would pin a stale sidecar against a legitimate body, while checksum sidecars and `maven-metadata.xml` are excluded by `maven_proxy_package_name`'s own skip rules. Several properties matter more than the headline, and the one-line summary invites more confidence than the code earns. **The gate fails open:** no usable digest — absent, malformed, non-canonical (uppercase hex is rejected as unauthoritative and therefore silently ungated), an npm SRI carrying only the unsupported SHA-384, or an upstream error fetching the sidecar — and the fetch proceeds unverified exactly as before, so the protection is only as good as the upstream's digest publication. **A mismatch is served, not refused:** the bytes still reach the client, which verifies independently (npm checks the SRI, pip the hash, Maven and Gradle the sidecar), and are only kept out of the cache — this prevents persistent poisoning of the shared cache rather than protecting the requesting client, and a raw `curl` of a proxied artifact is unprotected. **How much an adversary is slowed differs by format:** for npm and Maven the digest travels from the *same upstream over the same channel* as the artifact — the packument, the adjacent `.sha1` — so an actor able to tamper with the body can tamper with the digest, and what these two reliably catch is divergence and corruption (a truncated body, a clean-EOF cut, a broken CDN edge); PyPI is meaningfully stronger, because the `#sha256=` comes from the index host while the file comes from the file host, a different origin on public PyPI, so it catches a misbehaving or compromised file host while the index is intact. That places these below OCI's pre-existing gate, whose expected digest is the *client-requested* blob digest rather than a value supplied by the server serving the bytes. One shared sharp edge: the expected digest is itself resolved cache-first, so a packument, index page or sidecar already poisoned in the cache supplies the attacker's digest and the gate admits the matching bytes. **Coverage inside the three formats is not total:** repositories with scan-on-proxy enabled take a buffered fetch path that is not gated (in the PyPI scanned path the resolved `expected_sha256` is carried and then never used), and npm and Maven **virtual** repositories resolve members through the ungated `fetch_artifact_streaming` entry point, though PyPI virtual member fetches are gated. The "mirroring the Cargo #2929 / OCI #2274 posture" claim is accurate — both are the same serve-but-don't-cache shape and both are likewise fail-open — but the set it joins is larger than the message says: **Debian** has had this since #2459, and its index is verified against a GPG-signed `Release`, making it the strongest of the group. So this brings the total to six gated formats out of roughly twenty-five proxied ones; NuGet, goproxy, Helm, Conda, Hex, RPM, Composer, Terraform, Git LFS and the rest still commit without a digest gate, several despite publishing a perfectly usable one. **Observability gap worth knowing:** a rejected commit emits a single `warn!` carrying only `cache_key` — no `target: "security"`, no `repository` field, no expected/actual digests, and **no metric**, so unlike the pre-existing content-addressed gate these events do not increment `ak_proxy_cache_checksum_mismatch_total` and will not reach an operator already alerting on it. **Am I affected:** only with remote/proxy npm, PyPI or Maven repositories. Upgrading admits no further unverified entries but evicts nothing already cached — see the upgrade note above.

## [1.7.3] - 2026-08-13

### Note — there is no 1.7.2 release

**1.7.2 was skipped and the version number is retired.** During the 1.7.2 cut the backend and openscap images were published to both registries before the release gate completed, and were publicly pullable for roughly nine hours before those tags were deleted. Republishing the same number with different content is precisely the "same tag, different bytes" hazard the exact-version immutability check exists to prevent, so the number was retired rather than re-pointed — everything staged for 1.7.2 ships here instead, unchanged in scope. **If you pulled a `1.7.2` image inside that window, discard it and take 1.7.3**: those bytes carry no release-gate certification, and while the tags no longer resolve in either registry, a local or mirrored cache can still hold a copy. No GitHub Release was ever published for 1.7.2, so a reader going from 1.7.1 to 1.7.3 has not missed one.

### Upgrade note — migration 193 blocks OCI writes while it runs, and the backend is not listening yet

`backend/migrations/193_oci_blobs_storage_key_index.sql` builds a plain `CREATE INDEX` on `oci_blobs`. Plan a write window before deploying, and on Kubernetes raise your startup/liveness thresholds first.

**The mechanism.** Migrations run at `main.rs:223`; the HTTP listener does not bind until `main.rs:1183`. For the whole index build the backend is therefore **not listening at all** — health probes get connection-refused, not a `503`, because there is no server to answer them. `CREATE INDEX CONCURRENTLY` is deliberately not used: `sqlx::migrate!` runs each migration file inside a transaction and Postgres rejects `CONCURRENTLY` there, the same constraint migrations 166 and 194 are under. A plain `CREATE INDEX` takes a `SHARE` lock, which conflicts with `ROW EXCLUSIVE`, so for the duration of the build **writes to `oci_blobs` block and reads do not**: blob commits on the push path wait, as do blob GC's row deletes, while plain `SELECT`s take `ACCESS SHARE` and are unaffected. `docker pull` keeps working while `docker push` hangs, and that asymmetry is the diagnostic signature — if pushes are hanging during a deploy while pulls are fine, this is what you are looking at. It is *not* `ACCESS EXCLUSIVE`: nothing here rewrites the table, so plan the window against blocked writers, not blocked readers.

**The failure mode is a crash loop, not a slow start.** The migration runner raises its timeouts session-locally — `statement_timeout = '30min'` (`main.rs:217`) and `lock_timeout = '5min'` (`main.rs:220`). The lock timeout is the one that bites: if a long-running transaction holds a conflicting lock on `oci_blobs` for more than five minutes, the migration errors, `run_server` returns `Err`, and the process exits. Kubernetes restarts it, it takes the lock timeout again, and the deployment never comes up. Drain or kill long transactions touching `oci_blobs` before deploying.

**Kubernetes is the real risk.** A `livenessProbe` with a short `failureThreshold` kills the pod mid-`CREATE INDEX`. The transaction rolls back, the partially built index is discarded, and the replacement pod starts the build again from zero — on a large registry that never converges; it just spends a write stall per attempt, forever. Raise `startupProbe.failureThreshold` to cover the build (or the liveness thresholds, if you have no startup probe) **before** you deploy this release, not after the first pod is killed.

**No build-time estimate is given here, because none has been measured.** `oci_blobs` holds one row per stored blob and is much smaller than `artifacts`, so the build is expected to be short — but "expected" is not a number, and the only honest input is your own row count. Size it first, on the running 1.7.1 instance:

```sql
SELECT count(*) FROM oci_blobs;
```

**Avoiding the stall entirely.** Build the index out of band against the live table, before deploying:

```sql
CREATE INDEX CONCURRENTLY idx_oci_blobs_storage_key ON oci_blobs (storage_key);
```

The migration's `CREATE INDEX IF NOT EXISTS` then finds it and is a no-op. This is the only supported way to avoid the stall — the migration cannot simply be skipped for one file, because sqlx tracks applied migrations by version and checksum and a missing entry blocks the ledger. Do build it, one way or the other: without the index the OCI cleanup sweep's per-candidate liveness re-check falls back to a sequential scan (measured on 300k blobs at 87 ms per hold versus 0.19 ms with it, ~163x over a full 1,000-row batch), and the cost scales linearly with registry size, so on a large registry a batch approaches the 15-minute claim TTL, leases lapse mid-sweep, and storage reclamation stops making progress. Skipping it trades a bounded write stall for an unbounded GC stall (#3085).

### Upgrade note — rolling back to 1.7.1 requires `SKIP_MIGRATIONS=true`

The 1.7.3 schema is forward-compatible with a 1.7.1 binary, but a 1.7.1 binary will **refuse to start** against a 1.7.3 database unless you set `SKIP_MIGRATIONS=true`. You set the flag that sounds dangerous in order to roll back safely; that inversion is exactly what nobody works out mid-incident, so it is written down here rather than left to be discovered.

**The data is fine.** 1.7.3 adds three migrations and all three are additive. 193 creates an index. 194 adds `oci_upload_cleanup_keys.pending_delete_at` (`TIMESTAMPTZ`, nullable, no default) plus a partial index over it. 195 adds six columns to `curation_packages`: `attestation_state TEXT NOT NULL DEFAULT 'unverified'` and five nullable ones. Nothing is dropped, renamed or retyped, and every added column either carries a safe default or accepts `NULL`, so a 1.7.1 binary that never mentions them still writes correct rows. There is no data migration to undo and no restore-from-backup step.

**What blocks the rollback is the migration ledger, not the data.** `main.rs:223` runs `sqlx::migrate!("./migrations")`, whose `Migrator` carries `ignore_missing = false`. On startup sqlx validates the applied set recorded in `_sqlx_migrations` against the versions embedded in the binary and returns `MigrateError::VersionMissing` for any applied version the binary does not know about. v1.7.1 embeds migrations up to 192; a 1.7.3 database has 193, 194 and 195 applied. The 1.7.1 binary therefore fails validation and exits before it binds — the same symptom as the crash loop above, from an entirely different cause, which is why the two notes sit together.

**Am I affected?** Only if you roll back. The supported path is to set `SKIP_MIGRATIONS=true` on the 1.7.1 deployment: it takes the `if skip_migrations` branch at `main.rs:203-205`, logs `SKIP_MIGRATIONS=true, skipping automatic database migrations`, and never constructs the `Migrator` at all, so nothing validates the ledger. The one-shot legacy repair routines that run ahead of the gate (`main.rs:191-196`) still run and still no-op on a healthy database. Rolling forward again is simply removing the flag — 193 through 195 are already applied, so the runner has nothing to do. Do **not** delete rows from `_sqlx_migrations` as an alternative: sqlx will then try to re-run those files against a schema that already contains their objects.

### Upgrade note — authenticated callers now need a grant to pull private OCI/Docker repositories

OCI `/v2` reads are authorized. Previously the only check on the pull path was whether the caller was *anonymous*, so **any authenticated principal could pull any private image** by name or digest, and `GET /v2/<name>/tags/list` handed a private repository's tag names to the same audience. Writes were never affected.

**The mechanism.** `/v2` is nested in the router ahead of `format_routes`, and `repo_visibility_middleware` — the layer that authorizes every other format's reads — is attached to `format_routes` only, so the entire OCI read surface sat outside it. Inside the handlers the guard was `if is_anon && !repo.is_public`: `authenticate_oci` establishes *who* the caller is and never asked whether they may read this repository. The neighbouring gates did not close it either — `enforce_token_repo_scope` is unrestricted for a JWT session, `require_scope` is satisfied unconditionally by a principal carrying `scopes: None`, and `can_access_repo` is a token-scope ceiling (`AccessScope::Admin => true`), never a visibility check. All five read verbs — `GET`/`HEAD` blob, `GET`/`HEAD` manifest and `GET tags/list` — now run `require_oci_repo_read_access`, which asks `PermissionService::check_repository_action`, the same choke point the OCI write gate already used. That is deliberately not a new predicate: a role carrying `admin` (the `repository-owner` row repository creation seeds) always wins, an applicable fine-grained rule stays authoritative for the principals it names, and every other principal keeps its role-based capabilities. A denial is `403 DENIED` when fine-grained rules govern the repository and `404 NAME_UNKNOWN` when none do, with a body byte-identical to the one a genuinely missing repository returns, so the two answers cannot be used to probe which private repositories exist (#3261).

**Am I affected?** Yes, if any principal pulls from a private OCI/Docker repository while holding no role assignment, no fine-grained `read`/`admin` rule and no repository ownership — a CI service account carrying only an API token and no grant is the usual case. Give those principals a grant before upgrading. Anonymous pulls, public repositories, admin pulls, repository owners and every already-granted principal are unaffected, and the `401` + `WWW-Authenticate: Bearer` challenge that drives the `docker login` handshake is untouched. Vulnerability scanning of private images also keeps working: `_ak_scanner` is seeded with no grants by design, so the single-repository token `generate_scan_token` mints is accepted as the authorization it is, after `enforce_scan_pull_scope` has already confirmed the token names this repository.

**Finding those principals before you upgrade.** "Give them a grant" is only actionable if you can list them. This is the inverse of `PermissionService::check_repository_action(user, repo, 'read', false)` — the same admin-role arm, the same rules-are-authoritative-when-present `CASE`, the same group and project arms — evaluated over everyone who has actually pulled from a private OCI-backed repository, so it returns principals that will start being denied rather than every ungranted account in the instance:

```sql
SELECT u.username, r.key AS repository, max(ds.downloaded_at) AS last_pull
FROM download_statistics ds
JOIN artifacts a    ON a.id = ds.artifact_id
JOIN repositories r ON r.id = a.repository_id
JOIN users u        ON u.id = ds.user_id
CROSS JOIN LATERAL (
  SELECT
    EXISTS (SELECT 1 FROM role_assignments ra JOIN roles ro ON ro.id = ra.role_id
             WHERE ra.user_id = u.id
               AND (ra.repository_id = r.id OR ra.repository_id IS NULL)
               AND 'admin' = ANY(ro.permissions))                       AS role_admin,
    EXISTS (SELECT 1 FROM permissions p
             WHERE ((p.principal_type IN ('user','service_account') AND p.principal_id = u.id)
                 OR (p.principal_type = 'group' AND p.principal_id IN
                      (SELECT group_id FROM user_group_members WHERE user_id = u.id)))
               AND ((p.target_type = 'repository' AND p.target_id = r.id)
                 OR (p.target_type = 'project'    AND p.target_id = r.project_id))) AS has_rule,
    EXISTS (SELECT 1 FROM permissions p
             WHERE ((p.principal_type IN ('user','service_account') AND p.principal_id = u.id)
                 OR (p.principal_type = 'group' AND p.principal_id IN
                      (SELECT group_id FROM user_group_members WHERE user_id = u.id)))
               AND ((p.target_type = 'repository' AND p.target_id = r.id)
                 OR (p.target_type = 'project'    AND p.target_id = r.project_id))
               AND ('read' = ANY(p.actions) OR 'admin' = ANY(p.actions)))           AS rule_grants,
    EXISTS (SELECT 1 FROM role_assignments ra JOIN roles ro ON ro.id = ra.role_id
             WHERE ra.user_id = u.id
               AND (ra.repository_id = r.id OR ra.repository_id IS NULL)
               AND ('read' = ANY(ro.permissions) OR 'admin' = ANY(ro.permissions))) AS role_grants
) g
WHERE r.format::text IN ('docker','podman','buildx','oras','wasm_oci','helm_oci')
  AND r.is_public = false
  AND u.is_admin = false
  AND ds.downloaded_at > NOW() - INTERVAL '90 days'
  AND NOT (g.role_admin OR CASE WHEN g.has_rule THEN g.rule_grants ELSE g.role_grants END)
GROUP BY 1, 2
ORDER BY 3 DESC;
```

Read it as a floor, not a census: `download_statistics` records a row for a manifest `GET` and not for a `HEAD`, `user_id` is `NULL` for anonymous pulls, and a service account that has not pulled inside the window will not appear. Widen the interval, or drop the `downloaded_at` predicate entirely, if your CI fleet pulls seasonally.

**If you miss one, the symptom will not look like an authorization failure.** The `403 DENIED` arm only fires when the repository carries rows in the `permissions` table. Grants usually come from `role_assignments` instead, so the common denial is the other arm: `404 NAME_UNKNOWN`, with a body byte-identical to the one a genuinely missing repository returns. `docker login` still succeeds, because the token handshake is untouched; `GET /v2/<name>/tags/list` 404s the same way; and there is **no server-side log line on a denial** — only the lookup-failure paths log. The on-call engineer's first hypothesis will therefore be "GC deleted our images", and Docker lifecycle retention *also* changed in this release (#2998, #3026, under Fixed), so that hypothesis will look plausible for longer than it should. If images vanish for one principal and not another after this upgrade, check grants before you check retention.

**Known limitation, and what it costs (#3269).** `GET /v2/_catalog` still returns every repository key present in `oci_tags` to any authenticated caller, with no per-repository filtering — name enumeration, not content. It is left as-is because the Docker Registry HTTP API V2 makes catalog contents implementation-specific and most registries behave the same way, and it is targeted at 1.8.0. **Read it together with the `404` denial above, because the two interact.** The `404 NAME_UNKNOWN` arm was deliberately made byte-identical to a genuinely missing repository so that a denial could not be used to probe which private repositories exist. That property is real at the `/v2/<name>` seam and worth keeping — but it protects nothing an authenticated caller cannot obtain in a single request to `/v2/_catalog`. So the accurate summary of this release is narrower than "private OCI repositories are now protected": private image *content* is protected, and the repository *namespace* is not. Treat every repository key as visible to every authenticated principal until #3269 lands, and do not rely on an unguessable repository name as a layer of defence.

### Upgrade note — backups on filesystem storage start reporting `failed`, and your existing archives never held artifact bytes

On a deployment using the filesystem storage backend — the default — `POST /api/v1/admin/backups/{id}/execute` now answers `200` with `status: "failed"` and an `error_message` naming artifact objects it could not read, on any instance holding at least one artifact. In 1.7.1 the same call reported `completed` with `artifact_count=0`. **Nothing about what the archive contains has changed.** These archives did not contain artifact bytes before this release and do not contain them now. This is the first release in which the deployment tells you so, which is the whole of the change and also the reason it reads like a regression when it is not one.

**The mechanism.** Two things compose. The first is #2863, open and pre-existing: a filesystem repository is rooted at its own directory — `storage_path` is `{STORAGE_PATH}/{repository key}` — and the format handlers write objects relative to that root, so an object recorded as `storage_key = a1/36/a136b2c2…` physically lives at `{STORAGE_PATH}/{repository key}/a1/36/a136b2c2…`. The backup path reads `artifacts.storage_key` through the deployment-wide storage handle and therefore looks for it at `{STORAGE_PATH}/a1/36/a136b2c2…`, which is nothing. Not some artifacts: on a filesystem deployment *every* artifact read misses, and the database export in `database/*.json` is all the archive has ever held. The second is #3170, in this release: that read failure used to be discarded by an `if let Ok(content)`, so the job recorded `completed` over an archive with no content in it — a backup that reports success while holding nothing is the worst failure a backup can have, because it is discovered at restore time when there is no recourse. Read failures are now logged, counted into the manifest's `artifacts_unreadable`, and fail the job. #3327 then moved that failure to where an operator can see it: the trigger endpoint returned a bare `500 {"code":"INTERNAL_ERROR","message":"Internal server error"}`, discarding at the HTTP boundary the very message that names the missing objects, and now answers with the backup resource carrying `status` and `error_message`.

**Am I affected?** Yes, if `STORAGE_BACKEND` is unset or `filesystem` and the instance holds any artifact rows. An empty instance still reports `completed` — it has nothing to miss, which is why a fresh test namespace looks healthy and a real one does not. Object-storage deployments are **not** affected: S3, GCS and Azure resolve to one shared namespace for the whole deployment, so the recorded `storage_key` is the whole key and the read finds the object. A mixed deployment splits the same way, per repository: repositories assigned to a named cloud backend are captured, repositories on filesystem are not. Scheduled backups are affected identically to manual ones: the scheduler still takes the failed-run path, so the occurrence is finalized as failed in `backup_schedule_runs` and the backup metric records a failure — a behaviour change for anything alerting on either, and one that will fire on the first scheduled run after the upgrade rather than at the next restore.

**Reading an archive you already have.** The manifest is a plain file in the tarball, so an existing archive can be audited without restoring it:

```
tar -xzOf backup.tar.gz manifest.json | jq '{artifact_count, artifacts_unreadable, database_tables}'
```

`artifacts_unreadable` does not exist in an archive written before 1.7.3 and deserializes as `0`, so it cannot confirm anything about an old archive — **the tell in a pre-1.7.3 archive is `artifact_count: 0` on an instance that was not empty.** The `database_tables` list is genuine: `users`, `repositories`, `artifacts`, `download_statistics`, `api_tokens`, `roles`, `user_roles` and `permission_grants` were exported and are restorable. What you have is a metadata backup. Plan your disaster recovery on that basis until #2863 lands, and keep an independent copy of `{STORAGE_PATH}` — a filesystem-level snapshot or an `rsync` of that tree is what actually holds your artifact bytes today.

**Object storage is not a workaround for an instance that already exists.** Changing `STORAGE_BACKEND` does not move a single byte, and there is no supported in-place migration that re-lays a per-repository filesystem tree into the shared object namespace and reconciles `artifacts.storage_key` to match. Existing repositories keep the `storage_backend` recorded on their row. Object storage is the right answer for a deployment being stood up now, and no answer at all for one already running on filesystem.

**What `allow_partial_artifacts` buys, and what it does not.** Setting `allow_partial_artifacts: true` in a backup's `metadata` makes the run report `completed` instead of `failed`, keeps the miss count in the manifest, and still makes a restore of that archive record an error rather than reporting `errors=[]`. It **does not put a single byte into the archive** — it is an acknowledgement, not a repair, and an archive produced under it is exactly as empty of artifact content as one produced without it. Reach for it only if a `failed` job is breaking automation you cannot change quickly, and do not let `completed` be read as "the backup is fine". Note also that the flag has no API surface in this release: `POST /admin/backups` does not accept it and there is no update endpoint, so the only way to set it is an `UPDATE backups SET metadata = metadata || '{"allow_partial_artifacts":true}'::jsonb WHERE id = …` between creating the backup and executing it.

**Tracking.** #2863 is the root-cause fix — the read path, the archive's flat key namespace, and the restore side all have to move together, so it is milestoned 1.8.0 with an assessment recorded on the issue. Separately, OCI blobs live in `oci_blobs` rather than `artifacts` and are never enumerated by the backup at all, so they are absent from every archive on every backend for an independent reason.

### Upgrade note — two OCI `/v2` error paths now return the spec error envelope

A client that parses the **body** of an OCI scope denial or a curation block sees a different shape after upgrading. The status codes are unchanged; only the bodies are.

**The mechanism.** Two `/v2` error paths did not emit the `{"errors":[{"code":…,"message":…,"detail":…}]}` document the OCI distribution spec requires of a JSON 4XX body. `oci_forbidden_scope` — the API-token scope gate on the monolithic blob `POST`, upload `PATCH`/`PUT`, blob `DELETE`, manifest `PUT` and manifest `DELETE` — returned a bare string, `Token does not have required scope: …`; that text is now preserved verbatim inside an envelope with code `DENIED`. The shared curation gate reached from manifest `GET`/`HEAD` leaked the REST-shaped `{"error":"curation_blocked","package":…,"reason":…}` body onto the OCI wire, which is a differently-shaped JSON object and therefore a spec violation; it now renders `DENIED` with the `curation_blocked` marker kept in `message` **and the blocking rule's reason carried in both `message` and `detail`**, which an operator debugging a blocked `docker pull` previously did not get at all. The two fallible extractors on `/v2/token` (a duplicated `service` query parameter, and a request body over the token-endpoint limit) were the last bare-string 4XX bodies reachable on this router and are re-shaped the same way, keeping their original status and text under code `UNSUPPORTED`. Every non-OCI format's curation body is byte-identical to before, pinned by a test — the seam was split so the shared gate returns a structured verdict and each protocol renders it (#3110).

**Am I affected?** Only if something in your tooling reads those bodies rather than the status code. `docker`, `podman` and other spec-conformant clients are unaffected — they already parse the envelope. The companion change lands in `artifact-keeper-test`, where the curation DTF oracle selected on `.error` by field lookup and would have gone red on the first release carrying this fix; it now requires the envelope at ≥ 1.7.2 and accepts either shape below it. That floor names 1.7.2 deliberately even though no 1.7.2 shipped: the oracle compares with `>=`, so this release is held to the envelope, and keeping the floor one number lower also holds the `dev` images built from `main` while the workspace version read 1.7.2 — which already carried this fix.

### Upgrade note — the fine-grained permissions API is admin-only

`GET /api/v1/permissions`, `GET /api/v1/permissions/{id}` and `DELETE /api/v1/permissions/{id}` now require admin. Non-admin API consumers of those three endpoints will start receiving `403`.

**The mechanism.** The write endpoints were admin-gated, the reads and the delete were not. The two read handlers extracted no `AuthExtension` at all, so any authenticated principal — including a read-scoped service-account token — could page the whole ACL table and learn which principals hold `admin` on which repositories. `DELETE` checked only `require_scope("delete")`, and `AuthExtension::has_scope` returns `true` unconditionally when `scopes` is `None`, which is what every interactive JWT session carries — so any logged-in non-admin could delete arbitrary permission rows. All three now use the same `require_auth` + scope + `require_admin()` shape the create and update handlers already had, before any database work (#3229).

**Am I affected?** Only if a non-admin principal calls those three endpoints. The shipped consumers are all admin surfaces — the web UI reads the list only from its admin route group, and the CLI `permission` command is an admin management command — so no shipped workflow breaks. Admin-only was chosen over a filtered "grants naming me" view because the endpoint has no per-caller filtering model today; a self-service view should be a deliberately designed endpoint rather than a widening of this one.

### Upgrade note — eight smaller operational changes worth two minutes before you deploy

Each of these is documented in full under Security, Changed or Fixed. Collected here is only the part an operator has to act on, ordered by how likely it is to page someone who cannot immediately tell what happened.

- **Storage alerts will fire on upgrade day, and it is not an incident** (#3134, #3249). `total_storage_bytes` now sums the `repository_usage_ledger` instead of aggregating `artifacts`, so on a Docker-heavy registry the reported total steps up by the entire OCI blob volume — layers were previously invisible because only manifests land in `artifacts` — and proxy-cached bytes fold in as well. The daily `storage_metrics` snapshot changes basis on the same day, so growth charts show a one-day vertical step. **Any alert on `total_storage_bytes` or on storage growth rate will fire, and it will look like a real incident.** It is display-only: quota admission reads the per-repository `repository_usage_ledger` row and is unchanged, so **no repository becomes over-quota as a result of this**. Record the pre-upgrade values, silence or re-baseline storage-growth alerts for 48 hours, and annotate the series at the cut. Historical `storage_metrics` rows are deliberately not backfilled — the source tables hold no history, so a backfill could only fabricate one.
- **Curation freshness silently dropped 12x — in 1.7.1, not in this release, so if you are on 1.7.1 you are already affected** (#2219). This one is not an upgrade action; it is a *check now* action, and it is listed here because 1.7.1's published notes did not carry it. `curation_sync_interval_secs` was read into the row and never used, so every enabled staging repository re-synced on every 5-minute scheduler tick. **1.7.1 (2026-08-07) started honouring it** (`scheduler_service.rs:1518-1533` on this release, 60-second floor; migration 190 added the `curation_last_synced_at` stamp), and the column default is **3600** (`migrations/071_curation.sql:54`). So unless you explicitly set the field, curation freshness went from roughly 5 minutes to 1 hour **the moment you deployed 1.7.1** — it does not change again at 1.7.3. The failure signature is "a package we blocked upstream an hour ago is still installable", and there is no metric, no log line and no alert for it, which is why an instance can sit at the new interval for weeks without anyone noticing. Run this now if you are on 1.7.1, rather than at your next upgrade window, and lower the interval where an hour is too slow (`curation_last_synced_at` exists only on 1.7.1 and later; on 1.7.0 or below, drop it from the projection):
  ```sql
  SELECT id, curation_sync_interval_secs, curation_last_synced_at
  FROM repositories WHERE curation_enabled;
  ```
- **PyPI project names that were never valid become permanently unreachable** (#3186, #3198). Read routes now reject a `:project` segment that fails the PEP 508 name pattern with `404` before any lookup, and uploads reject the same with `400`. Malformed names were previously coerced rather than validated, and some of them were *stored* — an artifact whose project name is empty or otherwise outside the pattern still consumes quota but is now reachable at no URL, with no migration to repair it. This is the correct fix and it is a silent data-visibility loss, so inventory before you upgrade rather than after. The predicate is the enforced regex itself (`formats::pypi_name::PEP508_NAME_RE`), written in the spec's `^`/`$` spelling:
  ```sql
  SELECT r.key AS repository, a.name AS project, count(*) AS artifacts
  FROM artifacts a
  JOIN repositories r ON r.id = a.repository_id
  WHERE r.format::text IN ('pypi','poetry','conda')
    AND a.is_deleted = false
    AND a.name !~ '^([A-Za-z0-9]|[A-Za-z0-9][A-Za-z0-9._-]*[A-Za-z0-9])$'
  GROUP BY 1, 2 ORDER BY 3 DESC;
  ```
  Zero rows means nothing becomes unreachable. Rows returned are what to re-publish under a valid name (or delete) before upgrading.
- **The scan-gate widening only bites deployments that configured a policy** (#3220, #3143, #3142). `PolicyService::evaluate_artifact` short-circuits to allowed when no enabled policy matches (`policy_service.rs:152-173`), and nothing seeds a default row, so one query sizes the entire exposure:
  ```sql
  SELECT id, name, repository_id, is_enabled, block_unscanned, block_on_fail, max_severity
  FROM scan_policies;
  ```
  Zero rows means zero risk from all three changes. If rows come back, two amplifiers are worth knowing: a row with `repository_id IS NULL` is **global** and applies to every repository, and since migration 121 a policy created without an explicit `block_unscanned` gets `true`.
- **An OpenSCAP `critical` rule failure now costs 25 security-score points, where it cost 0** (#3294). The OpenSCAP adapter had no `critical` arm and filed such a result at `info`, which is inert: it carries a penalty weight of `0`, belongs to no `policy_service` severity array, and is selected by no gate's count projection. It now classifies as `critical`, and `recalculate_score` — which runs automatically after every scan and counts one row per `(artifact, scan_type)`, so OpenSCAP competes alongside Trivy and Grype rather than replacing them — charges 25 points each. On an otherwise-clean repository **1 such finding takes it from grade A to B, 2 to C, 3 to D, and 4 to F**. The per-finding effect is also broader than a re-grade: `critical` is a member of *every* `policy_service` severity array, so a single one violates a scan policy at **any** `max_severity`, not just the permissive settings — where as `info` it violated none. That reaches the artifact download gate (`403`), both promotion CVE gates, the approval gate, and any `min_health_score` or `min_security_score` quality gate. **Am I affected?** Only if you run OpenSCAP — it is opt-in and registers only when `OPENSCAP_URL` is set — *and* your SCAP content declares `severity="critical"` on a rule. XCCDF 1.2 defines the attribute as `unknown | info | low | medium | high` (NISTIR 7275r4 §6.4.4.2), so `critical` is outside the standard vocabulary and appears only in custom or vendor-extended content; Artifact Keeper's wrapper passes through whatever the rule declares. Stock SSG content is unaffected. Nothing is backfilled — stored `scan_findings` rows keep their recorded `info` and a repository moves only when its artifacts are next scanned — so the change lands one repository at a time and can be staged. Findings that are acknowledged are excluded from the score. To size it before upgrading:
  ```sql
  SELECT r.key AS repository, count(*) AS findings
  FROM scan_findings f
  JOIN scan_results s ON s.id = f.scan_result_id
  JOIN artifacts a ON a.id = s.artifact_id
  JOIN repositories r ON r.id = a.repository_id
  WHERE s.scan_type = 'openscap' AND f.severity = 'info' AND NOT f.is_acknowledged
  GROUP BY 1 ORDER BY 2 DESC;
  ```
  Rows returned are the repositories where a re-scan can move the grade; how far depends on how many of those findings the content grades `critical`. This is a fail-closed correction — a `critical` compliance failure filed at the floor meant a severity gate silently not firing — but it is not a silent one, which is why it is here.
- **`helm push oci://` into a classic Helm repository now returns `400`, and charts pushed that way disappear from the index** (#3150). Legacy OCI manifest rows are excluded from `index.yaml` generation (`helm.rs:119-134`, `is_oci_manifest_artifact`), so the operator-visible event is "our charts vanished from `helm repo update` and our publish job started failing". The index those rows produced was never valid, and reads are deliberately left open so the bytes can be pulled back out, but the disappearance is abrupt. Re-publish affected charts to a `helm_oci` repository, or push them as classic charts.
- **The OCI push path can now answer `503`, on a path that never did before** (#3187). When a push races the cleanup sweep reclaiming the same storage key, the monolithic blob `POST` (`oci_v2.rs:5497`) and the chunked completion (`oci_v2.rs:6962`) return `503` with code `BLOB_UPLOAD_INVALID`, the message `blob storage was being reclaimed concurrently; retry the upload`, and `Retry-After: 1`. The window is narrow — it needs a digest whose orphaned blob object is at least 24 hours old, unreferenced, and actively being reclaimed — but `BLOB_UPLOAD_INVALID` reads as terminal to some clients, so a CI pipeline with no push retry will see intermittent, unexplained failures. Add a retry around `docker push` if you do not have one.
- **The S3 bulk request timeout default moves from 30s to 1800s** (#3180). The knobs themselves are listed under Added; the default change is the operational item. A wedged S3 endpoint previously failed a bulk request in 30 seconds; it can now hold the connection for up to 30 minutes, which turns a fast fail into connection-pool and task pileup during a partial object-store brownout. Control-plane calls (head, exists, list, delete, multipart create/abort) keep the short 30-second cliff, so this applies only to payload movement — object reads and writes, multipart parts, and server-side `CopyObject`. If you sized your pool around a 30-second worst case, set `S3_BULK_TIMEOUT_SECS` deliberately rather than inheriting the new default.

### Security

- **A repository-scoped API token presented directly to `/v2/` is now confined to its repositories, for reads and writes** (#3316). A token minted with an `allowed_repo_ids` allow-list is the standard containment primitive for CI service accounts and registry mirrors. That ceiling — `enforce_token_repo_scope`, which #3261's upgrade note describes as running "as the first act of the read gate ahead of the admin bypass" — was only ever populated for tokens laundered through the `/v2/token` exchange. On the **direct-credential** path — the way `docker login`, `-u user:token` and a raw `Authorization: Bearer <token>` actually present a credential — `authenticate_oci_with_scopes` re-minted its claims through `generate_tokens(&user)`, which sets `allowed_repo_ids: None` (unrestricted), and the scope allow-list was silently discarded. A token scoped to repository A could therefore **read and write every other repository the owning identity holds any grant on**: `tags/list`, manifest and blob `GET`/`HEAD` all returned `200`, and a blob upload followed by a manifest `PUT` created tags in a repository the token was never scoped to. Only the repository ceiling was lost — the action-scope ceiling (read-only vs. write) travels through a separate channel and still bit, so a read-only token could not write even to its own repository. **Am I affected:** every prior version, on any deployment that issues repository-scoped API tokens and lets them reach `/v2/` directly (i.e. normal Docker/OCI usage — the token is sent as the Basic password or a Bearer, without an intervening `/v2/token` exchange). A token laundered through `/v2/token` was already confined; the direct path was not. To reason about exposure, treat any repository-scoped token that has been used against `/v2/` as if it held the owning identity's full read+write reach over the registry for the period it was valid. The fix threads the validated ceiling onto the minted claims on both direct-credential branches (Bearer slot and Basic password slot), mirroring the exchange path; a token whose allow-list resolves to nothing (`Restricted(vec![])`) maps to a deny-all, not to unrestricted. A JWT that was itself exchanged from a scoped token and then presented as the Docker password is now confined too — that arm re-minted and would otherwise have laundered the ceiling away, a bypass of this same fix. Only the repository ceiling is threaded here; the token's read-only-vs-write action ceiling was never lost and is unchanged. **This is a fail-closed behaviour change:** a CI pipeline whose scoped token has been reaching repositories outside its declared scope will begin receiving a denial on those repositories after upgrading. That is the intended outcome — the token is now doing what its scope always said — but scope any token to the repositories it genuinely needs before rolling this out, or the first failed pull/push after upgrade will be the notification. The denial shape depends on the repository kind: a direct (Local/Remote) repository outside scope answers `403 DENIED`, while a **Virtual** repository answers `404` — a token scoped to a virtual `V` clears the parent gate but each member is then re-checked against the token's scope (`member_passes_token_scope`) and dropped, so the parent resolves as if empty rather than returning an explicit 403. Both are refusals; only the status code differs.
- **A public Virtual repository no longer serves a private member's bytes to anonymous callers over the Helm, NuGet V2 and Swift download routes** (#3324). The route middleware admits an unauthenticated caller whenever the URL repository is public, and then authorizes only that repository — never the members a Virtual repository resolves through. Three download paths walked the member set unfiltered from there, each streaming a **private** member's stored artifact to an anonymous caller through a public virtual parent: `GET /helm/{repo}/charts/{filename}` served a hosted member's chart `.tgz` (and `.prov`) via `download_chart_via_index`, `GET /nuget/{repo}/v2/package/{id}/{version}` — the legacy V2/Chocolatey protocol — served a member's `.nupkg` while the V3 flat-container sibling was already authorized, and `GET /swift/{repo}/{scope}/{name}/{version}/Package.swift` served a member's manifest source while Swift's own archive download was already authorized. All three now thread the caller through and narrow the member walk with the same `proxy_helpers::authorize_virtual_members` predicate the OCI virtual walkers use — a member contributes bytes when it is public or the caller holds a grant on it and it passes the token-scope ceiling — rather than inventing a new predicate. A denied member reads as an ordinary miss, so neither the bytes nor the member's existence leak; a *failed* visibility query surfaces as a retryable server error rather than being flattened into a definitive "not found". Anonymous downloads of **public** members' content through the same virtual repositories are unchanged, as are authorized callers. **Am I affected?** Yes, if you publish a public Virtual Helm, NuGet or Swift repository that aggregates private members and rely on those members' contents staying private from unauthenticated callers; the members' direct routes were always gated and writes were never affected. Note the scope of what this closes: virtual-repository laundering is now shut for OCI (#3261, #3320) and for these three **byte** paths — metadata-level exposure through virtual parents (index documents, feeds and version listings that may still enumerate private members' package names and versions, including Helm's merged `index.yaml` and the NuGet OData feed) is a distinct, lower-severity gap tracked in #3323 and is **not** closed by this release.
- **OCI/Docker `/v2` reads are authorized rather than merely checked for anonymity** (#3261). See the upgrade note above for the operator-facing change. Two further gaps were closed alongside the main one: a **public Virtual repository laundered a private member's bytes** — `resolve_virtual_manifest` and `resolve_virtual_blob` walked the member set unfiltered, so an anonymous caller could receive a private member's manifest and layers through a public virtual parent. Both now filter members through `proxy_helpers::authorize_virtual_members`, the same helper Maven, npm, PyPI, Debian and goproxy use, and a denied member is simply dropped from the walk so member existence is not leaked either. Because `VirtualResolveKey` carries no principal term, the negative resolve cache is consulted and populated **only** when the caller can see every member — otherwise a denied caller's miss would be replayed to a granted one as a false "not found", turning the authorization filter into a cache-poisoning primitive. Second, the token repo-scope ceiling and the scoped-admin demotion now run as the first act of the read gate, ahead of the admin and scanner bypasses, matching the write gate — and `handle_tags_list` reaches that same gate, so all five read verbs run the ceiling through one path rather than only the manifest and blob handlers that also call it explicitly. One residual remains, filed as #3269: `GET /v2/_catalog` still lists every repository key to any authenticated caller (name enumeration, not content). See the upgrade note for why that residual constrains what the rest of this change buys you.
- **A public Virtual OCI repository no longer leaks a private member's tag names** (#3320). The #3261 work filtered `resolve_virtual_manifest` and `resolve_virtual_blob` through `proxy_helpers::authorize_virtual_members`, but the third virtual walker — `tags_list_virtual`, behind `GET /v2/<name>/tags/list` — was missed and still merged every member's tags unfiltered. Combined with the read gate's `if is_anon && !repo.is_public` short-circuit (an anonymous caller on a *public* parent never reaches the gate at all), an unauthenticated `GET /v2/<public-virtual>/tags/list` returned the tag names of the virtual's **private** members. Tag/repository names only, not artifact bytes — a metadata disclosure lower in severity than the manifest/blob laundering, but it contradicted the operator-facing claim above that OCI virtual laundering was closed. `tags_list_virtual` now threads the caller through and narrows the member set through the same `authorize_virtual_members` helper the manifest and blob walkers use: a member contributes its tags when it is public or the caller holds *any* grant on it (a role assignment or any fine-grained rule, regardless of the actions it carries) and it passes the token-scope ceiling, so anonymous and ungranted callers see public members only. Note the shared predicate is deliberately reused rather than a new one invented, and it is coarser than the direct read gate, which requires the `read` action — a principal holding only a write-scoped rule on a private member is refused a direct tags/list of it but still sees its tags through the virtual; that predicate gap is shared with the manifest/blob walkers and repository listing and is tracked separately. A denied member contributes nothing, which reads as "no tags there" and leaks neither tag names nor member existence, and a failed visibility query surfaces as a retryable server error rather than being flattened into the empty set (which the tags path would otherwise answer with a definitive `404 NAME_UNKNOWN`). An anonymous caller on a public virtual still sees the tags of its *public* members, so legitimate public pulls are unchanged. **Am I affected?** Yes, if you publish a Virtual OCI/Docker repository that aggregates private members and rely on those members' contents (including their tag names) staying private from unauthenticated callers. The private members' own direct read paths were already gated by #3261 and were never affected; writes were never affected.
- **Quarantine status and SBOM listings are gated on repository visibility, not token scope** (#3174). `can_access_repo` answers "is this repository within my token's scope?" — it resolves to `access_scope().grants(repo_id)` and `AccessScope::Admin => true` unconditionally, which is the shape of every browser JWT session, every unscoped API token and every global admin. Used as a visibility gate it returned `true` for every repository in the instance. `GET /api/v1/quarantine/{artifact_id}` therefore told any authenticated caller holding an artifact UUID from another tenant's **private** repository that the artifact exists, that it is held, and the free-text `quarantine_reason` — which carries the malware or CVE finding behind the hold. `GET /api/v1/sbom?repository_id=…` had the same single check in front of a private repository's whole dependency inventory. Both now use the canonical `is_public OR (in_scope AND (is_admin OR grants))` predicate. The reason text is deliberately gated **more** strictly than the existence check: it drops the `is_public` arm, so a caller who can see the artifact only because its repository happens to be public does not get the finding — which is what the pre-existing comment claimed and did not implement. A missing repository row now collapses into the same existence-hiding `404` instead of a message naming the key.
- **Virtual repositories no longer disclose members the caller cannot see** (#3163, #3081, #3177, #3178) — the same `can_access_repo`-as-visibility confusion, in five more places. `GET /api/v1/repositories/{key}/members` let any authenticated user who could reach a virtual repo enumerate the `key`, `name` and `repo_type` of every member including private repositories they hold no grant on, and the parent itself was never `require_visible`d, so a *private* virtual repository's membership was readable by anyone while `GET /{key}` on it correctly 404'd. `GET /api/v1/repositories/{key}/artifacts` on a virtual repo aggregated private members' artifacts — and, in the `group_by=maven_component` view, their Maven GAV coordinates — straight into the response. A virtual repository's `storage_used_bytes` was summed over every member reachable through the membership graph, so a user granted the parent but not its members learned those members' exact byte sizes. And byte resolution itself was open: `proxy_helpers::resolve_virtual_download` took no caller at all, so it structurally could not filter, while the one predicate that did exist gated on `can_access_repo` and then treated `Ok(false)` as *allow*, making a private member carrying no fine-grained rules readable by any authenticated caller. Every one of these now evaluates the same `build_visibility_clause_for` composition the repository listing uses — public ∪ `role_assignments` ∪ fine-grained `permissions`, including the group and project arms — reusing one predicate rather than inventing a fourth. A member is readable through a virtual exactly when its own `GET` would succeed; denials collapse to the `404` a genuine miss produces, so "this virtual is empty" and "this virtual has members you may not see" are no longer distinguishable; the member walk fails **closed** on a database error; and `RepoVisibility::All` renders a literal `true`, so admin callers see byte-identical figures to before.
- **A write-only grant no longer buys read access to a private virtual member** (#3325). **This is a fail-closed change: a principal that has been relying on write-only-grant read access through a virtual repository will lose it.** The member filter added above admits a member on the existence of *any* grant — `permissions_grant_exists_for` tests only `actions <> '{}'`, and the `role_assignments` arm never joins `roles` — while a direct read of that same member requires the `read` action through `PermissionService::check_repository_action`. So a principal holding exactly one rule on a private member, `actions = {write}`, was refused `GET /v2/M/tags/list` and then handed that member's **manifest and blob bytes** through any virtual repository containing it. Because `authorize_virtual_members` is the single choke point for virtual byte resolution, the same gap was reachable through Maven, npm, PyPI, Debian, Go and the ~28 formats that route through `resolve_virtual_download`, not only OCI. The fix layers the canonical action choke-point on top of the existing grant filter — the identical `check_repository_action` call, with the identical arguments, that the direct `/v2` gate and the native-format middleware make — so a member is now readable through a virtual exactly when a direct read of it would succeed, by construction rather than by assertion. The gate sits in the shared `try_authorize_virtual_members` body rather than the `authorize_virtual_members` wrapper, so the aggregating tags/list path added in the same release inherits it; two regression tests pin that placement, one at the unit boundary and one end-to-end through the route. This **supersedes the claim in the #3163 entry above that "a member is readable through a virtual exactly when its own `GET` would succeed"**: that was true of the REST `GET /api/v1/repositories/{key}`, which is also action-blind, but not of the `/v2` read gate, which is not. The shared SQL fragment is deliberately **unchanged** — it is also what repository *listing* evaluates, where "any grant means you may see this repository exists" is the intended contract, and narrowing it would have silently changed enumeration. Public members short-circuit ahead of the action check so the anonymous read baseline (#2329) is preserved, admins short-circuit, and a failed lookup drops the member rather than widening the set.

  **Am I affected?** Only if you have granted a principal a fine-grained rule on a private repository whose `actions` array omits `read` (a publish-only CI identity with `{write}`, or a cleanup identity with `{delete}`), or a role assignment to a custom role carrying no `read` — **and** that principal pulls that repository's content through a virtual repository rather than directly. That combination now returns not-found where it previously returned bytes. Grant those principals `read` on the member if they are also expected to read through the virtual; a direct pull was already refusing them, so nothing that worked on the direct path changes. Read-granted users, admins, public members, token scoping and every listing surface are unaffected. Two residuals are tracked separately and are **not** closed by this change: the virtual `storage_used_bytes` aggregate still sums over any-grant members, and `user_can_access_repo` remains action-blind on the REST content paths (#3331), which is why the virtual path is now *stricter* than the REST direct read of the same member.

- **Attaching a member to a virtual repository is authorized against repository visibility** (#3177). `authorize_virtual_member_mutation` checked the member being attached with a two-line wrapper over the same token-scope predicate, and `create_repository`'s member loop had no member check at all — so a principal could compose a virtual repository out of repositories it cannot read.
- **The PyPI download path is keyed on the normalized project name** (#3183). PyPI carries two name coercions that disagree: `normalize_pep503` drops any character outside `[A-Za-z0-9._-]` without marking a separator, while `PypiHandler::normalize_name` maps every non-alphanumeric to `-`. `serve_file` keyed the PEP 708 dependency-confusion isolation and the per-member curation gate on the first, then passed the **raw** path segment to the upstream fetch and the proxy-cache key. So `GET /pypi/{key}/simple/acme%20sdk/acme_sdk-9.9.9-py3-none-any.whl` evaluated the gate as `acmesdk` — owned by nobody, blocked by nobody — and then resolved `acme-sdk`, the private package the gate exists to protect, serving and caching the attacker's wheel bytes to an anonymous caller. The same divergence made a curation block on a direct Remote repository evadable by spelling. `serve_file` now takes the normalized name and the raw segment is removed from its scope entirely, so it cannot be reintroduced. Reachable in 1.7.1.
- **PyPI project names are validated against PEP 508 on every route that takes one** (#3186, #3198). The recurring normalizer divergence above has been fixed four times by making two coercions agree; this closes it structurally instead. PEP 503's normalization rule — `re.sub(r"[-_.]+", "-", name).lower()` — *presupposes* a valid name, and PEP 508 supplies the precondition (`^([A-Z0-9]|[A-Z0-9][A-Z0-9._-]*[A-Z0-9])$`, case-insensitive). A name like `acme sdk` is therefore outside the rule's input domain and has no correct normalization; two coercions disagreeing about it is the expected outcome, not an implementation slip. A `:project` segment that fails the pattern is now rejected with `404` — matching pypi.org, which 404s `/simple/acme%20sdk/`, `/simple/_leading/` and `/simple/trailing-/` while 301-redirecting names that merely need normalizing — before any gate, database lookup or upstream fetch, on the simple index, the distribution download and the PEP 658 `.metadata` resource. The twine upload route (`POST /pypi/{key}/`) takes its project name from a multipart form field rather than a path segment, so it never reached that validation and ran `normalize_name`, which *coerces*: `acme!sdk`, `acme@sdk`, `acme/sdk`, `acme sdk` and `acme\u{212A}sdk` were all accepted and published into the existing `acme-sdk` project's namespace, `acmé-sdk` landed in a third distinct project, `_leading`/`trailing-` were silently renamed, and `---` or an empty name stored an artifact at a path reachable at no URL while still charging quota. Uploads now validate with the same constructor the read paths use and return `400` on a name that is not a project name.
- **PyPI uploads must carry metadata that agrees with their declared identity** (#3107). `PypiHandler::validate()` held the name/version-vs-metadata consistency check and had no call site, so a wheel or sdist whose embedded `METADATA`/`PKG-INFO` declared a *different* project name or version than the twine `name`/`version` form fields was published unchecked into the declared project's namespace — an archive whose metadata says `evilpkg` accepted and served from `/simple/realclaim/`. The check now runs on the upload path, reading only the metadata entry out of the spooled body through new streaming extractors (ZIP central directory, or a single gzipped-tar entry) under a byte cap, so a 10 GiB upload is not buffered and a hostile archive cannot turn the check into an unbounded read; ZIP and tar parsing run on the blocking pool. Version comparison accepts PEP 440 equivalence, so declaring `1.0` against a `1.0.0` metadata version is not a false mismatch, and a body that does not parse as the declared archive type is passed through — it carries no metadata to contradict. Only a distribution that *does* carry metadata and disagrees with it is rejected, with `400`.
- **Hosted npm publishes reject non-ASCII package names** (#3007). The hosted publish validator checked length and path shape but not character set, so a Unicode homoglyph name such as `nｕmpy` could reach publication while the proxy path already applied a stricter validator — an asymmetry at the primary write boundary. The check runs after URL decoding and before `publish_package`, so a rejected name never reaches repository lookup, payload parsing, metadata persistence or object storage, and it covers the scoped and unscoped routes including percent-encoded input. It is an explicit ASCII test rather than a swap to `is_valid_npm_name`, which also rejects uppercase names that hosted publishing accepts today; that grammar migration is a separate compatibility decision.
- **SSO logins with an empty subject are rejected instead of collapsing onto one account** (#3204). Nothing validated that a SAML assertion's `NameID` was non-empty, and the NameID is the account key — it is what `get_or_create_user` stores in `users.external_id` and resolves accounts by. An empty NameID is therefore not "a user with no identifier", it is the *same lookup key for every such assertion*: the first empty-NameID login provisioned a row with `external_id = ''` and every later one — anyone else at the same IdP — silently resolved to that account. All four XML shapes reached the lookup as `""`: element absent, `<NameID></NameID>`, `<NameID/>`, and whitespace-only. Extraction now rejects an empty or whitespace-only NameID at the parse boundary before any identity is derived, and rejects `transient`-format NameIDs, which SAML 2.0 Core §8.3.8 says a relying party "SHOULD treat as an opaque and temporary value" and which therefore cannot key a durable account row; absent and `unspecified` formats stay accepted, since §8.3.1 leaves their interpretation to implementations and Artifact Keeper's own `NameIDPolicy` requests `unspecified`. `get_or_create_user` also refuses an empty account key outright for **both** SAML and OIDC, closing the equivalent hole on the OIDC side — OIDC Core §2 requires a non-empty `sub`, but an empty one deserialized fine and passed through both the ID-token and userinfo extraction paths.
- **The inline scan-and-block download gate reaches the virtual-member route of every format** (#3220). `enforce_download_gate` is the shared download choke point for quarantine *and* the repository's scan policy (`block_unscanned` / `block_on_fail` / `max_severity`). Every format's direct hosted route reached it; the virtual-member route of ~24 formats did not, because every virtual-member local serve funnels through `proxy_helpers::local_lookup_artifact`, whose only gate was the raw quarantine predicate. A scan-policy block on a hosted repository was therefore bypassable by pulling the same artifact through any virtual repository that listed it as a member — reproduced in one fixture, same repository and same policy, `403` on the direct route and `200` with the bytes on the virtual one. The gate is applied at the shared helper rather than per format, because hand-repeating the check is what produced the gap. Two consequences had to be resolved for that to be safe: virtual resolution treats an `Err` from a member fetch as "this member does not have it, try the next one", so a policy block had to be made **terminal** rather than a silent fallback to another member or upstream; and the quarantine half stays in-process on the row the lookup already selected, so no extra query is added. Two hand-rolled member loops that bypass the resolvers (`helm::download_chart_via_index`, `pypi::serve_file`) are fixed too, as is `maven_local_fetch_storage_fallback`, where a policy-blocked jar still had its whole row-less companion set (`.pom`, `-sources.jar`, `.sha1`, …) served on the direct hosted route as well as through a virtual, because the anchor's verdict is the only verdict those companions have.
- **The download gate is applied on the Generic and Terraform byte paths** (#3143). Two byte-serving paths called the raw quarantine predicate instead of the shared gate, so an artifact every other format would refuse was downloadable through them: `GET /general/{key}/{path}` (and the same handler re-exported at `/api/v1/repositories/{key}/download/*path`, including its `?version=` revision arm) and the Terraform module-archive and provider-binary routes. Generic is gated at the handler, *before* the branch into the presigned-redirect sub-path — a presigned-S3 deployment would otherwise have answered with a `302` straight to the object with no policy check at all. Terraform routes through a new streaming counterpart of the helper the buffered family already used. The gate is deliberately not pushed down into `local_lookup_artifact`'s callers for these paths, because a rejection raised inside a virtual member's fetch closure would be swallowed as a member miss. One scope correction to the issue as filed: the Helm hosted download was *already* gated, one level down through `proxy_helpers::serve_local_artifact`; it gains a regression test pinning that indirect coverage rather than a behaviour change.
- **`block_on_fail` reads the latest scan row per scanner instead of one global newest row** (#3142). This is the residual half of the multi-scanner fail-open closed for `max_severity` in 1.7.1. With more than one engine enabled: scanner A crashes and records `status='failed'`, scanner B then completes clean, the single newest row across all `scan_type`s reads `completed`, and `block_on_fail` never fires — while `classify_scan_state` is any-completed-wins, so `block_unscanned` stays quiet too. An artifact with a crashed scanner was served as fully vetted with `block_on_fail` explicitly enabled. The gate now reads the latest row per `scan_type`, mirroring the `DISTINCT ON` window `recalculate_score` already used, so a clean scan by a different engine cannot mask engine A's crash while a genuine rescan by engine A itself still clears it. The chosen semantics are stated explicitly — **block if any scanner's most recent attempt failed** — because "the most recent attempt" is ambiguous once several engines write independently. `not_applicable` rows are excluded through the existing predicate (covering both the dedicated status and the legacy `failed` + "does not apply" marker); without that, widening from one row to "any scanner" would have started blocking every artifact in every repository where an enabled engine simply does not apply to the format. The old test asserted the bug (`assert!(!post_scan_gates(Some("completed"), true, false).block_on_fail_applies)`) and passed with and without it; it has been replaced.
- **Proxy-cache readers that bypass the metadata sidecar are gated on it** (#3147). The proxy cache's safety model assumes readers consult the `__cache_meta__.json` sidecar — that assumption is what lets a rejected write protect itself by not writing one. Three readers reached the `__content__` key without it. Stale-if-error opened the body first and loaded the sidecar afterwards best-effort, with `unwrap_or(None)` collapsing "absent" and "unreadable" into "serve it anyway with guessed headers"; it served unvalidated bytes with no sidecar vouching for them, served bytes sitting under a negative-cache marker, and handed out entries under an active package age hold. `is_cache_fresh` reported an object fresh when its length disagreed with its sidecar, and the caller turns `true` into a presigned `302` that nothing downstream re-verifies. The write side was closed separately (below), but objects still reach the live key by other routes — the >5 GiB restream carve-out, a crash between the body write and the sidecar write, and any out-of-band write to the bucket — so the read side has to hold independently.
- **Proxy-cache streamed writes are published through a private staging key** (#3152). `CachePersister::tee_stream` wrote a streamed upstream body directly to the live cache key and classified it afterwards; on reject it issued a best-effort `delete` with no version pinning and failures swallowed at `debug!`. Under concurrent cold-cache requests for the same hot artifact a rejected writer's bytes land on the live key, and if the cleanup delete then fails silently a truncated or zero-byte object becomes the permanent current version — observed twice in production. The streaming cache-hit path does no integrity verification against the sidecar, so an entry corrupted this way never self-heals. Streamed writes now target `proxy-cache-staging/<uuid>` under a dedicated prefix and the live key is only ever touched by `StorageBackend::copy` once the write classifies as a commit and any digest validates; a rejected or failed write discards the staging object and never touches the live key. The dedicated prefix (rather than a suffix on the content key) keeps staging objects out of every `proxy-cache/`-scoped listing, makes them independently lifecycle-sweepable, and keeps the content-key length budget from having to grow. `FilesystemBackend::copy` was a bare `fs::copy` onto the destination — non-atomic, and now on the hot path — and is rewritten to copy-to-temp, fsync and rename.
- **Digests already on hand are enforced against cached proxy bytes, and the streamed cache write is bounded** (#2929, #2928). The Remote cargo `.crate` download called the wrapper that hardcodes `expected_checksum: None`, while the *same handler* serves the sparse-index entry carrying the `cksum` cargo itself verifies against — so the digest was decorative. It now resolves the index `cksum` for `{name}/{version}` and routes the download through the digest-gated sibling, which on a split-host registry catches a misbehaving or compromised download host while the index is intact, and catches ordinary corruption or a clean-EOF truncation on either. A body that fails the gate is still streamed to the client, which verifies independently; it is only kept out of the cache. The NuGet missing-file repair refetched from upstream and wrote the bytes back under the artifact row's own storage key without ever comparing them to that row's `checksum_sha256` — a column the `SELECT` already fetched and nothing read — so an admin releasing an artifact from quarantine was approving a hash never enforced against what clients then received; the repair now verifies before the write-back and fails on a mismatch rather than persisting and serving it. A digest is enforced only when it is a bare lowercase 64-hex SHA-256; a `sha256:`-prefixed or otherwise non-canonical value reads as "no digest available", because a value the comparison can never match would reject every entry, cache nothing, and re-pull upstream on every request. Separately, `exceeds_single_object_quota` read `quota_bytes` off the `Repository` that every handler-driven proxy fetch synthesizes, which hardcodes `None` — so the guard was structurally unreachable on every path that serves proxy traffic and a configured quota did not bound a streamed cache write.
- **The bundled scanner CLIs no longer link a vulnerable go-git** (#3307). **The vulnerable dependency is in the third-party scanner binaries Artifact Keeper ships, not in Artifact Keeper's own code** — `github.com/go-git/go-git/v5` v5.19.1 (GHSA-hc8v-wwc9-vgxm, CVE-2026-71556, HIGH) is linked into `grype` in the backend image and into `trivy` inside the scanner-adapter image. Nothing regressed to cause it: neither image changed, and the advisory was published against a dependency both already shipped — a clean scan is a statement about a moment, not a property of an image. The operator-visible effect was on the pipeline rather than on a running instance: `Docker Publish` on `main` began failing `Security Scan` on both Trivy gates, which hard-gate every multi-arch manifest, and would have stalled this release's cut the way #3039 stalled 1.7.0-rc.1. grype moves v0.116.0 to **v0.117.0**, the first release pinning go-git v5.19.2 — v0.116.1 is **not** the fix and still carries v5.19.1, measured against the gate's exact configuration rather than inferred from the version number. No fixed upstream trivy release exists at all, so the scanner-adapter re-pins to the in-house `artifact-keeper/trivy` 0.73.0-r2, which adds a go-git dependency override on top of the existing `oras-go` one; the shipped binary's own Go build info records `v5.19.2`, so the override is proven linked rather than asserted. `.trivyignore`'s own rule would have permitted a suppression here, and that is deliberately not what was done — **suppress when there is no fix; override when one exists and only the packaging is missing.** The bump changes nothing the backend parses: the same fixture through v0.116.0 and v0.117.0 yields identical matches with `relatedVulnerabilities` still top-level on the match, the trap #1375 closed. Nine `.trivyignore` tokens are now provably dead — the pinned digest scanned with no ignorefile finds none of them — and are marked **RETIRED** rather than deleted, because the preflight's superset check would fail while 1.5.x and 1.6.x still list them; removal is #3309.

### Added

- **Migration jobs can rename repositories on import** (#3035): `MigrationConfig.repo_mappings` maps a source repository key to the key it lands under (e.g. `network-team-python` -> `network-pypi`); unmapped repositories keep their source name, and a renamed member of a migrated virtual repository keeps its membership. Targets are validated like a hand-created repository key and two sources may not share one destination; either fails the job rather than migrating under the original name. A target that already exists is reused and the source's artifacts are merged into it, logged as a warning naming both repositories.
- **Curation can verify publisher attestations** (#2955). PyPI and npm curation ingestion now runs on demand, so publisher-trust rules can evaluate packages the proxy has seen. PyPI PEP 740 attestations are verified in process with sigstore-rs, and a package is marked verified only after the DSSE signature, Fulcio chain, SCT, Rekor entry and inclusion proof, integrated time, OIDC issuer, certificate identity, subject digest and publisher owner all match; the result is persisted on the curation package. sigstore-rs 0.14 does not verify the Rekor inclusion proof inside `verify_digest`, so the verifier calls the crate's `InclusionProof::verify` primitives directly. **npm attestations are ingested but not verified**, and fail safe rather than reporting trust, because their subjects use SHA-512 — an upstream limitation tracked as sigstore-rs#596.
- **Galaxy v3 import status is served at `/ansible/{repo}/api/v3/imports/collections/{id}/`** (#3282), the route the collection-upload response now points `task` at. See the entry under Fixed for what it reports and why the document is synthetic; the route is repo-scoped and sits behind `repo_visibility_middleware`, so a private repository's import tasks need a credential that can read the repository.
- **Dedicated timeout knobs for the S3 client**: `S3_BULK_TIMEOUT_SECS` (default 1800) and `S3_CONTROL_TIMEOUT_SECS` (default 30), both accepting `0` to disable (#3180).
- **`TRIVY_ADAPTER_REGISTRY_URL`** — a dedicated override for the registry URL handed to the scanner-adapter, so operators can point the adapter path somewhere without re-pointing grype (#3169).
- **Backups can record and tolerate unreadable artifacts**: `BackupManifest` gains `artifacts_unreadable` alongside `artifact_count` so an existing archive can be audited without re-running the backup, and `allow_partial_artifacts` in the backup metadata makes a partial backup an explicit operator choice (#3170).

### Changed

- **`MigrationConfig.dry_run` no longer writes** (#3035): it gated only the artifact upload, so a preview created every destination repository and wrote `artifacts` plus OCI index rows pointing at bytes it never uploaded. A dry run now logs the destination plan (create or reuse, under which key), skips provisioning and virtual member correlation, and enumerates the source without downloading or writing, so it still reports the artifact count and byte total it would transfer.
- **BREAKING API SEMANTICS: `PUT /api/v1/repositories/{key}/members` (virtual repository members) is a full-set replace** (#2795, ratified in #2899): **this changed in 1.7.0, not in this release** — #2795 shipped in v1.7.0 under the one-line entry "Virtual-repo total aggregation is corrected and member edits are allowed", and the ratification (#2899) that put the semantics in the OpenAPI operation description and pinned them with a regression test shipped in v1.7.1. It is restated here at full weight because neither release's notes said what the change does to a client, and **anyone already on 1.7.0 or later is already exposed**. As of #2795 the request body is the *complete* desired member list, not a partial update. Any existing member whose key is absent from the body is **removed**; members present in the body are inserted if missing, or have their priority updated if already present; an empty `members` array removes every member. Previously the endpoint only reordered members that already existed and returned 404 for a member that was not already present, which made it impossible to add or remove a member when editing a virtual repository after creation. **API clients that send a partial member list in order to reorder priorities will now delete the members they omit** and must be updated to always send the complete member list. The web UI (`virtual-members-panel.tsx` move-up/move-down) and the CLI (`members reorder`, documented as a bulk full-set operation) already send the full list and are unaffected. The behavior is stated in the OpenAPI operation description and pinned by a regression test as of v1.7.1.
- **The blob half of the OCI proxy scan gate honours `scan_on_proxy` and the manifest gate's verdict-freshness rule** (#3105, #3259). The blob re-block — which stops a blocked image being reconstructed one layer at a time — ran unconditionally, while the manifest half short-circuits when proxy scanning is off. The two halves therefore disagreed: after an operator set `scan_on_proxy: false` to unblock a previously-flagged image the manifest served again but every blob `GET` stayed `403 DENIED` forever, with no recourse short of deleting rows from `proxy_scan_results` by hand. The flag is now evaluated per **repository that owns the refs**, not per request: `Remote` honours it, `Virtual` ORs its own flag across the member set (so a virtual with proxy scanning off cannot become a bypass around a stricter member), and `Local`/`Staging` always re-block — because `scan_on_proxy` says nothing about hosted content and hosted repositories normally have no `scan_configs` row at all, so an absent row must not read as "disabled". An earlier revision that applied the flag as one whole-request boolean would have switched the blob re-block **off by default for every hosted OCI repository on the instance**, which is the only blob-level scan enforcement those repositories have. Separately, the blob half had no verdict-freshness rule at all — a bare `EXISTS` on `verdict = 'vulnerable'` with no TTL and no scanner-version predicate — so a verdict the manifest gate would have discarded and re-scanned still blocked that image's layers indefinitely, while on a fail-open repository the manifest itself served. Both blob query sites now pass the verdict's provenance to the manifest path's own predicate, so the two halves cannot drift. A fresh verdict still blocks, and a stale one does not simply serve: hosted repositories keep blocking unconditionally (nothing will ever re-scan them through the proxy-only manifest gate, so a TTL release there would be permanent rather than a deferral), fail-closed proxy repositories keep blocking (their manifest gate re-scans inline before serving a byte), and only a fail-open proxy repository's stale verdicts are released — into exactly the state its own manifest gate is already in. Any `scan_configs` read error keeps the block.
- **`rollup_scan_status` treats `not_applicable` as neutral** (#3144). The rollup matched only `running`/`pending`/`completed`/`failed`, so `not_applicable` fell into the unknown bucket that blocks both terminal collapses — and the default shape for most formats, a completed grype scan plus an OpenSCAP result that declined, rendered as **`partial`** in the Docker tag listing. `completed` + `not_applicable` now collapses to `completed` and `failed` + `not_applicable` to `failed` (a declined scanner must not soften a hard failure), while a result set that is *only* `not_applicable` reports `not_applicable` rather than `completed`, because when every scanner declined nothing was scanned. Mixed `completed`+`failed` still reports `partial`, `running`/`pending` still win, and unrecognized statuses still block collapse pessimistically. No gate reads this rollup — the download and quarantine gates read `scan_results` directly and already handle `not_applicable` — so this cannot un-block anything.
- **`scan_configs.block_on_policy_violation` is documented as inert** (#3144). The column has model, CRUD and DTO plumbing and **zero enforcement readers**; every gate that blocks today reads the separate `scan_policies` table. The `enforce_download_gate` doc comment claimed `max_severity` *was* `block_on_policy_violation`, which is false. The field documentation on the model, the upsert request and the public OpenAPI response schema now state plainly that the toggle is persisted and consulted by no gate. It is deliberately **not** wired up here: as a precondition it would silently disable scan-policy blocking on every repository that never touched the toggle (the column defaults to `false`), and as an additional blocker it needs a severity criterion that has the same problem. Disposition of both columns is tracked in #3246.
- **Updating a permission that collides now returns `409` instead of a `500` leaking raw Postgres error text** (#2826).

### Fixed

- **`ansible-galaxy collection publish` completes instead of crashing** (#3282). #3137 fixed discovery and authentication, which unblocked `install`; `publish` still died with `ERROR! Unexpected Exception, this is probably a bug: 'task'` immediately after a *successful* upload. The Galaxy v3 publish contract is two calls, not one: `GalaxyAPI.publish_collection` (`lib/ansible/galaxy/api.py`) ends with a hard index into the upload response — `return resp['task']` on `stable-2.17` through `stable-2.19`, `return urljoin(self.api_server, resp['task'])` on `stable-2.20` and `devel` — and Artifact Keeper's `202` carried no `task` key. `--no-wait` could not work around it, because `wait` is consulted one frame upstream in `collection/__init__.py::publish_collection`, after the dereference has already raised. The upload response now carries `task`, and the import-status route it points at is served alongside it. **Both halves had to land together**: `wait_import_task` treats a `404` on the task URL as "the import has not started yet" and retries, and the CLI defaults are `--import-timeout` `0` (never time out) with `--no-wait` `default=True` (wait by default), so emitting the field without the route would have traded the crash for an indefinite hang. The emitted value is a root-absolute path ending in `/{task_id}/`, the shape ansible-core documents for v3, which is the one spelling both client generations resolve correctly: `stable-2.17` through `stable-2.19` reduce it to its last non-empty path segment and rebuild the URL from `api_server`, while `stable-2.20` and `devel` `urljoin` it against the origin and poll it verbatim. **The import document is synthetic, and deliberately so**: Artifact Keeper has no asynchronous import pipeline — `upload_collection` validates, stores and records the collection inline — so by the time the `202` is written the import is already finished, and the route reports a completed import derived from the stored artifact rather than progress from a job queue. It reports no intermediate states because there are none. A publish that fails validation is unaffected and never reaches this route: it is returned as a `4xx` from the upload itself, which `_call_galaxy` raises on, so a rejected publish surfaces as a client-visible error rather than a task the client would poll forever. The route is repo-scoped — the lookup is keyed on `(task id, repository id)` — so a task id minted in one repository is a plain `404` in another and is not a cross-repo existence oracle, and it sits behind `repo_visibility_middleware` like every other `/ansible/{repo}/...` route, so a private repository's import tasks stay unreachable without a credential that can read the repository.
- **OpenSCAP `critical` findings are no longer downgraded to `info`** (#3294). `openscap_scanner::convert_findings` classified severities with its own private match — `high | medium | moderate | low`, and `_ => Severity::Info` for everything else. It had no `critical` arm, so content reporting a `critical` rule failure had its **most** severe result filed as its **least** severe one, which is invisible to every gate that reads severity: `Info` has a penalty weight of `0`, appears in no `policy_service` severity array, and has no `info_count` column in any gate's projection. `critical` is not in XCCDF 1.2's `unknown | info | low | medium | high` vocabulary (NISTIR 7275r4 §6.4.4.2), so only non-standard content reaches that arm and the volume is near zero — but when it does fire it is a fail-open, and it now classifies as `Severity::Critical`. Alongside it, `Negligible` — the Debian and Ubuntu security trackers' explicit "triaged, not worth fixing" grade — becomes a **recognised** token instead of a parse miss. On the Grype and native Trivy paths it lands in the bucket it always did (`Info`), so nothing about it changes behaviour; the point is that it now gets there deliberately, and stays there when the ungraded bucket moves. Note that **Harbor-sourced reports are not affected by that arm at all**: `image_scanner::normalize_harbor_severity` rewrites Harbor's `Negligible` to `Low` *before* the token reaches the classifier, and that mapping is unchanged here — deliberately, because `Low` violates a `max_severity = 'low'` policy where `Info` violates nothing, so reconciling the two would have relaxed a gate inside a patch. Artifact Keeper therefore still has two `Negligible` mappings, Grype's at `Info` and Harbor's at `Low`; reconciling them is left to #3243. Both arrive through one shared classifier, `Severity::from_scanner_token`, which replaces the three separate per-adapter fallbacks that let them drift apart in the first place. **No other token's classification changes on any adapter**: Grype and Trivy conversions are unchanged token for token, and on OpenSCAP only `critical` moves. In particular an *ungraded* finding — Trivy's `UNKNOWN`, XCCDF's `unknown`, an empty severity — still classifies at `Info`, which is a fail-open in its own right and a much larger one; closing it moves four gates and can drop a repository's security grade by several bands on the next scan, so it is deliberately not in this patch and is tracked as #3306 for 1.8.0. Refs #3243, which stays open for its later stages.
- **`ansible-galaxy` can reach an Ansible repository again** (#3137). Two independent breaks sat ahead of every collection endpoint, so neither `install` nor `publish` got past API version negotiation. First, the discovery route was registered only as `/ansible/{repo}/api/`; the CLI requests it without the trailing slash, because ansible-core's `g_connect` builds the URL as `_urljoin(n_url, '/api/')` and `_urljoin` strips `/` from every component (`lib/ansible/galaxy/api.py`) — and axum matches `/api` and `/api/` as distinct routes, so negotiation 404'd. Both spellings are now registered, which also fixes the configuration where the server URL is written out as `.../api` and ansible-core skips its fallback probe entirely. Second, the CLI authenticates Galaxy calls with `Authorization: Token <api_key>` (`GalaxyToken.token_type = 'Token'`; only the Keycloak/Automation-Hub variant uses `Bearer`), a scheme the Authorization-header extractor did not recognise, so the value was classified as malformed and every authenticated call — including the discovery probe, which carries the header whenever a token is configured — returned `401`. The `Token` scheme is now accepted as Bearer-equivalent: it resolves to the same internal variant and runs through the identical JWT / API-token validation chain, so it grants no capability that `Bearer` did not already grant, and an unresolvable or empty credential still fails closed with `401`. **`ansible-galaxy collection publish` is not yet fixed by this change** — the upload stores the collection but the response omits the `task` field the Galaxy v3 publish contract requires, so the client raises `KeyError: 'task'`; that needs a new import-status route and is tracked in #3282.
- **The dashboard `total_storage_bytes` now includes OCI blob and proxy-cached bytes** (#3134). `GET /api/v1/admin/stats` derived the figure from a raw aggregate over `artifacts` only, but OCI layer/config blobs live in `oci_blobs` — only manifests land in `artifacts` — so a docker repository contributed a few KiB of manifests while holding GiBs of layers, and legacy backfilled `proxy-cache/%` rows were double-counted against `proxy_storage_bytes`. The total is now summed from `repository_usage_ledger` (`hosted_bytes + proxy_bytes + oci_bytes`), the same trigger-maintained source the per-repository `storage_used_bytes` column and quota admission already read, so the dashboard total equals the sum of the per-repo figures by construction. **API consumers note:** `total_storage_bytes` now includes proxy-cached bytes, so `proxy_storage_bytes` is a *breakdown* of the total, not a disjoint bucket — clients that added the two fields must stop doing so. The daily `storage_metrics` snapshot (`capture_daily_snapshot`) now sums the same ledger, so the storage-trend charts stay consistent with the dashboard total instead of lagging it by OCI blob and proxy-cached bytes. The figures are **logical** bytes — a blob is counted once per repository that holds it, and a blob cross-repo-mounted into N repositories counts N times — chosen so the tile agrees with the per-repository column beside it and with what quota charges; physical, digest-deduplicated bytes remain the storage-GC footprint report's concern. `total_artifacts` still counts `artifacts` rows only, so for OCI it is manifests: blobs are content-addressed objects, not artifacts. Both accounting decisions are now stated on the `SystemStats`, `StorageSnapshot` and `GrowthSummary` schemas (#3249), and the daily snapshot's accounting is pinned by its own regression guard. **Historical `storage_metrics` rows are deliberately not backfilled**: the ledger and its source tables hold no history, so there is nothing from which a past date's OCI or proxy bytes could be recomputed, and any backfill would overwrite real observations with fabricated ones. The one-day upward step a growth series shows where the corrected accounting first lands is a measurement correction, not real growth.
- **Remote and Virtual cargo `.crate` downloads forward the upstream `Content-Encoding`** (#2920). Both download arms hand-roll their response and read `content_length` off the streaming fetch result while dropping `content_encoding`, so a content-coded `.crate` served through an intermediary was written to disk still compressed and failed cargo's checksum with no indication why. The proxy's HTTP client pins `Accept-Encoding: identity` and disables transparent decoding, so the bytes genuinely arrive coded and the header is the only thing that was missing. `Content-Length` and `Content-Encoding` now come from the same source on every serve path, including a cache hit, where the sidecar replays the recorded coding. A response with no upstream coding is unchanged and gains no header.
- **NuGet: the remote-repository cache-repair download now resolves the upstream `PackageBaseAddress` and streams**: when a remote NuGet repository still holds an `artifacts` row for a package whose cached blob has gone missing from storage, `flatcontainer_download` re-pulled the `.nupkg` by appending `v3/flatcontainer/{id}/{version}/{file}` to the configured `upstream_url`, bypassing the service-index discovery every other V3 call site performs (#2917, under the #2775 remote pull-through umbrella). NuGet V3 has no fixed layout — nuget.org serves package content from `https://api.nuget.org/v3-flatcontainer/`, and a configured upstream is normally the `.../v3/index.json` document itself — so that URL 404s and the repair was broken against a real feed regardless of package size. The same re-pull was also buffered under the 8 MiB metadata ceiling (#2181), which does not truncate but returns `502`, so a package legitimately larger than the ceiling (`Microsoft.CodeAnalysis.*`, `Microsoft.ML.*`, `SkiaSharp.NativeAssets.*`, essentially every native-runtime package) failed the repair outright. The repair now takes the same route as the primary cache-miss arm: service-index discovery plus a streamed body teed into the proxy cache, the fix already applied to PyPI wheel recovery in #2192 / #1608 Phase 4c. The proxy-cache key is unchanged, so entries cached by the previous code remain hits, and a blob that is present still streams straight from storage (a warm remote hit no longer buffers the whole `.nupkg` in memory either). Note one deliberate limitation: the repair warms the proxy cache but does **not** re-materialize the blob at the `artifacts` row's own `storage_key`, so the row stays dangling and every later request re-enters the repair branch (served from the proxy cache, with no upstream traffic). Downloads are unaffected, but subsystems that read `storage_key` directly — scanning, replication, promotion, signing, backup — continue to see a missing blob. For a remote whose `upstream_url` is a bare base rather than the service-index document, including an AK-to-AK edge/hub pair, the old concatenation happened to produce the correct URL and did re-materialize the blob, so those deployments lose that incidental healing. Re-materialization is tracked in #2921.
- **npm, PyPI, OCI, generic-repository and cargo sparse-index downloads forward the upstream `Content-Encoding`** (#3149). The same defect class as #2920, in the response builders that #2922 did not reach. `build_tarball_response_stream` (npm) took no `content_encoding` parameter at all, and `oci_v2.rs` and `repositories.rs` contained no reference to the field, so every proxied serve on those paths read the *coded* `Content-Length` off the streaming fetch result and dropped the coding one line later — leaving clients to write bytes they could not inflate. The cargo sparse index had a second cause: it fetched through a helper whose 2-tuple return type structurally discarded the coding, and then pinned the coded bytes into an in-process cache that has no room for one, so every later hit repeated the error. `Content-Length` and `Content-Encoding` now come from the same source on all of these paths; the OCI virtual-blob `HEAD` builds the GET response and drops the body so its headers cannot drift from GET again; and only uncoded sparse-index bodies are memoized. A response with no upstream coding is unchanged and gains no header. Each fixed arm ships its own regression test.
- **goproxy, RubyGems and Hex declare the upstream `Content-Encoding` on verbatim forwards** (#3260). The same class again, in nine sites across three formats that copy upstream bytes through untouched: the goproxy Remote `.info` and `.mod` arms and its Virtual arm, the RubyGems Virtual `gem_info` and `quick_spec` arms, and the Hex Remote `packages`/`names`/`versions` arms plus Virtual `packages`. Nothing on these paths decodes — the shared HTTP client disables gzip, brotli, deflate and zstd and advertises `Accept-Encoding: identity` — so the coded bytes really are what the client receives, labelled as if they were plain. Per RFC 9110 §8.4 `Content-Encoding` describes the coding of the representation as transferred and per §8.6 `Content-Length` describes the coded bytes actually sent, so the pair was only ever correct once the coding was declared alongside it. For goproxy the payloads are `.info`/`.mod` module documents, so a silently compressed file on the `go` client's disk is a correctness problem rather than a header nit, and the Hex Remote arms are the "signed registry bytes pass through as-is" contract, which is the same verbatim property that makes the coding load-bearing. Callers that *parse* the body and serve a rebuilt document — goproxy's `@v/list` filter and its `@latest` JSON path, the existence probe, and the virtual-metadata merge closures — still drop the coding, now explicitly and with a doc comment saying so. The seams were widened in place so every caller breaks at compile time and must state whether it forwards verbatim or parses, and all nine sites share one response builder so their shape cannot drift per format. Two adjacent residuals found while verifying are filed rather than folded in: goproxy's `@latest` and `@v/list` arms mishandle a *coded* upstream body outright (`@latest` 404s because the JSON parse rejects the coded bytes, `@v/list` serves them mangled through `from_utf8_lossy`), which needs decoding of an upstream-controlled body and its own size bounds (#3280); and the virtual metadata resolver still drops the member's `Content-Type`, so Hex serves a signed protobuf as `application/json` (#3281).
- **Maven `download_root` verbatim forwards declare the upstream `Content-Encoding`** (#3211), the same defect in the Remote and Virtual arms of the Maven repository-root path, which forwarded the upstream body untouched while fetching through a primitive that dropped the coding. The primitive is widened in place and every caller classified, so the parse-and-rewrite callers now drop the coding explicitly rather than by omission.
- **The buffered scanned-serve paths forward the upstream `Content-Encoding`** (#3184). #3149 fixed the streaming builders; the two buffered ones were left behind, so on any repository with `scan_on_proxy` enabled the original symptom was still live end to end — `pip install` and `npm install` received a coded body labelled as identity, with `Content-Length` set from the coded byte count. The coding never reached those builders because it was discarded one level up in the primitive's own signature. Widening that primitive in place surfaced a third live instance: the NuGet flat-container buffered arm, which carries no URLs and therefore forwards its body untouched, was dropping the coding for the same reason. The four NuGet arms that rewrite URLs correctly keep dropping it, because the bytes they emit are not the bytes that arrived.
- **PyPI PEP 658 metadata forwards the upstream coding, and a coded wheel no longer reports "metadata not available"** (#3193). `serve_remote_metadata` carried two independent content-coding bugs needing opposite remediations, on the ordinary remote-PyPI metadata path rather than a scan-gated one. The `.metadata` arm forwards the upstream body verbatim while pinning `text/plain; charset=utf-8`, and the coding was not in the fetch helper's return type at all — the third slot of its triple was the `Link` header — so a coded `.metadata` from a gzipping CDN reached `pip` as compressed bytes labelled as plain text. On an upstream `404` the handler falls back to extracting `METADATA` from the wheel, but the extractor opens the bytes as a zip and a coded wheel is not a parseable zip, so a present and perfectly valid wheel answered `404 Metadata not available`; the wheel fetch now goes through the capped variant that reports the coding and the bytes are decoded before the parser sees them, with decode and parse running under one ingest-extraction permit so both halves of the CPU work on upstream-controlled bytes stay admission-controlled together.
- **PEP 658 metadata resolves through a virtual repository's members** (#3175). #3077 fixed metadata routing for remote repositories; virtual repositories still queried metadata against the virtual itself, which owns no artifacts, so every request 404'd. Metadata now resolves through authorized members in priority order, preserving the remote `.metadata` fetch and the wheel-extraction fallback, and applying the same priority-aware dependency-confusion isolation the simple index and artifact downloads use — remote platform-specific wheels are allowed for a locally owned version when they cover a platform not available locally, while remote-only versions and competing builds stay blocked.
- **A `HEAD` is never answered with a method-scoped presigned redirect** (#3181, #3209). A presigned URL is signed for one HTTP method — the method is the first line of the SigV4 canonical request, and GCS's V4 signing is the same — so a URL signed for `GET` cannot validate a `HEAD`. The routers register `get(..)`/`put(..)` only, so axum answers a `HEAD` by running the GET handler, which short-circuited into a `302` pointing at a GET-signed URL; the client re-issued its `HEAD` against that signature and the object store returned `403`. Maven never HEADs an artifact, so this stayed invisible — Gradle HEADs before every download, so **every Gradle build against a repository with `S3_REDIRECT_DOWNLOADS` enabled failed**. The audit of the single 302 builder found three more reachable sites beyond the Maven one: the generic download handler (also re-exported at `/general/{key}/*path`), the shared local-serve helper behind PyPI `serve_file`, and the virtual-repo member cache redirect used by rpm, RubyGems, puppet, hex, huggingface, cran and ansible. Each now declines to presign on a `HEAD` and falls through to the normal serve path, which answers with the artifact's real `Content-Type` and `Content-Length`; a `HEAD` response body is dropped without being polled, so this costs a metadata round trip rather than a body transfer. One dormant helper with no production caller is guarded anyway, because it is the documented drop-in for the unguarded primitive and an unguarded landmine is how this class propagates.
- **Digest-addressed proxied OCI manifests stay immutable in the proxy cache** (#3206). Docker Remote repositories did not behave as pull-through caches: layers pulled through the proxy were re-fetched from upstream on every later pull and the storage volume never grew past one TTL window. Before #2312 every handler-driven proxy fetch synthesized its temporary repository with `format: Generic`, so the cache classifier never reached the OCI rules and content-addressed `v2/<image>/blobs/<digest>` paths fell to the mutable 5-minute default TTL instead of the immutable one; expired entries fail revalidation on the blob path and refill from upstream on every pull. That was fixed for the streaming blob arm in 1.6.0 — reproducing the report against 1.5.1 shows blob sidecars carrying a 300-second expiry and a repeat pull re-downloading every blob, while against current builds a warm pull performs zero upstream fetches and a fully pulled image stays retrievable with the upstream registry down. The same classification gap was still live in the **buffered manifest arm** (the direct Remote manifest `GET`/`HEAD` fallback, also reached by the remote HEAD-blob probe, and the virtual-member manifest fallback), and is fixed here, with pull-through regression guards added so the classification cannot silently regress again.
- **npm packument responses carry `ETag`, `Cache-Control` and answer `304`** (#3052). Every packument was an unconditional `200` with no cache headers and no conditional handling, so `npm`, `yarn` and `pnpm` re-downloaded the whole document on every metadata request even when nothing had changed — and packuments are the largest metadata Artifact Keeper serves, tens of megabytes for popular packages. This is the gap #2780 closed for the PyPI simple index. The tag is computed once from the identity body and stored on both the identity and gzip cache entries, so one packument has one tag regardless of transfer coding; hashing the stored bytes instead would hand gzip and identity clients different tags for the same document and break revalidation whenever a repository's cache eligibility changed. Responses now also carry `Vary: Accept, Accept-Encoding`, required once the response is cacheable at all, since `Accept` selects the full versus abbreviated document and a shared cache must not serve one for the other. The hosted, age-gated and cache-disabled paths get the same headers through their own builder. The conditional check deliberately stays out of the internal packument accessor, because `dist_tags_get` and the cache writer call it and parse the body — a `304` there would resolve dist-tags to `{}` and stop the cache storing anything.
- **Remote npm scoped packuments are cached and catalogued under the decoded `@scope/name`** (#3297). Scoped packages proxied through a Remote npm repository were catalogued — and shown in the web UI's Artifacts tab — as `@scope%2Fname`. The packument fetch sites build the `%2F`-encoded name because private upstreams such as Nexus, Verdaccio and GitHub Packages require the encoded scope separator on the URL, then passed that one string to a proxy helper whose single `path` argument is reused as **both** the upstream fetch path and the local cache key. The encoded form was therefore persisted verbatim as `proxy_cache_artifacts.path` and rendered as-is by `build_catalog_artifact_response`, whose `rsplit('/')` finds no separator and displays the whole encoded string. The npm-protocol responses served to clients were always correct; the defect was confined to the cache key and the catalog row derived from it. All five packument sites now use the fetch/cache-key split primitive the PyPI handler already uses, so the encoded name stays on the wire and the canonical decoded name is the key. **Five, not the four the report identified** — the fifth is the age gate's `npm_publish_time_for_version` probe, which left unfixed would have kept a second copy of every scoped packument under the encoded key and doubled upstream fetches on the age-gated download path. This is not a gate bypass: npm scope policy, on-demand curation entries, quarantine and the age gate all key on the decoded name the router supplies, and the cache TTL classifier deliberately treats `@scope/<pkg>`, `@scope%2f<pkg>` and bare names identically, so the two spellings never diverged in front of a policy check. No migration is needed — entries cached under the old key are packuments, classified mutable with a TTL, so they expire and their stale catalog rows disappear with them.
- **An untouched npm scope-policy form no longer blocks creating a Virtual npm repository** (#3299). Creating an npm Virtual repository — or saving an untouched settings form on an existing one — failed with `npm scope policy is only configurable on remote (proxy) repositories`. The gate is *presence*-based, and every released web UI attaches the scope-policy fields to every npm create whenever that section renders, so an untouched form serialises as `{"npm_allowed_scopes": [], "npm_allowed_name_patterns": [], "npm_allow_unscoped": false}` — where `[]` deserializes to `Some(vec![])` and `false` to `Some(false)`, both of which count as "a policy was supplied". A payload that configures nothing was therefore rejected outright. Fields that are all inactive (empty allow-lists, `allow_unscoped` absent or `false`) are now treated as absent on any target the policy is not configurable on — Virtual, Local, Staging, or a non-npm format — and **nothing is persisted**, so no dead configuration rows are written. An *active* configuration (a scope or pattern entry, or an explicit `allow_unscoped: true`) on such a target is still rejected up front with the same message, preserving the dead-state guard from #2327, and npm Remote repositories are unchanged: empty lists still clear a stored policy on update. The dedicated `PUT /api/v1/repositories/{key}/npm-scope-policy` endpoint is deliberately untouched, so an explicit policy write against a non-Remote target remains an error. The web half of this is fixed on the web repository's main branch but is in no released web version, so the backend fix is what makes every deployed UI work again.
- **`/api/v1/formats` is derived from the compiled-in handler registry** (#3157). `format_handlers` was seeded exactly once, with 13 rows, and every format added since extended only the `repository_format` enum — so the endpoint reported 13 of the 37 handlers the binary actually provides, and `pub`, `composer`, `swift`, `hex`, `sbt`, `cocoapods`, `cran`, `terraform`, `alpine`, `protobuf`, `incus` and the rest were absent. The second half matters as much: the rejection check only rejects an explicit `false`, so a format with no row could never be **disabled** — roughly 24 handlers were silently exempt from the enable/disable surface the other 13 had, and `POST /api/v1/formats/pub/disable` returned `404`. This is not upgrade-only; migrations behave identically on a fresh install. The listing is now derived from the enum and `format_handlers` is treated as an enablement overlay, rather than adding a second seed migration — three separate hand-written format lists existed in the codebase and all three had already diverged, so a fourth would have been a fourth thing to forget. Aliases collapse exactly as handler resolution does (`gradle`→`maven`, every OCI alias→`oci`, `lxc`→`incus`), which is the granularity `format_handlers` is keyed by and the granularity the enablement gate reads.
- **Publishing a Flutter package to a hosted Pub repository works** (#3058). `PubSpec` modelled dependency values as strings, but Pub allows a dependency value to be either a version string or a mapping — for SDK, git, path and hosted dependencies. Every Flutter package declares `flutter: {sdk: flutter}`, so `serde_yaml` rejected the pubspec outright with `dependencies.flutter: invalid type: map, expected a string`, and the extraction helper turned that into the `400` publishers saw on the multipart upload step while step 1 of the flow succeeded. Both dependency maps are now typed as `serde_json::Value` so every dependency form round-trips, mirroring how the cargo handler already solves the identical problem. Remote and virtual Pub repositories are unaffected: proxied metadata is passed through as raw JSON with only `archive_url` rewritten.
- **OCI pushes to classic Helm repositories are rejected** (#3150). An OCI Distribution request against a repository whose format is not backed by the OCI handler now returns `400 UNSUPPORTED` before any write; `helm_oci` and the other OCI-backed formats keep working. Repositories already affected produce a valid `index.yaml` again, because legacy OCI manifest rows are excluded from classic Helm index generation rather than migrated destructively.
- **Docker lifecycle retention counts versions per image, and ages tags by last push** (#2998, #3026). Docker manifest artifacts encode `image:tag` in `artifacts.name`, so the `max_versions` query treated every tag as its own one-item partition and never pruned anything; valid tag-shaped references are now partitioned by image name, preserving registry ports, while `image:sha256:<digest>` references stay distinct full-name partitions so multi-architecture child manifests — which can be stored by digest with no `oci_tags` row — are not grouped together. Separately, `max_age_days` aged a tag by the path-keyed artifact row's original `created_at`, so a moving tag such as `latest` looked 91 days old even though it had just been re-pushed to a fresh digest; live OCI tag-backed manifests are now aged by `oci_tags.updated_at`, which is refreshed on every successful manifest `PUT`, with `artifacts.created_at` retained as the fallback for non-OCI artifacts and unmatched legacy rows, and the same expression used for the preview and the deletion queries.
- **OIDC and SAML users with no directory email address get distinct synthetic addresses** (#3119, #3167). An OIDC provider is not obliged to release an `email` claim, and both claim-extraction paths defaulted a missing one to `""`. `users.email` is `UNIQUE NOT NULL`, so a deployment whose IdP does not release `email` could onboard exactly **one** federated user and then failed everyone else on `duplicate key value violates unique constraint "users_email_key"`. The address is now derived per subject — a sanitized prefix of the subject for operator recognizability plus a SHA-256 fingerprint of the raw subject, so distinct subjects stay distinct despite lossy sanitization, bounded inside `VARCHAR(255)` — under `.invalid`, reserved by RFC 2606, so it can never resolve or collide with a real mailbox. A claim that is present but blank is treated as missing. The SAML path had a narrower relative: it computed its `{username}@unknown` placeholder from the *pre-uniquification* username while inserting the uniquified one, so two distinct NameIDs whose username attribute happens to match got distinct usernames (`alice`, `alice_1`) and the **identical** placeholder address, and the second person could not log in at all. The SAML placeholder is now keyed on the NameID, which is already what the account row is stored and looked up by, so the synthetic address is unique and stable in exactly the cases the account is. Keying it on the username would have been worse than the original bug: the username attribute is self-settable at any IdP that maps it from a user-editable directory field, which would turn an availability defect into a targeted one. Both federated paths now share one derivation, and the addresses shipped for OIDC are unchanged byte for byte. `unknown` is also a resolvable TLD, so the old placeholder could collide with a real mailbox rather than merely with another placeholder.
- **Service-account grants show who they empower** (#2826). Permission list and get responses joined `users` only for `principal_type = 'user'`, so a service account's grant returned `principal_name: null`, and create and update hard-coded both display-name fields to null. The join is now keyed by the `users` primary key for every principal type, matching how permission enforcement itself resolves the row, and create and update return the same hydrated response out of the single statement that writes the row, so the display fields cost no second query and cannot drift from the list and get shape. It is deliberately **not** guarded on `is_service_account`: migration 057 introduced that column in February 2026 but `principal_type = 'service_account'` was not honoured by any resolver until #2499 in July, so in that window the only way to grant a service account fine-grained access was `principal_type = 'user'` plus the account's `users.id`. No migration re-types those rows and the enforcement path still matches them by bare `principal_id` — they are live, enforced grants today, and a type-guarded join rendered exactly those rows nameless, hiding from admins who a still-effective grant empowers. A guardless join keyed on the primary key can never attach the wrong name, only a missing one.
- **Backups never report success when artifact bytes are missing** (#3170, #3171). The backup path collected artifact bytes with `if let Ok(content) = self.storage.get(&key).await`, so a failed read was discarded: not counted, not logged, not in the manifest, not in the job status. The job reported `completed` with `artifact_count=0` and the restore reported `errors=[]` — the worst failure a backup can have, because it is discovered only at restore time when there is no recourse. Every read failure is now logged at `warn` with the key and the underlying error, counted into the manifest's new `artifacts_unreadable` field (`#[serde(default)]`, so existing archives still deserialize), and **fails the job** with a message naming the affected keys unless partial backups were explicitly opted into. The archive and manifest are still written before the job fails — partial data plus a recorded miss count is strictly more useful than discarding both, and the manifest is what lets the restore path refuse the archive later. Restore now reads the manifest and records an error when it declares unreadable artifacts, so a restore can never report `errors=[]` over a known-incomplete archive.
- **`POST /admin/backups/{id}/execute` reports a failed run on the backup, not as a `500`** (#3327). The trigger endpoint called `BackupService::execute`, whose `Err` for a failed run propagated straight through `Result<Json<_>>`, so a run that ended `failed` answered `500 {"code":"INTERNAL_ERROR","message":"Internal server error"}` — and the message #3170 composes for precisely this moment, naming every artifact object whose bytes could not be read, was written to `backups.error_message` and the server log and then discarded at the HTTP boundary. An operator triggering a backup got a bare 500 with no way to learn why. A run that reaches a terminal state has already recorded its own outcome on the backup row; that is a fact about the backup, not about the request, which was understood and carried out. The endpoint now answers with the `BackupResponse` it documents: `200` with `status: "failed"` and `error_message` carrying the cause, or `status: "completed"` for a clean run. **Clients must read `status`, not just the HTTP code** — nothing became quieter, the failure now reaches the caller instead of only the log, and `409` (another backup in progress), `404` and `500` (the run could not be started, or its outcome could not be recorded) are unchanged. `BackupService::execute` keeps its `Err`-on-failed-run shape for the scheduler, whose `backup_schedule_runs` bookkeeping depends on it; the new `execute_run` is what reports an outcome rather than reacting to one. This does not change which bytes a backup captures: on filesystem deployments artifact bytes are still missed for the reason tracked in #2863, and the miss is still reported, now on the resource the operator is looking at.
- **Objects larger than 5 GiB are copied server-side instead of through the application** (#3164). `S3Backend::copy` fell back to `get_stream` + `put_stream` above the 5 GiB single-copy limit, so every byte of a multi-gigabyte object came back through the backend and was re-uploaded. The integrity check was worse than the restream: it ran *after* `CompleteMultipartUpload` had already published the destination, so on a mismatch the corrupt bytes were already live — and it compared the tap hasher against the writer's hasher, two SHA-256s of the same in-process byte stream, so it was structurally unable to detect a transfer that diverged from the source at all. Copies now use one ranged `UploadPartCopy` per 5 GiB slice, as the S3 API requires above the single-copy ceiling; no payload byte passes through the application, and the destination only becomes visible at completion, after every part copied successfully — the same publish-is-the-last-step atomicity the ≤5 GiB native copy gives, which is the property the staging-key proxy-cache publish relies on. Failures abort the multipart upload and leave an existing destination untouched. The ranged call is hand-rolled against the S3 REST API because `object_store` 0.13 only exposes the un-ranged variant, but it is signed with that crate's own public SigV4 signer and the store's own credential chain, so static keys, IRSA, ECS/EKS container credentials and session tokens all keep working.
- **Multi-gigabyte S3 transfers are no longer capped by a 30-second client timeout** (#3180). `object_store`'s default request timeout applies "from when the request starts connecting until the response body has finished" — a cap on total transfer duration, not a stall detector — and it was never overridden. Two operations on the OCI blob-finalize path routinely exceed it for a multi-gigabyte layer: draining each upload part is a single HTTP request whose whole body must arrive inside the window, and promoting the finalized object onto its digest key is one `CopyObject` that S3 holds open for the entire server-side copy. Both surfaced as exactly-30-second stalls. The client is now split the way the GCS backend already splits its own, for the same reason: a control-plane store for head, exists, list, delete, multipart-create and presign keeps a short cliff so a wedged endpoint still fails fast, while a bulk store for gets, puts, part uploads, multipart completion and server-side copy gets a much longer ceiling. Azure already avoided a total timeout on bulk transfer, so S3 was the last backend applying a control-plane cliff to payload movement.
- **The scanner-adapter is handed a registry URL reachable from its own container** (#3169). The backend reused grype's host fallback, which defaults to `http://localhost:8080` when nothing is configured. That is correct only for grype, which execs inside the backend container — the scanner-adapter is a separate container that itself listens on `:8080`, so trivy inside it dialled its own Go server and got `404 page not found` on `GET /v2/`, then reported `unable to find the specified image`. No shipped configuration set a registry host, so the documented enable path in `docker-compose.yml` could never produce a working image scan. Resolution order is now the new dedicated override, then the explicitly configured grype chain (byte-identical to before), and otherwise: when the adapter host is non-loopback — i.e. it cannot share this network namespace — the backend advertises its own route-facing address with its bind port and logs the substitution, while a loopback adapter keeps the historical fallback for local development. A derivation failure also degrades to the historical fallback, so this is never a new failure mode. The commented compose block also mounted the trivy **server's** root-owned cache volume into the adapter, which runs as UID 1001, producing the reported `permission denied` on `fanal.db`.
- **`/health` and `/healthz` no longer fan out to live dependency probes on every request** (#3127). Both are declared at the router root, outside the `/api/v1` nest, so none of the rate-limit layers apply — and every request performed three uncached probes: a real service-account **directory bind** through a freshly constructed LDAP service, an OpenSearch `cluster_health()` query, and a Trivy probe over a brand-new HTTP client, where every client build does a blocking file read of the custom CA bundle on the async runtime when that variable is set. The LDAP leg is the sharpest: an unauthenticated request storm becomes a directory-connection storm capable of exhausting server-side connection limits or tripping the directory's own lockout and throttling counters — a dependency-side outage triggered from an anonymous endpoint. Measured against the real handler with counting stub servers, ten sequential requests produced ten binds and ten probes each, and 25 concurrent requests produced 25. All three now sit behind their own TTL cache at the same 5-second window the storage probe already used; the same load produces one of each, and the Trivy probes share one connection pool.
- **The Maven repository-delete path spares checksum sidecars and rollup documents another repository is still serving** (#3160, #3188, #3156, #3197). Four distinct gaps in the same purge list, all on cloud backends where the flat `maven/{path}` key space is shared between repositories — filesystem backends isolate each repository's key space and are unaffected. First, both `NOT EXISTS` guards tested the candidate key verbatim, and nothing ever references a checksum or signature sidecar *by name* (a parent artifact's `files[]` lists the companion, never the companion's `.sha1`), so both guards always missed on a sidecar — and in a `NOT EXISTS` guard a miss means DELETE. Deleting repository A therefore destroyed `<companion>.sha1` while repository B's live parent artifact was still serving `<companion>`, leaving B publishing an artifact whose checksum sidecar 404s: a warning under Maven's default `checksumPolicy=warn`, a **hard build failure** under `checksumPolicy=fail` or Gradle dependency verification. Each guard now runs a second time against the sidecar-stripped key, establishing the invariant that *a sidecar is spared whenever its base object is spared*. Second, three of those guards matched only the snake_case `files[]` key spelling while the affected rows record the companion under camelCase `storageKey` and the read path deliberately accepts both — protecting legacy rows on deployments upgraded through the version that wrote that spelling. Third, the repository-delete collector never grew the `maven-metadata.xml` anchor the storage-GC predicate has carried since #2668, so it destroyed a rollup document — the file the Maven layout defines for version-range resolution and the `LATEST`/`RELEASE` markers — along with its checksums, while GC would have spared it. All the new arms mirror the guards they shadow exactly apart from the stripped key, because any condition present on one and absent from the other re-creates the same bug for the rows it excludes.
- **The OCI cleanup-journal sweep can no longer delete a blob that was re-pushed underneath it** (#3085, #3187). The sweeps re-checked blob liveness when they claimed a key and again at the guarded row `DELETE` — but that `DELETE` runs *after* the storage object is already gone, and nothing consulted liveness immediately before the destructive delete. A push that commits an `oci_blobs` row for the same storage key during the sweep's walk lost its bytes: the pre-delete hook extended the claim lease without checking liveness, the object was deleted, and the guarded row `DELETE` then correctly refused to remove the journal row because `oci_blobs` now referenced the key — it saved the row, not the data, leaving a live row pointing at bytes that no longer exist. A claimed batch is up to 1,000 rows walked sequentially against an object store, so the window is the whole batch walk rather than an instant. The first fix re-asserted the full liveness predicate inside the same atomic lease-renewal `UPDATE`, which narrows the window but cannot close it — that `UPDATE` locks the journal row only for the duration of one autocommit statement, and the obvious repair does not transfer, because a racing push **INSERTs a new** `oci_blobs` row rather than conflicting onto an existing one, so there is nothing there to lock. The window is now genuinely closed with a two-phase tombstone: the push participates by taking the journal row lock at commit time, so no interleaving of a push and a sweep can leave the storage object deleted *and* an `oci_blobs` row referencing it. The trade is stated plainly: a push that loses the race is refused with a retryable `503` rather than silently succeeding.
- **The startup warning claiming permission enforcement is inactive is removed** (#3185). Every boot with at least one row in `permissions` logged that rules "are stored but not consulted during request authorization". That was inert text left in place after the enforcement work landed, and it misled operators about their security posture. Enforcement is genuinely active on both paths — `repo_visibility_middleware` gates native-protocol writes through `PermissionService::check_repository_action` deny-by-default and reads through the rules lookup plus `check_permission`, the REST path calls the same function, and `/api/v1/system/config` already reported `permissions.enforcement_enabled: true`, directly contradicting the warning. The message is replaced by a regression test that pins the enforcement it denied, so the two cannot drift apart again.

### Performance

- Health probes for LDAP, OpenSearch and Trivy are TTL-cached, collapsing an unbounded per-request fan-out on `/health` to one probe per dependency per window (#3127).
- npm packument responses revalidate with `304` instead of re-sending tens of megabytes per metadata request (#3052).
- Objects over 5 GiB are copied server-side rather than restreamed through the application (#3164).

### CI / Release Assurance

- **The release preflight refuses to tag when a version-pinned image would collide.** `v1.7.2` was tagged with `docker/scanner-adapter/VERSION` still at `1.2.2` while the go-git fix had re-pinned the bundled trivy base image inside `docker/Dockerfile.scanner-adapter`. Exact version tags are never republished, so the tag's `Docker Publish` refused the collision — which skipped every remaining multi-arch manifest, left `resolve-candidate-digest` with nothing to pin, and stalled the entire release chain on a tag that, being immutable, had to be deleted by hand. Nothing caught it earlier because the exact-tag check is only evaluated on a clean `refs/tags/v*` ref: `Docker Publish` passed on `main` for the **same commit**, and an `-rc.N` tag would have passed too, so the defect was reachable only at the one moment it is most expensive to find. `release-preflight.sh` now reads each component's pinned `VERSION`, asks the registry whether that exact tag is already published, and if it is, compares the published index's `org.opencontainers.image.revision` annotation against the very paths the publish job diffs. Changed sources block the cut and name the file to bump; an unchanged-source rebuild still passes, because re-pointing the floating tags for base-image errata is legitimate. A registry it cannot read reports `INFRA` (exit 2) — neither a pass nor a failure — the same distinction check 3 learned in #3308. The adapter moves to **1.2.3**; the scanner-adapter is the only component in this repo tagged from a checked-in `VERSION` file, and the check is a list so the second one is a one-line addition. Both directions are covered in `test-release-preflight.sh` (#3339).
- **Releases are cut from a release candidate first.** `RELEASING.md` now records that `vX.Y.Z-rc.N` should be tagged and the full chain allowed to complete before the real tag. v1.7.0 was cut this way and needed four candidates — rc.1, rc.2 and rc.3 all failed; v1.7.1 and v1.7.2 went straight to a real tag, and v1.7.2 left a dead one. This is safe because the repository ruleset applies tag immutability to `refs/tags/v*` but excludes `v*-rc*`, `v*-beta*` and `v*-alpha*`, so candidates are deletable and re-cuttable while releases are not. The runbook also notes what a candidate does *not* cover, since some publish logic keys on a clean `refs/tags/v*` ref (#3339).
- **Backend integration tests gate pull requests.** `🔗 Backend Integration Tests` ran only on pushes to `main` and `release/*`, and `ci-complete` treated the resulting skip as a pass — so a PR could break every integration test and still show all-green, with the breakage appearing only after merge on a push whose failure blocks nothing and notifies nobody. The suite now also runs on backend-touching PRs, classified from the same changed-file listing the docs-only gate already fetches, and it **fails open to running** (if the classifier job dies the suite runs anyway) while `ci-complete` **fails closed** (a skipped integration job on a backend-touching PR is now a hard failure). Docs-only PRs, version-bump-only release PRs and PRs touching no backend path still pass on a skip (#3124).
- **The container build and smoke chain runs on pull requests.** `detect-changes` carried `if: github.event_name == 'push'` directly under a section header reading "CONTAINER BUILDS (PR only)"; the header was the aspiration and the `if` was the behaviour, so the image builds and the smoke suite reported `skipped` on every PR and `ci-complete` passed the skip. The path filter supports `pull_request` natively, so dropping the `if` is the whole fix; push behaviour is unchanged. `ci.yml` itself is added to the backend filter, so a change to this gate exercises the gate — which is also how the hole went unnoticed, since the jobs were never observed *not* running (#3166).
- **The container CVE gate honours its declared `CRITICAL,HIGH` severity.** The gate declared `CRITICAL,HIGH` but failed on a fixable CVE of any severity, because the scanning action unsets the severity filter whenever the SARIF format is selected and severity limiting is not explicitly requested. Replaying the composite action against the real trivy binary reproduced a publication blocked by 17 findings whose maximum severity score was 6.3, none of them CRITICAL or HIGH. Scanning is now split from gating: one pass for visibility at all severities, one that gates. Both directions are proven — MEDIUM/LOW-only findings pass, and a build carrying fixable HIGHs still fails (#3122).
- **The shared-cargo-registry mutation flake is fixed and coverage build failures are no longer masked** (#3154). Rust jobs intermittently died one to two and a half minutes into their build with one of three signatures — a vendored C header truncated mid-read, a spawn failure because the child's working directory had been deleted, or a missing dep-info file whose listed sources had vanished — on commits whose sibling jobs built identically. The runner pods point `CARGO_HOME` at a hostPath volume shared by up to six concurrent runner pods, and two things treated that registry as private mutable state: the caching action tar-extracts over the live shared copy at job start and, on save, deletes and prunes the registry down to the saving job's own package set; and cargo re-extracts whatever goes missing under a lock that protects extraction against other extractions but not against sibling pods' deletions. At least six failed jobs in one week were attributable to this class, and every instance cleared on rerun — which is exactly the pattern that teaches people to ignore red.
- **Two cluster-wide test assertions no longer flake against concurrent suites.** The storage-GC footprint test asserted before/after deltas over cluster-wide `oci_blobs` totals while other suites delete rows from the same table, and under nextest fail-fast one flake cancelled roughly 1,500 other tests per occurrence; it now asserts exact equalities scoped to its own seeded digests — strictly stronger than the old `>=` deltas — while still executing the unscoped production report under race-free absolute lower bounds (#3129). The two empty-set `COALESCE(SUM(…), 0)` tests in the admin handler emptied a table inside a rolled-back transaction and then aggregated globally, which is not isolated under READ COMMITTED — any row a concurrent test commits between the `DELETE` and the `SELECT` is visible — and this took down `Backend Unit Tests` on an unrelated PR; both now scope the aggregate to a freshly generated repository UUID that is never inserted, which the foreign key guarantees stays empty (#3277).
- **`config.rs` tests no longer leak `DATABASE_URL` process-wide** (#3191). Those tests drive configuration parsing by writing environment variables through `std::env::set_var`, which mutates the whole process; `cargo test` runs the entire suite in one process across many threads, so while a config test held the sentinel `postgresql://127.0.0.1:1/testdb`, every DB-backed test that happened to start in that window failed with `Connection refused`. Saving and restoring the previous value and taking a module-local mutex do not help — the racing reader is a test on another thread that never takes it, and restoring promptly narrows the window without closing it. A test-only thread-local environment overlay now makes those writes visible only to the calling thread, so there is no window to lose; production code is untouched, since the non-test build still imports `std::env`. Postgres was never down — this was a false-signal bug.
- **The library test build is restored after parallel-merge signature drift** (#3214). `cargo test --lib` stopped compiling on `main`, blocking every Rust job: one PR added a second call site for a NuGet download helper in a test module while another added a parameter to that helper and updated only the pre-existing call site. Each PR was green against its own base and the merged tree was never rebuilt.
- **`release-preflight.sh` blocks on a measured-red Docker Publish instead of reporting READY** (#3308). The script measured the failing publish, printed `main's last Docker Publish did NOT cleanly publish the manifest -- a tag cut will likely stall the same way`, and then printed `READY: no blocking problems. Safe to cut the tag.` in the same run: the check called `warn`, which does not increment the problem count, where it needed `bad`. Three outcomes are now kept deliberately distinct, because *"I did not look"*, *"I could not look"* and *"I looked and it is red"* are different claims — not measured is a note and does not block, a failed query exits `2` as an infrastructure result rather than a readiness verdict, and a measured-red publish exits `1` as **NOT READY**. `PREFLIGHT_ALLOW_RED_PUBLISH=1` is a separately named human override rather than a reuse of `PREFLIGHT_SKIP_DOCKER_HEALTH`. The gating was backwards in a way this incident proves: the check that was already hard — that `main` is a superset of every `release/*` branch's `.trivyignore` — is only a **proxy** for "will Security Scan pass" and detects exactly one cause, and #3307 affected `main` and the release branches equally, so it passed clean while the release was definitively blocked. `scripts/ci/test-release-preflight.sh` covers all five outcomes against a stubbed `gh` and runs in `release-preflight.yml` ahead of the real check; restoring the single `warn` turns two of its cases red while the green-path case still passes.

## [1.7.1] - 2026-08-07

A maintenance release. The headline is that the bundled Trivy scanner is now built and hardened in-house, which clears a HIGH advisory that no upstream Trivy release fixes yet and had been blocking the release pipeline. Alongside it: NuGet remote-repository search now actually reaches the upstream feed, a scan-gate evaluation bug that could let a blocked artifact through is closed, and a cluster of Maven, PyPI, OIDC and multi-replica correctness fixes.

### Upgrade note — proxy downloads may start being blocked

The repository age gate is now genuinely enforced on **npm, PyPI and Go proxy downloads**. It was configurable before but silently did not apply to those formats, so a repository with an age gate configured was not actually gating them. After upgrading, package downloads that are younger than the configured `min_age_days` will return `451` where they previously succeeded.

**Am I affected?** Only if you have the age gate enabled on a remote/proxy repository serving npm, PyPI or Go. Check with `GET /api/v1/repositories/{key}/age-gate` (admin only as of this release). If you were relying on the previous non-enforcement, either raise the threshold or disable the gate before upgrading. The `451` body carries `min_age_days` so blocked callers can see the policy that stopped them.

### Security

- **The bundled Trivy scanner is now built in-house from pinned upstream source.** The scanner-adapter image vendored `ghcr.io/aquasecurity/trivy:0.73.0`, whose bundled `oras-go v2.6.1` carries CVE-2026-50163 (HIGH). Upstream merged the fix on `main` but has not released it, and 0.73.0 remains the latest release, so there was no upstream tag to move to. The scanner is now built from the same upstream v0.73.0 source with a pinned Go toolchain and one documented dependency override, on a hardened UBI 9 base, in [artifact-keeper/trivy](https://github.com/artifact-keeper/trivy) — with its own blocking CVE gate, DISA STIG evaluation and build verification. The shipped binary also now carries its licence, NOTICE and modification record, which the runtime previously omitted.
- **Scan gates are evaluated across all scanners, not just the most recent result** (#3138). A repository scanned by more than one scanner had its download gate decided by whichever row was newest, so an `OpenSCAP` result registering last could leave the severity gate inert for npm, PyPI, Maven and generic downloads.
- **Age-gate policy is enforced on npm, PyPI and Go proxy downloads**, and the age-gate config read endpoint now requires admin (#2264) rather than returning gate posture to any authenticated caller with read scope.
- **OIDC outbound responses are size-capped** (userinfo, token, discovery and JWKS), so a hostile or malfunctioning identity provider cannot exhaust memory through an unbounded response body.

### Added

- System stats now report proxy-cache storage consumption and artifact count.

### Fixed

- **NuGet remote repositories now proxy search to the upstream feed.** A remote repository advertised `SearchQueryService` in its service index but answered every query from the local database, so a proxy repository with nothing cached returned zero results — "No packages found" in Visual Studio — while `dotnet restore` against the same repository worked. Search now reaches upstream, rewrites result URLs back through the proxy, and degrades to local results rather than erroring if the upstream feed offers no search.
- **Per-repository storage-GC estimates work again.** The web UI's per-repository storage panel called `POST /api/v1/repositories/{key}/storage-gc` for its "reclaimable now" figure, but only the instance-wide route existed, so every estimate returned 404.
- **Maven catalog versions are derived from the GAV path** on the generic and chunked upload paths, and migration 192 no longer aborts startup on a UNIQUE violation when a package has two broken catalog rows and one candidate version.
- **PyPI proxying**: an upstream `<base href>` on a proxied Simple index no longer re-anchors rewritten download URLs onto the upstream origin; PEP 658 `core-metadata` is advertised for wheels so installers can skip downloading whole wheels to resolve; the PEP 691 `versions` array is ordered by PEP 440 rather than byte order; invalid PEP 440 local versions are rejected instead of silently mangled; and an advertised-but-unavailable metadata file now falls back instead of failing the install.
- **OCI**: a manifest pushed by digest is verified against that digest rather than accepted with a mismatch, and cross-repository blob mount (`?mount=&from=`) is implemented so a client can mount an existing layer instead of re-uploading it.
- **Proxy-cache redirects are gated on the object actually existing.** `local_fetch_or_redirect` passed a hardcoded `cache_is_fresh = true`, so an `artifacts` row alone was enough to sign a presigned redirect without consulting the object store. An object deleted out of band — a lifecycle rule expiring proxy-cache entries by age, for instance — produced a dead 302 that 404s at the object store, past any fallback the server controls, and kept serving dead redirects until repaired by hand. A miss now falls through to the buffered read and repopulates the cache.
- **Multi-replica background jobs** claim their remaining cluster-work items, so replicas no longer duplicate or drop work.
- **Scheduled backups claim one durable run per due occurrence** (#2219): multi-replica schedulers no longer create duplicate archives for the same schedule tick. Long runs renew their token-fenced claim, and run finalization plus advancement of the unchanged schedule occurrence is transactional. A schedule whose cron expression has no further occurrence is now disabled with a warning instead of being left permanently due, where it consumed a due-schedule slot and could starve other schedules.
- **Password-expiry notifications are claimed before SMTP** (#2219): concurrent replicas no longer email the same password-version warning in one scheduler tick. Failed attempts observe a retry delay, stale account/password candidates are rejected at claim time, and usernames are escaped in HTML mail.
- **Generic chunked uploads take a completion lease before finalizing** (#2219): a duplicate `complete` no longer re-checksums, re-copies, and re-upserts the same session, and a failed storage copy no longer leaves a session marked `completed` with no artifact. `cancel` can no longer delete the temp file out from under a live committer, and the expired-session reaper claims its cancels instead of racing them. Chunk uploads are rejected once a session is committing, and a `complete` that fails its authorization re-check releases the lease so the session stays retryable.
- **Incus chunked finalize takes an exclusive lease** (#2219): concurrent `complete` requests can no longer both append the final request body to the staging file and spawn two finalizers, and a reclaim after a crashed append truncates the unrecorded tail instead of double-appending it. Chunk uploads to a session that is already finalizing are rejected with `409` instead of appending to the file being checksummed. The stale-session reaper skips sessions with a live finalize lease.
- **OCI cleanup-journal sweeps claim keys before deleting storage objects** (#2219): concurrent GC ticks no longer issue the same destructive object delete, a failed delete releases the claim with the error recorded for retry, and dry runs stay read-only.
- **Health monitoring, lifecycle execution, and the OpenSearch bootstrap reindex are cluster-leased** (#2219): the health monitor's `consecutive_failures` counter has a single writer, due lifecycle policies execute once per cycle instead of once per replica, and a fresh multi-replica deployment starts one full reindex rather than one per replica. Long-running leases heartbeat while their owner is alive.
- **Curation sync honors `curation_sync_interval_secs`** (#2219): the configured interval was read and discarded, so every enabled staging repository re-fetched upstream metadata on every five-minute tick. Repositories now record `curation_last_synced_at` after a successful fetch-and-evaluate and are skipped until their interval elapses (60s floor); the manual single-repository sync trigger still runs immediately.
- Probe endpoints (`/health`, `/healthz`, `/ready`, `/readyz`, `/livez`) no longer emit a per-request log line, which had buried real request logs under Kubernetes polling. A non-success probe response is still logged. Set `LOG_PROBE_REQUESTS=1` to restore the previous behavior.
- The hardened backend image includes `xz`, which Incus `.tar.xz` extraction requires.

### Performance

- Repository listing reads storage totals from the usage ledger instead of computing a live `SUM`.

## [1.7.0] - 2026-07-31

A supply-chain-enforcement and release-assurance release closing the 1.7.0 milestone. The headline is that scan and curation policy now *block at serve time*: an inline scan-and-block gate rejects vulnerable artifacts on download across proxy and hosted repositories (PyPI, npm, Docker/OCI) and the virtual repos that aggregate them, while a new typed curation engine adds publisher-trust and popularity/typo-squat rules — including Unicode-confusable (homoglyph) and affix detection — that stop suspect packages before they reach a client. Underneath the features, the release re-architects the release pipeline into a real assurance gate (blocking security suite, DTF gate against the candidate digest, promote-by-digest, dispatch-rehearsable dry runs), makes the repository usage ledger authoritative to enable O(1) quota admission, and lands a broad performance pass (keyset pagination, GIN-indexed full-text search, off-hot-path side effects). It also closes a cluster of token, refresh, and federated-admin authorization gaps (several with GHSA advisories) and a deep set of cloud-storage, migration, and native-format fidelity fixes.

### Sponsors

Thank you to our sponsors for supporting ongoing development of Artifact Keeper.

**Backers**

- Ash A. ([@dragonpaw](https://github.com/dragonpaw))
- Gabriel Rodriguez ([@injectedfusion](https://github.com/injectedfusion))

[Become a sponsor](https://github.com/sponsors/artifact-keeper) to support the project and get your name listed here.

### Thank You

Huge thanks to the external contributors and issue reporters who shaped this release:

- **[@rockdrilla](https://github.com/rockdrilla)** — contributed exposing the APT repository GPG public key at the repository root for a more natural keyring URI, and reported the underlying request (#2694).
- **[@ThaSami](https://github.com/ThaSami)** — contributed the Maven `files[]` camelCase `storageKey` attribution fix (#2706) and the ledger-first metadata-files attribution index (#2942), and reported the npm hosted-publish packument staleness across replicas (#2490).
- **[@nicola-preda](https://github.com/nicola-preda)** — contributed immutable membership on SSO-synced groups (#2874), Nexus source-version resolution on connection test, and blocking a disabled format's aliases.
- **[@valfon1393](https://github.com/valfon1393)** — contributed the durable repository-owner capability (#2666).
- **[@StringKe](https://github.com/StringKe)** — contributed applying OpenSearch Basic auth in the health-monitor probes (#2945).
- **[@sergeishestakov-gh](https://github.com/sergeishestakov-gh)** — contributed omitting empty `shasums_url`/`shasums_signature_url` from the Terraform provider download response (#2934).
- **[@Mo0rBy](https://github.com/Mo0rBy)** — contributed the fix to get `docker-compose.local-dev.yml` working again (#2909).
- **[@ijustworkhereshrug](https://github.com/ijustworkhereshrug)** — reported first-time-setup admin-login lockout (#2492), the non-functional LDAP test-connection (#2486), the SSO local-login button not showing (#2621), and requested per-repository backup exclusions (#2772).
- **[@major-sam](https://github.com/major-sam)** — reported the empty Packages tab after Nexus migration (#2676) and Active Directory group sync failing (#2468).
- **[@BonjeBongo](https://github.com/BonjeBongo)** — requested custom S3 paths (#2508) and separate S3 buckets for storage and backups (#2507).
- **[@dsiebel](https://github.com/dsiebel)** — reported GCS Workload Identity failing `docker push` with an HTTP 500 from a dropped metadata-server connection (#2611).
- **[@TheClonker](https://github.com/TheClonker)** — reported valid Composer metadata being stripped on upload (#2846).
- **[@kochetkovSergeY](https://github.com/kochetkovSergeY)** — reported `maven-metadata.xml` not being cleared when an artifact is deleted from the web UI (#2845).
- **[@IButakov](https://github.com/IButakov)** — reported Composer v1-protocol upstreams returning 404 (#2693).
- **[@Tartanpion27](https://github.com/Tartanpion27)** — reported S3 object keys not carrying a repository identifier (#2624).

### Security

- **Inline scan-and-block on download** — vulnerable artifacts are now blocked at serve time on proxy-PyPI and hosted repositories, enforcing scan results and policy on the download path rather than only at ingestion (#2954).
- **Scan-and-block extended to npm and Docker/OCI proxy pulls**, over a shared scan-gate core, reaching parity with proxy-PyPI (#3003).
- **The inline scan-and-block gate now covers virtual-repo serves** (OCI/npm/PyPI), closing the aggregation bypass where a blocked artifact served 200-unscanned through a virtual repo (#3023).
- **Curation block rules are enforced on proxy pulls** across npm, Docker, Maven, Cargo, and NuGet — previously `curation_enabled` was settable but silently inert on 26 of 27 proxy formats (#2930).
- **Scan policy, admin quarantine transitions, and curation are enforced on PyPI proxies** (#2930).
- **Container publication enforces the stated Trivy CRITICAL/HIGH scan policy** instead of merely reporting findings (#2609).
- **Refresh-token consume-and-rotate is now atomic**, closing a token-replay race (GHSA-qxxr, #2813).
- **Effective admin is scope-gated for token principals** so a scoped token can no longer act with full admin authority (GHSA-vvc3, #2814).
- **Chunked-upload verbs enforce token write/delete scope** (GHSA-5f2q, #2812).
- **Webhook management requires admin and no longer returns the rotated secret** in responses (GHSA-qcmj, #2818).
- **Peer-announcement UPSERTs are atomic** (GHSA-gw36, #2816), and **peer data-plane management routes require admin** (#2815).
- **The first-run admin password file is written atomically at `0o600` and is no longer logged by default** (GHSA-8523, #2817).
- **Federated admin mapping requires an explicit admin group** — a fuzzy/substring group match can no longer auto-grant admin (#2759).
- **Startup admin provisioning no longer hijacks a federated identity** that shares the `admin` username (#2875).
- **Token minting validates and bounds requested scopes at the choke-point** — enforcing an allowed-scope set, a scope ceiling, and the admin-only-mint boundary (#2996).
- **Upload paths accept the colon-form `write:artifacts` scope** so least-privilege repo-scoped tokens stop 403-ing on upload (#2989), and **OCI write/delete paths require the colon-form artifact scope** (#3053).
- **The `/api/v1` Basic-auth boundary is enforced** — API tokens are accepted as a Basic password on format endpoints but rejected on the management API (#2809).
- **Cross-repository writes in the promotion and approval copy paths are guarded** on cloud storage (#2511).
- **Maven first-publish TOCTOU on a shared cloud flat namespace is closed structurally** via repo-scoped object keys (#2586).
- **Maven and sbt objects use repo-scoped cloud-storage keys**, so S3 keys carry a repository identifier and cross-repo reads are impossible (#2624).
- **Virtual PyPI local-ownership is guarded at distribution granularity** instead of coarsely by name, without suppressing remote platform-specific wheels (#2937).
- **Virtual Debian repos enforce each member's dist/component/architecture filter** (#2727).
- **RPM keyless sync defaults to fail-closed**, requiring a configured trusted GPG key (#2569).
- **The npm scope-policy allowlist fails closed** on a DB error or corrupt config (#2726), and **the Debian proxy filter fails closed** when it cannot be loaded (#2672).
- **Encoded `%2f`/`%5c` path separators are rejected** in the Debian proxy filter and proxy paths as defense-in-depth, reconciled to a uniform 400 (#2562, #2770).
- **`Content-Disposition` filenames are sanitized per RFC 6266**, removing a header-injection and panic surface (#2654).
- **PyPI simple-index responses are sniffed rather than trusting the upstream `Content-Type`**, and un-rewritten upstream URLs are never served (#2807).
- **NuGet remote proxying pins upstream credentials to the configured upstream host** rather than a host named by the upstream's own service index (#2925).
- **Diagnostic URL redaction strips userinfo**, so upstream credentials no longer leak in 502 bodies (#2926).
- **Updating a repository's security scan config requires repository admin** (#2750), as do the **repository-configuration subresources** (#2603).
- **Mutation paths enforce action-specific repository authorization** (#2987), and **migration source/job routes require global admin** (#2979).
- **The migration referenced-content walker is byte-capped** per blob and per image, tied to repo quota (#2575).
- **The repository scan-config `severity_threshold` is validated and normalized**, returning 400 instead of 500 on a non-lowercase value (#2953).
- **The scan tenant-refcount is registered inside its RAII guard**, closing a refcount leak on future cancellation (#2595).
- **Bundled Grype updated to v0.116.0** to clear CVE-2026-56852, with a non-reachable bundled-gRPC advisory suppressed (#2777).

### Added

- **Typed curation policy engine** — publisher-trust and popularity evaluators with global scope and decision dispatch, forming the base for allow/flag/block curation rules (#2947).
- **Typo-squat detection** — popularity-threshold rules plus Unicode-confusable (homoglyph) and affix-based typo-squat detection (#2956).
- **RPM curated snapshots** — versioned publications with signed, immutable `@N` snapshot serving (#2358).
- **Durable repository-owner capability**, so repository owners carry a real, persisted permission grant (#2666).
- **Per-artifact quarantine state is surfaced in the artifact listing** (#2940).
- **Age-gate reviews can be reopened and re-decided** instead of being terminal once approved or rejected (#2939).
- **Per-prefix folder-tree storage rollup with a `/storage/tree` endpoint** for deduplicated per-folder usage (#2601).
- **Browseable folder tree for generic proxy download recording and analytics** — generic-path proxy downloads are recorded and proxy counts are surfaced (#2705).
- **APT repositories expose the GPG public key at the repository root** for a natural keyring URI (#2694).
- **RPM/generic packages get header metadata extracted** so generically-pushed RPMs carry real `Source:` and related fields (#2588).
- **SSO-synced group membership can be made immutable**, blocking manual member edits on externally-sourced groups (#2874).
- **More OIDC configuration via environment variables**, including provider toggles for bootstrap (#2792).
- **Server version and git SHA are logged at startup** so support log dumps identify the running release (#2597).
- **Configurable first-run admin-password retrieval hint** (#2804).
- **Backup enhancements** — custom archive name (#2790), per-repository exclusions (#2772), incremental backup from a given date (#2789), a configurable `BACKUP_S3_PREFIX`, and a separate S3 bucket for backup archives (#2507).
- **Custom GCS endpoint support** for emulator-based E2E (#2646).
- **Release binaries are published by a standalone, gate-independent workflow** when the release gate is waived (#2719).

### Changed

- **Per-tenant fairness for the concurrent-decode cap** during ingestion extraction, so one tenant cannot starve decoding (#2598).
- **Web image release is decoupled from the backend release** in CI (#2709).
- **Releases are announced to the community Discord** on publish (#2690).
- **Added `ARCHITECTURE.md`** maintainer documentation (#2739).

### Fixed

- **npm hosted-publish packument invalidation fans out to all replicas**, fixing stale dist-tags and split `npm pack` filename/bytes across a cluster (#2490).
- **Composer preserves all valid `composer.json` properties on upload** (#2846) and **resolves packages from Composer v1-protocol upstreams** (#2693).
- **Maven/Gradle grouped listings normalize `packages.name` to `groupId:artifactId`** (#2723), and **`maven-metadata.xml` is cleared when an artifact is deleted** (#2845).
- **Hex serves a consumable registry as an upstream pass-through after first cache** (#2658) and **reconciles case-variant releases** so advertised tarballs download (#2674).
- **NuGet and Chocolatey remote pull-through proxying works** (#2779).
- **Remote Cargo crate downloads stream** instead of buffering at the metadata cap (#2918).
- **conda-forge and Hugging Face pull-through proxies are fixed**, preserving `Content-Length` on compressed upstreams (#2915).
- **PyPI simple-index responses gain HTTP caching** (ETag/`Cache-Control`/304) (#2780).
- **Terraform advertises stored provider platforms** (#2661), **excludes held packages from mirror index and versions** (#2662), and **omits empty checksum URLs** from provider responses (#2934).
- **RPM excludes non-`.rpm` objects from repodata listings** (#2590).
- **Helm advertises the stored chart filename in `index.yaml`** for bare-path uploads (#2753).
- **CRAN and RubyGems advertise stored-filename coordinates** for bare-path uploads (#2754), and **suffix-resolved formats advertise the stored basename** in download URLs (#2589).
- **Azure Shared Key requests are signed correctly for path-style endpoints** (#2649).
- **GCS metadata-server token fetches use a dedicated HTTP client**, fixing dropped Workload Identity connections on `docker push` (#2611).
- **Blob garbage collection reclaims row-less Maven sidecar and metadata objects** (#2668).
- **First-time-setup admin login works** — peer replicas unlock after the admin password change without a restart (#2492).
- **The admin LDAP test-connection performs a real bind** (#2486), with **per-provider LDAPS TLS skip-verify and custom-CA options** (#2796).
- **Active Directory groups sync to local groups on login** (#2468).
- **The local-login button shows in the UI when SSO is enabled** and local admin login is allowed (#2621).
- **OIDC group memberships resolve from candidate claims and userinfo** (GitLab `groups_direct`/inherited), including group claims emitted as a delimited string (#2833).
- **The federated group reconciler no longer over-grants** by attaching SSO/LDAP users to operator-managed groups by name alone (#2759).
- **Nexus migration populates the Packages catalog** for imported artifacts (#2676), **repairs hollow Docker repos on re-migration** (#2596), **creates rows for by-digest child manifests** (#2576), **migrates Go and APT repos** (#2797), **correlates group members to virtual-repo members** (#2799), **populates `upstream_url` for remote/proxy repos** (#2822), **skips artifact transfer for virtual (group) repos** (#2821), and **resolves the source version instead of reporting "unknown"** (#2872).
- **Dependency-Track integration resolves its API key file at use time** so a post-boot bootstrap needs no restart (#2975), with the **provisioned key file owned by the backend UID** (#2865).
- **Virtual-repo total aggregation is corrected and member edits are allowed** (#2795).
- **Large virtual-repo delete storage cleanup is deferred off the request path** (#2778).
- **Backup archive storage is reclaimed in retention cleanup** (#2800).
- **Health-monitor probes apply OpenSearch Basic auth** (#2945).
- **API tokens are accepted as the Basic-auth password on the generic API** (#2798).
- **A disabled format's aliases are blocked** (#2867).
- **The `docker-compose.local-dev.yml` stack works again** (#2909).
- **OpenAPI tags the artifact-signing operation** so SDK generation stops emitting empty `tags[]` (#2721).

### Performance

- **O(1) quota admission** — uploads no longer scan all live artifact sizes twice; admission reads maintained usage-ledger counters (#2523), and **the usage ledger is kept authoritative via row-level triggers** on the source tables (#2992).
- **Common upload routes stream** instead of buffering artifacts up to the 10 GiB limit (#2517), with **upload over-admission closed by a serialized usage ledger** (#2523).
- **Full-text search is backed by a GIN-indexed `search_vector`** instead of recomputing `to_tsvector` per row (#2871).
- **Keyset pagination** replaces full materialization for flat catalog listings and substring search (#2518), remote-cache browsing (#2519), grouped Docker/Maven listings past the 10k bound (#2520), and Maven hosted/virtual grouped listings with catalog-name backfill (#2723).
- **RPM repodata is served from a fingerprint-validated cached render** (#2521).
- **Download-path stats and audit writes move off the hot path** (#2522), with the **download side-effect dispatch bounded** by a queue plus flush-worker pool (#2522).
- **Metadata-files attribution consults an indexed claims ledger first** (#2942).
- **Buffered proxy metadata is bounded across Debian/npm/Composer/Maven/PyPI** against a shared byte budget (#2684), and **concurrent quality checks are bounded** to a shared capacity budget (#2522).

### CI / Release Assurance

- **The release security suite is blocking and the fake-green SAML oracle is fixed** (#2700).
- **The Deployment Test Framework runs as a required release gate against the candidate digest** (#2697).
- **Promote-by-digest** — `:VERSION`/`:latest` publish only after the gate passes, with a digest-aware republish guard and idempotent mirror (#2698).
- **Release-gate `resolve-candidate-digest` waits on the docker-publish run** instead of a 35-minute clock (#2711).
- **The release gate is dispatch-rehearsable** as a dry run (#2701), with a **pre-tag release-preflight check** to catch main-vs-release drift (#3042).
- **DB-backed tests fail loudly when the database is unreachable** instead of reporting fiction-green (#2924), and **the lib suite is green and hang-proof without `DATABASE_URL`** (#2986).
- **`ci.yml` is the sole owner of the required check contexts**, removing the duplicate docs-only shim (#2923).
- **The smoke E2E gate reflects every suite's exit code** (#2985), the **mesh E2E gate is dropped from the release path on main** (#2980), and the **docker-publish Security Scan gate is hardened against transient network failures** (#2982).
- **The migration-ledger gate retries transient git failures and separates infra exit codes** from genuine divergence (#2984), fixing the "shallow file has changed" flake (#2902).
- **Format tests drive advertised download/content URLs through the real router** (#2657), and **test-module builder copies are promoted into production** so literal-pinned tests catch format drift (#2657).
- Routine dependency and CI-toolchain bumps (serde_json, serde_with, Grype, and several GitHub Actions) (#2673, #2854).

## [1.6.0] - TBD

A security- and correctness-hardening release closing the 1.6.0 milestone: 24 security fixes, 9 features and enhancements, and 47 bug fixes, spanning multi-tenant isolation, supply-chain signing, ingestion/scan resource limits, native-client format fidelity across a dozen ecosystems, proxy/cache accounting, and SSO group mapping. It also folds in the cross-repository Maven flat-key attribution hardening and the npm replication-feed and webhook cluster-safety fixes carried over from the 1.5.x hotfix line. In-place upgrade from 1.5.x is automatic — a startup migration-ledger reconciliation resolves the history divergence with no operator action (#2686). The release date is stamped at cut.

### Security

- **Row-less Maven flat-key objects are attributed to their owning repository on shared cloud storage** (#2574, #2584): #2504 hardened shared cloud namespaces (S3/GCS/Azure) by refusing to serve any bare `maven/{path}` object not anchored to an artifact row scoped to the requesting repository. That also stopped serving legitimate *row-less* legacy objects — GAV-grouped companion files (`.pom`, `.module`, `-sources.jar`), verbatim `maven-metadata.xml`, and stored checksum sidecars — because a row-less object on a flat namespace carries no inherent repository attribution. This change adds a catalog-derived attribution mechanism (`services/maven_flat_attribution`, backed by the new `maven_flat_object_owner` table in migration 163) that resolves the single owning repository of a flat key from live artifact rows, parent-artifact metadata (including single-owner artifact-, SNAPSHOT-, and group-level `maven-metadata.xml` rollups), and a first-writer-wins claim recorded on write. Reads serve a row-less object only to its genuine owner and 404 for everyone else; keys owned by two or more repositories (ambiguous) and truly-orphan keys stay unreadable on cloud backends. Every Maven flat-key write is guarded before it reaches storage: overwriting a key owned by a different repository is refused with `403`. The pre-write guard is read-only; the attribution claim is committed only after the object bytes are successfully written, so an aborted or coordinate-invalid write can never flip ownership of a foreign unattributed key — closing the write-side cross-repository poisoning and claim-based read-back gap (#2584). Filesystem backends, which physically isolate each repository's key space, are unaffected — every legacy read and write behaves exactly as before.
- **Cross-repository authorization hardened** across the promotion-rules, approval, curation, signing, and quality routes (#2443).
- **Quality-check results can no longer be read across repository boundaries** (#2437).
- **gRPC methods enforce token action scopes** instead of authorizing on admin status alone (#2432).
- **npm virtual-repo scope policies now cover tarball and direct-remote paths**, support glob patterns, and are configurable at create time and in the UI (#2424).
- **Storage stats no longer expose a cross-tenant deduplication existence oracle** via shared-bytes on cloud backends (#2560).
- **The promotion "require signature" gate is now enforced** and honors key revocation (#2535).
- **Signing-key rotation is atomic** — concurrent rotations can no longer orphan keys or leave the instance without an active signing key (#2534).
- **Repeated signing-key rotations no longer wedge the key on a name-length limit** (#2543).
- **Rotating registry signing keys now requires authorization** — previously any authenticated user could rotate them (#2513).
- **Debian `Release`/`Release.gpg` are rendered deterministically** so the detached signature always matches the served bytes, eliminating spurious apt-secure "BAD signature" errors on untampered repos (#2652).
- **Helm `.prov` provenance files are stored and served** instead of silently discarded, so `helm pull --verify` can succeed (#2635).
- **Upload-time archive extraction is now bounded**, closing a decompression-bomb surface at ingestion (separate from scanning) (#2556).
- **Added a concurrent-extraction cap** so per-request extraction budgets can't be multiplied across parallel uploads (#2561).
- **Scan-archive extraction is bounded**, preventing scan-workspace disk exhaustion (#2514).
- **Added a global concurrent extraction budget for scanning**, on top of the per-archive cap (#2540).
- **Removed an unbounded dead-code extraction path** that could have reintroduced the decompression bomb if re-wired (#2541).
- **Remote-instance proxying no longer buffers request and response bodies unbounded** (#2515).
- **Scan scheduling applies per-user/tenant fairness** so a single tenant can't monopolize the scan queue (#2555).
- **Reading and modifying plugin configuration now requires authorization** — previously any authenticated user could do both (#2512).
- **Plugin callback SSRF guards hardened** ahead of enabling webhook/validator dispatch (#2536).
- **npm virtual-repo scope filtering scrubs package names in kept keys' values** (`/-/by-user`), scalar keys, and search siblings (#2594).
- **npm scope filtering also covers the legacy `/-/all` and `/-/by-user` map response shapes** (#2542).
- **npm metadata filtering fails closed on unrecognized response shapes** instead of passing them through (#2551).
- **CVE blast-radius reports enumerate users who can access a restricted repo but haven't downloaded the affected artifact** (#2386).
- **Scan policies are now enforced instead of only flagged** (#2912): an artifact that violates an enabled policy's `max_severity` threshold or `block_on_fail` is quarantined, blocking downloads across the format download routes and Docker/OCI manifest pulls. Enforcement also applies to artifacts under a quarantine-period hold, converting the expiring hold into a permanent block, and is re-derived on rescan so a policy created after the original scan takes effect. `block_unscanned` remains a promotion-gate check only — an artifact that was never scanned does not reach the post-scan hook.
- **Curation block rules now gate PyPI proxy index and download requests** (#2912) on curation-enabled remote and virtual repositories, rejecting matching packages with a 403 and the rule reason instead of only auditing them. Rule patterns match case- and separator-insensitively against the PEP 503 name, so a rule written the way PyPI displays a project (`PyYAML`, `my_package`) applies; the download path evaluates version constraints against the version in the distribution filename, while the versionless index path skips version-constrained rules. A virtual repository enforces each member's own rules.
- **The quarantine reason is readable through `GET /api/v1/quarantine/{artifact_id}`** (#2912), which is authenticated and repository-visibility checked. Blocked-download errors stay generic on purpose: those routes serve anonymous callers on public repositories, and the reason carries policy names, per-artifact finding counts, and admin incident notes.

### Added

- **Opt-in upstream change-feed subscription for proactive npm packument invalidation** (#2249): a small, generic feed-subscription framework (`services/upstream_feed.rs`) that consumes an upstream's change stream and proactively drops now-stale cached metadata, tightening freshness below the packument cache's TTL/stale-while-revalidate floor. The first adapter polls npm's `replicate.npmjs.com/_changes` and, per changed package, evicts the computed packuments in every remote npm repository whose upstream the feed covers (host-scoped to `registry.npmjs.org`/`registry.npmjs.com`) and in every virtual repo containing one — reusing the same fan-out as the local-write invalidation path. Consumption is **off by default** and gated behind `NPM_UPSTREAM_FEED_ENABLED` (also requires the packument cache; feed URL configurable via `NPM_UPSTREAM_FEED_URL`, validated against SSRF at the entry point). A single consumer runs cluster-wide via the existing advisory-lock primitive in bounded 5-minute leadership terms so a silently-lost lock reconverges to one consumer; first enablement starts at the feed head (never replaying npm's full change history); the resume cursor is persisted per feed in `upstream_feed_state` (migration 156). The feed is a freshness optimisation, never the correctness mechanism — the TTL/SWR floor still bounds staleness if the consumer is disabled or failing.
- **Advanced Debian/APT repository functionality** (#2407).
- **Debian proxy hardening** — SSRF, integrity, and cache safety for remote APT repositories (#2459).
- **Debian distribution/component/architecture filtering for proxied repositories** (#2460).
- **Debian hosted repositories support `Release` metadata customization** (#2489).
- **RPM curation: the trusted GPG key is now configurable through the create/update API** (#2568).
- **Real deduplicated storage usage per repository and folder** (#2056).
- **Structured audit-log export with a published, versioned schema for SIEM ingestion** (#2413).
- **Proxy-cache downloads are now counted correctly on first serve** (#2537).
- **Scanner UX** — not-applicable image-family rows are collapsed in results, and `TRIVY_ADAPTER_URL` is documented in the base compose (#2471).
- **Configurable first-run setup password hint** (#2802): the first-time-setup login screen previously hardcoded a `docker exec` instruction for retrieving the generated admin password, which is wrong on Kubernetes and packaged installs. A new optional `SETUP_PASSWORD_HINT` env var overrides that instruction, and the public `/api/v1/setup/status` response now carries the operator-supplied hint. Unset leaves the existing Docker Compose default in place.
- **New admin endpoint, `POST /api/v1/quarantine/{artifact_id}/quarantine`, to quarantine a flagged, clean, or held artifact manually**, completing the flagged-to-blocked transition without waiting for a new scan (#2912). Idempotent, and converts an expiring quarantine-period hold into a permanent block so the block cannot lapse; `409` only for an artifact already rejected.
- **`curation_enabled` and `curation_default_action` are returned by the repository API** (#2912), so an operator can confirm whether curation enforcement is active rather than inferring it from a blocked download.

### Fixed
- **Maven flat-key attribution no longer sequentially scans `artifact_metadata` on every row-less read** (#2942): the metadata-files owner lookup is rewritten to GIN-indexable jsonb containment (new expression index in migration 179), and the layer order now consults the `maven_flat_object_owner` claims ledger (a primary-key probe) *before* the metadata derivation — which also guarantees a recorded write-time claim outranks an attribution derived from another repository's metadata, keeping reads consistent with the write guard. The previous per-row `EXISTS` predicate could not use any index, so on deployments with millions of metadata rows every row-less flat-key read (and every cloud-backed local member probed during a virtual-repository walk, for keys that don't exist at all) paid a multi-second sequential scan that queued up under concurrent load.
- **Remote cargo `.crate` downloads stream instead of buffering at the 8 MiB metadata cap**, so crates above that size no longer fail with a 502 (#895 / #2192 class).
- **Maven flat-key attribution now matches the `files[]` entries the upload handler actually writes**: the metadata-files owner layer and migration 163's `metadata_files` backfill matched `storage_key` (snake_case), but the GAV-grouped upload handler has always serialized `files[]` entries with camelCase `storageKey` (#418) — so the layer never matched real data, and every legacy row-less companion (`.pom`, `.module`, `-sources.jar` of a GAV-grouped upload) failed closed on cloud backends despite the #2574 fix intending to keep them served. The read-time lookup now accepts both spellings, and migration 169 re-runs the corrected backfill (idempotent, `ON CONFLICT DO NOTHING`, same single-owner-per-backend semantics as 163/168).
- **npm replication-feed adapter now speaks the real `replicate.npmjs.com` dialect** (#2249, #2258): the feed consumer spoke a CouchDB long-poll dialect the real endpoint (`engine: npm-replicate`) rejects with 400 — it sent `feed=longpoll`, `timeout=`, `limit=` and `since=now`, all unsupported — so the consumer error-looped at the backoff cap and could never invalidate anything. It now polls with a numeric `since` + `limit` only, bootstraps the head cursor from the feed root's `update_seq` (never replaying npm's ~119M-event history from genesis), paces idle polls at 30s (no long-poll to block server-side), caps `_changes` and root response bodies at 8 MiB (an unbounded upstream body previously OOM'd AK, #1607/#1608), escalates persistent poll/bootstrap failures from debug to warn so a permanently-broken feed is visible, and scrubs credentialed feed URLs out of error logs via `without_url()`. The feed remains off by default and a best-effort freshness optimisation over the packument cache's TTL/SWR floor.
- **Webhook retry deliveries are claimed atomically before the re-POST** (#2219): with multiple backend replicas sharing one database, every replica's retry tick selected the same due deliveries, so one failed delivery could be re-POSTed once per replica per tick — and exhausting `max_attempts` could fire the dead-letter auto-disable notifier once per replica. Due deliveries are now claimed in a single `FOR UPDATE SKIP LOCKED` statement with a per-claim token (the same `cluster_work` pattern as the #2275 sync-task claim), the claim is re-extended immediately before each send, and success/retry/dead-letter updates are token-guarded so a stale owner cannot clobber a re-claimed delivery. A crashed replica's claim simply expires, making the delivery due again with no operator action.
- **Alpine: APKINDEX is now consumable by real apk clients** (checksum, tar typeflag, and size fields fixed) (#2633).
- **Alpine: APKINDEX signing failures now fail the request** instead of silently serving an unsigned index (#2660).
- **RubyGems: specs index is served in the Marshal format** gem/bundler expect, making hosted repos usable (#2630).
- **conda: token-authenticated channels no longer return 401** on repodata and downloads (#2629).
- **NuGet: registration leaf no longer advertises a download URL that 404s** (#2656).
- **NuGet: `dotnet nuget push --api-key` works** — the API-key header is now recognized by repo-visibility checks (#2642).
- **Hex: hosted repositories serve the signed registry format `mix deps.get` requires**, instead of JSON (#2641).
- **CocoaPods: hosted repos serve a CDN layout** so `pod repo add-cdn` and `pod install` work (#2638).
- **Terraform: hosted providers are usable by real clients** — fixed the advertised download URL and opened the network mirror beyond remote-only (#2647).
- **Helm: remote/proxy repositories serve provenance**, so `helm pull --verify` works against them (#2653).
- **RPM: fixed hosted repositories returning 404** (#2580).
- **RPM: repodata signing now produces a verification chain dnf accepts** (#2636).
- **RPM: remote sync and on-demand package search (phase 2)** (#2357).
- **RPM: large-repodata tier pinned with a regression test** (#2664).
- **Metadata signing configuration rejects key types that can never produce verifiable signatures (RSA)** (#2651).
- **Git LFS: file locking works** instead of returning 500 on every lock request (#2627).
- **Mixed-backend deployments no longer 404 their own objects** — Maven storage keys are qualified by backend (#2671).
- **Azure deployments no longer silently lose all remote/proxy repositories** (#2670).
- **Invalid `STORAGE_BACKEND` values now fail startup** instead of silently falling back to filesystem and disabling cross-tenant guards (#2669).
- **Docker repository storage usage now includes OCI layer blobs** (previously showed KiBs for GiB repos) (#2625).
- **OCI blob report endpoint no longer returns 500** (#2626).
- **Per-repository storage usage now includes proxy-cached storage** (#2218).
- **Download statistics no longer silently miss artifact downloads** (#2260).
- **Maven/Ivy downloads on S3 use presigned-URL redirects** instead of streaming through the backend (#1945).
- **Proxy metadata buffering is now capped across concurrent requests**, not just per request (#2665).
- **Proxy-cached artifacts are now cataloged in the database**, fixing storage accounting, download tracking, and cache visibility (#2270).
- **Warm proxy-cache hits no longer pay 300ms+ per request** in repeated cache-metadata roundtrips (#2301).
- **Virtual Maven repositories no longer iterate all members on every `maven-metadata.xml` request** (~200ms saved per request) (#2302).
- **Remote repositories support a custom User-Agent on outbound requests** (#2080).
- **Service-account-typed private-repo permission grants are honored** instead of silently ignored (#2433).
- **Service-account grants are validated for principal type/ID correspondence**, and direct service-account grants now surface on group targets (#2503).
- **SAML: assertion groups map to group memberships**, matching OIDC behavior (#2333).
- **Web UI toggle for the OIDC "map groups to groups" setting** (#1879).
- **Guest-access guard recognizes format-specific credentials** (conda URL token, NuGet API key) when guest access is disabled (#2655).
- **Docs: SSO admin guidance on group mapping** — trusting the IdP's group taxonomy and privileged-group name collisions (#2493).
- **In-place upgrade from 1.5.x boots cleanly** — the migration-ledger divergence between the 1.5.x hotfix line and 1.6.0 is reconciled automatically at startup (#2686).
- **CI now fails the build when main and a release branch reuse a migration version number for different content** (#2689).
- **Fixed a column-decoding error when migrating to 1.2.0-era schemas** (#2456).
- **Curation package detail routes no longer 404 for everyone** (#2447).
- **Quality-checks admin page works** — the backend now supports list-all and repository filtering (#2419).
- **Backend Trivy health check works with Docker service hostnames**, not just direct IPs (#2478).
- **Release-gate CVE image check no longer flakes on Trivy vulnerability-DB rate limits** (#2167).
- **docker-publish tag verification no longer wrongly requires a version-matched scanner-adapter image** (#2428).
- **Fixed a source-scan false failure on clean builds** (streaming-invariant allowlist drift) (#2491).
- **Fixed a flaky test that asserted row order from an unordered query** (#2663).

## [1.5.3] - 2026-07-12

Security hotfix: closes a systemic cross-repository authorization gap where artifact/repository-scoped routes returned (and in some cases mutated) another repository's data for any authenticated non-member. Found by red-team; the HIGH-impact subset (vulnerability/SBOM data) is fixed here, with the remaining medium/low routes tracked for the next feature release (#2443).

### Security

- **Cross-repo vulnerability & SBOM authorization enforced** (#2439): the security-scan reads (`GET /security/scans`, `/security/scans/{id}`, `/security/scans/{id}/findings`, `/security/artifacts/{id}/scans`), the SBOM reads (`GET /sbom/{id}`, `/sbom/{id}/components`, `/sbom?artifact_id=`, `/sbom/by-artifact/{id}`, `POST /sbom/{id}/convert`), and the SBOM writes (`POST /sbom` generate, `POST /sbom/cve/status/{id}`) now enforce the canonical repository-visibility gate — token repo-scope + admin bypass + private-repository membership — returning an existence-hiding 404 to non-members. Previously any authenticated user could read another repository's CVE findings and SBOM inventory, and generate/convert SBOMs on artifacts in repositories they could not see. The `require_repo_access` helper was upgraded to check membership (not just token scope), and the scan-not-found responses were unified to remove an existence oracle. CVE-status writes remain admin-gated (#2321). Public repositories stay public; members and admins are unaffected.
- **Cross-repo quality-check metadata authorization enforced** (#2437): the artifact-scoped quality-check routes (`GET /quality/checks`, `/quality/checks/{id}`, `/quality/checks/{id}/issues`, `/quality/health/artifacts/{id}`) now apply the same repository-visibility gate, so quality-check results/scores no longer leak across repositories.

### Added

- **Admin quality-checks list-all endpoint** (#2419): `GET /api/v1/admin/quality-checks` (admin-only, paginated, filterable by `repository_id`/`artifact_id`/`status`) backs the admin quality-checks view. The artifact-scoped `GET /api/v1/quality/checks` contract (400 without `artifact_id`) is unchanged.

## [1.5.2] - 2026-07-12

Security hotfix: closes a token-exchange privilege-escalation where a scope-restricted API token could be exchanged (via docker-login `/v2/token` or Conan `users/authenticate`) into an unrestricted JWT — a read-only token could gain write/delete. Also completes the GHSA-vvc3 write-scope enforcement on the eight remaining format publish handlers.

### Security

- **Token-exchange no longer launders away an API token's action-scope ceiling** (#2430): a JWT minted in exchange for an API token (Conan `users/authenticate`, OCI `/v2/token` docker-login, Bearer/JWT-as-password swaps, and refresh rotation) now carries the presenting token's action-scope allowlist on a new `Claims.scopes` claim. `AuthExtension::has_scope` keys the decision on that inherited ceiling rather than on `is_api_token`, so a read-only token can no longer be exchanged into a write/delete-capable credential. Interactive password/TOTP logins and federated CI-OIDC continue to mint action-unrestricted tokens (`scopes: None` ⇒ full), and the download-ticket path (`scopes: Some([])`) still denies. Complements the repo-scope ceiling from #2290.
- **Write-scope enforcement on the eight deferred format publish handlers** (#2417, GHSA-vvc3-h39c-mrq5). The Ansible, Chef, CocoaPods, JetBrains, pub_registry, Puppet, SBT and VSCode publish paths authenticated the caller but did not check the token's declared scopes, so a service-account/PAT token minted with `scopes: ["read"]` could push. These handlers now call `require_auth_basic_scope(auth, "<realm>", "write")` — the same pattern already applied to the ~25 other format handlers — so a read-scoped token receives `403 Token does not have required scope: write`. The pub upload-URL preflight (`GET .../versions/new`) is likewise gated so a read token cannot obtain an upload URL. JWT user sessions are unaffected (scopes apply only to SA/PAT tokens); pull/list paths remain ungated. Completes the GHSA-vvc3-h39c-mrq5 follow-up.

## [1.5.1] - 2026-07-11

Patch release: eleven validated bug/security fixes across audit correlation, observability noise, virtual-repository resolution and scanning, the Composer and OCI/npm surfaces, and the SSRF trust model — each replicated, fixed, and re-verified on an isolated stack.

### Added

- **Server-side npm scope policy for virtual repositories** (#2327): an optional, admin-defined per-repository scope allow-list (`npm_allowed_scopes` / `npm_allow_unscoped`, stored in `repository_config`) with new admin-only, Remote-only `PUT`/`GET /api/v1/repositories/{key}/npm-scope-policy` endpoints. npm virtual resolution now skips a policy-mismatched Remote member before querying it, so a broad linked remote can be restricted to specific scopes. Default behavior is unchanged (no policy ⇒ allow all); tarball-path, direct-remote, glob patterns and the web UI are tracked as a follow-up (#2424).

### Security

- **CI-OIDC identity-provider fetches are now context-aware** (#2405): the CI-OIDC discovery/JWKS fetches use the SSO trust-class client, so a CI-OIDC identity provider on a private address is reachable when the operator opts in via the existing `SSO_ALLOW_PRIVATE_IPS` / `AK_SSRF_ALLOW_PRIVATE_CIDRS` knobs — completing the #2380/#2389 context-aware SSRF work for the last identity-provider surface still pinned to the fail-closed upstream default. With no toggle set, behavior is unchanged (fail-closed), and cloud-metadata, loopback and link-local addresses remain hard-blocked regardless of any toggle.
- **OCI `/v2/token` exchange enforces API-token scope** (#2290): the minted OCI bearer now carries the requesting token's `allowed_repo_ids`, and a repo-scope check is applied on the OCI write and scan-pull paths ahead of the admin/public bypass — bringing OCI to parity with the REST token-scope gate (previously only backstopped by downstream per-repo checks).
- **Vulnerable transitive crates bumped** (#2272): openssl 0.10.78→0.10.80, tar 0.4.45→0.4.46, opentelemetry_sdk 0.28→0.32 (paired set), cmov 0.5.2→0.5.4, and rand patch bumps — clearing the trivy/RUSTSEC advisories (9→0) with no source changes. Supersedes dependabot #1943/#1926.

### Fixed

- **Audit events preserve the request correlation ID** (#2414) instead of generating an unrelated one, so an action and its audit record can be tied together.
- **Admin Settings environment badge is sourced from runtime config** (#2325) rather than always showing "Production".
- **Observability noise reduced** (#2308): the duplicate `http_request` span and blanket ERROR-level logging that misclassified routine 4xx responses as failures are removed.
- **Maven virtual downloads consult Remote members** (#2328, regression of #1562): a remote-only artifact with a valid cached copy is served from a virtual repository instead of 404ing.
- **Composer remote/proxied metadata emits an absolute `dist.url`** (#2370): `#1652`-rewritten metadata is now RequestBaseUrl-prefixed so Composer clients fetch through the proxy cache instead of falling back to a source clone. (Resolves the #2383 warm-dist-cache report, which was a release-gate test-path issue, not a caching defect.)
- **Repository-wide scans enumerate virtual-repository members** (#2228): triggering a scan on a virtual repository now resolves its member repos (recursively) and enqueues their artifacts instead of reporting "0 artifacts". The local-repository path is unchanged.

## [1.5.0] - 2026-07-11

Value-proposition parity release: the enterprise pitch claims are now true in code — a functional audit trail, per-download attribution, CVE blast-radius, first-class artifact versioning, tightened RBAC, and a coherent connect-time SSRF story — each surfaced in the web UI.

### Added

- **Functional audit log** (#2366): security-relevant events — artifact upload/download/delete, user/role/repository CRUD, and permission-denied — are now recorded, with a new admin-only query endpoint `GET /api/v1/admin/audit` (filters + pagination). The response embeds the actor username (#2392), resolving deleted/system actors as null.
- **Per-download IP + user telemetry** (#2365): the real client IP (XFF-aware, trusted-proxy-CIDR gated) and user are recorded for downloads across all 40+ formats into `download_statistics`, with new admin endpoints `GET /api/v1/admin/downloads` (+ `/by-ip/{ip}`, `/by-user/{user_id}`). Recording now covers every content-serving path, including the generic `/artifacts/{path}` and virtual-member-local download branches (#2394, #2398). This also closed the #2023 trusted-proxy XFF-spoofing gap on the download path.
- **CVE blast-radius** (#2364): admin endpoints `GET /api/v1/admin/security/cve/{cve_id}/blast-radius` and `/artifact/{artifact_id}/blast-radius` join CVE findings to the users and IPs that downloaded the vulnerable artifact, plus a bounded per-repository access-scope classification (`public` / `restricted_acl` / `restricted_roles`) that flags "public repo — everyone exposed" without enumerating unbounded populations.
- **First-class versioning for generic and mlmodel artifacts** (#2367): opt-in per-repository `versioning_enabled` flag; re-uploading to an existing path now appends an immutable revision instead of overwriting in place. New `GET /api/v1/repositories/{key}/versions/{path}`, a `?version=<rev|label>` selector on download/metadata, and `ArtifactVersionResponse` (revision, version_label, uploaded_by). Existing single-copy artifacts and the release-immutability control are unaffected (flag defaults off).
- **RPM proxy hardening — Phase 1** (#2354): connect-time SSRF IP validation, RPM cache classification, content-integrity verification, and mirrorlist rejection for RPM upstream/proxy repositories.
- **Filesystem and incus scans routed through the scanner-adapter** (#2363): restores first-class Trivy filesystem coverage on the hardened (CLI-free) image by adding a filesystem-scan endpoint to the in-repo scanner-adapter and routing `TrivyFsScanner`/`IncusScanner` through it over HTTP (fixes #2362). Adapter-unavailable degrades gracefully to `not_applicable` (grype still covers), preserving the no-grade-floor contract.

### Changed

- **Repository RBAC and admin-surface authorization tightened** (#2321): the generic-REST and OCI artifact write/delete paths now enforce action-specific permissions (a read-only grantee can no longer write or delete) instead of collapsing read/write/delete into one predicate; the dependency-track integration, SBOM CVE-status updates, webhook creation, scan triggering, and cache invalidation are gated to admin / repo-admin; each new denial emits a `PERMISSION_DENIED` audit event. Promotion and peer-replication paths are unaffected.

### Security

- **SSRF gate is now context-aware** without weakening protection. Operator-configured internal-service clients (scanner-adapter, openscap, dependency-track) reach their private-network endpoints via a dedicated `TrustedInternal` client (#2389), and webhook delivery and SSO/OIDC identity-provider fetches honor their existing `WEBHOOK_ALLOW_PRIVATE_IPS` / `SSO_ALLOW_PRIVATE_IPS` toggles at connect time (#2380). Attacker-influenceable upstream/proxy/plugin paths stay fail-closed, and cloud-metadata, loopback, and link-local addresses remain hard-blocked in every context. This resolves the #2354 over-block that had blocked private-network scanner adapters and toggle-enabled webhook/SSO targets.
- **Trivy code-scanning backlog burned down** (#2391): bundled scanner tools bumped to releases built on a patched Go toolchain (trivy 0.71.2 → 0.72.0, grype v0.114.0 → v0.115.0), the scanner-adapter recompiled with Go 1.26.5 and moved to alpine 3.24, and `docker-publish` now cache-busts the package-refresh stages so OS errata and the Grype DB re-resolve on every publish instead of serving a stale layer. The frozen `:latest-alpine` scheduled scan (a suspended image, source of 143 of the 188 alerts) was removed, and `.trivyignore` refreshed to the current per-CVE residual set.

### Fixed

- **Versions API serializes `uploaded_by`** (#2397) so the version-history UI can attribute each revision.
- **OpenAPI param de-duplication** on the downloads `by-ip`/`by-user` endpoints (#2409) so the generated SDK builds cleanly across all languages.

## [1.4.2] - 2026-07-10

### Fixed

- **Composer Local/hosted repositories now emit an absolute `dist.url`** (#2361). The Composer metadata (`packages.json`, `p2/`, and legacy `p/`) previously returned a root-relative `dist.url` (`/composer/{repo}/dist/...`) with no scheme/host, so `composer install`/`update` failed to download the archive. The Composer handler now threads the external base URL (via the `RequestBaseUrl` extractor, honoring `AK_EXTERNAL_URL` / `X-Forwarded-Host` / Host) — matching npm/cargo/nuget/oci/gitlfs. Also fixed `dist.shasum` (Composer verifies it with `sha1_file()`; it's now emitted empty since AK stores SHA-256, with integrity carried on `dist.reference`). Remote/proxied Composer repos are unaffected (separate rewrite path, tracked in #2370).

## [1.4.1] - 2026-07-10

### Fixed

- **OpenAPI spec aligned with runtime behavior on six endpoints** (#2335), and 5 OpenAPI schema-name collisions resolved with the collision ratchet emptied (#2340).
- **Auth returns 503, not 401, from the guest-access guard when the auth path is overloaded** (#2315), and the credential watermark check now tolerates app-vs-DB clock skew (#2350).
- **Authenticated users are granted the anonymous read baseline on public repositories** (#2346), so logging in can no longer see less than an anonymous client.
- **Azure storage**: the health-check HEAD probe is signed in Shared Key mode (#2295), SAS signed start is backdated 15 minutes to tolerate clock skew (#2294).
- **npm**: raised the packument cache buffer cap (#2313).
- **Swift**: release versions are aggregated across all virtual repo members (#2342).
- **Signing**: `key_type` is normalized so RSA algorithm variants no longer 500 at key creation (#2344).
- **Scanner**: records `not_applicable` when the trivy CLI is absent instead of failing the scan (#2324).
- **Uploads**: concurrent duplicate chunk uploads are serialized (#2316, #2348).
- **OCI**: the packages catalog is populated on manifest push (#2337).
- **Downloads**: presigned-redirect downloads are recorded in `download_statistics` (#2349).
- **Migration**: `storage_backend` is persisted for auto-provisioned repos (#2336, #2338).
- **PyPI**: virtual-repo dependency-confusion isolation is now priority-aware (#2351).

### Security

- **Scan-policy `max_severity` and `repository_id` are validated before insert** (#2345).

## [1.4.0] - 2026-07-09

### Added

- **Keyless CI/CD token exchange via OIDC** (#1142) -- CI pipelines can exchange a workload OIDC token for an Artifact Keeper token without long-lived secrets.
- **OCI: OAuth2 refresh-token grant and `access_type=offline` support** (#1166), and `/v2/token` accepts repeated `scope` query params (kaniko cross-repo push) (#2276).
- **OIDC providers can opt into accepting ID tokens signed with RSA keys shorter than 2048 bits** via the new per-provider `allow_legacy_rsa_keys` flag (migration 144, defaults to `false`). The strict signature path goes through `jsonwebtoken` + `aws_lc_rs`, which enforces RFC 7518's effective baseline by rejecting every RSA modulus below 2048 bits. That keeps Artifact Keeper aligned with NIST SP 800-131A Rev.2 (RSA < 2048 disallowed since 2014) and OWASP ASVS 4.0 V6.2.5 by default, but leaves legitimate operators stranded when the IdP still publishes a 1024-bit RSA key on its JWKS (e.g. Lark AnyCross's OIDC offering at time of writing). When the flag is enabled on a specific provider, the ID token signature is verified through a restricted manual `rsa` + `sha2` PKCS#1 v1.5 path that accepts RS256, RS384, or RS512 only; PS* (RSA-PSS), ES* (ECDSA), and HS* (HMAC) tokens never engage the legacy branch regardless of the flag. The audience, issuer, expiry and nonce checks remain in force on the fallback path. Operators who do not turn the flag on see the strict path's exact pre-144 behaviour with no change.
- **PyPI: non-PEP-503 upstream indexes** supported via the `upstream_index_path` repo config field (#1995).
- **Dart: Pub upload protocol compliance and Remote/Virtual metadata resolution** (#2008).
- **Audit: TOTP, password-change and session-invalidation auth events are recorded** (#2263).
- **npm: cross-replica single-flight for stale-while-revalidate background refreshes** (#2256).
- **Storage: tracing spans around storage backend operations** (#1947).
- **Proxy quality gate adopts the package age gate, with coverage** (#2066).
- **Canonical `/users/me` self-service aliases** for own record, tokens and password (#2265).

### Changed

- **Repo-scope authorization internals**: `AuthExtension.allowed_repo_ids` and `ApiTokenValidation.allowed_repo_ids` became an explicit `AccessScope`, and token issued-at folded into `AuthExtension` (#2206, #2257, #2259, #2261).

### Fixed

- **Sync**: `sync_tasks` are claimed before peer side effects, stopping multi-replica double execution (#2221).
- **apt**: simplified InRelease cache invalidation (#2232); virtual `dists` no longer swallows a 502 cap-exceeded into an empty 200 (#2255).
- **npm**: canonical `/-/` tarball paths resolve in the generic download/delete handlers (#2280).
- **OCI**: virtual-repo blob cache-fill streams instead of buffering at the 8 MiB metadata cap (#2281).
- **Composer**: `uid` added to inline root `packages.json` version objects (#2250, #2279).
- **Conan**: recipe/package files-list proxied to upstream for remote repositories (#2288).
- **SBOM**: SBOM/scan is no longer offered for proxy-cached remote artifacts (#2291).
- **Azure storage**: Content-Length left empty in the Shared Key string-to-sign for zero-length bodies (#2293).
- **Dependencies**: cleared cargo-audit advisories (crossbeam-epoch RUSTSEC-2026-0204, crypto-bigint yank) (#2157).

### Security

- **Sync-policy read/preview routes require admin** (#2252), and **build read endpoints require authentication** (#2254).
- **`/health` detail and gRPC reflection are gated behind config, off by default** (#2253), closing version/topology information leaks.
- **Dedicated strict rate limit for the login endpoint** (#2297), separating login throttling from the general limiter.

## [1.3.0] - 2026-07-06

Streaming-focused minor release: uploads and pull-through downloads stream end-to-end through the storage backend, blob GC becomes two-phase and idempotent, and the auth/audit epic (#1617) lands its first phases.

### Added

- **Streaming uploads to storage** for chef/ansible/pub multipart (#2176), helm chart metadata-parsing uploads (#2180), and pypi/nuget via a shared content-addressed primitive (#2199), part of the #1608 streaming-invariant effort.
- **CI streaming-invariant enforcement gate** (Phase 1 of #1608) (#2171), and `StorageBackend::put_stream` is now a required trait method so backends can't silently inherit in-memory buffering (#2207).
- **Storage GC: `pending_delete` marker with idempotent blob GC delete and two-phase mark-and-sweep** (#1660; #2209, #2213).
- **Audit: federated logins and API-token lifecycle events are audited** (#1617 Phase 1) (#2187); `require_admin` consolidated onto the `AuthExtension` gate (#1617 Phase 3) (#2185); new `AccessScope` enum for repo-scope authorization (#2195) threaded through SBOM and search read paths (#2194, #2205).
- **Composer: upstream dist artifacts are cached for remote/proxy repositories** (#2204).
- **npm: computed packuments cached with stale-while-revalidate** (#2166).
- **SSO end-to-end regression harnesses**: OIDC login/callback against a mock IdP (#2210) and SAML ACS signed-assertion (#2214).

### Fixed

- **Proxy**: cross-replica single-flight for the pull-through cache via Postgres advisory lock (#2172); pull-through artifact-blob downloads stream (#2178); buffered upstream metadata reads are capped (#2181, #2191); remaining buffered blob fallback paths stream (#2203); quality-gate content reads stream (#2174).
- **OCI**: referenced `oci_blobs` are locked FOR UPDATE before ref-insert (#1660, #2190); upload cleanup-journal hygiene plus a Range integration test (#1533, #1410, #2202).
- **Repositories**: OCI upload temp objects are purged after repo-delete and the listing query is batched (#2198).
- **Storage**: GCS rewrite-loop token refresh; S3 copy digest-check and abort-on-drop (#2200).
- **Cache**: authorization-cache invalidation fans out across replicas (#2169).
- **Metrics**: unmatched HTTP paths collapse to a single label, preventing scanner-driven cardinality explosion (#2217).
- **Auth cookies**: `Secure` flag gated on explicit `AK_ENFORCE_HTTPS` (#2233, #2234) with HTTPS auto-detection via `X-Forwarded-Proto` (#2236).
- **Scanner adapter**: registry pull credential scoped to the target image so the trivy DB pull is not rejected (#2238; scanner-adapter 1.1.0, #2242).
- **Dependencies**: bcrypt bumped to 0.19.2 (RUSTSEC-2026-0199) (#2216).

### Security

- **Global security-policy create/update/delete requires admin** (#2223).
- **Direct artifact delete is gated on promotion-only release repos** (#2239).
- **A vulnerability scan that errors now fails closed instead of reporting the artifact clean** (#2240).

## [1.2.5] - 2026-07-03

### Added

- **SAML providers can opt into emitting an absolute `AssertionConsumerServiceURL`** via the new per-provider `use_absolute_acs_url` flag (migration 139, defaults to `false`). The `saml_login` and `saml_acs` handlers historically emitted the relative path `/api/v1/auth/sso/saml/<id>/acs` in the AuthnRequest's `AssertionConsumerServiceURL` attribute and used the same string when validating the IdP-asserted Destination/Recipient on the ACS callback. Stricter SAML 2.0 IdPs (and certain enterprise deployments) reject a relative ACS URL outright, so those IdPs were unreachable without rebasing the URL onto the SP origin. When the new flag is enabled on a SAML provider, the ACS URL is prefixed with the operator-configured `AK_EXTERNAL_URL` (a trusted process-env source, never derived from `Host` / `X-Forwarded-Host` request headers — those would let an attacker steer the signed AuthnRequest's `AssertionConsumerServiceURL` toward a hostile origin). If the flag is on but `AK_EXTERNAL_URL` is unset, the handler fails closed to the historical relative form and logs a warning so the misconfiguration is visible. Existing providers keep their pre-139 wire format unchanged because the flag defaults to `false`.
- **Dependency-Track now finds CVEs for purl/language dependencies via an opt-in OSV mirror** (closes #1972). On first bootstrap, `docker/init-dtrack.sh` enabled only the NVD mirror (`nvd.api.enabled`). NVD matches by **CPE**, so purl-based application dependencies (Maven, PyPI, npm, Go, NuGet, RubyGems, crates.io, Packagist, ...) produced few or no findings and Dependency-Track looked empty for application code even though Artifact Keeper submits well-formed purls in the CycloneDX BOM (`build_dependency_info_from_packages` -> `pkg:{type}/{name}@{version}`, `format_to_purl_type` covers every OSV ecosystem). OSV matches by **purl** and aggregates GitHub Security Advisories, PyPA, RustSec, Go and npm advisories. A new `DTRACK_INIT_OSV_ENABLED` toggle (default off, mirroring the opt-in resource posture of the bundled DT itself, #1432) enables `google.osv.enabled` for the ecosystems Artifact Keeper hosts; the ecosystem list is overridable via `DTRACK_INIT_OSV_ECOSYSTEMS`. Wired through `docker-compose.yml` and documented in `.env.example`. Covered by new phases in the `docker/test-init-dtrack.sh` regression harness (NVD-still-on + OSV-off by default, OSV-on with the full ecosystem list under the toggle, and a custom ecosystem override).

### Changed

- **Performance pass on hot paths**: Maven HTTP cache headers, moka in-memory cache and a GAV index (#2079, #2100), plus skipping the always-missing artifacts lookup for remote repos (#2104); negative-caching of repos with no upstream credentials (#2106); quarantine per-repo config resolution cached off the hot path (#2109); `last_login_at` DB writes throttled to reduce WAL pressure (#2133); catalog upsert collapsed to two statements on the cached-remote write path (#2134); cold-negative virtual fan-out parallelized using the member's format (#2069, #2074).
- **The scanner-adapter image now carries its own independent semver** (starting at 1.0.0) (#2123).

### Fixed

- **SAML SP now binds the IdP-asserted response delivery target and enforces single-use InResponseTo** (#2096). The ACS callback previously validated status, issuer, audience, time window and XML signature, but ignored the response `Destination`, the assertion `SubjectConfirmationData` `Recipient`, and the `InResponseTo` correlation to the AuthnRequest it issued. The SP now (a) rejects a response whose `Destination` or assertion `Recipient` does not match this SP's own ACS URL — enforced only when a trusted ACS URL is available (`AK_EXTERNAL_URL` set) and the IdP actually asserted the attribute, so permissive IdPs and deployments without a trusted external URL are unaffected; and (b) requires every response to carry an `InResponseTo` that matches a pending, unexpired, not-yet-consumed AuthnRequest the SP itself minted, persisted as a single-use SSO session. This closes SAML response/assertion redirection and replay, and rejects unsolicited (IdP-initiated) assertions — AK is SP-initiated only. No new migration (reuses `sso_sessions`); no wire-format change for the flag-off default path.
- **Overload shedding**: sqlx pool-acquire timeouts map to 503 instead of 500 across the error type (#2101) and format handlers (#2102), and to 503 instead of 401 in the auth pre-check (#2139).
- **Quarantine**: upload-time hold applied across all hosted formats via a shared helper (#2138), and the presigned-redirect fast path is gated on quarantine hold (#2075, #2137).
- **SSO**: opt-in strict SSO enforcement flag (#2018) so operators can disable the local-login break-glass; flaky scan-dedup DB test stabilized (#2000) (#2131).
- **Admin**: promote scope is grantable, webhook-secret errors return 4xx, signing-key repository validation (#2127).
- **Auth/Conan**: OIDC auto-create toggle honored; Conan package-search route added (#2128).
- **Dart/Maven**: `dart pub publish` handshake fixed; Maven proxy reserved-prefix probe (#2130).
- **Proxy**: multipart ETags tolerated in pull-through cache revalidation (#2132).
- **Terraform**: hardened mirror URL handling (#2094).
- **Scanner**: image-vuln scanners gated on OCI config mediaType (#2113).
- **Migration**: report served for completed jobs; typed 4xx on source-client build failure (#2136).
- **Peers**: Maven package metadata replicates to peers (#2153).
- **Compose**: hardened backend starts without a `/bin/sh` entrypoint (#2126).

### Security

- **quick-xml bumped to 0.41 for XML-parser DoS hardening** (#2160).

## [1.2.4] - 2026-07-01

### Added

- **In-house multi-arch Trivy Harbor scanner adapter** (#2091, #2092) -- container-image scanning moves to a dedicated adapter sidecar, with the release gate verifying the scanner-adapter image before releasing (#2115).
- **Private-repo image scans via short-lived repo-scoped tokens** (#2098).

### Fixed

- **Migration**: Artifactory FEDERATED repos map to the Local repository type (#2030); leading `/` stripped from Nexus asset paths (#2026, #2037); absolute `storage_path` stored for auto-provisioned repos (#2029).
- **PyPI**: PEP 691 JSON served with upload-time for proxied simple indexes (#1944).

### Security

- **Container images are scanned via the Harbor adapter and fail closed** (#2088, #2090) -- fixes the trivy false-clean regression introduced by the runtime-image hardening in #2059, where image scans silently reported clean.
- **Per-member authorization enforced on PyPI virtual repo downloads** (#2073, #2087).
- **API token writes are blocked in demo mode** (#2071).
- **Startup requires absolute `STORAGE_PATH`/`SCAN_WORKSPACE_PATH` for the filesystem backend** (#2089).

## [1.2.3] - 2026-06-29

### Fixed

- **Storage health probe is concurrency-safe with reduced probe churn** (#2050).
- **Curation rules OpenAPI schema reconciled with the handler, plus a by-id read** (#2052).
- **Proxy-cached Maven artifacts are indexed into the package catalog** (#1999, #2051), and the proxy cache is purged on repository delete (#2055).
- **Scanner: local OCI images scan with Grype without internal registry auth** (#2054).

### Security

- **Repository visibility honors fine-grained permission grants** (#2049).
- **Promotion-only gate enforced on all format-native publish paths** (#2045), closing direct-publish bypasses of the release-promotion workflow.
- **Rate limiting: `X-Forwarded-For` is only trusted from configured trusted-proxy CIDRs, with a `ConnectInfo` assertion at startup** (#2046), so clients can't spoof their source IP to evade limits.
- **Hardened backend runtime image** (#2059) and wasmtime bumped 36.0.11 -> 36.0.12 (RUSTSEC-2026-0188) (#2065).

## [1.2.2] - 2026-06-25

Security-focused patch release hardening authorization, the promotion workflow, and scanner correctness.

### Security

- **Admin authorization derives from the server-side role, not the JWT claim** (#1939).
- **Object-level authorization enforced on webhook endpoints** (#1942).
- **Promotion workflow hardening**: `promotion_rules` enforced on manual single and bulk promote (#1940); an approved approval request is required and consumed before promote (#2006); promotion-only repos reject all direct artifact uploads (#2005); rule authoring restricted to admins with `auto_promote` defaulting to false (#2013); tenant-ownership check on the promote target plus separation-of-duties on approval (#1961).
- **Release-immutability can no longer be swapped via delete + re-upload of different content** (#1941).
- **`/system/config` disclosure restricted and fail-open auth gates closed** (#1960).
- **Per-token ownership enforced on repository token read/revoke** (#1974).
- **Per-user/tenant ownership enforced across the migration subsystem** (#2007).
- **Peers: PUT labels authorized (BOLA/cross-tenant) and self-referential probes return 4xx** (#1963).
- **Outbound SSRF validator blocks the RFC 6598 CGNAT range (100.64.0.0/10)** (#1968).
- **Rate limiting**: search rate limit keyed per authenticated user, not per IP (#1962); the login rate limiter is per-identifier so junk floods cannot lock out all accounts (#1979).
- **`repository:admin` required to change the proxy cache TTL** (#1985).
- **PyPI HTML escaping covers apostrophes** (#1981); quinn-proto bumped for RUSTSEC-2026-0185 (#1969).

### Fixed

- **Auth**: baseline `user` role seeded for federated role mapping (#1919); mandatory first-login password-change flow allowed through the 428 gate (#1993), with the self-lookup exemption anchored to the exact `/auth/me` route (#2014); LDAP env bootstrap no longer duplicates an existing provider (#1984).
- **SSO**: OIDC discovery may reach configured private-IP IdPs (#1891, #1953).
- **Scanning**: Grype accepts bare-string licenses (#1929); Trivy image scan targets qualified with the owning repository key (#1965); Grype OCI scans on docker repos use a repo-scoped image ref (#1903, #1952); OCI image indexes resolve to a concrete child platform before scanning (#1971, #1992); scan-dedup short-circuit made atomic under concurrency (#1989).
- **Maven**: `WWW-Authenticate` headers included in guest-access 401s (#1920); metadata resolution centralized for remote and virtual repos (#1922); hosted Maven repositories made production-ready (#1907); virtual fall-through to remote-only POMs pinned by test (#1562, #1986).
- **npm**: abbreviated metadata served with response compression (#1931, #1932).
- **Proxy**: proxy-cache downloads presign via the no-prefix backend instead of streaming (#1555, #1917).
- **Peers/migration**: repository-to-peer assignment repaired (DATABASE_ERROR) (#1954); migration-connection/job create repaired (DATABASE_ERROR) (#1959).
- **Lifecycle**: `repository_id` required at policy create for repo-scoped policy types (#1850, #1951); the sole `oci_tags` row protecting a live image is retained (#1987).
- **Quota**: `quota_bytes` 0/null treated as unlimited with corrected usage accounting (#1970).
- **Robustness**: 400 on over-length artifact paths and migration-connection input (#1967); free-text search sanitized into a valid tsquery so search endpoints cannot 500 on metacharacters (#1991).
- **Uploads**: `artifact_version` derived on chunked uploads to format repos (#1983).
- **Conan**: recipe/package revisions resolved from upstream for remote repos (#1990).
- **Base URL**: external base URL derived from the HTTP/2 authority (#1921).
- **OpenAPI**: quarantine reject given a unique operationId, unblocking SDK publishing (#1937).

### Changed

- **Docker images hardened**: bundled trivy bumped to 0.71.2, base OS errata refreshed, residual scanner CVEs documented (#2004).
- **Docs**: `QUARANTINE_ENABLED` mode documented (#1976); `tag_pattern_keep` clarified as a deletion policy, not protection (#1980); Bearer vs Basic auth asymmetry documented for `/api/v1` and format endpoints (#1978); `PORTFOLIO_MANAGEMENT` added to the Dependency-Track permission hint (#1977).

## [1.2.1] - 2026-06-21

Large patch release: credential-invalidation hardening (sub-second token-invalidation precision), a broad authorization/security wave, streaming downloads across formats, proxy-cache correctness, and multi-format fixes.

### Security

- **PyPI virtual repositories now isolate locally-owned project names by default (PEP 708 dependency-confusion mitigation)** (#1600). Previously, when a local member owned a project name, a virtual repo could union an unrelated upstream package of the same name into its `/simple/` index and serve it on download, so an unpinned `pip install <name>` could resolve to the higher public version (a supply-chain hole). A PyPI virtual now serves only the owning member's distributions for a locally-owned name, in both the simple index and the download, unless an operator declares a PEP 708 `tracks` relationship for that project. The Simple API now advertises v1.2 and emits `meta.tracks` / `pypi:tracks` where declared.

  **Behavior change (action may be required):** if you intentionally relied on a mixed virtual unioning a local project's versions with the same project upstream (e.g. split version ranges of the *same* package), declare it via the new endpoint so the union is restored:

  ```
  PUT /api/v1/repositories/{local_repo_key}/pypi-tracks/{project}
  { "tracks_url": "https://pypi.org/simple/{project}/" }
  ```

  Names a local member does not own are unaffected and continue to proxy normally.
- **Authorization hardening across the API surface**: authentication required for group read endpoints (#1756); admin required for plugin install/lifecycle (#1759), curation policy writes (#1760), direct artifact promotion (#1761), repository signing-key management (#1762), mutating `/quality/*` routes (#1805, #1814), and HTTP license-policy create/delete to match the gRPC gate (#1869); per-repo authorization on artifact access -- private repos are members-only (#1764) -- and on `/tree` and `/search` (#1803, #1813); per-member authorization on virtual repo downloads (#1804, #1816); private-repo authorization when minting repo-scoped tokens (#1783, #1786); repository write authorization on chunked upload sessions (#1833, #1834); repository visibility on artifact label endpoints (#1835) and the packages listing/detail (#1836); write authorization on OCI v2 blob/manifest writes (#1837); repository-admin permission for virtual-repo member mutations (#1830); repository tenant ownership enforced on all write paths independent of fine-grained RBAC (#1867); admin required for federation writes with the SSRF allowlist applied to peer `endpoint_url` (#1868); rule-less private repositories no longer default-allow in the native middleware (#1802, #1817); the anonymous repo-existence oracle on native paths is closed (#1808, #1812).
- **SSRF hardening**: hostnames in the URL allowlist resolve to block internal targets (#1763); the allowlist applies to LDAP provider config and connectivity tests (#1831), OIDC issuer URLs on config write and discovery fetch (#1832), and Artifactory `downloadUri` (#1423, #1750).
- **Session/credential hardening**: sub-second precision for credential-change token invalidation with a millisecond issued-at claim (#1933, #1934); tokens invalidated on admin/role privilege change (#1821, #1827); the refresh-token family is revoked on logout (#1807, #1811); `must_change_password` enforced in the auth middleware (#1818, #1824); the TOTP 2FA verify path hardened (#1819, #1820, #1822, #1825) and TOTP bcrypt offloaded to the capped auth path with a global concurrency/timeout backstop (#1897); correct credentials are allowed past the failed-login lockout (#1871); admin break-glass local login preserved when SSO is enabled (#1873); JWT secret strength validated at startup in all environments (#1766, #1829, #1840).
- **Supply chain**: plugin install enforces signature verification and rejects unsigned WASM (#1892); deletion of immutable released artifacts is blocked (#1765) and the replication delete exemption requires a trusted identity (#1895); the quarantine package-age policy applies to remote/proxy downloads and is based on release date (#1841); Helm gzip/tar decompression capped to prevent DoS (#1806, #1815); group read endpoints scoped to caller membership/grants for non-admins (#1896).
- **PyPI dependency confusion**: local-precedence for the virtual simple index (#1600, #1738) with PEP 708 mitigation (#1613) -- see the headline entry above.

### Added

- **Per-artifact cache metadata on `GET /:key/artifacts/:path`** (#1541, #1542) and an HTTP endpoint for proxy cache invalidation (#1539, #1540).
- **Storage GC for OCI blobs**: read-only blob footprint report and orphan blob-layer reclaim via the new `manifest_blob_refs` table with push-write and startup backfill (#1408, #1409, #1621, #1635, #1641, #1655).
- **Proxy cache correctness**: immutable/mutable classifier with conditional revalidation (#1611, #1708, #1732) and single-flight coordination with streaming broadcast fan-out (#1631).
- **Proxy cache lookup observability** (#1263 follow-up) -- adds `ak_proxy_cache_lookups_total{repository, result}` Prometheus counter and explicit structured logs on every branch of the fresh proxy-cache read (`CacheStore::get` with `allow_stale = false`, behind `get_cached_artifact`). The `result` label is one of `hit`, `miss_no_metadata`, `miss_expired`, `miss_no_content`, `miss_checksum_mismatch`, or `error`, covering every previously-silent cache-miss reason. Only the fresh per-request lookup is counted; the stale-fallback body read is excluded so revalidated entries are never double-counted. Operators investigating "why are repeat fetches not getting cache hits" can chart `rate(ak_proxy_cache_lookups_total[5m])` by `result` and isolate the responsible branch without redeploying with debug logs. Repository label cardinality is bounded by the operator's repo count, matching the shape of existing `ak_artifact_downloads_total`. The previously-existing `Cache hit`/`Cache expired`/`Cache checksum mismatch` log lines are preserved as structured-field events so log scrapers that already grep for them keep working.
- **`promotion_only` write policy on repositories** blocks direct release writes (#1769).
- **OIDC**: custom `OIDC_NAME` (#1397), `OIDC_DEFAULT_ROLE`, and `OIDC_GROUP_ROLE_MAP` wired into the admin check (#1745).
- **`RATE_LIMIT_ENABLED` master off switch** (#1602, #1739).
- **`not_applicable` terminal scan status** (#1470, #1693).
- **Terraform provider network mirror protocol for remote repos** (#1730).
- **npm dist-tags are persisted and served, with `latest` derived by semver, not recency** (#1557).
- **Incus: `.tar.zst` accepted as a unified-tarball image extension** (#1296).
- **Debian hosted repositories made production-ready** (#1741).

### Changed

- **Streaming end-to-end**: artifact downloads stream across all package formats (#1393), OCI blob pulls and uploads stream through the storage backend (#1534, #1448), the generic local-serve path streams (#1713), remaining full-body buffers stream (#1736), and PyPI remote package downloads stream instead of buffering in memory (#1866).
- **Proxy subsystem refactor** into `CacheKeys`/`CacheStore`/`CachePersister`/`UpstreamClient` seams (#1618 series).
- **S3 adaptive multipart part sizing lifts the ~50 GiB upload ceiling** (#1701).

### Fixed

- **Promotion gates**: `block_unscanned` honored and fail-closed on unscanned artifacts for manual and auto promotion (#1643, #1648, #1728), fires on failed/pending scans (#1649, #1750); the open-CVE gate repointed from the never-populated `cve_history` to `scan_findings` (#1640); the scan-on-proxy gap is surfaced instead of silently skipped (#1274, #1650); legacy CVE-status acks fall back to `scan_findings` (#1561, #1692).
- **OCI**: manifests served and deleted by digest independent of tags (#1684); final blob key journaled so a failed `oci_blobs` commit can't orphan it (#1711); `manifest_blob_refs` backfill runs in the background at startup (#1642, #1749).
- **Repository delete**: storage objects deleted and FKs cascaded (#1598), with OCI keys excluded from the purge (data-loss regression fix, #1724).
- **Migration**: Nexus enum-type mapping (#1575); Maven/sbt group prefixes preserved (#1586); JFrog forward-port consolidated (#1420); artifact downloads spill under `STORAGE_PATH`, not `/tmp` (#1699, #1702); Nexus migrations unblocked for empty `include_repos` and repo-type aliases (#1902).
- **Format fixes**: npm cache metadata looked up under the upstream URL path (#1580, #1581), `/-/<meta>` requests routed to the npm meta handler (#1885); PyPI version-aware shadowing guard for virtual downloads (#1584); Swift virtual package lookups across members (#1554, #1734); Composer p2/p metadata for virtual repositories (#1715, #1740); Maven remote checksum lookups (#1549), empty artifact paths forwarded upstream (#1884), group-level plugin-prefix `maven-metadata.xml` via virtual repos (#1842), artifact-level metadata checksums proxied upstream (#1775, #1791); sbt short-filename parsing with storage-fallback security gates (#1900).
- **Format QA waves** resolving reported bugs across npm (#1774), cargo (#1777), rpm (#1780), pypi (#1773), helm (#1779), nuget (#1778), oci (#1776), composer (#1781), generic/go/conda (#1782), scanning-lifecycle (#1784), repos-rbac-auth (#1783), and health/metrics (#1785).
- **OIDC/SSO**: the +1 watermark offset that self-invalidated tokens after OIDC login removed (#1915); env-managed OIDC/LDAP providers reconciled on every boot (#1661); IdP error redirects handled on callback per RFC 6749 (#1662); roles.name cast to text in the role-mapping privilege probe (#1888).
- **Incus**: large uploads staged under `STORAGE_PATH` with orphan reaping wired into the hourly scheduler (#1573, #1622, #1654, #1751); uploads finalize on a background task returning 202 with observable status (#1494); HTTP Range honored on image download (#1848).
- **Uploads**: `total_size` bounded and repository quota enforced at upload-session creation (#1870); truncated streamed cache writes rejected (#1912, #1913).
- **Peers**: peer replication sync worker and ranged reads hardened (#1665); hosted Debian and PyPI packages replicate to peers (#1810, #1865).
- **DB/infrastructure**: pooled Postgres connections failing a liveness probe are evicted (#1878); object-level GCS health probe (least-privilege) (#1737); 503 preserved on the API-token auth branch under bcrypt saturation (#1843); cached-artifact listing paginates without O(N) sidecar loads and guards `per_page=0` (#1571, #1747).
- **Scanning**: repository passed to the Grype scanner for OCI artifacts (#1664); writable shared scan-workspace volume for the non-root backend (#1563, #1697); openscap rootfs pulls el9_8 z-stream errata (#1544, #1733); Dependency-Track API-key team permissions ensured (#1530, #1531).

## [1.2.0] - 2026-06-02

### Pre-upgrade check

- **Stuck-scan janitor will reap accumulated `running` rows on first tick** (#1015, #1062) -- this release introduces a background janitor that transitions `scan_results` rows wedged in `status='running'` past the configured `STUCK_SCAN_THRESHOLD_SECS` (default 1800s / 30 min) to `status='failed'`. On long-running installs predating this release, previously-stuck rows from crashed scan workers (OOM, pod evicted, deploy mid-scan) will flip to `failed` in batches of up to 1000 rows per janitor tick (the per-tick cap bounds memory and audit-log write volume). With the default `STUCK_SCAN_CHECK_INTERVAL_SECS=600`, a backlog of N stuck rows therefore takes roughly `ceil(N / 1000) * (STUCK_SCAN_CHECK_INTERVAL_SECS / 60)` minutes per replica to fully drain. Multiple janitor-running replicas drain proportionally faster (the cap is per-replica, per-tick). Operators with a large backlog who want to accelerate the drain can lower `STUCK_SCAN_CHECK_INTERVAL_SECS` for one or two ticks after deploy and revert. Alerting on `status='failed'` deltas should expect this drain (one or many ticks depending on backlog size) at upgrade and tune accordingly.

  Count the rows that will be reaped before upgrading:

  ```sql
  SELECT count(*) FROM scan_results
  WHERE status = 'running'
    AND started_at < now() - interval '30 min';
  ```

  The same count also approximates migration `105_partial_index_running_scans.sql`'s ACCESS EXCLUSIVE lock duration on `scan_results`. The partial index is built non-concurrently (forced by sqlx's per-migration transaction wrapper), and the build scan touches every `running` row that exists at migration time. New `scan_results` inserts block on that lock for the build's duration. On installs with a small backlog the build is sub-second; on installs with hundreds of thousands of historical stuck rows the build can take seconds-to-tens-of-seconds. Plan the upgrade window using the count above. On fresh installs (no historical `running`-state rows) the build is sub-millisecond and the warning above does not apply.

  Rollback: the migration is reversible without operator state changes by running `DROP INDEX IF EXISTS idx_scan_results_running_started;` against the registry database. The janitor sweep continues to function without the index (it falls back to scanning `scan_results` filtered by `status='running'`); the index is purely a query-plan accelerator. If the migration window is too short to complete the index build, abort the deploy, drop the partial index if it was created, then add it out-of-band against a quiescent table with `CREATE INDEX CONCURRENTLY idx_scan_results_running_started ON scan_results (started_at) INCLUDE (id) WHERE status = 'running';` (run as the DB owner outside any transaction).

  Each reap also writes one `SCAN_REAPED` entry to `audit_log` (#1063) carrying `scan_id`, `artifact_id`, `repository_id`, `started_at`, `reaped_at`, `threshold_secs`, `reason='stuck_running_janitor'`, and `actor='system:stuck_scan_janitor'` so the burst is auditable and SIEM rules can filter system-initiated reaps via `details->>'actor'`. Note that `audit_log` entries are subject to the `audit_retention_days` retention sweep (default 90 days), and the `scan_results` row itself is `ON DELETE CASCADE` from `artifacts`/`repositories`; for long-term compliance retention, export `SCAN_REAPED` entries to durable SIEM storage rather than relying on either table as the long-term source of truth. Artifacts whose vulnerability scan was reaped this way are now visible to operators investigating an incident; rescan with `POST /api/v1/security/artifacts/{id}/rescan` if the scan never reported findings.

  Misconfiguration guard: `STUCK_SCAN_THRESHOLD_SECS` below 60 s is clamped to 60 s with a startup warning (a 0-second threshold would reap every in-flight scan on every tick), and `STUCK_SCAN_CHECK_INTERVAL_SECS` below 30 s is clamped to 30 s (a 0-second interval would panic the spawned scheduler task). The defaults of 1800 s and 600 s remain unchanged; the clamps only apply to operator-supplied overrides.

- **Migration 106 takes ACCESS EXCLUSIVE on `artifacts`** (#1217 audit follow-up). Migration `106_artifacts_lower_name_index.sql` builds the functional partial index `idx_artifacts_repo_lower_name ON artifacts (repository_id, LOWER(name)) WHERE is_deleted = false` non-concurrently (forced by sqlx's per-migration transaction wrapper). On installs with millions of artifact rows, plan the upgrade window using a row count: `SELECT count(*) FROM artifacts WHERE is_deleted = false;`. Inserts to `artifacts` (new uploads and proxy-cache writes) block on the index build for its duration; on small installs the build is sub-second, on installs with tens of millions of rows it can take seconds-to-tens-of-seconds. Operators who cannot accept the lock window can skip the migration and build the index out of band against a quiescent table with `CREATE INDEX CONCURRENTLY idx_artifacts_repo_lower_name ON artifacts (repository_id, LOWER(name)) WHERE is_deleted = false;` (run as the DB owner outside any transaction); the migration is idempotent (`CREATE INDEX IF NOT EXISTS`) so re-running it afterwards is a no-op. Functionality is correct without the index; it is purely a query-plan accelerator for the cross-format shadowing guard and the existing `find_artifact_by_name_lowercase` helper.

### Added

- **Stuck-scan janitor** (#1015) -- new `ScanResultService::cleanup_stuck_scans` background job registered in `scheduler_service::spawn_all` (90s startup delay, 600s default cadence) transitions `scan_results` rows wedged in `status='running'` past `STUCK_SCAN_THRESHOLD_SECS` (default 1800) to `status='failed'`. Configurable via `STUCK_SCAN_THRESHOLD_SECS` and `STUCK_SCAN_CHECK_INTERVAL_SECS` env vars. Emits the existing `ak_cleanup_items_removed_total{type="stuck_scans"}` counter. Forward-ported from `release/1.1.x` PR #1064.
- **Stuck-scan janitor partial index** (#1061) -- migration `105_partial_index_running_scans.sql` adds `idx_scan_results_running_started ON scan_results (started_at) WHERE status = 'running'`. The janitor sweep added in #1015 filters on `status='running'` without a `repository_id` predicate, which would degrade on installs with very large `scan_results` tables. The partial index only contains in-flight rows so the planner goes straight to the candidates. `CREATE INDEX CONCURRENTLY` is intentionally not used because `sqlx::migrate` runs each migration in a transaction; in-flight rows are bounded by the janitor itself so the synchronous build is short.
- **Audit-event emission on stuck-scan reap** (#1063) -- `ScanResultService::cleanup_stuck_scans` now writes one `SCAN_REAPED` entry to `audit_log` per reaped row, capturing `scan_id`, `artifact_id`, `repository_id`, `started_at`, `reaped_at`, `threshold_secs`, and `reason='stuck_running_janitor'` in the `details` JSON column. Previously the janitor emitted only the `ak_cleanup_items_removed_total{type="stuck_scans"}` counter, so operators investigating an incident could not tell which vulnerability scans never completed. Audit writes are best-effort: a failure to record the event is logged at warn level but does not roll back the reap, since leaving the row wedged in `running` is the worse outcome. Adds `AuditAction::ScanReaped` and `ResourceType::ScanResult`.
- **Auth: download-ticket consumer middleware** (#930) -- the `?ticket=<v>` query parameter minted by `POST /api/v1/auth/ticket` is now accepted as a fallback authenticator on read routes (`auth_middleware`, `optional_auth_middleware`, and `repo_visibility_middleware`). Tickets are single-use, expire after 30 seconds, are restricted to GET/HEAD methods, and only authenticate the request whose URL path matches the ticket's bound `resource_path`. Useful for browser anchor-tag downloads and `EventSource` SSE streams where `Authorization` headers cannot be set.

### Fixed

- **SBOM generation now includes the artifact's declared dependencies and stops emitting authoritative empty inventories** (closes #870). The SBOM read path sourced components only from scanner output (`scan_packages`, then `scan_findings`). An artifact a scanner could not enumerate, a bare Maven `.jar` with no lockfile, or any upload that was never scanned, produced an SBOM with `"components": []` that was indistinguishable from a genuinely dependency-free artifact, which the reporter correctly flagged as a security problem. SBOM generation now adds a second source: the artifact's own declared dependencies, parsed from its manifest. Maven direct dependencies come from the stored POM metadata (with a fallback that reads the sibling `.pom` from object storage and resolves `${property}` versions against the POM's own `<properties>`); npm dependencies come from `package.json` (`dependencies` + `optionalDependencies`, excluding `devDependencies`); Helm dependencies come from `Chart.yaml`. Declared dependencies are merged with scanner output (deduplicated by purl, scanner data winning), and the document carries an honest completeness signal: `complete` (full scanner inventory), `declared` (direct deps only, no scanner inventory), `partial` (CVE-only findings, or unresolved declared versions), or `none` (no source at all). Both the on-demand `POST /api/v1/sbom/generate` endpoint and the on-scan Dependency-Track submission path use the merged source. Scope: declared dependencies are direct only (transitive resolution remains the scanner's job), and versions managed by a parent or `dependencyManagement` BOM that is not in the registry are emitted with a null version and mark the SBOM `partial`. This change also fixes a latent bug where any POM declaring `<properties>` failed to parse entirely (`invalid type: map, expected a string`), which previously rejected such uploads at validation and left their metadata without dependencies. The declared-dependency lookups also cast the `repository_format` enum column to text in SQL (`format::text`); without the cast the query failed to decode into a Rust `String` and the error was silently swallowed, so the declared-dependency source produced nothing and the SBOM still came back empty. These query paths now log on failure instead of degrading silently. Reported by @Firjens and @flopma.
- **OpenSCAP scanner accepts the default scan workspace path out of the box** (closes #1466). The wrapper sidecar (`scripts/openscap-wrapper.py`) validates incoming scan paths against `OPENSCAP_ALLOWED_SCAN_DIRS`, which defaulted to `/tmp/:/var/tmp/`. The backend, however, writes per-artifact scan workspaces under `SCAN_WORKSPACE_PATH` (defaults to `/scan-workspace`) and sends that path to the wrapper, so every fresh docker-compose and Helm install hit `HTTP 400 {"error": "scan path not found or not allowed"}` on first Docker/OCI scan unless the operator knew to set the env var. The wrapper's default allowlist now includes `/scan-workspace/` alongside the previous `/tmp/` and `/var/tmp/` entries, so the default deployment topology works without per-deployment tweaks. Operators who customise `SCAN_WORKSPACE_PATH` or want a stricter allowlist can still override via `OPENSCAP_ALLOWED_SCAN_DIRS`. The backend's HTTP-400 surface in `services::openscap_scanner::call_openscap` also now includes the rejected path in the error chain so future allowlist mismatches are debuggable from backend logs alone, without enabling wrapper-side debug logging. Three new unit tests in `openscap_scanner::tests` cover the prepare-and-scan happy path (asserting the request body carries the configured workspace path), the workspace-under-base invariant, and the error-message-includes-rejected-path invariant.
- **Incus image scanner can scan real multi-GiB OS images again** (closes #1492, follow-up to #1428). The extraction hardening added in #1428 made the `incus-image` scanner reject every legitimate `incus export` OS rootfs: the compressed-size cap was 2 GiB (a real image is several GiB compressed), the post-extraction guard walked the tree as the non-root scanner UID and hit `EACCES` on a real rootfs's restrictive modes (combined with `--no-same-owner` extraction and tar's implicitly-created parent dirs that miss the archive `--mode`), and the symlink-traversal guard resolved absolute targets against the host root, so the ubiquitous `/var/run -> /run` present in essentially every OS rootfs was flagged as escaping the workspace. Three changes, with the decompression-bomb guards kept in force: (1) the compressed and extracted byte caps are now env-tunable (`MAX_INCUS_SCAN_COMPRESSED_BYTES` / `MAX_INCUS_SCAN_EXTRACTED_BYTES`) with OS-image-sized defaults (16 GiB compressed / 64 GiB extracted); (2) the extracted tree is made owner-traversable (`u+rwX`) before the guard walk and the trivy scan, done in-process via `symlink_metadata` so the chmod never follows a symlink target out of the workspace (which a `chmod -R` would); (3) absolute symlink targets are re-rooted under the workspace (chroot semantics) so `/run` resolves in-tree and is accepted, while `../`-style targets that normalise outside the workspace are still rejected. Adds unit tests for the env-cap resolver, the owner-traversable walk over a `0o000` tree, the guard passing a restrictively-permissioned tree, acceptance of absolute intra-rootfs links, and rejection of both relative and absolute climbing-out escapes. The silent-completion gap the reporter also noted (a failed format-native scanner still rolling up to an overall `completed` status) is tracked separately as #1497.
- **Scan dedup short-circuits on identical-bytes uploads instead of writing duplicate rows** (closes #1373). `ScannerService::prepare_artifact_scan` previously inserted a fresh `running` placeholder row on every call, even when the target artifact already had a completed scan for the same checksum + scan_type within the dedup TTL window. The placeholder was returned to the trigger-scan caller and then fell through `scan_artifact_inner`'s `should_skip_reuse_for_same_artifact` branch, which (pre-fix) skipped the reuse-copy path and ran a fresh scan, leaving the artifact with two completed `scan_results` rows for one logical scan. The release-gate `scan-dedup-checksum` suite (run 26344757642) caught this with `Per-artifact scan list for B contains exactly one completed scan; got 2` and `Second scan on identical bytes returns same scan_id`. The fix adds `ScanResultService::find_existing_scan_for_artifact`, scoped to `(artifact_id, checksum_sha256, scan_type, status='completed')` within `DEDUP_TTL_DAYS`. `prepare_artifact_scan` consults it before inserting a placeholder and returns the existing scan id when one is found, and `scan_artifact_inner` now `continue`s without inserting a new row when the matched reusable scan belongs to the same artifact (the auto-scan-on-upload path). If a placeholder was already committed in a race window between prepare and execute, the inner loop converts it to a reused row pointing at the existing source rather than leaving it stuck in `running`. The cross-artifact dedup path that copies a source scan's results into a new row for a different `artifact_id` (`find_reusable_scan` + `copy_scan_results` / `convert_to_reused`) is unchanged. Seven integration tests in `tests/scan_dedup_short_circuit_tests.rs` cover the new method's happy path, all four negative cases (different artifact, different checksum, different scan_type, `running` instead of completed), the TTL-window cutoff, and the most-recent-wins ordering.
- **Deactivated user's API token now rejected with 401 within seconds, not silently downgraded to anonymous, on optional-auth routes** (closes #1371). `optional_auth_middleware` previously treated an explicitly-presented but invalid Bearer credential the same as no credential: it set `Extension(Option<AuthExtension>::None)` and let the handler decide what to do. For `GET /api/v1/repositories` and other optional-auth routes the handler returns a public-only list with HTTP 200 in the anonymous case, which meant a deactivated user's API token continued to receive 200 responses for up to `API_TOKEN_CACHE_TTL_SECS` (5 min) after `is_active=false`, masking the off-boarding signal. The cache invalidation from #931 worked correctly (the cache hit IS rejected, and on cache miss the SQL `WHERE is_active = true` filter rejects too), but the middleware swallowed the resulting error. `try_resolve_auth_outcome` now returns a tri-state `AuthOutcome { Resolved, NoCredential, InvalidCredential }`, and both `optional_auth_middleware` and `repo_visibility_middleware` short-circuit with 401 when an explicitly-presented credential failed to validate (and no `?ticket=` fallback resolves). The legacy `try_resolve_auth` helper continues to flatten Invalid into None for back-compat with the guest_access guard and other call sites that need the Option-shape. The guest_access guard's long-lived `AuthService` is also now registered for global cache flush so its cache participates in the #931 invalidation registry (defense-in-depth). Adds three integration tests in `optional_auth_deactivation_tests.rs` that fail on `main` and pass on this branch, and three new unit tests pinning the tri-state outcome. Release-gate `auth-user-deactivation-revokes-tokens / Deactivated user's API token is rejected` (run 26344757642) now passes deterministically.
- **`lxc`-format repositories now respond on `/lxc/*` instead of returning 404** (closes #1272). Repository creation accepted `format: lxc` and the rest of the stack already aliased `lxc` to the Incus handler (the `RepositoryFormat::Lxc` enum variant, the `formats::get_handler_for_format` dispatch, the `resolve_incus_repo` resolver that explicitly accepts both `incus` and `lxc`, and the on-disk `incus/<repo-id>/...` storage key prefix are all shared), but `create_router` only mounted `handlers::incus::router()` under `/incus`. Result: a repo created with `format: lxc` 404'd on every `/lxc/<key>/streams/v1/index.json`, `PUT /lxc/<key>/images/...`, and chunked-upload request, while the identical request against an `incus`-format repo at `/incus/<key>/...` returned 200. The fix nests the same `IncusHandler` router under `/lxc` as well, matching the dispatch table's existing two-label intent. Note: the SimpleStreams index served via `/lxc/` currently emits absolute `/incus/...` download URLs because `build_download_url` and `build_upload_location` hardcode the `/incus/` prefix; clients that follow absolute URLs blindly are unaffected, clients doing prefix-based auth or relative resolution against `/lxc/` get redirected to `/incus/`. Two source-level meta-tests pin the alias so a future refactor cannot silently drop the `/lxc` nest. Making the URL builders prefix-aware (or collapsing the `Lxc` enum variant into `Incus` outright, since they share every implementation detail) is tracked as a follow-up.
- **CocoaPods podspec JSON now preserves every field uploaded by the publisher** (closes #1286). `GET /cocoapods/{repo}/Specs/{name}/{version}/{name}.podspec.json` previously returned a stripped podspec because `PodSpec` was deserialized into a struct with only `name`, `version`, `summary`, `homepage`, `license`, `authors`, `source`, `platforms`, and `dependencies` named; every other field in the uploaded JSON (`vendored_frameworks`, `xcconfig`, `preserve_paths`, `requires_arc`, `documentation_url`, `screenshots`, `description`, `source_files`, `frameworks`, `swift_version`, `resource_bundles`, `subspecs`, ...) was silently dropped by serde during the publish-then-serve round-trip. The CocoaPods client could not link binary frameworks against a pod served by Artifact Keeper because `vendored_frameworks` and `xcconfig` never reached `pod install`. `PodSpec` now carries a `#[serde(flatten)] extra: HashMap<String, serde_json::Value>` catch-all so the served JSON is a faithful round-trip of what was uploaded. Two regression tests pin the behaviour: `test_podspec_preserves_unknown_fields_round_trip` at the format layer and `test_extract_podspec_from_archive_preserves_linker_fields` at the handler layer, both built around the exact payload from the issue report.
- **Scanner archive extraction no longer depends on host `tar`/`unzip` binaries** (closes #1243, follow-up to #722). `ScanWorkspace::extract_archive` previously shelled out to `tar xzf` and `unzip` to extract scan inputs. The Alpine variant of the backend container image (`docker/Dockerfile.backend.alpine`) does not install `tar` or `unzip`, so npm `.tgz` and other archive scans silently failed at the `Command::new("tar")` call, the workspace fell through to scanning the raw archive bytes, and Trivy returned zero findings. Extraction is now in-process via the `tar`, `flate2`, and `zip` crates (already direct deps), runs on `spawn_blocking` to avoid stalling the tokio runtime on large archives, and benefits every deployment variant (slim Debian, Alpine, distroless) without per-image package list changes. The supported extension set is unchanged (`.tar.gz`, `.tgz`, `.crate`, `.gem`, `.zip`, `.whl`, `.jar`, `.war`, `.ear`, `.nupkg`, `.egg`). Five unit tests cover the npm tgz happy path, the `.crate` aliasing, jar/zip extraction, the unknown-extension no-op, and corrupt-archive error reporting.
- **Replica-safe credential check no longer rejects the second admin request from a fresh non-admin user** (#1248 follow-up). `is_token_invalidated_replica_safe` consulted the in-memory `is_token_invalidated` fast-path before the DB-watermark check. The same `invalidation_map` is also used by `fetch_credential_change_watermark` to cache DB lookups for `CREDENTIAL_DB_CACHE_TTL_SECS` (5s), so the first admin request from a fresh non-admin populated the cache with the user's `password_changed_at`, and every subsequent request within the cache window hit the sync fast-path's `<=` comparator against that same wall-clock second and was rejected with 401. Release-gate `tests/rbac/test-admin-protection.sh` against `v1.2.0-rc.1` saw exactly this: the first endpoint (`/api/v1/admin/settings`) returned 403 as expected, every subsequent endpoint in the loop returned 401. The fix removes the sync fast-path call from `is_token_invalidated_replica_safe`; the cache hit inside `fetch_credential_change_watermark` already provides the in-replica acceleration without the `<=` conflation, and the strict `<` comparator from #1248 is now the sole gate. The sync `is_token_invalidated` helper is retained for `validate_access_token` (the non-replica-safe sync entry point that does not touch the DB cache). Adds `two_sequential_admin_requests_with_fresh_jwt_both_return_403` integration test that fails on rc.1 and passes on this branch.
- **Proxy cache HTTP 500 on filesystem backend, every cached read after the first** (closes #1278). `ProxyService::cache_artifact` previously wrote proxy-cached bodies through the global `self.storage` AND inserted a row in `artifacts` with `storage_key = "proxy-cache/<repo_key>/<path>/__content__"`. Every format handler's subsequent read resolved storage via `state.storage_for_repo(repo.storage_location()).get(&artifact.storage_key)` -- which on filesystem deployments creates a per-repo `FilesystemStorage` rooted at `repo.storage_path` and resolves to a doubled-prefix path (`<repo.storage_path>/proxy-cache/<repo_key>/...`) that does not exist on disk, returning HTTP 500 with `Storage error: Storage key not found`. S3 / object-store backends were unaffected because `StorageRegistry::backend_for` returns the shared instance regardless of `location.path`. The fix is surgical: `cache_artifact` no longer inserts proxy-cached items into the `artifacts` table. The cached body and metadata sidecar still live on disk under `self.storage`, and the format-handler hot path already checks the proxy cache via `proxy_check_cache` -> `get_cached_artifact_by_path` -> `self.storage.get` BEFORE falling through to upstream, so cache hits are served through that path with no `artifacts` row needed (read backend matches write backend). Tradeoff: proxy-cached items no longer surface in the repository artifact-listing endpoint or `storage_used_bytes` accounting -- that UX gap is tracked separately and is a graceful degradation, not a correctness regression. Existing rows from prior versions stay in `artifacts` and continue to surface in listings until invalidated. Adds a source-level meta-test (`test_cache_artifact_does_not_insert_into_artifacts_table`) that pins the no-INSERT invariant so a future refactor can't silently restore the bug.
- **Virtual repository creation now rejects requests with no members at create time, not at first fetch** (closes #1279). `POST /api/v1/repositories` with `repo_type=virtual` previously accepted both `member_repos` omitted entirely (e.g. when operators typed `members: [...]` -- the more natural field name -- which serde silently dropped because the struct field is `member_repos` and does not enable `deny_unknown_fields`) and `member_repos: []` (empty array). Both shapes returned HTTP 200 with a successfully-created repo whose `virtual_repo_members` row count was zero, and every subsequent fetch returned `404 Resource not found: Virtual repository has no members` -- so operators only discovered the misconfiguration minutes later when they actually tried to use the repo. The handler now validates up front via `validate_virtual_repo_member_count` and returns a `400 Bad Request` that names the expected `member_repos: [{repo_key, priority}, ...]` shape, echoes the offending repo key, and points at `PUT /api/v1/repositories/{key}/members` for post-creation updates. Five unit tests pin the validator (accept non-virtual types, reject None / empty for virtual, accept non-empty) plus a regression test pinning the `members:`-instead-of-`member_repos:` silent-drop behaviour so any future `deny_unknown_fields` hardening can simplify the validator.
- **First-time admin login surfaces the generated password in startup logs and points at the web UI forced-change flow** (#1009). The bootstrap path previously wrote the random admin password only to `${STORAGE_PATH}/admin.password` and printed the file path. Operators had to `docker exec` into the container to read it, then run two `curl` commands to change it. The startup banner now echoes the plaintext password (and the file path) once at INFO level when the password was generated by Artifact Keeper itself, and the banner now explicitly walks operators through the web UI flow: log in with the printed password, the UI redirects to the forced-change-password screen, and the API unlocks automatically. The new `ARTIFACT_KEEPER_HIDE_ADMIN_PASSWORD=true` env var suppresses the plaintext echo for installs where the backend log stream is shared with other tenants; the file is still written so the credential is recoverable. The plaintext is never echoed when the operator supplied `ADMIN_PASSWORD` from the environment (they already have it). The default password is single-use anyway: `must_change_password=true` is still set, the `setup` middleware blocks every mutating API call until the change runs, and tokens issued during the locked window are invalidated by the existing `change_password` flow.
- **Swift manifest endpoint serves Package.swift from the uploaded source archive** (#1100). `GET /swift/{repo}/{scope}/{name}/{version}/Package.swift` previously returned `404 {"detail":"Manifest not found for this release"}` for any release published via raw `PUT ... application/zip`, because the publish handler populated `artifact_metadata.metadata["manifest"]` only from the optional `X-Swift-Package-Manifest` header. SwiftPM manifests are multi-line and cannot reliably be sent as an HTTP header value, so this gate effectively required clients to know about a custom header to ship a working release, and broke `swift package resolve` against the registry. The publish path now extracts `Package.swift` from the uploaded zip when the header is absent (top-level `Package.swift` wins, `<single-prefix>/Package.swift` for GitHub-archive-style layouts is the fallback, and the shallowest path wins when multiple candidates exist). The fetch path also gained a fallback: when the cached `manifest` field is `NULL` (legacy uploads from before this fix), the handler reads the stored zip on demand. The explicit header still takes precedence so clients with their own preferred manifest text can override the archive contents.
- **`upload_chunks.status` check constraint now accepts `'uploading'` (closes #1168, part 2)**. Migration 072 created the table with `CHECK (status IN ('pending','completed','failed'))`, but the chunked-upload service in `services/upload_service.rs` atomically claims chunks via `UPDATE ... SET status = 'uploading' WHERE status = 'pending'` before writing data, then sets `'completed'` or `'failed'` after the write. The first PATCH against any chunk therefore aborted with `new row for relation "upload_chunks" violates check constraint`, surfacing in the browser as "upload big file failed because of database table error". Migration 089 drops and re-adds the constraint with the full status set (`'pending','uploading','completed','failed'`); the constraint is looked up via `pg_constraint` rather than by hard-coded name so prior fix migrations under non-default names are still handled.
- **Chunked upload finalize writes to the repo-scoped, content-addressable storage key (closes #1168, parts 1 and 3)**. Two bugs interacted: the create-session handler queried `SELECT id FROM repositories WHERE key = $1 AND is_deleted = false`, but `repositories` has no `is_deleted` column (the soft-delete pattern lives on `artifacts`, not `repositories`), so the lookup failed with a SQL error before any chunks could be uploaded. The `AND is_deleted = false` clause is removed. Second, the `complete` handler called `state.storage.put_file("uploads/<repo_id>/<path>", ...)` against the global default backend (no repo path prefix), while `download_artifact` resolves via `state.storage_for_repo(&repo.storage_location())` which prepends the repo's `storage_path`. The two paths never agreed, so a 201-CREATED chunked upload could not be downloaded. Finalize now resolves the repo-scoped backend and writes using `ArtifactService::storage_key_from_checksum` (`<sha[:2]>/<sha[2:4]>/<sha>`), matching how non-chunked uploads land on disk.
- **Migration runner recovers from the legacy duplicate-073 state without operator intervention (closes #1129)**. Between PRs #975 and #1138 the migrations directory contained two files numbered 073 (`073_account_lockout.sql` and `073_download_tickets_cascade.sql`). `sqlx migrate run` only inserts one row per version, and which checksum landed in `_sqlx_migrations` depended on filesystem-walk order, so installs that bootstrapped in that window now refuse to start with `Migration(VersionMismatch(73))` even though the `account_lockout` columns are physically present on `users`. The backend now runs a pre-migration repair step at startup: if `_sqlx_migrations` already has a version-73 row whose checksum doesn't match the current `073_account_lockout.sql`, and the `users.failed_login_attempts` column is present (proof account_lockout was applied at least once), the checksum is rewritten to the current value and the migrator continues. The check is conservative; unrelated checksum drift still aborts startup so accidental tampering surfaces loudly.
- **`POST /v2/token` now accepts OAuth2 password-grant credentials in the form body** (#894). Docker's distribution token endpoint flow uses `Content-Type: application/x-www-form-urlencoded` with `grant_type=password&username=...&password=...&service=...&scope=...` in the request body, but the handler previously read credentials only from the HTTP Basic Auth header and returned the anonymous token when the body carried the credentials. Result: `docker push` to a private repository failed with `unauthorized` because the OCI client received an anonymous token despite valid credentials. The handler now extracts username and password from the form body when no Basic Auth header is present, accepts `application/x-www-form-urlencoded` (with optional charset suffix), validates `grant_type=password` when supplied, and falls through to the existing Bearer / anonymous flow on any malformed input. No protocol regression: existing Basic Auth and Bearer-refresh paths are unchanged.

### Changed

- **`/readyz` no longer returns 503 when only the default admin password is unchanged** (#889). Previously, `setup_required = true` drove the readiness gate to "not ready", which made Kubernetes mark the pod NotReady and eventually restart it. The restart killed any `kubectl exec` session the operator was using to change the password, so they could never complete setup and the pod stayed in a restart loop. Now `/readyz` returns 200 OK once the database is reachable and migrations have run, even if setup is still required. The `setup_complete` field remains in the JSON body as informational; its `status` value changed from `healthy`/`unhealthy` to `complete`/`incomplete` so the value no longer implies the field is gating the response code. A `tracing::warn!` is emitted once at process startup when `setup_required` is true so log-based alerting (which previously relied on the 503 signal) can still page on the condition. The `setup` middleware that gates mutating API endpoints until setup completes is unchanged.
- **Webhooks v2 wire contract finalized** (#919). The webhook delivery wire format is now `X-ArtifactKeeper-Signature: t=<unix_secs>,v1=<hex_hmac_sha256>` over `<unix_secs>.<raw_body>`, replacing the placeholder `X-Webhook-Signature: hmac-signature` literal that shipped in v1.1.9. New headers on every delivery (test endpoint, retry path, manual redeliver): `X-ArtifactKeeper-Delivery` (UUID, idempotency key), `X-ArtifactKeeper-Event` (event type), `X-ArtifactKeeper-Event-Version` (pinned schema version, currently `2026-04-01`), and `X-ArtifactKeeper-Retry-Attempt` on retries. During the 24h secret-rotation overlap window the signature header carries two `v1=` tokens, current first, so receivers using either secret validate. The retry schedule moves from 5 attempts capped at 4h to 12 jittered attempts capped near 24h (30s, 1m, 2m, 5m, 10m, 30m, 1h, 2h, 4h, 8h, 16h, 24h, +/- 20% jitter). Webhooks that exhaust the retry budget are auto-disabled with a `disabled_reason` set, and `ak_webhook_dead_letter_total{event}` is incremented. Per-webhook event payload pinning is exposed via the new `event_schema_version` field on POST/GET/LIST `/api/v1/webhooks` (existing webhooks default to `2026-04-01`; unsupported versions return HTTP 400). Legacy `X-Webhook-*` headers continue to be emitted alongside the new headers for one release so existing receivers keep working; they will be removed in v1.3.0. See https://artifactkeeper.com/docs/advanced/webhooks for the wire contract reference and Python/Node/Go receiver samples.
- **`scan_findings.affected_component` is now the bare package name** (#903, PR #1150, closes #1159). The field previously carried the parenthetical target alongside the name (`'body-parser (package-lock.json)'`); the target now lives on its own column at `scan_packages.source_target`, and `affected_component` is the package name alone (`'body-parser'`). The split makes `affected_component` usable as a join key against `scan_packages` and the SBOM CycloneDX/SPDX output, and removes the format ambiguity that prevented per-component dedupe across scan sources. The official Web, iOS, and Android SDKs render `affected_component` as a bare string with no parenthetical parsing, so no client-side migration is required; downstream integrations that built custom string matchers on the old format should switch to reading `source_target` from the joined `scan_packages` row instead of splitting `affected_component` on `' ('`.

### Security

- **API-token cache invalidation on user deactivation** (#931) -- when an admin deactivates or deletes a user (`PATCH /api/v1/users/{id}`, `DELETE /api/v1/users/{id}`), updates a service account (`PATCH/DELETE /api/v1/service-accounts/{id}`), or runs a federated SSO offboarding sync (`AuthService::deactivate_missing_users`), every cached API-token validation belonging to that user or service account is now rejected immediately rather than continuing to authenticate for up to 5 minutes (the previous `API_TOKEN_CACHE_TTL_SECS` window). Caveat: the invalidation map is per-process. In multi-replica deployments (Helm `replicas > 1`) only the replica that handled the admin action evicts immediately; other replicas still reject the cached entry within the same 5-minute window via the existing `WHERE is_active = true` SQL filter, but cache hits on those replicas can still authenticate during that window. A v1.2.0 follow-up will move the signal into the database or a Redis pub-sub channel so it is observed by every replica.
- **Hex virtual repo tarball shadowing guard** (#973). `GET /hex/{repo}/tarballs/{name}-{version}.tar` now blocks a Remote upstream from serving a tarball whose package name is already owned by a non-Remote member of the same virtual repo. Previously, a virtual hex repo configured with `[hex-remote (priority 1), hex-local (priority 2)]` could serve a malicious upstream `phoenix-1.0.0.tar` when the operator had published their own `phoenix` locally, because `resolve_virtual_download` iterates members in configured priority order. The metadata side of this guard (`GET /packages/{name}`) shipped in #1209 via `order_members_local_first`; this completes the pair on the tarball download path by checking non-Remote members for ownership via a single `SELECT 1 FROM artifacts WHERE repository_id = ANY($1) AND LOWER(name) = LOWER($2) LIMIT 1` query and calling `resolve_virtual_download` with `proxy_service: None` when ownership is established. The filename parser (`package_name_from_tarball_filename`) is fail-closed: filenames not matching `[a-z][a-z0-9_-]*-<version>.tar` (case-insensitive on the extension) return `None` and the guard does not activate. On guard-query DB failure the request fails closed to 500 with `event=shadowing_guard_db_error` in structured logs rather than falling through to upstream. Forward-ports the remaining piece of the abandoned PR #974 (todpunk). Other format handlers (cargo, npm, pypi, maven, rubygems) have the same shadowing attack surface and will receive analogous guards in follow-up PRs.
- **`require_scope` enforcement on admin and format push paths** (GHSA-vvc3-h39c-mrq5). A service-account token minted with `scopes: ["read"]` was accepted by `POST/PUT/DELETE /api/v1/permissions`, `POST/PUT/DELETE /api/v1/groups` and member-management endpoints, and the upload paths of most format handlers (npm, PyPI, NuGet, Maven, OCI v2, Cargo, Helm, Debian, RPM, Alpine, Conan, Conda, Cran, Git LFS, Hex, Hugging Face, Incus, Protobuf, Rubygems, Swift, Terraform, Composer, Go, ML model, Goproxy). The auth middleware was authenticating the token but the handlers were not checking the token's declared scopes. A read-scoped token could chain into full admin via `POST /api/v1/permissions` (grant self `admin` on the `system` sentinel). New `require_auth_basic_scope` and `require_scope_response` helpers in `api/middleware/auth.rs` enforce `read`/`write`/`delete`/`admin` (with `*`/`admin` wildcards). JWT user sessions pass through unchanged because scopes apply only to SA/PAT tokens. OCI v2 push and delete (`handle_start_upload`, `handle_patch_upload`, `handle_complete_upload`, `handle_put_manifest`, `handle_delete_manifest`) are gated; pull paths intentionally remain ungated. Eight low-volume format handlers (Ansible, Chef, CocoaPods, JetBrains, pub_registry, Puppet, SBT, VSCode) are tracked for a follow-up PR alongside a shared raw-upload helper refactor to keep the duplication-gate metric clean; until then those handlers retain the pre-existing `require_auth_basic` and remain affected by the advisory.
- **Cross-format virtual-repo shadowing guard for cargo, npm, pypi, maven, rubygems** (#1217 audit follow-up). The supply-chain name-shadowing guard introduced for hex in #1217 now also protects the virtual-repo download paths for these five formats. A new primitive `proxy_helpers::virtual_non_remote_owns_name` runs the same `SELECT 1 FROM artifacts WHERE repository_id = ANY($1) AND LOWER(name) = LOWER($2) LIMIT 1` existence check across every non-Remote member of a virtual repo, and a new `DownloadResponseOpts::suppress_upstream_proxy` flag threads the result into the existing `try_remote_or_virtual_download` helper so Remote members are `Skip`'d when a local member owns the name. Per-format filename / path parsers and validators (`package_name_from_crate_filename`, `package_name_from_tarball_path` for npm, `package_name_from_gem_filename`, plus PEP 503 normalization for pypi and `MavenHandler::parse_coordinates` for maven's artifactId) live in `formats/*.rs` and reject path-traversal, unicode-homoglyph, and uppercase-only inputs so the guard refuses to interpret malformed names as legitimate identifiers. Maven uses `coords.artifact_id` (matching how `artifacts.name` is populated on publish); this is strictly a safety net since different groupIds may share an artifactId, but the trade-off is preferable to leaving the shadowing attack open. PyPI normalizes the requested project name via `PypiHandler::normalize_name` (PEP 503) before the guard query so name-variant attacks (`MyCompany-Utils` vs `mycompany-utils`) cannot bypass it. The `None`-to-`resolve_virtual_download` pattern that makes the guard load-bearing in hex.rs is preserved in every wire-up site.
- **Apply hex package-name validator at publish time** (#1217 audit follow-up, ak-xf8w). `POST /hex/{repo}/publish` previously checked only that the `metadata.config` `name` field was non-empty, so a publisher could craft a tarball whose name was `../evil` or `Phoenix` (uppercase) and have the malformed name persist into `storage_key`, `artifact_path`, and `artifacts.name`. The download-side guard already refused to interpret such names, but the upload side did not. Publishing now rejects any name that fails `is_valid_hex_package_name` (the same `[a-z][a-z0-9_-]*` shape the download parser enforces) with `400 Bad Request: Invalid hex package name`. Together with the move of `is_valid_hex_package_name` and `package_name_from_tarball_filename` from `api/handlers/hex.rs` to `formats/hex.rs` (ak-niid), uploads and downloads now share one source of truth for what counts as a valid hex package name.
- **Functional `LOWER(name)` index on `artifacts`** (#1217 audit follow-up, ak-wgzr). New migration `106_artifacts_lower_name_index.sql` adds `idx_artifacts_repo_lower_name ON artifacts (repository_id, LOWER(name)) WHERE is_deleted = false`. The shadowing-guard SQL compares case-insensitively (`LOWER(name) = LOWER($2)`), which is not sargable against the existing `idx_artifacts_repo_name_version`; the new functional index makes the per-download guard query a single-row index lookup instead of a sequential scan, and also accelerates the existing `find_artifact_by_name_lowercase` helper used by every metadata endpoint that resolves a package by name. See the Pre-upgrade check section for lock-window planning notes.

### Removed

- **System B notification subscriptions (BREAKING)** (#920). The deprecated `/api/v1/repositories/{key}/notifications` GET/POST/DELETE endpoints, the `notification_subscriptions` table, and the `notification_dispatcher` service are removed in v1.2.0. The endpoints carried RFC 8594 `Deprecation` / `Sunset` headers since v1.1.9 (sunset 2026-08-01) and a one-release migration window has elapsed. Replacement surfaces:
  - **Webhooks** (System A): the dedicated `/api/v1/webhooks` API shipped in v1.1.9 with the v2 wire contract (signed timestamps, replay window, secret rotation). Existing webhook-channel subscriptions were migrated by migration 081 in v1.1.9.
  - **Email**: new `/api/v1/repositories/{key}/email-subscriptions` GET/POST/DELETE endpoints (this release) backed by the `email_subscriptions` table (migration 082, also shipped in v1.1.9). Email-channel rows from `notification_subscriptions` were seeded by migration 082 in v1.1.9; no operator action is required for already-migrated rows.
  - Migration 086 drops `notification_subscriptions` after a pre-DROP `UPDATE config = '{}'` that overwrites the table's last plaintext webhook secrets. Operators with PCI-DSS / HIPAA requirements should additionally run `VACUUM FULL notification_subscriptions` in the same maintenance window (cannot be folded into the migration because VACUUM FULL cannot run inside a transaction) and rotate every webhook secret that ever transited the table, since pre-migration base backups and WAL archives may still contain plaintext bytes.
  - The 5 generated SDKs (TypeScript, Kotlin, Swift, Rust, Python) will lose the `notifications` tag, the `NotificationsApiDoc` schemas, and the three notification request/response types on the next API sync; downstream consumers should pin to a pre-v1.2.0 API tag or migrate before bumping.
  - Operator-visible log message changed: `Notification dispatcher started` is now `Email dispatcher started`; update any log-based alerting that keyed on the prior string.

## [1.2.0-rc.3] - 2026-05-30

Third release candidate for v1.2.0. Folds in ~59 PRs merged since rc.2, with a heavy focus on closing security defects (cross-tenant chunked-upload bypass, Conan and DT auth surfacing, zeroized PGP/RSA secrets, admin-gated bypass_dedup), eliminating a class of data-loss bugs on cloud storage (Incus and OCI blob uploads now stream end-to-end through `StorageBackend::put_stream` instead of buffering in heap), and a wide pass of format-handler fixes across npm, Maven, RPM, Ansible Galaxy, OCI, Incus, and SBOM. Also includes a CI infrastructure fix (clippy OOM cap) and a docker-compose change that makes Dependency-Track opt-in to drop idle resource use.

### Sponsors

Thank you to our sponsors for keeping Artifact Keeper development moving.

- [@dragonpaw](https://github.com/dragonpaw) (Ash A.)
- [@injectedfusion](https://github.com/injectedfusion) (Gabriel Rodriguez)

[Become a sponsor](https://github.com/sponsors/artifact-keeper) to support the project and get your name listed here.

### Thank You

Community contributors who shipped fixes in this release candidate:

- [@dragonpaw](https://github.com/dragonpaw) for the Incus extraction hardening + setgid-dir prior-run-state fix (#1428), the GCS `get_stream`/`put_stream` overrides so multi-GiB artifacts no longer buffer in heap (#1431), the mmap-backed scan-input streaming so scanner inputs don't buffer either (#1453), `spawn_scan_on_upload` so format-native uploads (incus, oci, helm, npm, pypi, cargo, maven, etc.) trigger scans after publish (#1468), CycloneDX non-SPDX license emission via `license.name` rather than `license.id` (#1475), and the incus `packages` index population so images finally appear in the Packages tab (#1477)
- [@axellpadilla](https://github.com/axellpadilla) for the artifact-download race fix (#1355) -- collapses concurrent cold-cache fetches via an in-process singleflight coordinator so the herd no longer reads partial files
- [@Myrenic](https://github.com/Myrenic) for deduplicating the `packages` catalog so each name appears once with all versions stored in `package_versions`, plus version ordering corrections (#1456)
- [@ivolnistov](https://github.com/ivolnistov) for reporting OCI upload `Range` as inclusive per the spec (#1345)
- [@junsung-cho](https://github.com/junsung-cho) for serving Maven classifier artifacts from virtual repositories (#1399)

### Security

- **Chunked-upload sessions are now bound to the URL repo** (closes #1317). Both incus (`upload_chunk` / `complete_chunked_upload` / `cancel_chunked_upload` / `get_upload_progress`) and OCI v2 (`handle_patch_upload` / `handle_complete_upload`) session lookups now include `AND repository_id = $2` so a session created against repo A cannot be advanced through repo B's URL. Cross-repo URL with a foreign session_id returns 404 (not 403) to avoid leaking session existence. Plus regression tests in both the integration matrix and a new lib-side unit-test module so future drift is caught at the coverage gate.
- **`bypass_dedup` on `POST /scans/trigger` is admin-only** (paired with #1469). The new `bypass_dedup` knob on `TriggerScanRequest` was wired without an admin gate, exposing a fan-out DoS amplifier where any authenticated caller could force-rescan an entire repository with no rate limit. The handler now returns 403 when a non-admin sets `bypass_dedup: true`, mirroring the existing admin-only inventory-backfill semaphore on the same surface.
- **Conan `users/authenticate` now mints a real JWT instead of echoing base64 Basic creds** (closes #1433). The endpoint previously returned the literal base64 of `user:pass` as the "token," which the Bearer middleware then rejected on every follow-up call. The bearer-fallback path tried to bcrypt-compare the echoed credential against the password hash, eventually triggering account lockout on API-token clients. The endpoint now signs an access JWT via `AuthService::generate_tokens`, matching every other token-issuing path.
- **Surface 401/403 from Dependency-Track SBOM submission instead of swallowing them** (closes #1472). DT call paths previously returned generic 500s on auth failures, hiding misconfigured team permissions behind a confusing "internal error." Auth-class statuses now propagate as `AppError::BadGateway` (HTTP 502) with a precise operator-facing hint listing the four required DT team permissions (`BOM_UPLOAD`, `PROJECT_CREATION_UPLOAD`, `VIEW_PORTFOLIO`, `VIEW_VULNERABILITY`) and the exact endpoint URL. The bundled `docker/init-dtrack.sh` now grants those permissions automatically.
- **`SigningService` plaintext PGP and RSA secret material is zeroized** (closes #1328). Decrypted secret-key bytes (OpenPGP armored keys, RSA PKCS#8 PEM) are now held in `Zeroizing<Vec<u8>>` so they are wiped from heap when the buffer drops, rather than waiting for the allocator to reuse the slot. Upstream `rsa::RsaPrivateKey` and `pgp::SignedSecretKey` already self-zeroize; this closes the intermediate-buffer gap.
- **SSO callback installs auth cookies on the redirect itself** (closes #1405). OIDC and SAML callbacks previously 307-redirected to the frontend `/callback` page and only set cookies on the subsequent `POST /exchange`. On multi-replica backends the frontend's eager `GET /auth/me` could land on a replica before the cookie was installed, return 401, and bounce the user back to `/login` even though authentication had succeeded. Cookies are now installed on the 307 itself with `HttpOnly`, `Secure`, `SameSite=Strict`, and the access-token `Max-Age` honors the configured `jwt_access_token_expiry_minutes`. `/exchange` continues to re-set cookies idempotently for frontends that don't read the redirect cookies.
- **Incus extraction guards accept real OS images** (closes #1492). The guards added in #1428 were tight enough to reject every legitimate `incus export` rootfs: the 2 GiB compressed cap excluded real 3+ GiB exports, the post-extract guard walked the tree as non-root and EACCES'd on `/var/log`/`/root`, and absolute symlinks like `/var/run -> /run` (every real distro) were lexically rejected as workspace escapes. The compressed cap is now env-tunable (`MAX_INCUS_SCAN_COMPRESSED_BYTES`, default 8 GiB), the guard walks under `chmod -R u+rwX`, and absolute symlinks resolve chroot-style against the archive root. Path-traversal escapes (`../../../etc/shadow`, `/../../../etc/shadow`) remain rejected.

### Fixed

- **Migration worker streams artifact downloads to a temp file before put_stream** (closes #1422). `Artifactory` / `Nexus` clients were calling `response.bytes()`, buffering the full body in heap before storage write. Combined with the storage trait's default `put_file` (which read the temp file into a `Bytes` on cloud backends), large-artifact migrations OOM'd the host. The migration worker now opens upstream as a `bytes_stream`, spills to a `NamedTempFile` with incremental sha256/sha1 hashing, verifies expected checksums BEFORE the storage write (mismatches drop the temp file; corrupt bytes never reach permanent storage), and routes the final write through `put_stream` so S3 multipart, GCS resumable, Azure block-blob, and filesystem temp-and-rename all flow chunked.
- **OCI blob uploads stream end-to-end** (closes #1449). `handle_start_upload`, `handle_patch_upload`, and `handle_complete_upload` previously read the request body into a `Bytes`, wrote it to storage, then re-buffered on finalize. For multi-GiB container layer pushes this was catastrophic. Uploads now spill to a local temp file (256 KiB chunks, configurable via `AK_OCI_UPLOAD_TMP_DIR`), digest is verified against the on-disk file before storage commit (mismatch returns 400, temp dropped), and the final write uses `put_stream` so cloud backends ingest the layer chunked.
- **Incus uploads route through StorageBackend (S3 / GCS / Azure data-loss fix)** (closes #1471). Both `upload_image` (monolithic) and `complete_chunked_upload` ended with a `tokio::fs::rename` onto the server's local filesystem regardless of the repo's actual storage backend, so on S3/GCS/Azure-backed deployments the uploaded bytes never reached the bucket. Incus uploads now spill to a scratch temp file and call `StorageBackend::put_stream` at completion; `download_image` is the matching read fix via `storage.get_stream()`. The async-completion + 202 pattern for the multi-GiB gateway-timeout problem is filed as a v1.2.1 follow-up.
- **OCI manifest proxy writes an `artifacts` row so proxied tags appear in the WebUI** (closes #1357). `proxy_service::cache_artifact` had been forbidden from writing into `artifacts` (#1278) to avoid the doubled-prefix bug, but the OCI manifest cache path (which goes through the per-repo backend, not the global proxy cache) still needed its own indexing or the WebUI's Docker tag listing JOIN dropped every proxied tag. The handler now writes both digest-keyed and human-readable-tag-keyed `oci_tags` + `artifacts` rows for proxy pulls, and calls `record_oci_manifest_refs` for image-index manifests so multi-arch proxy pulls also surface their children.
- **Conan tokens accepted as Bearer on follow-up calls** (closes #1433). Pairs with the security item above; documented separately because the user-facing failure was "conan upload fails with 401."
- **npm `audit` no longer 404s** (closes #1400). The npm `audit` CLI calls `POST /-/npm/v1/security/advisories/bulk` (npm ≥ 7) and `POST /-/npm/v1/security/audits/quick` (npm 6 / yarn) as its first step. AK had neither route, so every `npm audit` against a Remote npm repo aborted. Remote repos now forward both endpoints to the configured upstream (graceful empty-`{}` fallback when upstream is down); Local/Staging/Virtual repos return a zero-vuln shape so the command exits clean.
- **Ansible Galaxy CLI publish works end-to-end** (closes #1451). The Galaxy CLI's first call is `GET /api/` for version negotiation; that endpoint did not exist, so every CLI invocation aborted on connect. `GET /:repo/api/` and `GET /:repo/api/v3/` now return the Pulp Galaxy NG service-index shape. The upload handler is rewritten to read the actual on-the-wire fields (`file` multipart with the canonical `<namespace>-<name>-<version>.tar.gz` filename + `sha256` text field), with the old JSON descriptor preserved as a galaxykit fallback.
- **RPM Remote proxy repos actually proxy upstream** (closes #1447). Every repodata handler (`repomd.xml`, `primary.xml.gz`, `filelists.xml.gz`, `other.xml.gz`, `updateinfo.xml.gz`, `repomd.xml.asc`) was generating index XML from the local `artifacts` table without consulting `repo.upstream_url`, so a freshly-created Remote RPM repo served an empty repomd that `dnf` interpreted as "nothing to install." Each handler now proxies the upstream object for Remote repos before falling back to local generation, with a new catch-all route handling hash-prefixed metadata paths and packages hosted at the upstream repo root (the `packages.gitlab.com` layout).
- **Maven proxy no longer caches zero-byte POMs** (closes #1365). `tee_upstream_to_cache` and the buffered `cache_artifact` path persisted a metadata sidecar even when `bytes_written == 0`, so an empty upstream 200 was permanently cached as a zero-byte POM and served forever (Gradle: "Content is not allowed in prolog."). Empty bodies are now refused at the cache layer; the next fetch self-heals.
- **OCI v2 proxy supplements client `Accept` for ghcr.io** (closes #1360). ghcr.io is strict about manifest media types: it returns 404 when the `Accept` header doesn't list a type matching the stored manifest. Docker Hub and Quay tolerate the missing media type with a fallback, so the bug only surfaced on ghcr-hosted images stored as OCI image indexes (e.g. `ghcr.io/gurucomputing/headscale-ui`). The proxy now supplements the client's `Accept` with the canonical OCI + Docker manifest media-type set on every manifest GET/HEAD fetch, preserving the client's q-value ordering at the front.
- **Composer hosted uploads populate the packages index** (closes #1341). The Composer upload handler wrote `artifacts` and `artifact_metadata` rows but never called `PackageService`, so Composer packages were served over the wire but never appeared in the WebUI Packages tab. Mirrors the npm/pypi/nuget pattern that was already in place.
- **Incus uploads populate the packages index** (#1477). Same gap as Composer above; Incus images now appear in the Packages tab on upload.
- **`scan_on_upload` triggers on every format-native upload** (closes #1467, #1468). The post-upload scan dispatch was missed by every format-native handler (incus, oci, helm, npm, pypi, cargo, maven, etc.). New `spawn_scan_on_upload` helper centralizes the trigger; format handlers now call it after a successful artifact insert.
- **Storage GC tests are isolation-safe under parallel llvm-cov** (closes #1493). Three GC tests had asserted on a global `storage_keys_deleted` counter that was inflated by other db-backed tests' in-flight soft-deletes when run in parallel under `cargo llvm-cov`. The assertions are now per-key via `select_orphans()` membership; same pattern as the prior #1179 fix.
- **Lifecycle policies cascade `oci_tags` removal for every policy type** (closes #1407). The cascade fix landed in #1416 but the regression suite only covered `max_age_days` and `tag_pattern_delete`. Tests are now in place for the four uncovered policies (`max_versions`, `no_downloads_days`, `tag_pattern_keep`, `size_quota_bytes`) plus an end-to-end test that re-runs storage GC's orphan predicate against post-cascade state.
- **Scanner dedup TTL is shortened for zero-finding rows** (closes #1469). A prior scan completing with zero findings could be a genuinely clean artifact OR a silent extraction failure; the 30-day dedup TTL was hiding rescans of broken scans. Rows with `findings_count = 0` now use a 1-day TTL while non-empty results retain the standard 30-day window. New `bypass_dedup: Option<bool>` admin-only knob on `TriggerScanRequest` for the operational case where dedup must be explicitly skipped.
- **Grype "binary not available" diagnostic distinguishes spawn vs runtime errors** (closes #1465). The wrapper substring-matched `"not found"` and `"No such file"` against grype's stderr to detect a missing binary, but those phrases also appear in legitimate grype errors (e.g. registry 404 on the image being scanned). A misleading "binary not available" log was firing on every registry-side failure. The check now uses `io::ErrorKind::NotFound` on the spawn result (the actual kernel signal for a missing executable); non-zero exits surface real stderr/stdout so registry/DB/auth failures are diagnosed at the right layer.
- **OpenSCAP wrapper allowlist accepts `/scan-workspace/`** (closes #1466). The wrapper's path allowlist defaulted to `/tmp/:/var/tmp/`, but the backend writes per-artifact scan workspaces under the configured `SCAN_WORKSPACE_PATH` (default `/scan-workspace`). No deployment surface set the override, so every fresh install hit HTTP 400 "scan path not found or not allowed" on the first OCI/Docker scan.
- **Scanner image refs use `@` for digest-pinned references** (closes #1483). Both `GrypeScanner` (registry mode) and `ImageScanner` (Trivy CLI) joined `name + ":" + reference`, which for digests produced `name:sha256:...` -- invalid per the OCI ref grammar (`:` is not allowed in tags). Both scanners now route through a `join_oci_image_ref` helper that switches to `@` when the reference is a digest. The Trivy HTTP/Twirp path was unaffected.
- **Single source of truth for Dependency-Track enabled state** (closes #1395, closes #1480). Three independent code paths (`DependencyTrackService::from_env`, the `system_config` handler, the health-monitor probe loop) each derived "is DT enabled?" from a different signal, so the UI reported contradictory states. The toggle is now a first-class `Config` field driven by `DEPENDENCY_TRACK_ENABLED`; system_config reports `state.dependency_track.is_some()`; the health monitor refuses to probe when the toggle is off (no log, no alert churn).
- **Incus URL builders honor the request mount prefix** (closes #1320). `build_download_url` and `build_upload_location` hardcoded `/incus/`, so repositories served under the alias `/lxc/` returned URLs that didn't work. A new `mount_prefix_from_uri` derives the prefix from the inbound `OriginalUri` so SimpleStreams catalogs and Location headers stay consistent. Substring-misclassification guard rejects `/incus/lxc-images/...` from being mistaken for `/lxc`.
- **SBOM non-SPDX licenses emitted as `license.name` not `license.id`** (closes #1474, #1475). Dependency-Track's CycloneDX validator rejected SBOMs containing any license string that wasn't on the SPDX enumeration (`license.id`), 400-ing the submission. Non-SPDX strings now emit as `license.name` (free-text) per the CycloneDX schema.
- **CVE acknowledge path repointed at `scan_findings`** (closes #1426). The Security tab read had been moved to `scan_findings` synthetic projections (#1375), but the write path (`POST /sbom/cve/status/{id}`) was still pointed at the long-empty `cve_history` table, so every acknowledge click 404'd. New `POST /api/v1/sbom/cve/status/by-artifact/{artifact_id}/by-cve/{cve_id}` bulk-updates `scan_findings.is_acknowledged` directly. `cve_history` is retained for legacy hand-populated rows but marked deprecated for v1.3.0 removal.
- **Failed format-native scanners no longer roll up to overall `completed`** (closes #1497). `fetch_docker_tag_rows` previously aggregated scan status by picking the most recent row across all scanners; a successful grype run finishing after a failed incus-format scan rolled up to `completed`, masking the failure. Status is now aggregated per-scanner via `array_agg(DISTINCT status)` over the latest row per `scan_type` and collapsed by a new `rollup_scan_status` helper (precedence: `running` > `pending` > all-completed > all-failed > mixed-terminal → `partial`).
- **`flaky` npm coverage test root-caused and fixed** (closes #1490). `npm::tests::test_remote_proxy_download_scoped_tarball_hits_encoded_upstream_path` had been blocking the Coverage CI job on every PR (and main itself for three commits) but passing in regular Unit Tests. Diagnosed: Unit Tests run without `DATABASE_URL`, so `tdh::Fixture::setup` returns `None` and the test silently no-ops; Coverage provisions Postgres so the test runs for real. The test's wiremock mock expected `%2F`-encoded scope in the upstream path, but PR #1478 had changed the proxy to keep the scope separator as a literal `/`. One-line mock-path fix; 5/5 deterministic passes under `llvm-cov`.
- **`dtrack-init` grants required DT team permissions on warm restart, not just fresh deploy** (closes #1530). PR #1511 added the four-permission grant loop (`BOM_UPLOAD`, `PROJECT_CREATION_UPLOAD`, `VIEW_PORTFOLIO`, `VIEW_VULNERABILITY`) but placed it after the `exit 0` short-circuit that fires when `/shared/dtrack-api-key` already exists. Every operator upgrading in place from v1.1.x (which is most of them through this release candidate) therefore kept the pre-#1511 empty permission set, and SBOM uploads silently 403'd. The grant loop is now hoisted above the short-circuit, with a `KEY_ALREADY_PROVISIONED` flag still gating the rotate/mint block so existing keys are not regenerated. DT's permission endpoint is idempotent (200 on first grant, 304 thereafter), so warm-restart re-grants are cheap. The bundled `docker/test-init-dtrack.sh` Phase 2 now asserts `POST count == 8` (4 cold + 4 warm) after a restart cycle, which fails on the pre-fix script.

### Known issues (target v1.2.1)

- **SBOM generation returns `components: []` for native-protocol uploads** (#870). Two stacked gaps cause this for npm tarballs, Maven JARs, and most other format-native uploads:
  1. `extract_dependencies_for_artifact` reads only from `scan_packages` / `scan_findings` and does not fall back to format-native parsed metadata stored in `artifact_metadata.metadata` (POM dependencies, npm `version_data.dependencies`, etc.).
  2. The `spawn_scan_on_upload` helper added in #1468 is wired only to the incus handler. Every other format-native handler (maven, npm, pypi, cargo, nuget, rubygems, composer, helm, rpm) skips the scan trigger even with `scan_on_upload = true` on the repo (and the default is `false` anyway).
  Combined, a native-protocol-uploaded artifact never has a scan, so `scan_packages` stays empty, so the SBOM stays empty.
  Workaround: enable `scan_on_upload` on the repository and trigger `POST /api/v1/security/scan` once per already-uploaded artifact, then regenerate the SBOM. A proper fix (per-format metadata fallback + wiring the scan trigger across all native handlers) is multi-PR and tracked for v1.2.1.

### Changed

- **Dependency-Track is opt-in in `docker-compose.yml`** (closes #1432). The bundled DT apiserver runs a continuous NVD mirror and OSS-Index analyzer even at zero load, consuming ~4 GiB RAM and pegging the shared Postgres at ~60% CPU. The default compose stack no longer starts DT; operators who want it run `docker compose --profile dtrack up`. `.env.example` documents the opt-in flow.
- **CI Clippy step caps parallel jobs to avoid runner OOM** (closes #1515). `cargo clippy --workspace --all-targets` was OOM-killing the ARC runner pod ("sccache: Compile terminated by signal 9") on PRs intermittently. The step now sets `CARGO_BUILD_JOBS=4` to keep the lib-test compile under the runner's memory budget while staying under the Tier-1 < 5min target.

### Dependencies

(No new dependency bumps in this RC beyond what landed in rc.2.)

Second release candidate for v1.2.0. Folds in ~40 PRs merged since rc.1, covering smoke-E2E CI breakage, the v1.1.9 -> v1.2.0 migration repair path, scanner archive extraction, format-handler bugs across Maven, PyPI, OCI, NuGet, Debian, Hex, Incus/LXC, CocoaPods, and several auth scope-enforcement fixes uncovered during the release-gate audit.

### Sponsors

Thank you to our sponsors for keeping Artifact Keeper development moving.

- [@dragonpaw](https://github.com/dragonpaw) (Ash A.)
- [@injectedfusion](https://github.com/injectedfusion) (Gabriel Rodriguez)

[Become a sponsor](https://github.com/sponsors/artifact-keeper) to support the project and get your name listed here.

### Thank You

Community contributors who shipped fixes in this release candidate:

- [@ThaSami](https://github.com/ThaSami) for the reverse-path functional index that makes suffix LIKE queries indexable (#1285), the migration session timeout fix (#1269), the MigrationService column-name corrections (#1268), PyPI virtual member union on `simple/<project>/` (#1267), the non-admin admin-scope escalation block (#1261), and the `/users` router split so non-admins can self-manage tokens (#1258)
- [@dragonpaw](https://github.com/dragonpaw) for the Incus server-wide `STORAGE_PATH` staging fix (#1297) and runtime-agnostic admin-password retrieval docs (#1271)
- [@danatri](https://github.com/danatri) for the JFrog 7.38.10 cache-artifact migration reliability fixes (#1295)
- [@lesaux](https://github.com/lesaux) for the generic-proxy streaming fix (#1294)
- [@joonhwan](https://github.com/joonhwan) for trailing-slash acceptance on NuGet push plus the packages index population (#1289)
- [@axellpadilla](https://github.com/axellpadilla) for refetching stale PyPI proxy-cache hits instead of returning 500 (#1283)
- [@junsung-cho](https://github.com/junsung-cho) for switching Dependency-Track API-key creation to PUT (#1270)
- [@D13410N3](https://github.com/D13410N3) for real OpenPGP signatures on Debian repository metadata (#1236)
- [@JojoMee](https://github.com/JojoMee) for the docker-compose file updates (#1231)

### Security

- **Non-admins can no longer grant admin-class scopes on token issuance** (#1261). The token-issuance path accepted any scope string the caller asked for without checking that the caller themselves held the scope they were granting, so a non-admin with a valid session could mint a personal access token carrying `admin` or `*` and chain into full admin. Token issuance now refuses any scope the caller does not already hold, with admin-class scopes (`admin`, `*`, write-on-system) gated explicitly. Existing tokens are not invalidated by this change; rotation is recommended on any token whose origin cannot be audited.
- **`/users` router split so non-admin self-service paths no longer share admin middleware** (closes #1257, #1258). The router previously mounted both admin user-management endpoints and self-service endpoints (`/users/me/tokens`, `/users/me/password`) under the same admin-only middleware tree, which forced every self-service request through the admin gate and returned 403 for legitimate users. The router is now split into an `/admin/users` subtree for admin-managed operations and a `/users` subtree for self-service, each with the correct middleware. No protocol change; the previously-broken self-service paths now work as documented.

### Fixed

- **Smoke E2E virtual repo creation now supplies `member_repos`** (closes #1353, #1354). The smoke E2E suite created virtual repos without `member_repos`, which #1281 then started rejecting at create time with HTTP 400. The smoke suite now passes the required `member_repos` array, restoring green CI on the release gate.
- **v1.1.9 -> v1.2.0 upgrade no longer aborts with `VersionMismatch(73)`** (closes #1277, #1335). Customers upgrading from v1.1.9 to v1.2.0-rc.1 hit a hard startup failure because the migration runner detected checksum drift between the on-disk `073_account_lockout.sql` and the version-73 row recorded in `_sqlx_migrations` from the legacy duplicate-073 window. The pre-migration repair step introduced in #1138 now also covers the v1.1.9-shipped checksum, so affected installs upgrade cleanly without operator intervention. See `backend/src/migration_repair.rs` for the exhaustive list of accepted prior checksums.
- **Remaining buffered proxy fetches now stream** (closes #1215, #1334). A subset of proxy-fetch paths still buffered upstream responses fully before relaying to the client, defeating the streaming refactor that landed earlier in the v1.2.0 cycle. Those paths now stream end-to-end, capping per-request memory at the configured buffer size regardless of upstream artifact size.
- **Grype-matched components included in `scan_packages` inventory** (closes #1273, #1333). Grype's component matches were recorded against `scan_findings` but never written to `scan_packages`, so the SBOM inventory view and `affected_component` -> `scan_packages` join surface showed empty package lists for Grype-only scans. Grype-matched components are now upserted into `scan_packages` alongside Trivy's, keyed by `(package_name, package_version, package_type)` with `source_target` populated from the scan input path.
- **Scanner archive extraction uses the `tar` crate, not host `tar`** (closes #1243, #1330). Builds on the #722 refactor: the npm `.tgz` extraction path still shelled out to `Command::new("tar")`, so Alpine-based images (which do not install `tar`) silently fell through to scanning raw archive bytes. Extraction is now fully in-process via the `tar` crate on `spawn_blocking`, matching the path other archive formats already took.
- **Generic proxy streaming** (#1294). Generic-format proxy fetches now stream upstream responses through to the client instead of buffering. Aligns the generic handler with the streaming guarantees other format handlers already provide.
- **Dependency-Track project name uses the artifact name** (closes #1276, #1324). The DT project record was created with the repository key as its name, which collapsed every artifact in a repo onto one DT project and made findings ambiguous. The DT project name is now the artifact name; existing one-per-repo projects continue to work, new uploads create one project per artifact.
- **Dependency-Track API-key creation uses PUT** (#1270). DT 4.13 changed the API-key creation endpoint from POST to PUT, so AK was returning 405 from the DT integration setup flow. The handler now issues PUT, matching the current DT API.
- **Maven virtual local-member match honors `groupId`** (closes #1287, #1323). The virtual-repo download-side shadowing guard for Maven matched only on `artifactId`, so a local member's `com.example:utils` blocked an upstream `org.other:utils` even though the coordinates differ. The match now uses the full coordinates (`groupId:artifactId`), restoring the upstream fetch for non-shadowing requests.
- **Multipart upload honors the custom artifact path** (closes #1237, #1322). Multipart `POST /repositories/{key}/artifacts` ignored the optional `path` field and always wrote to `<repo>/<filename>`, so operators using path-prefixed layouts (Maven-style `com/example/...`, generic `releases/2026/...`) had to PUT a second time to move the file. Multipart now respects `path` end-to-end, matching the single-PUT upload behavior.
- **CocoaPods served podspec preserves every uploaded field** (closes #1286, #1321). The serve path round-tripped podspec JSON through a struct with only a fixed set of named fields, dropping `vendored_frameworks`, `xcconfig`, `requires_arc`, `swift_version`, `resource_bundles`, `subspecs`, and ~30 other linker-affecting fields. `PodSpec` now carries a `#[serde(flatten)] extra` catch-all so the served JSON is a faithful round-trip of the upload.
- **`lxc`-format repositories respond on `/lxc/*`** (closes #1272, #1318). Repositories created with `format: lxc` 404'd on every request because `create_router` only mounted the Incus handler under `/incus`. The same `IncusHandler` router is now nested under `/lxc` as well, matching the `Lxc -> Incus` aliasing the rest of the stack already used.
- **Image scanner emits bare package name** (closes #1311, #1312). The image-scanner code path emitted `affected_component` carrying the parenthetical target alongside the package name, contradicting #903 / #1150 which already established the bare-name convention everywhere else. The image scanner now matches, completing the rollout of #1159.
- **Incus uses server-wide `STORAGE_PATH` for staging** (#1297). The Incus handler resolved its staging directory from `repo.storage_path`, which on filesystem deployments with per-repo storage paths pointed at a directory the handler did not own. Staging now uses the server-wide `STORAGE_PATH` like every other handler, fixing publish on filesystem backends with custom per-repo storage.
- **JFrog 7.38.10 cache-artifact migration reliability** (#1295). The Artifactory 7.38.10 migrator's cache-artifact phase mis-handled responses that lacked an explicit `Content-Length`, causing intermittent migration failures. The phase now reads the response body to completion before recording the migrated artifact.
- **NuGet push accepts trailing slash and populates packages index** (#1289). `PUT /nuget/{repo}/v3/registration5-semver1/` (with the trailing slash) returned 404 because the route was registered without the slash, and successful uploads did not refresh the `/v3/index.json` packages list. Both are fixed.
- **PyPI refetches stale proxy-cache hits instead of returning 500** (#1283). When the proxy-cache had a stale entry for a PyPI project, the handler returned 500 instead of refetching upstream. The handler now treats stale entries as cache misses, refetches, and stores the fresh response.
- **PyPI virtual members union on `simple/<project>/`** (#1267, #1230). Virtual PyPI repos previously returned only the first member's simple index for a given project; the handler now unions entries across all non-Remote members (Remote members fall back to the existing remote-fetch path).
- **Functional `reverse(path)` index on `artifacts`** (closes #1266, #1285). Suffix-match queries (`WHERE path LIKE '%/Cargo.toml'`) were not sargable against the existing `path` btree, so they fell back to a sequential scan on installs with many artifacts. A new `idx_artifacts_reverse_path ON artifacts (reverse(path))` lets the planner answer suffix LIKEs as a prefix-match against the reversed text. Build cost is a single index pass at migration time; runtime cost is the existing planner choice.
- **Migration session timeouts** (#1269). `sqlx migrate run` ran with the cluster's default `statement_timeout` and `lock_timeout`, which on installs with tuned-low defaults (5s) aborted long-running schema migrations partway through. The migration session now sets `statement_timeout=0` and `lock_timeout=0` for the duration of the migration run, restoring the previous "migrations are not subject to OLTP timeouts" behavior.
- **MigrationService column names and NOT NULL columns** (#1268). Two recently-added migrations referenced columns that did not match the live schema (a leftover from a rename mid-PR) and omitted NOT NULL columns that the migration runner inserts into. Both are corrected; affected migrations were never executed against any production install because the runner rejected them at parse time.
- **Repair sync fast-path on replica-safe credential check** (#1248 follow-up, #1265). The sync `is_token_invalidated` fast-path inside `is_token_invalidated_replica_safe` collided with the `fetch_credential_change_watermark` DB cache TTL on fresh non-admin users, returning 401 on every request after the first within the 5-second cache window. The sync fast-path is removed from the replica-safe entry point; the existing DB cache provides the same acceleration without the conflation. Adds the `two_sequential_admin_requests_with_fresh_jwt_both_return_403` regression test.
- **Forward client `Accept` header to upstream on OCI manifest pulls** (#1256). The OCI manifest-pull proxy stripped the client's `Accept` header before relaying upstream, so upstreams that content-negotiate on manifest media type (OCI vs Docker v2 vs Docker v2.1) returned the wrong manifest, breaking `docker pull` against some Docker Hub images.
- **Persist `sha1`/`md5` on upload and index for checksum lookup** (#1254). Per-upload `sha1` and `md5` were computed but only written to OpenSearch as metadata; the database row only carried `sha256`. Search-by-checksum on `sha1` or `md5` returned no results even when the artifact was indexed. Both checksums are now persisted on `artifacts` and indexed for lookup parity with `sha256`.
- **Pin Grype DB auto-update and drop `-q`** (#1252). The Grype invocation passed `-q` and let Grype auto-update its vulnerability DB at scan time, which on offline / network-restricted installs aborted every scan with a DB-download error and on online installs masked the error behind the `-q` quiet flag. Auto-update is now pinned (`db.auto-update: false`) so the operator-curated DB is the source of truth, and `-q` is removed so errors surface in scan output.
- **Self-service password change mounted under `/auth`, not `/admin`** (#1250). `POST /api/v1/users/me/password` was mounted under the admin router, so a non-admin user could not change their own password through the documented self-service path. The endpoint now lives under `/auth` where the rest of the self-service auth surface is mounted.
- **Debian repository metadata uses real OpenPGP signatures** (#1236). The Debian handler emitted `Release.gpg` and `InRelease` carrying placeholder ASCII-armored blocks instead of real detached OpenPGP signatures over the `Release` file, so `apt update` aborted with `BADSIG` against any repository with `Signed-By` configured (the documented setup). The handler now signs `Release` with the repository's configured signing key, producing valid `Release.gpg` and inline-signed `InRelease`.
- **Apply rustfmt to unblock v1.2.0-rc.2** (closes #1337, #1339). Mechanical `cargo fmt` pass over the tree to satisfy the rustfmt check gate after a batch of merges that landed slightly mis-formatted.
- **Test-email endpoint accepts `recipient` alias** (closes #1332, #1338). `POST /api/v1/admin/smtp/test` rejected requests carrying `recipient` (the field name the web UI and docs advertised) because the handler only accepted `to`. The handler now accepts both, with `recipient` as the canonical name going forward.
- **Reject virtual repo create with no members at 400** (closes #1279, #1281). `POST /api/v1/repositories` accepted virtual repos with no members at create time, then 404'd every subsequent fetch. The handler now returns `400 Bad Request` at create time, naming the expected `member_repos: [{repo_key, priority}, ...]` shape.
- **Stop inserting proxy-cached items into `artifacts`** (closes #1278, #1280). `ProxyService::cache_artifact` previously wrote a row to `artifacts` for every proxy-cached item, which on filesystem backends produced a doubled-prefix storage key that 500'd on every subsequent read. The proxy hot path already serves cache hits via the storage layer directly, so the `artifacts` insert is removed.

### Changed

- **Server-side Docker tag aggregation** (closes #1193, #1336). The Docker tag listing page in the web UI previously fetched every manifest individually and aggregated tags client-side, which on repos with thousands of tags ran into request fan-out limits. Tag aggregation now happens on the backend via a single `GET /repositories/{key}/docker/tags?aggregate=true` endpoint that returns tag lists pre-grouped by digest. The legacy per-manifest path remains for clients that still need it.
- **`AK_SSRF_ALLOW_PRIVATE_CIDRS` env var** (closes #1224, #1325). The SSRF guard rejected outbound requests to RFC1918 / loopback / link-local addresses unconditionally, which broke proxy-fetch against on-prem upstreams (a local Nexus / JFrog inside the same VPC). The new `AK_SSRF_ALLOW_PRIVATE_CIDRS` env var accepts a comma-separated allowlist of CIDR ranges that bypass the private-address check. The default is the empty list, preserving the previous behavior; operators must opt in explicitly.
- **Enforce PR-issue link via GitHub Actions** (#1310). New repo workflow requires every PR to link a tracking issue via the `Closes #<n>` / `Fixes #<n>` footer. Aligns with the linked-issue rule already documented for releases.
- **Docs note `affected_component` format change** (closes #1159, #1223). Adds an upgrade-notes entry making the v1.2.0 `affected_component` -> bare-name change explicit in the CHANGELOG and pointing integrations at `scan_packages.source_target`.
- **Update docker-compose files** (#1231). Refreshes the bundled `docker-compose.local-dev.yml` and `docker-compose.demo.yml` to match the v1.2.0 image set, service names, and required env vars; clears two stale env keys that no longer have any effect.
- **Runtime-agnostic admin password retrieval docs** (#1271). The first-time-setup docs previously assumed Docker as the runtime when telling operators how to retrieve the generated admin password; the new copy works for Docker, Podman, and Kubernetes deployments without rewording.

### Dependencies

- **Bump `wasmtime-wasi` to 36.0.10** (closes #1351, #1352). Picks up the upstream RUSTSEC-2026-0149 fix.
- **Bump `ubi9/ubi` from 9.7 to 9.8** (#1293).
- **Bump `ubi9/ubi-micro` from 9.7 to 9.8** (#1291).
- **Bump `github/codeql-action` from 4.35.4 to 4.35.5** (#1292).
- **Bump `actions/stale` from 10.2.0 to 10.3.0** (#1290).
- **Bump `totp-rs` from 5.7.0 to 5.7.1** (#1227).
- **Bump `mimalloc` from 0.1.48 to 0.1.51** (#1226).
- **Bump `anchore/grype` from v0.111.1 to v0.112.0** (#1082).

## [1.1.10] - 2026-05-11

### Security

- **`dtrack-init` no longer silently revokes operator-attached `Automation` team keys** (#978, #1039, #1041) -- prior to this release, [`docker/init-dtrack.sh`](docker/init-dtrack.sh) deleted *every* `publicId` attached to the Dependency-Track `Automation` team on each cold start (empty `/shared/dtrack-api-key`). Any third-party integration (CI scanner, dashboard, webhook receiver) with a key on that team would have its credential revoked with no audit trail. The init container now records the `publicId` it minted in `/shared/.dtrack-publicid` (mode `0600`, atomically rewritten via `.tmp` + `rename`) and **refuses to rotate (exit 2)** when any foreign `publicId` is present on the team, naming each foreign key in stderr. Operators who want the previous unconditional-rotation behavior can set `DTRACK_INIT_FORCE_ROTATE=true`; the script then logs a `WARNING` listing every revoked `publicId` so the rotation is auditable.

  **Discoverability:** a refusal exits the init container non-zero with the diagnostic on stderr, which surfaces as `Init:CrashLoopBackOff` on the pod and as a non-zero `restartCount` in standard Kubernetes alerting (Prometheus `kube_pod_container_status_restarts_total`). Operators who page on init-container failures will see this signal without reading the CHANGELOG.

  **To audit your `Automation` team for foreign keys before upgrading**, run (substituting your DT URL and admin token):

  ```sh
  curl -sf -H "X-Api-Key: $DT_ADMIN_KEY" "$DT_URL/api/v1/team" \
    | jq '.[] | select(.name=="Automation") | .apiKeys[] | {publicId, maskedKey}'
  ```

  Every `publicId` in that list other than the one recorded in your running pod's `/shared/.dtrack-publicid` (or, on first install of v1.1.10, every entry) is foreign and must be either removed via the DT UI (`Administration` -> `Teams` -> `Automation` -> `API Keys`) before upgrade or explicitly accepted via `DTRACK_INIT_FORCE_ROTATE=true`.

  **Recommended long-term posture:** do not attach third-party integrations to the `Automation` team. Provision a separate DT team for external integrations; only the bundled artifact-keeper backend should consume keys from the `Automation` team.

### Added

- **CI: shell-tests are now a Tier 1 required check** (#1040) -- new `shell-tests` job in [`.github/workflows/ci.yml`](.github/workflows/ci.yml) runs `docker/test-init-dtrack.sh` on `ubuntu-latest` (capped at `timeout-minutes: 5`) and is aggregated into `ci-complete`. Self-contained — uses only `bash`, `python3`, `jq`, `curl` from the runner image; kept off the Rust critical path so cargo jobs are not blocked.

### Changed

- **`dtrack-init`: post-upgrade orphaned-key hygiene check** (#1039) -- during Kubernetes rollouts two init containers can race against the same `Automation` team and create two keys, leaving one orphaned (still a valid bearer credential against the DT API until the next cold-start rotation deletes it). The orphan window can be days or weeks in a stable cluster, and an orphan that leaks via a backup, screenshot, or log scrape remains a fully-valid key for that entire period. After every `helm upgrade`, review `Administration` -> `Teams` -> `Automation` -> `API Keys` in the DT UI and delete any entry whose `publicId` does not match the value recorded in the running pod's `/shared/.dtrack-publicid` (`kubectl exec <pod> -c <init-container> -- cat /shared/.dtrack-publicid`). A leader-election fix that closes the race in code is tracked as a follow-up and will land in a future release.
- **Mock fidelity and assertion coverage in `docker/test-init-dtrack.sh`** (#1041) -- the regression-test mock now returns HTTP 200 on `POST /api/v1/team/{uuid}/key` to match the DT 4.11 swagger contract, and the assertion suite was expanded to cover (a) warm-start short-circuit log content (not just `POST` count), (b) foreign-key refusal and `DTRACK_INIT_FORCE_ROTATE` override paths, (c) an injected 5xx from `POST /key` to exercise the script's negative branch, and (d) absence of half-written `dtrack-api-key` / `.tmp` files after a failed run. The mock-fidelity change alone is **not** drift detection — the init script uses `curl -sf` which accepts any 2xx — but the new explicit response-code assertions in the negative-path test do detect drift in the failure direction.
## [1.1.6] - 2026-04-20

### Sponsors

Thank you to our backers for supporting ongoing development:

- **Ash A.** ([@dragonpaw](https://github.com/dragonpaw))
- **Gabriel Rodriguez** ([@injectedfusion](https://github.com/injectedfusion))

[Become a sponsor](https://github.com/sponsors/artifact-keeper)

### Fixed

- **PyPI: simple index now normalizes package names per PEP 503** (#798) -- packages with underscores, dots, or mixed case are now normalized to lowercase hyphens in the simple index, and lookups are case-insensitive.
- **npm: version-specific metadata endpoint** (#799) -- `GET /npm/{repo}/{package}/{version}` now returns the specific version object extracted from the packument, supporting both regular and scoped packages.
- **Go, Terraform, Swift: return 404 for nonexistent resources** (#800) -- version listings and release lookups for modules/packages that don't exist now return 404 instead of an empty 200. Empty repositories still work correctly.
- **Conda native: .conda package format key corrected** (#801) -- `derive_format_key()` was producing `"condanative"` instead of `"conda_native"` for multi-word format variants, breaking format handler registration. Also affects `wasm_oci` and `helm_oci`.
- **HuggingFace: long model name validation** (#802) -- model names over 255 characters, revisions over 255 characters, or paths over 2036 characters now return 400 with a descriptive error instead of a 500 database constraint violation.
- **Conan: revision count no longer inflated** (#803) -- uploading multiple files to the same recipe revision (conanfile.py, conanmanifest.txt, conan_export.tgz) now correctly counts as one revision, not three. The revisions query uses GROUP BY on the revision hash.

## [1.1.5] - 2026-04-19

### Sponsors

Thank you to our backers for supporting ongoing development:

- **Ash A.** ([@dragonpaw](https://github.com/dragonpaw))
- **Gabriel Rodriguez** ([@injectedfusion](https://github.com/injectedfusion))

[Become a sponsor](https://github.com/sponsors/artifact-keeper)

### Thank You

- @thimomulder for the OpenSearch backend request that drove the search engine migration (#462), and for an extensive backlog of enhancement requests around password policy, notifications, SMTP, and rate limiting that shaped the v1.2.0 roadmap
- @Firjens for three high-quality bug reports with clean reproductions: OTLP gRPC `FRAME_SIZE_ERROR` behind Envoy Gateway (#729), Dependency-Track HTTP rejection on private networks (#764), and the matchit route conflict that blocked startup after the Debian fix (#832). Also for the OpenTelemetry http/protobuf prototype PR (#733) that became #812.
- @ReneBeszon for reporting that virtual Maven repos could not serve SNAPSHOT artifacts (#839)
- @ddevz for two Debian remote proxy bugs: missing `.xz` index support (#810) and `admin.password` not appearing on first boot in some volume configurations (#787)
- @junsung-cho for catching the OCI 401 loop where blob `HEAD` requests dropped the bearer token because `WWW-Authenticate` omitted `scope` (#811)
- @Dreamacro for the S3 HTTP connection pool tuning PR (#772), which fixes TIME_WAIT exhaustion under sustained load

### Breaking Changes

- **Search backend replaced**: Meilisearch has been removed in favor of OpenSearch 2.x as the indexing backend (#462, #830). This enables HA search clustering without Meilisearch's Enterprise license.
  - Removed environment variables: `MEILISEARCH_URL`, `MEILISEARCH_API_KEY`
  - Added environment variables: `OPENSEARCH_URL`, `OPENSEARCH_USERNAME`, `OPENSEARCH_PASSWORD`, `OPENSEARCH_ALLOW_INVALID_CERTS`
  - Default port changed from 7700 to 9200
  - Health response field renamed from `meilisearch` to `opensearch`
  - No data migration required. The backend auto-reindexes from PostgreSQL on startup.
  - Operators using the bundled Docker Compose stack should pull the updated `docker-compose.yml` and `install.sh` (#858) before upgrading.

### Added

- **OpenSearch search backend** (#462, #830) - new `opensearch_service.rs` using the `opensearch` crate v2.x, with path_hierarchy tokenizer, edge_ngram analyzer, text+keyword multi-fields, bulk indexing via `_bulk` NDJSON, and refresh disabled during full reindex. `ArtifactDocument` gains an `is_public` field used for visibility filtering. 64 unit tests added.
- **OTLP http/protobuf transport** (#729, #812) - selectable via the standard `OTEL_EXPORTER_OTLP_PROTOCOL` environment variable. Works around the h2/tonic `FRAME_SIZE_ERROR` that occurred when running behind Envoy Gateway. Based on the prototype contributed by @Firjens in #733.
- **PermissionService for fine-grained access control** (#816, #824) - resolves user-direct and group-based permissions via a single SQL query against the `permissions` and `user_group_members` tables (migration 018). Provides `check_permission`, `list_user_permissions`, and helpers for evaluating effective access.
- **Repository-level permission enforcement in visibility middleware** (#817, #826) - `repo_visibility_middleware` now consults PermissionService after existing visibility and token-scope checks. Admin users bypass; rules are silently ignored when no rules exist on a repository, preserving existing behavior for installations that have not configured permissions.
- **Fine-grained permission checks on admin-level endpoints** (#818, #827) - `create_repository`, `update_repository`, and `delete_repository` now accept non-admin users that hold the relevant permission action on the system sentinel or target repository.
- **`enforcement_enabled` exposed in system config** (#819, #828) - the `/api/v1/system/config` endpoint reports `enforcement_enabled: true` so frontends can surface permission UI affordances.
- **Configurable S3 HTTP connection pool** (#772) - new `S3_POOL_MAX_IDLE_PER_HOST` (default 256) and `S3_POOL_IDLE_TIMEOUT_SECS` (default 90) environment variables let operators tune connection reuse and prevent TIME_WAIT exhaustion under sustained load. Contributed by @Dreamacro.
- **Group detail endpoint returns members** (#813) - `GET /api/v1/groups/{id}` now returns a `GroupDetailResponse` including the actual member list, fixing a frontend rendering bug where every group appeared empty.

### Changed

- **Search authorization model** (#829) - all six search endpoints now resolve the caller's accessible repository IDs from `role_assignments` and filter results accordingly, replacing the previous binary `public_only` flag. Closes five High-severity authorization findings discovered during OpenSearch migration review.
- **Coverage gate scope** (#822) - the new-code coverage gate now parses unified diffs and measures only added or modified lines, rather than the entire file. This unblocks small fixes to large handler files (e.g., `debian.rs`, `oci_v2.rs`) that previously could not meet the 70% threshold for unrelated existing code.
- **Coverage workflow skipped on non-Rust changes** (#855) - dependabot bumps and other non-`.rs` changes no longer trigger the 3-5 minute instrumented coverage build.
- **Bundled Trivy bumped from 0.69.3 to 0.70.0** (#807, #823) - resolves 1 CRITICAL and ~16 HIGH container scan findings in the bundled scanner binary.
- **Permission gap warnings** (#794, #820) - the backend now emits `warn!` log entries on startup when permission rules exist in the database but enforcement middleware is not active, making the previously silent gap visible to operators upgrading from earlier 1.1.x versions.
- Dependency bumps: rust 1.94-bookworm to 1.95-bookworm (#846), anchore/grype v0.111.0 to v0.111.1 (#845), openssl 0.10.76 to 0.10.78 (#849), aquasecurity/trivy-action 0.35.0 to 0.36.0 (#842), actions/setup-node 4 to 6 (#843), docker/build-push-action 7.0.0 to 7.1.0 (#841).

### Fixed

- **Maven virtual repos serve SNAPSHOT artifacts** (#839, #859) - both the version-level `maven-metadata.xml` and the SNAPSHOT JAR/POM download paths now traverse member repositories and resolve correctly. Reported by @ReneBeszon.
- **Debian backend panics on startup** (#832, #854) - axum's matchit router rejected the wildcard and parameter routes introduced in #814 because they shared a prefix segment. The catch-all dists proxy has been restructured so the router accepts both routes. This was causing a panic loop on every backend start since the Debian remote proxy fix landed.
- **Debian remote proxy `.xz` indices** (#810, #814) - remote Debian repos now serve `.xz`-compressed `Packages` files (using the `xz2` crate already in the dep tree) and proxy unrecognized files under `dists/` through to the upstream, fixing 404s on i18n Translation files. Reported by @ddevz.
- **OCI bearer token loop on blob HEAD** (#811, #821) - the `WWW-Authenticate` challenge now includes the OCI Distribution Spec `scope` parameter, so Docker clients correctly key their token cache and attach the token to subsequent blob and manifest requests. All OCI endpoints now accept Basic auth in addition to Bearer. Reported by @junsung-cho.
- **Dependency-Track HTTP rejection on private networks** (#764, #825) - the reqwest client previously enforced `https_only(true)`, causing opaque "builder error for url" failures when `DEPENDENCY_TRACK_URL` pointed at localhost, RFC 1918 private networks, or in-cluster Kubernetes services. The client now allows HTTP for private network targets. Also fixes the missing SBOM submission path so SBOMs are uploaded after artifact ingest. Reported by @Firjens.
- **`admin.password` missing on first boot** (#787, #815) - the storage directory is now created explicitly before the password file is written, fixing first-boot under bind mounts, Podman rootless, Kubernetes `emptyDir`, and custom `STORAGE_PATH` values. Reported by @ddevz.
- **OTLP http/protobuf NoHttpClient panic** (#812, #835) - added `opentelemetry-http` with the `reqwest` feature as a direct dependency to ensure the HTTP client registers when two `reqwest` versions coexist in the dep tree.
- **jsonwebtoken CryptoProvider test failures** (#835) - enabled the `aws_lc_rs` feature on `jsonwebtoken 10.3` so unit tests that bypass `main()` no longer panic on missing CryptoProvider. Fixes 11 pre-existing test failures in `auth_service` and `grpc::auth_interceptor`.
- **Search results leaked private repos to authenticated users** (#829) - any authenticated user previously saw all repository search results regardless of role assignments. Now filtered per-caller using `role_assignments`.
- **Conan `recipe_latest` and `recipe_revisions` now scope by user/channel** - the two endpoints previously ignored the `{user}` and `{channel}` path segments, so a recipe uploaded as `mylib/1.0.0@myuser/stable` could appear in the latest/revisions response for `mylib/1.0.0@_/_` (and vice versa), breaking dependency resolution for Conan clients that share a recipe name across namespaces. Both queries now filter on `am.metadata->>'user'` and `am.metadata->>'channel'`, matching the existing pattern in `recipe_files_list` and the package download handler. Discovered during the v1.2.0-rc.1 release-gate run (24934467423).

### Security

- **rustls-webpki CRL parsing panic** (#835) - bumped `rustls-webpki` from 0.103.12 to 0.103.13 (RUSTSEC-2026-0104, reachable panic in CRL parsing).
- **Permission enforcement gap closed** (#794, #816, #817, #818, #819, #820, #824, #826, #827, #828) - the `permissions` table introduced in migration 018 was previously CRUD-only with no enforcement, so administrators could create rules that the backend silently ignored. Phases 1 through 4 of the enforcement plan are now merged: PermissionService, repo_visibility_middleware enforcement, handler-level checks on admin endpoints, and the `enforcement_enabled` system-config flag. Operators upgrading from 1.1.x with existing permission rules should review the rules before deploying 1.2.0, since they will now be enforced.
- **Search authorization tightened** (#829) - five High-severity authorization findings resolved by replacing the binary `public_only` model with per-caller repo ID resolution.

## [1.1.2] - 2026-04-09

### Sponsors

Thank you to our backers for supporting ongoing development:

- **Ash A.** ([@dragonpaw](https://github.com/dragonpaw))
- **Gabriel Rodriguez** ([@injectedfusion](https://github.com/injectedfusion))

[Become a sponsor](https://github.com/sponsors/artifact-keeper)

### Thank You

- @feicipet for reporting the Maven checksum validation failure on virtual repos (#660)
- @ReneBeszon for reporting the Nexus migration stall (#654) and npm virtual repo metadata issue (#652)
- @m1m1x for reporting the APK remote repo multi-version failure (#653)
- @thonby for reporting the OIDC redirect_uri issue behind reverse proxies (#655)
- @jamie-kemp for reporting the Go proxy `go get` failure (#651)

### Fixed
- **Maven checksum 404 on virtual repos** (#660) - checksum requests (.sha1, .md5, .sha256) through virtual Maven repositories now traverse member repos and proxy from upstream, matching the behavior of artifact downloads.
- **Migration assessment stalls forever** (#654) - the assessment handler now spawns a background worker to run the assessment. Previously it set the job status to "assessing" but never executed the work. Assessment results are stored and retrievable via the GET endpoint.
- **APK remote proxy 500 across Alpine versions** (#653) - when a proxy-cached artifact's storage key is inaccessible, the handler now falls through to re-fetch from upstream instead of returning HTTP 500.
- **OIDC redirect_uri behind reverse proxy** (#655, #657) - the redirect URI is now constructed as an absolute URL using the request's Host header, fixing Keycloak/OIDC callbacks that failed when running behind a reverse proxy.
- **npm virtual repo metadata for local members** (#652, #659) - virtual npm repositories now include metadata from local and staging member repos, not just remote members.
- **Go proxy sumdb verification paths** (#651, #658) - `go get` through proxy repos no longer fails with "Bad Request" when the Go toolchain requests sumdb verification paths.

### Dependencies
- quick-xml 0.39.0 -> 0.39.2
- zip 2.4.2 -> 8.5.1
- docker/login-action 4.0.0 -> 4.1.0
- aws-actions/configure-aws-credentials 6.0.0 -> 6.1.0

Note: bergshamra was held at 0.3.x in this release. See #691 for upgrade blocker tracking.

## [1.1.0] - 2026-04-01

This is the first stable release of Artifact Keeper.

### Sponsors

Thank you to our backers for supporting ongoing development of Artifact Keeper:

- **Ash A.** ([@dragonpaw](https://github.com/dragonpaw))
- **Gabriel Rodriguez** ([@injectedfusion](https://github.com/injectedfusion))

[Become a sponsor](https://github.com/sponsors/artifact-keeper) to support the project.

### Thank You

This release includes fixes reported by the community. A special thanks to everyone who took the time to file detailed issues with reproduction steps:

- @Kimahriman for five high-quality bug reports: PyPI relative URL handling for Nexus/devpi remotes (#610), NPM scoped package encoding for private registries (#616), PyPI remote vs virtual divergence (#625), PyPI proxy cache misses (#603), and PyPI virtual download delegation (#602)
- @andrlange for four OCI/Docker bugs: manifest push error handling (#594, #595, #596) and API token auth on /v2/token (#593)
- @mtatheonly for reporting that remote repos don't list cached packages (#624)
- @gaetanmetzger for the Docker Hub remote proxy support request that led to the token exchange feature (#612)
- @ivolnistov for the SSO admin permissions fix PR (#609) and the SSO bug report (#608)
- @todpunk for the metrics endpoint feature (#571) and clippy docs fix (#572)
- @arnaudmut for reporting the Trivy image tag issue (#585)
- @myannou for the Docker remote pull failure (#584)
- @TechEnchante for the chunked upload feature request (#563)
- @Tartanpion27 for the AWS ECS/Fargate credential chain issue (#613)
- @injectedfusion for the Cargo remote 404 report (#611)
- @pipelineRat for reporting virtual repo member display issues (#461) and the SSO lockout problem (#443)
- @Lerentis for continued testing on the Maven S3 upload issue (#361)
- @msegura501 for the Caddy proxy env var fix (#445)

### Added
- **OCI bearer token exchange** (#612, #626) - remote Docker/OCI repos now handle the 401/token handshake required by Docker Hub, GHCR, and private registries. Tokens are cached in memory with TTL-based eviction. Includes SSRF validation on token endpoint URLs.
- **Chunked/resumable upload API** (#563, #564) - new `/api/v1/uploads` endpoints for multi-GB file uploads. Supports configurable chunk sizes (1 MB to 256 MB), SHA-256 verification, session expiry cleanup, and resume after interruption.
- **Upstream authentication for remote repositories** (#451) - remote (proxy) repos can now authenticate against private upstream registries using Basic or Bearer credentials. Credentials are encrypted at rest with AES-256-GCM.
- **ALLOW_LOCAL_ADMIN_LOGIN env var** (#443) - break-glass recovery mechanism that allows the built-in admin account to log in with local credentials even when SSO is configured.
- **Optional unauthenticated metrics endpoint** (#571) - contributed by @todpunk. Configurable via `METRICS_PORT` for Prometheus scraping without auth.
- **Proxy-cached artifacts in repo listings** (#624, #626) - artifacts fetched through remote proxy repos are now recorded in the database, making them visible in repository listings and storage size calculations.
- **Docker compose guide comments** (#448) - inline guidance explaining each service, what is optional, and what to change for production.

### Fixed
- **PyPI relative URL resolution** (#610, #622) - registries like Sonatype Nexus, devpi, and Artifactory that use relative hrefs in their simple index HTML now work correctly. URLs are resolved using the `url` crate's RFC 3986-compliant Url::join method.
- **PyPI virtual/direct remote parity** (#625, #627) - virtual repos now use the exact same remote proxy logic as direct remote access, including content-type preservation, conditional URL rewriting, and proxy cache checks.
- **NPM scoped package encoding for remotes** (#616, #622) - scoped packages (`@scope/pkg`) are now URL-encoded (`@scope%2Fpkg`) in upstream requests per the npm registry wire protocol. Private registries (Nexus, Verdaccio, GitHub Packages) that require the encoded form now work.
- **Chunked upload security hardening** (#621) - six P0 bugs fixed: memory-safe file streaming on complete (C1), bounded chunk body buffering (C2), session ownership verification (C3), strengthened path traversal validation (C4), total_size validation (C5), atomic chunk claim to prevent race conditions (C6).
- **SSRF protection hardening** (#622) - SSRF blocklist now rejects single-label hostnames and `.svc.cluster.local` Kubernetes addresses. Resolved URLs from upstream index pages are validated before fetch. Non-HTTP schemes rejected after relative URL resolution.
- **SSO admin permissions preserved on login** (#608, #609) - when an SSO user with admin permissions logs in and no admin group pattern matches, their existing admin flag is no longer reset. Contributed by @ivolnistov.
- **S3 ECS/EKS credential chain** (#613, #617) - switched to `AmazonS3Builder::from_env()` so ECS task roles, EKS IRSA, and instance profiles are automatically detected.
- **OCI manifest push bugs** (#594, #595, #596, #600) - three fixes: re-push after delete clears `is_deleted`, INSERT errors are no longer silently swallowed, and duplicate image name in log lines is resolved.
- **Docker /v2/token accepts API tokens** (#593, #599) - service account API tokens can now be used as Basic Auth passwords for Docker login.
- **Revoked tokens excluded from listings** (#592, #598) - service account token listing no longer includes revoked tokens.
- **Trivy image pinned** (#585, #597) - Docker image uses `ghcr.io/aquasecurity/trivy:0.69.3` instead of the removed `latest` tag.
- **S3 PUT errors surface response body** (#361) - S3 rejection details (e.g. 403 AccessDenied) now appear in error messages and structured logs.
- **Virtual repo members displayed in UI** (#455, #461) - fixed member panel rendering and OpenAPI spec field mismatch.
- **Caddy proxy env var interference** (#445) - cleared inherited Docker daemon proxy env vars on the Caddy service.
- **OCI Basic Auth on /v2 endpoint** (#457) - the `/v2/` version check endpoint now accepts Basic Auth in addition to Bearer tokens.
- **SSO login redirect URLs** (#454) - corrected OIDC/SAML callback URLs and added LDAP custom CA certificate support.

### Security
- **OIDC ID token signature verification** (#590) - validates ID token signatures via JWKS, plus nonce/iss/aud/exp claims.
- **SSRF protection on repository upstream URLs** (#590) - `validate_outbound_url` blocks private IPs, cloud metadata endpoints, and internal hostnames.
- **NPM package name validation** (#622) - decoded package names are validated for null bytes, path traversal, slash count, and length.
- **Chunked upload path traversal hardening** (#621) - rejects null bytes, backslashes, percent-encoded traversal, bare dot components, and double slashes.

### Performance
- **CI pipeline 60% faster** (#591) - sccache, dependency caching, and parallel jobs cut PR check time significantly.
- **Regex compilation moved to statics** (#622) - three per-request `Regex::new()` calls in PyPI handler replaced with `once_cell::sync::Lazy` statics.
- **N+1 download stats query eliminated** (#590) - batch query replaces per-artifact loop in artifact listing.

## [1.1.0-rc.8] - 2026-03-17

### Thank You
- @inspired-geek (Alexey Ivanov) for three quality contributions: paginated Dependency-Track list endpoints preventing truncated security data (#434), paginated Meilisearch reindex preventing OOM on large registries (#435/#440), advisory cache eviction with error visibility (#433/#439), S3 status code handling (#410), and Cargo sparse index field name fix (#419)
- @pipelineRat for reporting Maven pull-through cache failures (#427)
- @dispalt for continued feedback on storage backend behavior (#428)

### Added
- **Per-repository storage backend selection** (#431) - repositories can now be configured to use different storage backends (filesystem, S3, Azure, GCS) independently of the global default.
- **Correlation ID middleware** (#432) - every HTTP response now includes an `X-Correlation-ID` header for distributed tracing. The middleware was previously defined with 13 unit tests but never wired into the router.
- **Configurable rate limits** (#436) - auth and API rate limits are now configurable via `RATE_LIMIT_AUTH_PER_MIN` (default 120), `RATE_LIMIT_API_PER_MIN` (default 5000), and `RATE_LIMIT_WINDOW_SECS` (default 60) environment variables.

### Fixed
- **bcrypt blocking the async runtime** (#436) - moved all `bcrypt::verify` and `bcrypt::hash` calls to `tokio::task::spawn_blocking()`. Previously, bcrypt at cost-12 (~250ms per call) ran synchronously on the tokio event loop, serializing all concurrent requests. At 5 concurrent logins, max latency was 2.7s. Now runs on the blocking thread pool.
- **Rate limiter shared bucket in Kubernetes** (#436) - without `ConnectInfo`, all clients shared a single `ip:unknown` rate limit bucket. Now falls back to `X-Forwarded-For` from trusted ingress controllers. Auth rate limit raised from 30 to 120 req/min (bcrypt cost-12 already provides brute-force protection).
- **Sync policy create doesn't auto-evaluate** (#438) - creating, updating, deleting, or toggling a sync policy now automatically calls `evaluate_policies()` to populate `peer_repo_subscriptions`. Previously, uploads after policy creation wouldn't trigger sync tasks until a manual `/evaluate` call.
- **Maven SNAPSHOT timestamp treated as classifier** (#432) - `strip_snapshot_timestamp()` now correctly strips timestamp-build suffixes (e.g., `-20260314.155654-1`) from SNAPSHOT filenames so they aren't misidentified as classifiers.
- **Maven SNAPSHOT re-upload not updating primary** (#432) - when a new SNAPSHOT build replaces an existing primary artifact, the artifact record now correctly updates path, checksum, and storage key.
- **Dependency-Track truncated results** (#434) - list endpoints now paginate through all results instead of returning only the first page. Contributed by @inspired-geek.
- **Meilisearch OOM on large reindex** (#440) - full reindex now uses cursor-based pagination (1000 rows per batch) instead of loading all artifacts into memory. Download count subquery scoped to batch. `MeiliService::new` returns `Result` instead of panicking. Based on contribution by @inspired-geek.
- **Advisory cache silent error swallowing** (#439) - replaced `if let Ok` patterns with `match` + `warn!` logging for OSV and GitHub advisory API deserialization failures. Added bounded cache eviction. Based on contribution by @inspired-geek.
- **S3 backend error propagation** (#410) - improved status code handling and fallback error messages. Contributed by @inspired-geek.
- **Cargo sparse index field name** (#419) - renamed `version_req` to `req` in dependency entries to match the sparse registry protocol spec. Contributed by @inspired-geek.
- **Maven secondary GAV file serving** (#430) - resolve SNAPSHOT downloads and serve secondary files (POM, javadoc, sources) from the correct GAV path.
- **Maven SNAPSHOT checksum resolution** (#417) - resolve checksum requests for timestamped SNAPSHOT versions.
- **Maven GAV coordinate grouping** (#418) - group deploy artifacts by GAV coordinates to prevent incorrect artifact association.

### Changed
- Auth rate limit default raised from 30 to 120 req/min
- API rate limit default raised from 1,000 to 5,000 req/min
- Dependency bumps: docker/metadata-action 5 to 6, docker/build-push-action 6 to 7, docker/login-action 3 to 4, actions/github-script 7 to 8, anchore/grype v0.109.0 to v0.109.1, aquasecurity/trivy-action 0.34.2 to 0.35.0, rust 1.93-bookworm to 1.94-bookworm, quinn-proto security patch

## [1.1.0-rc.7] - 2026-03-08

### Thank You
- @todpunk for the security advisory heads-up and a Cargo token performance fix (#378, #377)
- @arp-mbender for reporting the broken quickstart commands (#368)
- @dispalt for catching the staging repo filter issue and SNAPSHOT soft-delete collision (#363, #321)
- @Lerentis for reporting the Maven path upload bug and the S3 IRSA panic (#361, #343)
- @lpreiner for flagging private repository visibility leaking to unauthenticated users (#333)
- @CJLove for continued follow-up on Docker login behind reverse proxies (#322)

### Added
- **Package curation engine** (#405) - intercept packages from upstream mirrors through staging repos, evaluate against configurable rules (glob patterns, version constraints, architecture filters), and approve or block before exposing to consumers. Includes rules CRUD, bulk operations, re-evaluation, and stats API endpoints.
- **Curation upstream sync** (#405) - background scheduler fetches and parses RPM primary.xml and DEB Packages indexes from remote repos, populating the curation catalog automatically.
- **Artifact content viewing endpoint** (#407) - `GET /api/v1/tree/content` returns inline file content for browsing artifacts in the web UI.
- **Automatic stale peer detection** (#402) - scheduler marks peers as stale when heartbeats stop, preventing sync attempts to unreachable nodes.
- **Failed sync retry on peer recovery** (#401) - automatically retries previously failed sync tasks when a peer comes back online.
- **Shared virtual metadata resolution** (#399) - extracted reusable helpers for resolving metadata across virtual repository members, reducing duplication in format handlers.
- **Build traceability** (#367) - `/health` endpoint now includes the git commit SHA for identifying deployed versions.

### Fixed
- **Path traversal in FilesystemStorage** (#387, #380) - sanitize storage keys to prevent directory traversal attacks via crafted artifact paths.
- **Peer identity endpoint exposed to non-admins** (#388, #381, #382) - restrict peer announce, heartbeat, and identity endpoints to admin users only.
- **gRPC missing admin authorization** (#390, #383) - add admin privilege checks to the gRPC auth interceptor.
- **Admin password file permissions** (#391, #384) - create password file with mode 0600 instead of world-readable.
- **Timing side-channel in token validation** (#392, #385) - use constant-time comparison for API token prefix lookup.
- **Authentication audit logging** (#393, #386) - wire up audit log entries for login, logout, and token operations.
- **Quickstart commands in README** (#394, #368) - fix incorrect docker compose commands in the getting started guide.
- **Air-gapped deployment issues** (#379) - fix offline installation and configuration for disconnected environments.
- **Cargo sparse index proxy path** (#342, #341) - strip `index/` prefix when proxying to upstream Cargo registries.
- **Soft-deleted artifact collision on re-upload** (#339, #321) - clean up soft-deleted records before INSERT to prevent unique constraint violations.
- **Maven version-level metadata** (#362, #361) - serve maven-metadata.xml from storage at the version path level.
- **Staging repo filter** (#364, #363) - accept `repo_type` query alias for filtering staging repositories.
- **Repository format enum casting** (#376) - cast `repository_format` enum to text in quality check queries to prevent type mismatch errors.
- **S3 IRSA TLS panic** (#348, #343) - install rustls CryptoProvider before S3 client initialization to prevent panics with IAM Roles for Service Accounts.
- **Fork PR SonarCloud gate** (#396) - detect fork PRs correctly in the quality gate workflow.
- **E2E PKI container cleanup** (#400) - kill lingering gpg-agent processes before cleanup to prevent non-zero exit codes.
- **Cargo token auth performance** (#377) - contributed by @todpunk.

### Tests
- Curation E2E test suite (#406) - 33 assertions across 13 phases covering sync, rules, manual/bulk status, stats, CRUD, global rules, and DEB format. Uses mock upstream repos (nginx serving RPM/DEB fixture files).

### Changed
- Trivy CI scanner bumped from 0.69.1 to 0.69.3 (#404)
- ALLOW_HTTP_INTEGRATIONS added to compose backend environment (#397)
- CI mirror namespace switched to GHCR for fork PR E2E parity (#366)
- Dependency bumps: actions/checkout 4 to 6, actions/upload-artifact 6 to 7, actions/download-artifact 4 to 8, github/codeql-action 3 to 4, alpine 3.21 to 3.23, trivy-action 0.34.1 to 0.34.2

## [1.1.0-rc.6] - 2026-02-28

### Thank You
- @CJLove for reporting the OCI v2 auth challenge issue behind reverse proxies (#315)
- @dispalt for identifying the Maven SNAPSHOT re-upload bug and scanner storage backend resolution (#297, #296)
- @msegura501 for reporting private repository visibility enforcement (#280)

### Added
- **Azure RBAC authentication for Blob Storage** (#312) - support managed identity and service principal authentication for Azure Blob Storage, removing the need for connection strings
- **Alpine-based Docker image variant** (#306) - lighter alternative image based on Alpine Linux alongside the existing UBI image
- **Release gate integration** (#317) - backend releases now run the full artifact-keeper-test suite (38 formats, stress, resilience, mesh) before proceeding
- **Quality of life improvements** (#298) - 9 features: configurable GC/lifecycle cron scheduling, stale proxy cache fallback, deletion replication to peers, webhook delivery retry with exponential backoff, soft token revocation with `last_used_at` tracking, per-repo cache TTL endpoint, search reindex API, quota warning events, and replication filters with regex include/exclude patterns

### Fixed
- **OCI v2 auth challenge uses wrong scheme/host behind reverse proxy** (#315, #316) - `Www-Authenticate` header now respects `X-Forwarded-Proto` and `X-Forwarded-Host`, fixing `docker login` failures when running behind Caddy, Nginx, or other reverse proxies
- **Maven SNAPSHOT re-upload and hard-delete** (#297, #301) - SNAPSHOT artifacts can now be re-uploaded (overwritten) as expected, and hard-delete properly removes files from storage
- **Scanner storage backend resolution** (#296, #301) - security scanners now use the configured storage backend (S3, Azure, GCS) instead of defaulting to filesystem
- **Format route key extraction** (#302) - fix repo key parsing for format handler routes when the key contains path separators
- **Private repository visibility enforcement** (#300) - anonymous users can no longer access private repository metadata
- **Storage probe path traversal** (#293, #308) - validate that health check storage probe paths stay within the base directory
- **Code scanning alerts** (#307) - address CodeQL alerts #16 and #39 for taint flow and input validation
- **Wasmtime CVE bump** (#292) - upgrade to wasmtime 24.0.6 for CVE-2026-27572 and CVE-2026-27204

### Tests
- Unit tests for ArtifactFilter matching logic (#309)
- E2E tests for Maven SNAPSHOT re-upload and S3 scanner (#304)
- Flaky cron policy test fix and Postgres added to coverage job (#311)
- Custom CodeQL workflow replacing default setup (#305)

### Changed
- Docker Hub documented as alternative registry (#289)
- Dependency bumps: actions/attest-build-provenance 3 to 4, SonarSource/sonarqube-scan-action 6 to 7

## [1.1.0-rc.4] - 2026-02-25

### Added
- **Service accounts and token scoping** (#205, #208, #209) - service account entities with API tokens, scope enforcement across all repository handlers, and RepoSelector-based token restrictions
- **Incus/LXC container image support** (#206) - new `incus` repository format implementing the SimpleStreams protocol for container and VM images
- **SSE event stream for live data refresh** (#269) - server-sent events endpoint allowing the web UI to receive real-time cache invalidation signals
- **Physical storage garbage collection** (#233) - background task to reclaim disk space from soft-deleted artifacts
- **Tag-filtered peer replication** (#243) - filter replication to only sync artifacts matching specified tag patterns
- **WASM plugin v2: handle-request** (#256) - plugins can now serve native package format wire protocols directly
- **`SKIP_ADMIN_PROVISIONING` env var** (#224) - skip admin user creation on first boot for SSO-only deployments
- **Artifact filter enforcement with retroactive sync** (#204) - evaluate filters against existing artifacts when policies change

### Fixed
- **Storage backend hardcoded to filesystem** (#237, #244, #245, #246) - use the configured storage backend (S3, Azure, GCS) instead of always defaulting to local filesystem
- **OIDC env var config ignored** (#249) - environment variable configuration for OIDC providers was not being read
- **Local login not blocked with SSO** (#223) - block local password login when SSO providers are configured
- **LRU eviction for size quotas** (#226) - change storage quota eviction from FIFO to least-recently-used ordering
- **Lifecycle policy execution** (#225) - implement `tag_pattern_keep` lifecycle policy type
- **Streaming uploads for Incus** (#217, #242) - fix chunked upload handling for large container images
- **96 code scanning alerts resolved** (#267, #268) - taint-flow fixes, safe string handling, and input validation improvements
- **DNS rebinding protection** - bound allocations, upgrade KDF from static HMAC to HKDF with domain separation
- **HTTPS enforced in Dockerfile healthchecks** (#251)

### Security
- **Privilege escalation fix** (#273) - enforce admin checks on user creation and all admin routes
- **Archive extraction hardening** (#274) - path traversal protection, safe file handling, parameterized SQL
- **Encryption and rate limiter hardening** (#275) - improved encryption key derivation, LDAP injection prevention, CSP headers, XSS/SSRF mitigations
- **SSRF and path traversal fixes** (#277) - close server-side request forgery vectors and path traversal in file operations
- **KDF upgrade** - migrate from static HMAC key to HKDF with domain separation, fix CodeQL hard-coded crypto alerts

### Changed
- SonarCloud scanner added to CI (#247)
- Code coverage reporting with cargo-llvm-cov (#229)
- All environment variables documented in .env.example (#227)
- Mergify auto-merge configuration (#215)
- Dependency bumps: actions/upload-artifact 4 to 6, actions/checkout 4 to 6, actions/attest-build-provenance 2 to 3

### Tests
- Unit test coverage increased toward 80% quality gate (#253, #254)

## [1.1.0-rc.3] - 2026-02-17

### Fixed
- **Token creation broken** (#195, #197) — `POST /api/v1/auth/tokens` and `DELETE /api/v1/auth/tokens/{token_id}` were documented in the OpenAPI spec but never registered in the router, causing silent 404s from the frontend
- **Non-admin users could request admin scope** (#197) — backend now returns 403 when a non-admin user attempts to create a token with the `admin` scope
- **Podman / standalone Docker Compose compatibility** (#194, #196) — SELinux `:z` bind-mount labels, replaced `service_completed_successfully` dependency with polling entrypoint, changed healthcheck from `/readyz` to `/livez`, downgraded web/caddy depends_on to `service_started`
- **Caddyfile missing `/livez` and `/readyz` routes** (#196) — reverse proxy now forwards liveness and readiness probes to the backend

### Added
- **OpenAPI route audit test** (#197) — `test_all_openapi_paths_have_handlers` cross-checks every documented endpoint against handler source files, catching annotated-but-unregistered routes at compile time

### Changed
- Renamed `UserResponse` to `AdminUserResponse` in users handler to avoid DTO collision (#187)
- CI skips Docker publish for docs-only changes (#185)

## [1.1.0-rc.2] - 2026-02-15

### Added
- **Promotion Workflow** (#151) — approval chains, age gates, signature verification, and rejection with audit trail
- **Auto-Promotion Rules Engine** (#152) — configurable rules for automatic artifact promotion based on quality gates, age, and scan results
- **K8s Health Probes & OpenTelemetry Tracing** (#147) — structured health endpoints for liveness/readiness and distributed tracing with span propagation
- **SECURITY.md** — vulnerability reporting policy

### Changed
- **UBI 9 Micro Runtime** (#160) — all containers migrated from Alpine to Red Hat UBI 9 Micro for STIG compliance
- **Container Hardening** (#163, #164) — Cosign image signing, Trivy vulnerability scanning in CI, and STIG hardening
- **UBI 9.5 → 9.7** (#170, #172, #173) and **Alpine 3.19 → 3.23** (#171)
- **SonarCloud Integration** (#158, #159, #162) — static analysis and Dockerfile scanning in CI
- **Dockerfiles Consolidated** (#157) — all Dockerfiles and Caddyfile moved to `docker/` directory
- **Deploy Folder Relocated** (#154, #155) — moved to `artifact-keeper-iac` repository
- **Self-Hosted ARC Runner** (#148) — smoke E2E tests run on self-hosted Actions Runner Controller
- **Dependabot Bumps** — codeql-action 3→4, upload-artifact 4→6, download-artifact 4→7, aws-actions/configure-aws-credentials 4→6, stale 9→10

### Fixed
- **Duplicate OpenAPI operationIds** (#182) — explicit operation IDs for sync_policies and repository_labels handlers to fix SDK generation
- **Release Build Pipeline** (#181) — add protoc installation and vendored OpenSSL for cross-platform binary builds
- **CI Pipeline Repairs** (#174, #175, #178) — Docker publish, security scan, and Trivy scan fixes
- **Native Test Scripts** (#177) — PyPI sed portability, NPM auth config, Cargo registry config fixes
- **E2E Test Failures** (#146, #180) — Go, Docker, Helm, Protobuf test fixes; switched release gate to smoke profile
- **arm64 Docker Builds** — use arch-appropriate protoc binary
- **Artifact Download Filter** — release workflow only downloads binary archives, not E2E artifacts

### Tests
- Backend unit test coverage pushed toward 80% (#153)

## [1.1.0-rc.1] - 2026-02-13

### Added
- **Artifact Health Scoring & Quality Gates** (#129)
  - Pluggable quality check system with composite health scores (A-F grade) and configurable quality gates for promotion gating
  - MetadataCompletenessChecker (all formats) and HelmLintChecker (in-process chart.tgz validation)
  - QualityCheckService orchestrator with weighted scoring (security=40, quality=25, license=20, metadata=15)
  - 15 new API endpoints under `/api/v1/quality`; async checks triggered on artifact upload
- **Sync Policy Engine & Background Sync Worker** (#109, #122)
  - Declarative label-based replication policies with JSONB selectors for repos and peers
  - 8 new API endpoints (`/api/v1/sync-policies`) for CRUD, evaluation, and preview
  - Peer instance labels API (`GET/PUT/POST/DELETE /api/v1/peers/:id/labels`) for `match_labels` resolution (#122)
  - Auto-evaluate triggers on repo label, peer label, and new peer registration changes (#122)
  - 5-minute periodic re-evaluation scheduler to catch drift (#122)
  - Background sync worker with per-peer sync windows, exponential backoff, and concurrent transfer limits
- **Remote Proxy Repositories** (#112)
  - Remote repos now proxy artifacts from upstream registries (npmjs.org, PyPI, Maven Central, etc.) on cache miss
  - Automatic local caching with 24-hour TTL and ETag-based revalidation
  - ProxyService wired into all 28 format handlers for download endpoints
  - Write guards return 405 Method Not Allowed on remote repos
- **Virtual Repository Resolution** (#112)
  - Virtual repos aggregate multiple member repos (local + remote) with priority-based resolution
  - Metadata merging for npm (`get_package_metadata`) and PyPI (`simple_project`) so native clients (`npm install`, `pip install`) work through virtual repos
  - Write guards return 400 Bad Request on virtual repos
  - Tarball URL rewriting to route downloads through the virtual repo key
- **Protobuf/BSR Format Support** (#119)
  - New `protobuf` repository format implementing BSR-compatible Connect RPC endpoints
  - 10 endpoints: GetModules, CreateModules, GetCommits, ListCommits, Upload, Download, GetLabels, CreateOrUpdateLabels, GetGraph, GetResources
  - Full proxy/virtual repository resolution support
- **Repository Key Renames** (#120) — `PATCH /api/v1/repositories/{key}` now accepts a `key` field to rename the URL slug
- **Repository Labels API** (#108)
- **Artifact Upload Sync Trigger** (#108)
- **Full-stack Kubernetes Manifest** (#104)
- **Proxy/Virtual E2E Test Suite** (#112)
  - 21-test script covering proxy downloads, write rejection, virtual resolution, and native client integration
  - Docker Compose `proxy` profile for CI
  - Bootstrap script creates remote, local, and virtual repos with member wiring
- **Mesh Replication E2E Workflow** (#127) — GitHub Actions workflow for automated mesh replication testing via ArgoCD
- **Stale Bot** (#121) — auto-labels inactive issues

### Fixed
- **Proxy cache key collision**: Metadata cached as file blocked tarball paths that needed same prefix as directory; fixed with `__content__` leaf file scheme (#112)
- Fix `replication_mode` enum type cast in sync policy evaluate (#126)
- Fix `format` column type mismatch in sync policy evaluate (#125)
- Fix peer instance labels auth middleware mismatch (#124)
- Use AWS default credential chain instead of env vars only (#106)
- Ensure admin login works on fresh installs and fix Dependency-Track startup race (#102)
- Add setup instructions to `admin.password` file so users know to login first (#100)
- Auto-enable NVD API 2.0 and add proxy passthrough for Dependency-Track (#98)
- Set global 512 MB body limit to prevent silent upload truncation (#97)

### Changed
- Moved `site/` to separate `artifact-keeper-site` repository (#101)
## [1.0.0-rc.3] - 2026-02-08

Bug fix release resolving 9 issues found by automated stress testing, plus build hygiene improvements.

### Fixed
- **Promotion handler**: Fix storage_key bind using `artifact.path` instead of `artifact.storage_key`, causing promoted artifacts to be undownloadable (#65, #72)
- **Promotion handler**: Replace direct `tokio::fs::copy` with `FilesystemStorage` abstraction to respect content-addressable sharding (#65, #72)
- **Repository key validation**: Add strict allowlist rejecting path traversal, XSS, SQL injection chars, null bytes, and keys over 128 characters (#69, #70)
- **Upload size limit**: Add `DefaultBodyLimit::max(512MB)` to repository router; Axum default 2MB was blocking legitimate uploads (#67)
- **Rate limiting**: Increase API rate limit from 100 to 1000 req/min, auth from 10 to 30 req/min (#66, #68, #71, #73)
- **Download panic**: Lowercase `X_ARTIFACT_STORAGE` header constant for `HeaderName::from_static()` compatibility
- Correct `AuthExtension` type in promotion handlers (#62)
- Remove extra blank lines in promotion handlers (#63)
- Fix pre-release banner overlapping content on mobile (#64)
- Use dev tag for main builds, latest only on release tags (#60)

### Added
- DevOps stress test agent script (12-phase, 71-test suite)

### Changed
- Documentation gaps filled for v1.0.0-a2 features (#61)

## [1.0.0-rc.1] - 2026-02-03

### Added
- First-boot admin provisioning and Caddy reverse proxy
- OpenSCAP compliance scanner service
- Package auto-population and build tracking API
- httpOnly cookies, download tickets, and remote instance proxy
- SSO single-use exchange codes for secure token passing
- Complete SSO auth flows with real LDAP bind, SAML endpoints, and encryption key handling
- Admin-configurable SSO providers (OIDC, LDAP, SAML)
- Web frontend service in all docker-compose files
- Native apps section on landing page with macOS, iOS, Android demos

### Changed
- Use pre-built images from ghcr.io instead of local builds
- Rename frontend to web in Docker deployment docs
- Use standard port 3000 and correct BACKEND_URL env var for web service
- Clean up operations services and handlers
- Simplify SSO backend code for clarity and consistency

### Fixed
- NPM tarball URL and integrity hash in package metadata
- Hardcoded localhost:9080 fallback URLs removed from frontend
- Logo transparency using flood-fill to preserve silver highlights
- Duplicate heading on docs welcome page
- GitHub links updated to point to org instead of repo
- CORS credentials support for dev mode

## [1.0.0-a2] - 2026-02-08

Second alpha release with staging promotion workflow, Dependency-Track monitoring, red team security hardening, and landing page refresh.

### Added
- **Staging Promotion Workflow**
  - New staging repository type for promotion-based artifact lifecycle
  - Promotion API endpoints for staging → release workflow
  - Policy gate integration for automated promotion decisions
  - Simplified promotion policy and handler code (#49)
- **Dependency-Track Monitoring** (#57)
  - Backend API for Dependency-Track integration
  - OpenSCAP and Dependency-Track added to health monitoring dashboard
- **Red Team Security Testing Suite** (#52)
- **STS Credential Rotation E2E Tests** (#56)
- **Pre-release banner** on landing page and README

### Changed
- Updated landing page to LCARS color scheme with new brand colors
- Pre-release banner changed from warning to release announcement

### Fixed
- Refresh credentials before presigned URL generation (#55)
- Calculate storage_used_bytes for repository list view (#58)
- Position banner above navbar without overlap
- CI fixes: fmt, clippy, and broken migration (#48)
- CI fixes: PKI file handling in E2E tests (tar archive, explicit patterns)

### Security
- Hardened 7 vulnerabilities identified by red team scan (#53)

## [1.0.0-a1] - 2026-02-06

First public alpha release, announced on Hacker News.

### Added
- **OWASP Dependency-Track Integration** (#46)
  - Docker service configuration for Dependency-Track API server
  - Rust API client for SBOM upload, vulnerability findings, policy violations
  - Comprehensive SBOM & Dependency-Track documentation
  - E2E test script for Dependency-Track integration
- **Multi-cloud Storage Backends** (#45)
  - Azure Blob Storage backend
  - Google Cloud Storage backend
  - Artifactory migration mode with fallback path support
- **S3 Direct Downloads** (#38)
  - 302 redirect to presigned S3 URLs
  - CloudFront signed URL generation
  - Configurable via `STORAGE_S3_REDIRECT_DOWNLOADS`
- **SBOM Generation & gRPC API** (#31)
  - CycloneDX and SPDX format support
  - CVE history tracking
  - gRPC service for SBOM operations
- **WASM Plugin E2E Tests** (#37)
- **SSO E2E Test Suite** - LDAP/OIDC/SAML authentication tests
- **TOTP Two-Factor Authentication**
- **Privacy Policy Page** for app store submissions
- **Migration Pipeline** - Artifactory and Nexus OSS support
- **OpenSCAP Multi-arch Image** with scanning enabled by default

### Changed
- Simplified and deduplicated code across backend and scripts (#27)
- Updated docs to use peer replication model instead of edge nodes
- Docker build cache optimization with cargo-chef and native arm64 runners
- Streamlined CI pipeline with CI/CD diagram in README

### Fixed
- E2E test infrastructure improvements (bootstrap, setup containers)
- CI workflow fixes (clippy warnings, YAML indentation)
- SSO e2e test infrastructure fixes
- Logo resized to exact 512x512 for app stores
- Metrics endpoint proxied through Caddy
- Various Caddy and port configuration fixes

### Security
- Secure first-boot admin password with API lock
- GitGuardian integration for secret scanning
